# BACKLOG — 후속 작업 후보

현재 작업 범위 밖이지만 나중에 가치 있는 작업을 기록합니다. 새 항목은 표 최하단에 추가합니다.

## BACKLOG 관리 규칙

### 추가 기준 (다음 조건 중 하나 이상)
- 지금 당장 실행하지 않지만 나중에 가치 있는 버그·개선·기술부채
- 발견된 문제 중 현재 작업 범위 밖인 것
- 다음 세션에서 이어받아야 할 미완료 작업

### 제외 기준 (추가하지 않음)
- 사소한 스타일 정리, 타입 힌트 추가 등 임팩트 낮은 것
- 이미 완료된 작업
- 즉시 실행해야 할 것 (바로 처리하거나 TODO 주석으로)

### 상태 표기 규칙
- **완료 항목**: 표에서 행을 삭제한다. 취소선(~~텍스트~~) 사용 금지. 히스토리는 git log로 추적.
- **보류 항목**: 항목명 앞에 `[보류]` 접두어를 붙인다. 명시적 지시가 있기 전까지 착수하지 않는다.
- **저우선 항목**: 항목명 앞에 `[저우선]` 접두어를 붙인다. 리뷰는 완료됐으나 상대적으로 우선순위가 낮아 후순위로 미룬 항목.
- **일반 항목**: 접두어 없음. 우선순위에 따라 착수 가능.

### ID 규칙
- 형식: `BL-MMDD-NN` (MM=월, DD=일, NN=당일 순번 01부터)
- 한 번 부여된 ID는 변경·재사용하지 않는다 (삭제해도 번호 유지)
- 외부 문서에서 참조 시: `BL-0412-01` 형태로 사용

| ID         | 날짜         | 항목                                                 | 위치                                                                 | 실행 용이성   | 기대효과     | 메모                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ---------- | -------------------------------------------------- | ------------------------------------------------------------------ | -------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BL-0412-01 | 2026-04-12 | [보류] Instagram CDN 403                             | `server.py`, `web_viewer/script.js:handleImgError`                 | `██░░░░` | `██████` | wsrv.nl 프록시 미사용 케이스 존재. server.py 이미지 프록시 엔드포인트 또는 local_images 다운로드 파이프라인 확장이 근본 해법                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| BL-0412-02 | 2026-04-12 | [보류] Masonry 성능                                    | `web_viewer/script.js`                                             | `██░░░░` | `████░░` | 975개 포스트 렌더 시 setTimeout 500ms+ 발생. 가상 스크롤 또는 페이지네이션 도입                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| BL-0412-05 | 2026-04-12 | [보류] Twitter 본문 오염 (봇 차단 리다이렉트)                    | `twitter_scrap.py`, `twitter_scrap_single.py`                      | `██░░░░` | `██████` | X 봇 차단으로 0.5~1초 내 리다이렉트 → 원본이 아닌 다른 페이지 본문 수집됨. 수집 79건 중 33건 중복 본문 경고. 오염 감지(본문 중복 체크) 또는 수집 재시도 로직 필요. **추가(2026-04-17)**: URL이 다른 두 트윗이 동일 본문으로 수집되는 사례 확인 — X가 스크랩 방지 목적으로 리다이렉트(인기 페이지 등)로 보내면 다른 URL에서 같은 내용이 수집됨. 중복 예시: `x.com/kana_option/status/2025027520668864965` (2026-02-21)와 `x.com/kana_option/status/2024968812987142469` (2026-02-20)가 "몇년 전만 해도 아마존 본사에서 풀타임직원이면…"으로 동일 본문 수집됨. URL 기반 중복 제거만으로는 불충분 → 본문 해시 기반 중복 감지 로직 추가 필요. 참고: SPEC `docs/specs/20260412_01_SNS-스크래퍼-수집누락-개선.md`, session `0ca38559-5818-41f1-ba30-f02f2fbe7d8d` |
| BL-0417-04 | 2026-04-17 | 업데이트 완료 후 추가 건수가 0건으로 표시되는 버그                      | `server.py:96`, `web_viewer/script.js:176-177`                     | `████░░` | `████░░` | `server.py:/api/run-scrap`이 `stats` 없이 응답(`{"status":"success","output":...}`)하므로 `script.js`의 `result.stats \|\| {total:0,...}` fallback이 항상 0건을 표시. `total_scrap.py` stdout에서 플랫폼별 추가 건수를 파싱하거나, `total_scrap.py`가 마지막 줄에 `STATS:threads=N,linkedin=N,total=N` 형식으로 출력하고 `server.py`가 이를 파싱해 `stats`로 응답하도록 수정 필요. |
| BL-0421-01 | 2026-04-21 | 업데이트 버튼이 "Running..."에서 멈춰 있는 버그 (태그 필터 활성 상태 의심)    | `index.html`, `web_viewer/script.js:/api/run-scrap` 호출부, `server.py:/api/run-scrap` | `████░░` | `████░░` | 증상: 업데이트(신규만) 실행 후 10분 이상 버튼이 "Running..." 상태로 고정. 진단 시점에 실제 `python.exe` 스크래퍼 프로세스는 없었고, `threads_py_full_20260421.json`은 09:44에 이미 저장 완료. 재현 조건 후보: "디자인" 태그 필터 활성화 상태에서 업데이트 실행 → 필터 해제 + 페이지 새로고침하니 "완료"로 전환됨. 원인 후보: (1) `/api/run-scrap` 응답 후 UI polling 종료 신호 누락, (2) 태그 필터링 렌더 루프가 polling 콜백 지연, (3) 프론트 상태 플래그가 서버 응답과 동기화 실패. 재현 시 DevTools Network에서 `/api/run-scrap`, `/api/status`, `/api/posts` 타임라인 확인 필요. 참고 session: `553e1d18-2b55-410f-a2b4-4aa8eb5a3188` |
| BL-0423-01 | 2026-04-23 | [보류] LinkedIn 수동 URL 추가 기능 | `index.html`, `web_viewer/script.js`, `server.py`, `linkedin_scrap.py`, `total_scrap.py` | `██░░░░` | `██████` | LinkedIn 북마크가 저장 목록에 반영되지 않는 경우를 보완하기 위해 헤더에 `+` URL 추가 버튼을 두는 기능. 구현 전 `activity_id` 기준 중복 감지, manual ledger 보존, 전체 재크롤링 시 수동 추가 글 유지, URL 키 기반 태그·즐겨찾기 상태 분리, 동시 쓰기 lock, LinkedIn 도메인 allowlist 검증이 필요하다. 참고: [[docs/20260423_02_LinkedIn-수동-URL-추가-기능-검토]] |
| BL-0504-01 | 2026-05-04 | 실제 플랫폼 저장시간 수집 가능성 조사 | `linkedin_scrap.py`, `thread_scrap.py`, `tests/fixtures/snapshots/`, `utils/post_schema.py` | `██░░░░` | `████░░` | 현재 출력 파일과 원본 스냅샷에는 `saved_at`, `savedAt`, `bookmarked_at` 같은 실제 저장시간 필드가 없음. LinkedIn은 `saved: true`, Threads는 게시글 작성시간(`taken_at`)만 확인됨. 후속 조사에서 저장 목록 네트워크 응답을 더 깊게 추적해 실제 저장시간이 안정적으로 제공되는지 확인하고, 불가하면 `platform_sequence_id`를 "저장 목록 순서"로만 설명해야 함. 참고: `docs/specs/20260504_01_SNS-수집결과-정합성-검증.md` |
| BL-0504-02 | 2026-05-04 | LinkedIn 저장글 업데이트 인증 실패 상태 기록 | `linkedin_scrap.py`, `scripts/auth_runtime/renew.py`, `server.py:/api/run-scrap` | `████░░` | `██████` | 2026-05-04 23:05 KST 웹 업데이트에서 LinkedIn producer가 `SNS_AUTH_REQUIRED login_required`로 종료됨. `C:\Users\ahnbu\.config\auth\linkedin\storage_state.json`은 존재하지만 마지막 갱신 시각이 2026-04-23 18:01 KST로 확인됐고, 실행 URL이 `https://www.linkedin.com/login`으로 리다이렉트됨. 최신 LinkedIn full 산출물은 2026-05-04 18:38 KST의 313건 상태라 이후 저장글은 반영되지 않음. 후속 작업: 인증 갱신 성공 검증 기준을 명확히 하고, 인증 실패 시 통합 파일 `updated_at`만 바뀌는 상황과 사용자 메시지를 분리해 표시할 것. 관련: BL-0423-01 |
| BL-0505-03 | 2026-05-05 | [보류] 죽은 web 인증 갱신 흐름 제거 | `web_viewer/script.js:showAuthRequiredPanel/authRenewalState`, `server.py:/api/auth/start\|complete\|status`, `scripts/auth_runtime/renew.py:--web/_web 함수`, `scripts/verify_auth_panel_headless.mjs` | `██░░░░` | `████░░` | 2026-04-23 `f1cf901`로 도입된 인증 패널(`/api/auth/*` + `--web` + `showAuthRequiredPanel`) 흐름은 **운영에서 동작하지 않아 2026-05-04 `f7205f4`로 호출이 끊긴 상태**. 함수와 백엔드 API는 남아있으나 누구도 호출하지 않아 죽은 코드. 변경 범위가 약 500~700줄로 광범위해 즉시 실행 시 회귀 리스크가 큼. 보류 결정 (2026-05-05 13:30 KST). 실행 트리거: (1) 새 web 흐름 plan 작성 시점에 함께 정리 / (2) 사용자가 명시적으로 "정리하자" 결정 / (3) 죽은 코드가 실제 운영 문제를 일으킨 경우. 관련 plan: [[20260505_04_web-인증-갱신-흐름-제거]] (보류 상태 표시 포함). 관련: BL-0505-01 (안내문구 정비는 본 항목과 별개로 먼저 가능). |
| BL-0721-01 | 2026-07-21 | X(Twitter) 참여지표 수집 미구현 | `twitter_scrap.py:155-190`, `twitter_scrap_single.py` | `██████` | `████░░` | 실측: `output_total/total_full_20260718.json`의 x 95건 전부 `like_count` 키 자체가 없음. `twitter_scrap.py:155`에서 이미 `legacy` 객체를 파싱 중이고 여기에 `favorite_count`/`reply_count`/`retweet_count`/`quote_count`/`bookmark_count`가 포함되므로 **post_info에 필드만 추가하면 됨**. 3개 플랫폼 중 난이도가 가장 낮음. 스키마 반영 시 `utils/post_schema.py`의 `STANDARD_FIELD_ORDER`에도 지표 필드 위치를 정해야 함(현재 지표 필드가 표준 스키마에 아예 없음). |
| BL-0721-02 | 2026-07-21 | Threads 참여지표 커버리지 확대 (1,129건 중 106건만 보유) | `thread_scrap.py:315-318`, `thread_scrap.py:613-616`, `thread_scrap.py:719-722` | `████░░` | `████░░` | 실측: threads 1,129건 중 `like_count` 키 보유 106건, 값 ≥ 0은 83건뿐. 원인은 "플랫폼 제약"이 아니라 **수집 경로 차이**. `source: "network"` 경로(613-616행)만 실제 값을 채우고, DOM 경로(315-318행, 719-722행)는 `-1`을 하드코딩하며 `legacy_migration` 583건·`consumer_detail` 440건은 키 자체가 없음. 후속: (1) DOM 경로에서도 화면 표시 수치를 파싱할지 결정, (2) `consumer_detail` 상세 수집 경로에 지표 추출 추가, (3) 기존 레거시 건 재수집 필요 여부 판단. |
| BL-0721-03 | 2026-07-21 | LinkedIn 참여지표 수집 경로 신설 | `linkedin_scrap.py`, `scripts/linkedin_opencli_shadow_collect.mjs`, `scripts/linkedin_opencli_shadow_parse.py` | `██░░░░` | `████░░` | 실측: linkedin 613건 전부 지표 없음. `linkedin_scrap.py`에 like/reaction/social 문자열 0회 등장. **주의 — "파서에 필드만 추가하면 된다"는 성립하지 않음**: 보관된 GraphQL raw(`output_linkedin/opencli_shadow/raw/linkedin_opencli_raw_20260707143552_page001.json`, 373KB)를 직접 검사한 결과 `numLikes`/`numComments`/`reactionTypeCounts`/`numShares` 모두 0회, `socialActivityCountsInsight`는 10회 모두 `null`. 즉 저장한 게시물(saved posts) 목록 응답에는 반응 수치가 애초에 실려오지 않음. 지표를 얻으려면 게시물별 상세 요청 또는 DOM 파싱 등 **별도 수집 경로 신설**이 필요하며, 3개 플랫폼 중 난이도가 가장 높음. 관련: BL-0721-01, BL-0721-02 |
| BL-0726-03 | 2026-07-26 | `.omo` 잔재와 OpenCLI 계보 문서 정리 | `.omo/plans/`, `.omo/drafts/`, `.omo/evidence/`, `docs/20260707_04·06`, `docs/20260709_01`, `scripts/linkedin_opencli_shadow_collect.mjs`, `scripts/linkedin_opencli_shadow_parse.py` | `████░░` | `██░░░░` | `.omo`는 현재 사용하지 않는 도구의 산출물인데 정리되지 않고 남아 있다. **실측**: `.omo/` 84개 파일이 전부 git 추적 중(`git ls-files .omo/` → 84; plans 3, drafts 2, evidence 77). `.gitignore:73-76`이 `drafts/`·`evidence/`·`boulder.json`을 등재했으나 **이미 추적 중인 파일에는 gitignore가 적용되지 않아 무효**이며, 정리하려면 `git rm --cached`가 필요하다. `plans/`와 `drafts/`에 같은 이름 파일이 서로 다른 내용으로 존재(예: `linkedin-opencli-owned-window.md` 28KB vs 5.2KB). 디스크 기준 137개 파일 16MB. 계획 3개 모두 OpenCLI 전환 작업인데 OpenCLI는 폐기되고 Playwright로 롤백됨(`docs/20260726_01`). `docs/`에 OpenCLI 계보 문서 3개, `scripts/`에 코드 잔재 2개가 있어 문서만 정리하면 코드와 상태가 어긋나므로 함께 판단 필요. 관련: BL-0726-01(완료) |
| BL-0824-01 | 2026-08-24 | Threads 상세 수집 파서 무력화 (응답 구조 변경 대응) | `utils/threads_parser.py:36`, `thread_scrap_single.py:collect_one`, `scripts/recover_threads_detail_failures.py` | `████░░` | `██████` | **증상**: Threads 상세 수집(`thread_scrap_single.py`)이 모든 대상에서 0건 반환. 2026-08-24 실행 시 42건 일괄 실패로 `fail_count`가 2→3에 도달(백업 복원함). 본문 결측 9건(전부 `media_type=18` 리포스트)도 이 때문에 채워지지 않는다. **원인**: Threads가 응답 구조를 바꿨다. `utils/threads_parser.py:36` `extract_json_from_html()`이 `if "thread_items" not in html_content: return None`으로 즉시 포기하는데, 실패 스냅샷 4건과 **성공 경로로 저장된 스냅샷 10건 모두** `thread_items=0`이다. 게시글은 `"result":{"data"` → `media` 단일 객체로 들어온다. 파서가 지원하는 대체 경로(최상위 `containing_thread`)도 없다 — 같은 이름 키가 있으나 `text_post_app_info.containing_thread` 아래 빈 페이지네이션 토큰이다. **실증**: `@dddesign.io/post/DbkBrexCRqX` 요청 시 서버는 정상 응답했다 → `code=DbkBQE2CXD8`, `user=tofukyung/김재경`, caption 정상. 리다이렉트는 작동하고 파서 입구만 막혔다. **착수 순서**: (1) `tests/fixtures/snapshots/threads_failed/*.html` 4건으로 새 구조 확정 (현재 세션이 남겨둔 실증 자료. `.gitignore` 대상이라 로컬에만 있으며, 유실됐으면 위 (3)의 `recover_threads_detail_failures.py --dry-run`을 돌리면 `collect_one()`이 0건일 때 같은 경로에 자동 재생성된다) → (2) `extract_json_from_html` 앵커 확장 + `extract_items_multi_path`에 `data.media` 경로 추가 → (3) `recover_threads_detail_failures.py --codes DbjwVtCD54j DbkBrexCRqX Dbx5c6pCbx5 DcAPoSAmJgy --min-fail-count 0 --dry-run`으로 검증 (이 스크립트는 실패해도 `fail_count`를 올리지 않는다) → (4) `fail_count>=3`인 35건 재시도. **주의**: 한 페이지에 `"result":{"data"` 블록이 5개 있고 그중 2개가 caption을 보유한다. "어느 블록이 요청한 글인가"를 요청 code/username과 대조해 판정해야 한다. 순서에 의존해 첫 블록을 채택하면 **이번에 X에서 고친 것과 동일한 저자·본문 오염을 Threads에 만든다**. 같은 세션에서 `utils/twitter_cli_adapter.py:select_focal_tweet()`을 fail-safe로 만든 이유가 그것이다. **실행 환경**: python은 `/c/Users/ahnbu/AppData/Local/Microsoft/WindowsApps/python`만 의존성을 갖는다. 병합은 `total_scrap.py --mode update` 대신 `scripts/merge_total_only.py`를 쓴다(전자는 수집기까지 돌려 `fail_count`를 올린다). **보류된 후속**: `thread_scrap_single.py:211-215`의 `is_existing_canonical` 조건 완화(원 계획 B-1)는 상세 수집이 살아난 뒤에야 의미가 있다. 파서 수리와 함께 처리한다. 참고: `_docs/20260824_02_수집데이터-본문결측-중복-오염-수정계획.md` 5.4·5.6절, session `4efc85ee-1841-4417-bd43-ced95062bdc2` |

> **컬럼 기준** — `실행 용이성`: `██████` 쉬움 / `████░░` 보통 / `██░░░░` 어려움 | `기대효과`: `██████` 높음 / `████░░` 보통 / `██░░░░` 낮음. 두 값의 합이 클수록 우선순위 높음. AI 초안이므로 실행 전 재평가 권장.
