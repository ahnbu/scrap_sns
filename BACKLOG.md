# BACKLOG — 후속 작업 후보

현재 작업 범위 밖이지만 나중에 가치 있는 작업을 기록합니다. 새 항목은 표 최하단에 추가합니다.

## BACKLOG 관리 규칙

### 추가 기준 (다음 조건 중 하나 이상)
- 지금 당장 실행하지 않지만 나중에 가치 있는 버그·개선·기술부채
- 발견된 문제 중 현재 작업 범위 밖인 것
- 다음 세션에서 이어받아야 할 미완료 작업

### 제외 기준 (추가하지 않음)
- 사소한 스타일 정리, 타입 힌트 추가 등 임팩트 낮은 것
- 이미 완료된 작업
- 즉시 실행해야 할 것 (바로 처리하거나 TODO 주석으로)

### 상태 표기 규칙
- **완료 항목**: 표에서 행을 삭제한다. 취소선(~~텍스트~~) 사용 금지. 히스토리는 git log로 추적.
- **보류 항목**: 항목명 앞에 `[보류]` 접두어를 붙인다. 명시적 지시가 있기 전까지 착수하지 않는다.
- **저우선 항목**: 항목명 앞에 `[저우선]` 접두어를 붙인다. 리뷰는 완료됐으나 상대적으로 우선순위가 낮아 후순위로 미룬 항목.
- **일반 항목**: 접두어 없음. 우선순위에 따라 착수 가능.

### ID 규칙
- 형식: `BL-MMDD-NN` (MM=월, DD=일, NN=당일 순번 01부터)
- 한 번 부여된 ID는 변경·재사용하지 않는다 (삭제해도 번호 유지)
- 외부 문서에서 참조 시: `BL-0412-01` 형태로 사용

| ID         | 날짜         | 항목                                                 | 위치                                                                 | 실행 용이성   | 기대효과     | 메모                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ---------- | -------------------------------------------------- | ------------------------------------------------------------------ | -------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BL-0412-01 | 2026-04-12 | [보류] Instagram CDN 403                             | `server.py`, `web_viewer/script.js:handleImgError`                 | `██░░░░` | `██████` | wsrv.nl 프록시 미사용 케이스 존재. server.py 이미지 프록시 엔드포인트 또는 local_images 다운로드 파이프라인 확장이 근본 해법                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| BL-0412-02 | 2026-04-12 | [보류] Masonry 성능                                    | `web_viewer/script.js`                                             | `██░░░░` | `████░░` | 975개 포스트 렌더 시 setTimeout 500ms+ 발생. 가상 스크롤 또는 페이지네이션 도입                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| BL-0421-01 | 2026-04-21 | 업데이트 버튼이 "Running..."에서 멈춰 있는 버그 (태그 필터 활성 상태 의심)    | `index.html`, `web_viewer/script.js:/api/run-scrap` 호출부, `server.py:/api/run-scrap` | `████░░` | `████░░` | 증상: 업데이트(신규만) 실행 후 10분 이상 버튼이 "Running..." 상태로 고정. 진단 시점에 실제 `python.exe` 스크래퍼 프로세스는 없었고, `threads_py_full_20260421.json`은 09:44에 이미 저장 완료. 재현 조건 후보: "디자인" 태그 필터 활성화 상태에서 업데이트 실행 → 필터 해제 + 페이지 새로고침하니 "완료"로 전환됨. 원인 후보: (1) `/api/run-scrap` 응답 후 UI polling 종료 신호 누락, (2) 태그 필터링 렌더 루프가 polling 콜백 지연, (3) 프론트 상태 플래그가 서버 응답과 동기화 실패. 재현 시 DevTools Network에서 `/api/run-scrap`, `/api/status`, `/api/posts` 타임라인 확인 필요. 참고 session: `553e1d18-2b55-410f-a2b4-4aa8eb5a3188` |
| BL-0423-01 | 2026-04-23 | [보류] LinkedIn 수동 URL 추가 기능 | `index.html`, `web_viewer/script.js`, `server.py`, `linkedin_scrap.py`, `total_scrap.py` | `██░░░░` | `██████` | LinkedIn 북마크가 저장 목록에 반영되지 않는 경우를 보완하기 위해 헤더에 `+` URL 추가 버튼을 두는 기능. 구현 전 `activity_id` 기준 중복 감지, manual ledger 보존, 전체 재크롤링 시 수동 추가 글 유지, URL 키 기반 태그·즐겨찾기 상태 분리, 동시 쓰기 lock, LinkedIn 도메인 allowlist 검증이 필요하다. 참고: [[docs/20260423_02_LinkedIn-수동-URL-추가-기능-검토]] |
| BL-0504-01 | 2026-05-04 | 실제 플랫폼 저장시간 수집 가능성 조사 | `linkedin_scrap.py`, `thread_scrap.py`, `tests/fixtures/snapshots/`, `utils/post_schema.py` | `██░░░░` | `████░░` | 현재 출력 파일과 원본 스냅샷에는 `saved_at`, `savedAt`, `bookmarked_at` 같은 실제 저장시간 필드가 없음. LinkedIn은 `saved: true`, Threads는 게시글 작성시간(`taken_at`)만 확인됨. 후속 조사에서 저장 목록 네트워크 응답을 더 깊게 추적해 실제 저장시간이 안정적으로 제공되는지 확인하고, 불가하면 `platform_sequence_id`를 "저장 목록 순서"로만 설명해야 함. 참고: `docs/specs/20260504_01_SNS-수집결과-정합성-검증.md` |
| BL-0504-02 | 2026-05-04 | LinkedIn 저장글 업데이트 인증 실패 상태 기록 | `linkedin_scrap.py`, `scripts/auth_runtime/renew.py`, `server.py:/api/run-scrap` | `████░░` | `██████` | 2026-05-04 23:05 KST 웹 업데이트에서 LinkedIn producer가 `SNS_AUTH_REQUIRED login_required`로 종료됨. `C:\Users\ahnbu\.config\auth\linkedin\storage_state.json`은 존재하지만 마지막 갱신 시각이 2026-04-23 18:01 KST로 확인됐고, 실행 URL이 `https://www.linkedin.com/login`으로 리다이렉트됨. 최신 LinkedIn full 산출물은 2026-05-04 18:38 KST의 313건 상태라 이후 저장글은 반영되지 않음. 후속 작업: 인증 갱신 성공 검증 기준을 명확히 하고, 인증 실패 시 통합 파일 `updated_at`만 바뀌는 상황과 사용자 메시지를 분리해 표시할 것. 관련: BL-0423-01 **추가(2026-08-24)**: 인증 만료를 순회 시작 전에 감지하는 프로브 가드는 `_docs/20260824_06` W4 에서 러너 수준으로 구현·검증했다(실제로 배치 6개째에서 만료를 잡아 즉시 중단). 이 항목에 남은 범위는 **웹 업데이트 UI 쪽 표시**다 — 인증 실패와 `updated_at` 만 바뀌는 상황을 사용자에게 구분해 보여주는 것. |
| BL-0505-03 | 2026-05-05 | [보류] 죽은 web 인증 갱신 흐름 제거 | `web_viewer/script.js:showAuthRequiredPanel/authRenewalState`, `server.py:/api/auth/start\|complete\|status`, `scripts/auth_runtime/renew.py:--web/_web 함수`, `scripts/verify_auth_panel_headless.mjs` | `██░░░░` | `████░░` | 2026-04-23 `f1cf901`로 도입된 인증 패널(`/api/auth/*` + `--web` + `showAuthRequiredPanel`) 흐름은 **운영에서 동작하지 않아 2026-05-04 `f7205f4`로 호출이 끊긴 상태**. 함수와 백엔드 API는 남아있으나 누구도 호출하지 않아 죽은 코드. 변경 범위가 약 500~700줄로 광범위해 즉시 실행 시 회귀 리스크가 큼. 보류 결정 (2026-05-05 13:30 KST). 실행 트리거: (1) 새 web 흐름 plan 작성 시점에 함께 정리 / (2) 사용자가 명시적으로 "정리하자" 결정 / (3) 죽은 코드가 실제 운영 문제를 일으킨 경우. 관련 plan: [[20260505_04_web-인증-갱신-흐름-제거]] (보류 상태 표시 포함). 관련: BL-0505-01 (안내문구 정비는 본 항목과 별개로 먼저 가능). |
| BL-0726-03 | 2026-07-26 | `.omo` 잔재와 OpenCLI 계보 문서 정리 | `.omo/plans/`, `.omo/drafts/`, `.omo/evidence/`, `docs/20260707_04·06`, `docs/20260709_01`, `scripts/linkedin_opencli_shadow_collect.mjs`, `scripts/linkedin_opencli_shadow_parse.py` | `████░░` | `██░░░░` | `.omo`는 현재 사용하지 않는 도구의 산출물인데 정리되지 않고 남아 있다. **실측**: `.omo/` 84개 파일이 전부 git 추적 중(`git ls-files .omo/` → 84; plans 3, drafts 2, evidence 77). `.gitignore:73-76`이 `drafts/`·`evidence/`·`boulder.json`을 등재했으나 **이미 추적 중인 파일에는 gitignore가 적용되지 않아 무효**이며, 정리하려면 `git rm --cached`가 필요하다. `plans/`와 `drafts/`에 같은 이름 파일이 서로 다른 내용으로 존재(예: `linkedin-opencli-owned-window.md` 28KB vs 5.2KB). 디스크 기준 137개 파일 16MB. 계획 3개 모두 OpenCLI 전환 작업인데 OpenCLI는 폐기되고 Playwright로 롤백됨(`docs/20260726_01`). `docs/`에 OpenCLI 계보 문서 3개, `scripts/`에 코드 잔재 2개가 있어 문서만 정리하면 코드와 상태가 어긋나므로 함께 판단 필요. 관련: BL-0726-01(완료) |
| BL-0824-05 | 2026-08-24 | [보류] LinkedIn `reactionTypeCounts` 이모지별 분해 수집 | `linkedin_scrap_by_user.py`, `utils/post_schema.py` | `██░░░░` | `██░░░░` | LinkedIn 활동탭 응답의 `SocialActivityCounts` 엔티티에 `reactionTypeCounts` 필드가 **존재한다는 것만 확인**했고(2026-08-24), 내부 구조(좋아요·공감·축하 등 이모지별 분해 형태)는 조사하지 않았다. 현재는 총합만 `like_count`로 수집한다. **보류 근거**: 소비처가 없다 — 뷰어는 총합 배지만 표시하고, 이모지별 분해를 요구하는 기능이 아직 없다. 표준 스키마(`STANDARD_FIELD_ORDER`)에 중첩 객체를 넣는 결정도 함께 필요해 스키마 설계 부담이 있다. **착수 조건**: "어떤 반응이 많은지"를 실제로 보고 싶다는 요구가 생겼을 때. 그때 (1) raw 응답에서 구조 확정 → (2) 별도 필드(`reaction_breakdown` 등)로 담을지 결정 → (3) 뷰어 표시 방식 설계 순으로 진행한다. 관련: BL-0824-02 |
| BL-0824-06 | 2026-08-24 | [보류] 리포스트 흡수 시 원 출처(리포스트한 계정) 뷰어 미표시 | `web_viewer/script.js` 카드 렌더링, `thread_scrap_single.py:222-231`(`redirect_aliases` 생성부) | `████░░` | `██░░░░` | 리포스트 A(예: `@dddesign.io/DbkBrexCRqX`)를 저장했고 원본 B(`@tofukyung/DbkBQE2CXD8`)도 저장돼 있으면, 상세 수집 시 A는 목록에서 제거되고 B의 `redirect_aliases`에 `{code, url, username}`으로만 기록된다. **데이터에는 남지만 뷰어는 `redirect_aliases`를 전혀 읽지 않는다**(`web_viewer/script.js` grep 0건). 결과적으로 "내가 저장한 건 dddesign.io의 리포스트였다"는 사실이 화면에서 사라져, 저장 당시 본 화면과 뷰어가 달라진다. 개선안: 카드에 `↳ @dddesign.io 가 리포스트한 것으로 저장함` 형태의 보조 라인 추가. **실측(2026-08-24)**: `output_threads/python/threads_py_full_20260824.json` 기준 `redirect_aliases` 보유 레코드 10건, 흡수된 A 10건(전부 `output_total/total_full_20260824.json`에서 제거 확인). **보류 근거**: 사용자 판단으로 리포스트 정보는 현재대로 비표시 유지(2026-08-24). 대상이 10건뿐이고 본문·미디어는 원본으로 정상 표시되므로 열람에 지장이 없다. **재개 트리거**: (1) 리포스트 저장 빈도가 늘어 "이 글을 왜 저장했는지" 혼동이 실제로 발생 / (2) BL-0824-07 처리 시 지표 출처 표기가 함께 필요해질 때. 관련: BL-0824-01, BL-0824-07. 참고 session: `b8c63cc4-3441-4a0f-af9b-a2d3d3c23134` |
| BL-0825-01 | 2026-08-25 | `preserve_existing_metrics()` 공용 모듈 승격 (Threads·LinkedIn 중복 제거) | `thread_scrap_single.py:181`, `linkedin_scrap.py:58`, 승격 대상 `utils/post_schema.py` 또는 신규 공용 모듈 | `███░░░` | `██░░░░` | **정정(2026-08-27)**: 복사본은 두 곳이 아니라 **세 곳**이고, 세 번째가 실제로 사고를 냈다. `utils/my_posts_adapter.py:33` `NON_LOGIN_PATH_METRIC_FIELDS` 가 `metrics_updated_at` 을 빠뜨려, 로그인 경로가 매 실행마다 갱신 시각을 수집 시각으로 덮었다 — 신선도 정책이 "방금 읽었다"고 오판해 지표를 영원히 다시 읽지 않는 상태였다(`_docs/20260827_04` P9). 앞의 두 곳은 주석에 그 위험을 정확히 적어놨는데(`thread_scrap_single.py:179-181`) 세 번째만 그 교훈을 못 받았다. **이 항목이 예고한 "한쪽만 고치면 다른 쪽이 조용히 썩는다"가 실현된 사례다.** 2026-08-27 커밋 `c5c1bd4` 에서 세 번째 복사본만 고쳤고 승격은 하지 않았다 — 세 함수는 이름만 비슷하고 판정 규칙이 다르다(앞의 둘은 「새 값이 None 일 때만 이월」, my_posts_adapter 는 「로그인 경로가 안 건드림」이라 **무조건 이월**). 합치려면 매개변수화 설계가 필요하고 잘못 합치면 세 플랫폼 지표가 조용히 망가진다. 지표 보존 가드가 Threads 와 LinkedIn 두 곳에 같은 로직으로 존재한다. LinkedIn 쪽은 `metrics_updated_at` 을 포함해 7개 필드를 보존하고, Threads 쪽은 `METRIC_FIELDS` 6개만 본다. 한쪽만 고치면 다른 쪽이 조용히 썩는다. **주의**: 승격 시 Threads 경로 회귀 검증 범위가 넓어지므로 `tests/unit/test_threads_metrics.py` 와 `tests/unit/test_linkedin_merge_metrics_guard.py` 를 함께 돌려야 한다. 참고: `_docs/20260825_01_LinkedIn-참여지표-비로그인-수집전환-계획.md` 3.6절, session `722d6a8c-c119-4521-b7b8-81052685e5c5` |
| BL-0825-02 | 2026-08-25 | 뷰어 「업데이트」 버튼 비동기화 (동기 블로킹 해소) | `scrap_sns_server.py:/api/run-scrap`, `web_viewer/script.js`, `index.html` | `██░░░░` | `██░░░░` | `/api/run-scrap` 은 자식 프로세스 stdout 을 다 읽을 때까지 응답을 보류하는 **동기 블로킹**이다(`scrap_sns_server.py:1120`). SPEC `_docs/specs/20260825_01` 의 요구사항 **R4** 가 이것을 해소하라고 적었으나, **2026-08-25 사용자 승인으로 이번 구현 범위에서 제외**했다. **제외 근거(실측)**: 일상 경로인 `--mode update` 는 신규 0건일 때 8초에 끝난다(`logs/youtube.log` 2026-08-25 15:06:56~15:07:04). 오래 걸리는 것은 76건 일괄 요약(약 50분)뿐이고 그것은 뷰어 버튼이 아니라 터미널에서 실행한다. **착수 조건**: 실제로 버튼이 묶여 불편이 발생했을 때. 그때 신규 3개(비동기 잡 엔드포인트·상태 폴링·진행률 UI)를 함께 설계한다. 관련: BL-0421-01(버튼이 Running 에서 멈추는 별개 버그). 참고: `_docs/20260825_02_유튜브-요약-파이프라인-재설계-구현계획.md` 3.4절 **🔴 재평가 2 (2026-08-27 20:12)**: **착수 조건이 충족됐다.** `_docs/20260827_04` T5-c 가 내 글 지표 1회 상한을 20→40 으로 올려 consumer 웨이브가 **약 6분** 늘어났다(건당 약 11초 x 33건 실측, 커밋 `c5c1bd4`). 제외 근거였던 "일상 경로는 8초에 끝난다"가 더 이상 성립하지 않는다. **다만 착수는 계속 보류한다** — 상한을 20 으로 되돌리면 약 3분이고 그것은 `utils/metric_refresh.py` 상수 한 줄이라 되돌리기가 싸다. 사용자가 대기를 불편해하면 (1) 상한 되돌리기 (2) 이 항목 착수 중에서 고른다. **재평가(2026-08-27)**: `BL-0826-04` 수정으로 update 모드가 대기분을 요약하게 되면서 버튼이 매 실행 15건을 요약할 수 있게 됐다. 다만 유튜브 대기분을 이번에 전량 소진하면 평시 실행에서 요약 대상은 신규분뿐이라 증상이 사라진다. 대기분이 다시 쌓이는 경우(재생목록에 15건 넘게 저장)에만 재부상하므로 착수 조건은 그대로 둔다. 참고: `_docs/20260827_01` |
| BL-0825-03 | 2026-08-25 | 타 플랫폼 전량 재수집 시 「저장순」 정렬 붕괴 | `total_scrap.py:save_total`, `thread_scrap.py`, `linkedin_scrap.py`, `twitter_scrap.py` | `████░░` | `████░░` | 「저장순」 1차 정렬 키가 `crawled_at`(수집 시각)이다. 한 번에 전량 재수집하면 `crawled_at` 이 한 시점으로 뭉쳐 순서를 만들지 못하고, 남는 초 단위 차이는 **처리 순서**(플랫폼 API 반환 순서)라 실제 저장 순서와 무관해진다. **실측 선례**: 유튜브 81건이 정확히 이 경로로 무너져 뷰어 순서와 실제 저장 순서의 스피어만 상관이 **−1.000**(완전 역순)이었다. 유튜브는 `playlist_added_at` 이라는 진짜 저장 시각을 API 가 줘서 `_saved_at_key()` 분기로 해결했다(`_docs/20260825_02` 3.7절). **Threads·LinkedIn·X 는 저장 시각을 얻을 방법이 아직 없다** — 그래서 같은 해법을 못 쓴다. **착수 조건**: 전량 재수집을 실제로 돌리기 전. 선행 조사는 BL-0504-01(실제 플랫폼 저장시간 수집 가능성)과 같은 문제다 — 그것이 풀리면 이 항목도 같이 풀린다. 관련: BL-0504-01 |

| BL-0826-01 | 2026-08-26 | 내 Threads 게시물 수집 (본문·노출수) | `my_threads_scrap.py`(신규), `utils/my_threads_adapter.py`(신규), `total_scrap.py` consumer 웨이브·`merge_results()`, `utils/metric_refresh.py`, `.gitignore` | `████░░` | `█████░` | **계획서 있음**: `_docs/20260827_02_내-쓰레드-글-수집-계획.md`. 내 게시물 성과지표 통합(`_docs/20260826_03`) 1차 범위에서 의도적으로 제외했던 항목이며, 2026-08-27 세션에서 Threads API를 직접 호출해 조사한 뒤 계획을 확정했다. **실측(2026-08-27, API 직접 호출)**: 토큰 유효(`~/.claude/settings.json` `mcpServers.threads`) · `get_my_threads` 34건 반환 · 그중 리포스트(`REPOST_FACADE`) 2건 제외하면 **내 원본 글 32건** · 기간 2025-12-19~2026-08-25 · 노출수(`views`·`likes`·`replies`·`reposts`·`quotes`) 정상 조회 · 시각은 **UTC**(`+0000`) 반환. **🔴 최대 난점은 본문 잘림이다**: Threads 본문 상한이 499자라 사용자는 넘치는 분량을 자기 답글로 이어 쓴다. `get_my_threads`는 최상위 글만 주므로 그대로 수집하면 **32건 중 21건(66%)의 본문이 끊긴 채 저장되고 6,905자가 누락된다**. 실제 사례: 2026-08-25 글이 "아래 댓글 확인해주세요 👇"에서 끝난다. **해결**: 글마다 `get_thread_replies`를 호출해 (1) 작성자가 내 계정이고 (2) 원글 이후 **30분 이내**인 답글만 시간순으로 이어붙이고 `is_merged_thread: true`를 찍는다 — 이 필드와 병합 방식은 이미 저장글에서 쓰고 있다(쓰레드 저장글 1,273건 중 1,187건이 `is_merged_thread: true`). **기각한 판별법 2가지**: `replied_to` 필드는 `fields`로 요청하면 오지만 본문 이어쓰기·남의 댓글·9시간 뒤 대화형 답글이 **전부 원글을 가리켜** 구분 불가. "남이 댓글 달기 전에 올린 것" 규칙은 23개 중 21개만 맞아 2026-05-15 본문 보충 1건을 놓친다. **30분 근거**: 자기 답글 23개 실측에서 본문 이어쓰기 22개가 0~11.6분, 대화형 답글 1개가 551.7분 — 아래 여유 18분·위 여유 522분. 표본이 1건뿐이라 이어붙인 내용을 로그에 남겨 반례를 잡는다. **UTC→KST 변환은 어댑터에서 끝낸다** — 이 레포는 전 플랫폼이 KST naive 문자열로 저장한다(`utils/common.py:109` `datetime.fromtimestamp()`가 머신 로컬 사용). 하류에 미루면 날짜 정렬·필터·검색이 9시간 어긋난다. **⚠️ 기존 착수 조건("LinkedIn 경로가 최소 2회 업데이트에서 노출수 정상 반영") 폐기**: 조건과 그 근거가 연결되지 않는다. 근거는 "두 플랫폼 실패 모드가 섞이면 원인 분리가 어렵다"였는데, 원인 분리는 이미 `utils/auth_status.py:20` `emit_auth_required()`가 `{"platform": ..., "scope": ...}`를 찍어 해결돼 있다(`my_posts_scrap.py:126`). 또 "2회 **정상**"은 성공 2회를 요구할 뿐 만료 상황을 겪게 하지 않아 검증하는 바가 없다. **대신 요구하는 것**: 쓰레드 수집기도 실패 시 `emit_auth_required("threads", ...)`를 반드시 낼 것(계획 T1 필수 요건). 정정은 2026-08-27 사용자 결정. **구조는 LinkedIn 선례 복제**: `my_posts_scrap.py`(188줄)처럼 직접 수집하지 않고 `sns_insight_update`의 수집기를 호출해 산출물만 받는다. MCP 서버는 `threads_mcp_runner`가 `subprocess`로 자기 안에서 띄우므로 scrap_sns 코드에 MCP 의존이 번지지 않는다. 관련: `_docs/20260826_03`, `BL-0827-01`(2단계). 참고 session: `c31b5cef-2f02-4063-8cd7-75139cfe5bfd` |
| BL-0826-02 | 2026-08-26 | e2e 폴더 일괄 실행 시 sync Playwright · asyncio 루프 충돌로 17건 ERROR | `tests/e2e/*`, `pytest.ini` | `████░░` | `████░░` | **증상**: `python -m pytest tests/e2e -q` 로 e2e 전체를 한 번에 돌리면 17건이 ERROR 로 죽는다. 개별 파일로 나눠 실행하면 전부 통과한다. 오류는 `playwright._impl._errors.Error: It looks like you are using Playwright Sync API inside the asyncio loop.` 이며, 발생 위치는 sync Playwright 를 쓰는 모듈들의 `browser` fixture(`with sync_playwright() as p:`)다 — `tests/e2e/test_metric_layout_ui.py:63`, `test_author_filter_ui.py`, `test_tag_suggestion_ui.py`, `test_viewer_console_cleanup_ui.py`. **재현 최소 조합(2026-08-26 실측)**: `python -m pytest tests/e2e/test_web_viewer_e2e.py tests/e2e/test_author_filter_ui.py -q` → `4 passed, 2 skipped, 13 errors`. 이번 W1·W1-T 변경과 무관한 **기존 결함**이며, 손대지 않은 파일 조합으로 재현을 확인했다. **원인 가설**: async 기반 e2e 모듈(pytest-asyncio)이 이벤트 루프를 실행 중인 상태로 남기고, 뒤이어 실행되는 sync Playwright fixture 가 그 루프 안에서 초기화되면서 거부된다. `pytest.ini` 의 `asyncio_default_fixture_loop_scope` 설정도 함께 확인이 필요하다. **왜 고쳐야 하나**: 지금은 "폴더 통째 실행"이 사실상 불가능해, 회귀가 개별 실행 조합을 잘 골라야만 드러난다 — 실제 결함이 초록불 뒤에 숨는다. **해결 방향 후보**: (1) sync·async 모듈을 분리 실행하는 마커/설정 (2) sync Playwright fixture 를 async API 로 통일 (3) 루프를 남기는 async 테스트의 fixture scope 정정. **주의**: `tests/e2e` 는 실행 중인 5000번 뷰어 서버를 전제로 하고 브라우저는 headless 로 고정돼 있다 — 그 전제를 깨지 않는다. 참고 session: `7d3daa6a-72b3-4ae5-8431-95717010b657` |
| BL-0826-05 | 2026-08-26 | 포트를 안전 구간 17600 블록으로 이전 (3000 공유 + 9222 크롬 디버그 기본값) | `grep -rn "3000\|5000\|5002\|9222" --include="*.py" --include="*.js" --include="*.mjs" --include="*.json"` 로 확인 | `███░░░` | `████░░` | 예약 구간 위험은 없지만 두 가지가 걸린다. (1) 3000번을 link-cleaner·my_bookstation·open-design·ticktick-clone과 공유한다. (2) **9222는 크롬 원격 디버깅 기본 포트**라 Playwright·chrome-devtools MCP 같은 도구가 그 포트를 잡으면 바로 부딪힌다. 대장에 17600(백업 17601·17602)이 예약돼 있다. 배경·근거: `D:/vibe-coding/_docs/20260826_02_포트-안전구간-이전과-포트대장-구축-실행계획(실행완료).md`. 포트 대장: `D:/vibe-coding/_ports/PORTS.md`. 참고 session: `5caad60c-dd1f-4469-bd83-62193a684d0c` |
| BL-0827-01 | 2026-08-27 | 내 게시물 교차게시 연결 (`cross_post_id`) — LinkedIn·Threads 동일 원고를 한국시간 발행일로 자동 짝짓기 | `utils/post_schema.py`, `utils/common.py`, `utils/cross_post.py`(신규), `total_scrap.py` `merge_results()`, `utils/post_meta.py`, `utils/query-sns.mjs`, `web_viewer/script.js` `createCard()` | `████░░` | `█████░` | **2단계 작업. 착수 조건은 `BL-0826-01`(내 Threads 게시물 수집) 완료다.** 같은 원고를 LinkedIn과 Threads에 함께 올리는데 두 레코드가 서로를 몰라 성과 비교가 화면에서 불가능하다. **실측(2026-08-27, Threads API 직접 호출)**: 리포스트 제외 내 Threads 원본 글 32건, LinkedIn 내 글 36건(36일, 하루 2건 올린 날 0일). 한국시간 날짜로 짝지으면 **32쌍 전부 매칭, Threads만 있는 날 0일**. LinkedIn만 있는 날 4일(2025-12-09·11·13·18)은 전부 Threads 시작 전이라 정상. 즉 발행일이 사실상 고유 번호라 사람이 짝을 지정할 필요가 없다. **확정 설계**: 레코드를 합치지 않고 평평한 문자열 필드 `cross_post_id` 하나로 묶는다. 값은 `xp:<KST 발행일>` — 별도 상태 저장 없이 결정적으로 재계산되므로 통합본을 재생성해도 안정적이고 마이그레이션이 없다. **가드**: 같은 KST 날짜에 같은 플랫폼 글이 2건 이상이면 그 날짜 전체 부여를 보류하고 경고 로그를 남긴다. **레코드 병합은 기각**: 별표·메모 12건(`post_key` 기준)과 태그 2,147건(URL 기준)의 키가 바뀌어 귀속이 불명해지고, `web_viewer/script.js:2115` 플랫폼 필터와 상단 집계가 깨진다. 지표 합산도 `web_viewer/script.js:214`가 같은 이유로 이미 기각한 방식이다. **본문 유사도는 매칭 기준으로 쓰지 않는다** — 확인용 리포트로만 쓴다. **⚠️ 유사도는 `BL-0826-01` 완료 후에 다시 재야 한다**: 이전 측정(같은 날 쌍 30건 평균 0.77·최저 0.34)은 Threads 본문이 자기 답글 부분만큼 잘린 상태에서 잰 값이라 실제보다 낮다. 본문 이어붙이기가 끝난 뒤 재측정한다. **시간대**: LinkedIn은 KST naive, Threads Graph API는 UTC를 준다. 변환은 이 작업이 아니라 `BL-0826-01`의 어댑터에서 끝나므로, 짝짓기는 `date` 문자열 비교만 하면 된다. **1단계를 먼저 하는 것이 이 작업에 이득이다** — 짝지을 상대가 생겨 검증이 가능해지고, 유사도 확인도 의미를 갖는다. 계획: 별도 계획서로 작성 예정. 확정 설계 근거는 `_docs/20260827_02_내-쓰레드-글-수집-계획.md` 10절과 `_docs/plan-check/20260827_02_내-게시물-교차게시-연결-계획_검수보고서*.md`(이전 판 검수 2건). 참고 session: `c31b5cef-2f02-4063-8cd7-75139cfe5bfd` |
| BL-0827-02 | 2026-08-27 | 뷰어 내 글(MY) 필터와 플랫폼 탭 단일화 (X 0건 및 탭 고착 해소) | `index.html`, `web_viewer/script.js`, `tests/e2e/test_my_posts_filter_ui.py` | `█████░` | `██████` | MY 버튼이 독립 토글(AND 결합)로 동작하여 X 클릭 시 0건, Threads 클릭 시 내 글 32건만 노출, All 클릭 시 내 글 지속 노출되던 연쇄 이상 현상을 상단 필터 바 단일 탭으로 통합하여 해소. All/플랫폼 탭에서는 내 글 제외(!isOwnPost), Favorites 탭에서는 별표 글 전체 허용, MY 탭에서는 내 글(68건)만 노출. 계획서: `_docs/20260827_05_뷰어-내글필터-플랫폼탭-단일화-개선계획.md`. 검수보고서 2회 완료. **🔴 선후 관계(2026-08-27 20:12)**: `_docs/20260827_04`(커밋 `c5c1bd4` 실행완료) **다음에** 실행한다 — 04 는 저장 데이터를, 이 계획은 화면을 바꾸므로 데이터가 먼저 확정돼야 한다. **04 가 넘긴 요구사항 1건**: 탭 단일화로 `tests/e2e/test_my_posts_filter_ui.py` 를 개편할 때 **내 글 정렬 최신순을 단언하는 테스트를 반드시 남길 것**. 개편 목록에서 `test_my_posts_newest_first_in_viewer` 가 빠져 있었고, 그 테스트는 MY 토글과 플랫폼 칩을 동시에 걸어야 성립해 탭 상호배타 모델에서는 조작 자체가 불가능해진다. 대체 단언은 「MY 탭 상단 = 전체 내 글 중 최신」이며 04 의 T2 가 두 플랫폼을 날짜순으로 섞어 그것이 성립한다. 상세는 계획서 3장 (3.1) 절. **또한 `tests/e2e/test_own_post_interleave_ui.py` 4건도 `#myPostsBtn` 을 쓰므로 함께 개편 대상이다.** |
> **컬럼 기준** — `실행 용이성`: `██████` 쉬움 / `████░░` 보통 / `██░░░░` 어려움 | `기대효과`: `██████` 높음 / `████░░` 보통 / `██░░░░` 낮음. 두 값의 합이 클수록 우선순위 높음. AI 초안이므로 실행 전 재평가 권장.
| BL-0901-01 | 2026-09-01 | 뷰어 총건수 headless 검증 스크립트의 낡은 전제 수정 (파일 2604건 vs 화면 2533건, 항상 실패) | `scripts/verify_viewer_total_count_headless.mjs`, `web_viewer/script.js` `updateTotalPostsLabel()` | `█████░` | `███░░░` | **증상**: 스크립트가 뷰어 상단 총건수(`#totalPostsCount`)와 최신 `output_total/total_full_*.json` 게시글 수가 같아야 통과하도록 만들어졌으나(2026-08-24 커밋) 항상 exit 1. **원인**: 그 숫자를 만드는 `updateTotalPostsLabel()` 주석이 "지금 화면에 몇 개가 걸렸는지만 말한다"고 설계를 명시한다(출처 `_docs/20260827_05`). 총건수는 파일 전체 수가 아니라 현재 필터 적용 결과 수다. **실측(2026-09-01)**: 파일 2604건, `/api/posts?limit=5000` 도 정확히 2604건 반환, 화면 표시만 2533건. 차이 71건인데 `sns_user_metadata.json` 숨김은 3건뿐이라 나머지 68건의 출처를 먼저 규명해야 한다. **할 일**: (1) 71건이 빠지는 기본 필터를 규명한다. (2) 검증 기준을 "필터 적용 후 기대 수와 비교" 또는 "필터를 모두 해제한 뒤 파일 전체 수와 비교" 중 하나로 고친다. **주의**: 서버는 `wscript sns_hub.vbs` 대신 `python scrap_sns_server.py` 를 백그라운드로 띄운다 — 런처는 브라우저 탭까지 열어 사용자 작업을 방해한다. 검증은 headless 로만 한다. 출처: `_docs/20260901_01_링크드인-이미지-복구와-외부요약-자동화-아이콘-개선-계획.md` |
