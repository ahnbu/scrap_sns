# SNS 신규 샘플 정합성 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 수집 완료 후 플랫폼별 신규 글 샘플이 로컬 서버의 화면용 데이터에 실제 반영됐을 때만 업데이트 완료로 인정한다.

**Architecture:** `/api/run-scrap` 실행 전후의 최신 통합 JSON을 비교해 플랫폼별 신규 샘플을 최대 3개씩 만든다. 웹 뷰어는 수집 후 이미 호출하는 `/api/posts` 결과에서 샘플 키 존재 여부만 메모리 비교하고, 정상 통과 시 정합성 메시지를 숨긴다.

**Tech Stack:** Python Flask, JSON output files, vanilla JavaScript, pytest, headless Playwright verification runner.

---

## Source Spec

- `D:\vibe-coding\scrap_sns\docs\specs\20260504_01_SNS-수집결과-정합성-검증.md`
- Current reference implementation: commit `83d2617 feat(consistency): 신규 수집 샘플 정합성 확인 추가`

---

## Execution Result

- Status: implemented and verified
- Implementation commit: `83d2617 feat(consistency): 신규 수집 샘플 정합성 확인 추가`
- Documentation update commit: pending at the time this section was written
- Implemented behavior:
  - `/api/run-scrap` returns `consistency_probe.new_counts` and `consistency_probe.new_samples`.
  - Server compares the before/after total JSON files and samples up to 3 new posts per platform.
  - Viewer checks the samples against the already reloaded `/api/posts` data without calling `/api/search`.
  - Normal pass hides the consistency section.
  - Missing samples or a 3-second timeout changes the modal title to `업데이트 확인 필요`.
- Verification run:
  - `pytest tests/unit/test_web_viewer_scrap_result_helpers.py -q` -> 6 passed
  - `pytest tests/integration/test_run_scrap_stats.py -q` -> 5 passed
  - `pytest tests/integration/test_posts_api.py tests/integration/test_search_api.py -q` -> 16 passed
  - `node scripts\verify_auth_panel_headless.mjs` -> `ok: true`
- Performance run:
  - Current local data: 1,222 posts
  - New sample check: 3 samples
  - Click-to-check elapsed time: 452ms
  - `/api/search` calls during consistency check: 0
  - Normal pass consistency section visible: false

---

## Persistence Surface

- DB tables: 없음
- Cache/index files: `server.py`의 `_POSTS_CACHE` 읽기 캐시만 사용. 캐시 스키마 변경 없음
- Output JSON schema: 기존 `output_total/total_full_*.json` 파일은 읽기만 한다. 파일에 새 필드를 쓰지 않는다
- API response surface: `/api/run-scrap` 응답에 `consistency_probe.new_counts`, `consistency_probe.new_samples`를 추가한다
- Browser state: 기존 `localStorage` 구조 변경 없음
- Existing data migration: 필요 없음. 기존 출력 파일과 DB를 수정하지 않는다
- Migration command: 없음
- Migration verification command: 없음

---

## File Structure

- Modify: `D:\vibe-coding\scrap_sns\server.py`
  - 수집 전 최신 통합 파일 payload를 읽는 helper를 추가한다.
  - `platform_id` 또는 canonical URL 기반 샘플 키를 만든다.
  - 수집 후 신규 글을 플랫폼별로 분류하고 최대 3개 샘플을 `/api/run-scrap` 응답에 포함한다.
- Modify: `D:\vibe-coding\scrap_sns\web_viewer\script.js`
  - 수집 후 `fetchData()` 결과에서 신규 샘플 키가 존재하는지 확인한다.
  - 기본 검사에서 `/api/search`를 호출하지 않는다.
  - 정상 통과 시 정합성 섹션을 숨기고, 실패 또는 3초 초과 시에만 `업데이트 확인 필요`를 표시한다.
- Modify: `D:\vibe-coding\scrap_sns\index.html`
  - 정렬 UI 용어를 `작성일순`, `로컬 수집순`으로 바꾼다.
- Modify: `D:\vibe-coding\scrap_sns\README.md`
  - `created_at`, `crawled_at`, `sequence_id`, `platform_sequence_id`, `platform_saved_at` 의미를 문서화한다.
- Modify: `D:\vibe-coding\scrap_sns\BACKLOG.md`
  - 실제 플랫폼 저장시간 수집 가능성 조사를 백로그로 분리한다.
- Modify: `D:\vibe-coding\scrap_sns\tests\integration\test_run_scrap_stats.py`
  - 서버 응답의 신규 샘플 계약을 검증한다.
- Modify: `D:\vibe-coding\scrap_sns\tests\unit\test_web_viewer_scrap_result_helpers.py`
  - 뷰어 view model과 정합성 helper를 검증한다.
- Modify: `D:\vibe-coding\scrap_sns\scripts\verify_auth_panel_headless.mjs`
  - 브라우저 흐름에서 정상 통과 시 정합성 섹션이 숨겨지고 `/api/search`가 호출되지 않는지 확인한다.

---

## Task 1: Server New-Sample Contract

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\server.py`
- Test: `D:\vibe-coding\scrap_sns\tests\integration\test_run_scrap_stats.py`

- [x] **Step 1: Write failing integration test for platform samples**

Add this test to `tests/integration/test_run_scrap_stats.py`. It must create a before file with existing LinkedIn/Threads posts, then an after file with 4 new LinkedIn posts, 2 new Threads posts, and 1 new X post.

```python
def test_run_scrap_response_includes_platform_new_samples(client, tmp_path, monkeypatch):
    import server

    monkeypatch.setattr(server, "OUTPUT_TOTAL_DIR", str(tmp_path))
    _write_total_file(
        tmp_path,
        "20260416",
        total=2,
        threads=1,
        linkedin=1,
        twitter=0,
        posts=[
            {"sequence_id": 1, "platform_id": "li-old", "sns_platform": "linkedin", "url": "https://www.linkedin.com/feed/update/urn:li:activity:1", "full_text": "Old LinkedIn post"},
            {"sequence_id": 2, "platform_id": "th-old", "sns_platform": "threads", "url": "https://www.threads.com/@user/post/th-old", "full_text": "Old Threads post"},
        ],
    )

    def write_after_file():
        _write_total_file(
            tmp_path,
            "20260417",
            total=9,
            threads=3,
            linkedin=5,
            twitter=1,
            posts=[
                {"sequence_id": 1, "platform_id": "li-old", "sns_platform": "linkedin", "url": "https://www.linkedin.com/feed/update/urn:li:activity:1", "full_text": "Old post"},
                {"sequence_id": 2, "platform_id": "th-old", "sns_platform": "threads", "url": "https://www.threads.com/@user/post/th-old", "full_text": "Old Threads post"},
                {"sequence_id": 3, "platform_id": "li-new-1", "sns_platform": "linkedin", "url": "https://www.linkedin.com/feed/update/urn:li:activity:3", "full_text": "New LinkedIn post 1"},
                {"sequence_id": 4, "platform_id": "li-new-2", "sns_platform": "linkedin", "url": "https://www.linkedin.com/feed/update/urn:li:activity:4", "full_text": "New LinkedIn post 2"},
                {"sequence_id": 5, "platform_id": "li-new-3", "sns_platform": "linkedin", "url": "https://www.linkedin.com/feed/update/urn:li:activity:5", "full_text": "New LinkedIn post 3"},
                {"sequence_id": 6, "platform_id": "li-new-4", "sns_platform": "linkedin", "url": "https://www.linkedin.com/feed/update/urn:li:activity:6", "full_text": "New LinkedIn post 4"},
                {"sequence_id": 7, "platform_id": "th-new-1", "sns_platform": "threads", "url": "https://www.threads.com/@user/post/th-new-1", "full_text": "New Threads post 1"},
                {"sequence_id": 8, "platform_id": "th-new-2", "sns_platform": "threads", "url": "https://www.threads.com/@user/post/th-new-2", "full_text": "New Threads post 2"},
                {"sequence_id": 9, "platform_id": "x-new-1", "sns_platform": "x", "url": "https://x.com/user/status/x-new-1", "full_text": "New X post 1"},
            ],
        )
        return 0

    with patch("subprocess.Popen", return_value=_make_process(write_after_file)):
        resp = client.post("/api/run-scrap", json={"mode": "update"})

    assert resp.status_code == 200
    consistency = resp.get_json()["consistency_probe"]
    assert consistency["new_counts"] == {"threads": 2, "linkedin": 4, "twitter": 1}
    assert [sample["platform_id"] for sample in consistency["new_samples"]["linkedin"]] == ["li-new-4", "li-new-3", "li-new-2"]
    assert [sample["platform_id"] for sample in consistency["new_samples"]["threads"]] == ["th-new-2", "th-new-1"]
    assert [sample["platform_id"] for sample in consistency["new_samples"]["twitter"]] == ["x-new-1"]
```

- [x] **Step 2: Run the failing test**

Run:

```powershell
pytest tests/integration/test_run_scrap_stats.py::test_run_scrap_response_includes_platform_new_samples -q
```

Expected before implementation: fail with `KeyError: 'new_counts'`.

- [x] **Step 3: Implement server helpers**

Add these helpers near `_read_latest_metadata()` in `server.py`.

```python
def _read_latest_total_payload():
    try:
        latest_file = _get_latest_total_file()
    except FileNotFoundError:
        return None
    with open(latest_file, 'r', encoding='utf-8-sig') as f:
        data = json.load(f)
    return {"path": latest_file, "metadata": data.get("metadata") or {}, "posts": data.get("posts") or []}


def _consistency_platform(value):
    platform = str(value or "").strip().lower()
    if platform in {"threads", "thread"}:
        return "threads"
    if platform in {"linkedin", "linked_in"}:
        return "linkedin"
    if platform in {"x", "twitter", "x/twitter", "x_twitter"}:
        return "twitter"
    return platform


def _consistency_post_key(post):
    platform = _consistency_platform(post.get("sns_platform"))
    post_id = str(post.get("platform_id") or post.get("code") or "").strip()
    if platform and post_id:
        return f"{platform}:id:{post_id}"
    post_url = post.get("canonical_url") or canonicalize_url(post) or post.get("url") or post.get("post_url") or post.get("source_url") or ""
    post_url = str(post_url).strip()
    if platform and post_url:
        return f"{platform}:url:{post_url}"
    return ""


def _consistency_sample(post):
    return {
        "sequence_id": post.get("sequence_id"),
        "platform_id": post.get("platform_id") or post.get("code"),
        "sns_platform": _consistency_platform(post.get("sns_platform")),
        "url": post.get("canonical_url") or canonicalize_url(post) or post.get("url"),
        "display_name": post.get("display_name") or post.get("username") or post.get("user"),
    }
```

- [x] **Step 4: Modify `_build_consistency_probe()`**

Change the signature to accept `before_posts=None`. Keep the existing metadata, platform count, `probe`, and `source_file` logic. Insert the following block after the existing `platform_counts` fallback calculation and before the existing `probe_post` selection.

```python
before_keys = {
    key
    for key in (_consistency_post_key(post) for post in (before_posts or []))
    if key
}
new_posts_by_platform = {"threads": [], "linkedin": [], "twitter": []}
for post in posts:
    key = _consistency_post_key(post)
    platform = _consistency_platform(post.get("sns_platform"))
    if not key or key in before_keys or platform not in new_posts_by_platform:
        continue
    new_posts_by_platform[platform].append(post)

for platform_posts in new_posts_by_platform.values():
    platform_posts.sort(key=lambda post: post.get("sequence_id") or 0, reverse=True)

new_counts = {
    platform: len(platform_posts)
    for platform, platform_posts in new_posts_by_platform.items()
}
new_samples = {
    platform: [_consistency_sample(post) for post in platform_posts[:3]]
    for platform, platform_posts in new_posts_by_platform.items()
}
```

Then add the two keys below to the existing return dictionary.

```python
"new_counts": new_counts,
"new_samples": new_samples,
```

- [x] **Step 5: Pass before posts into `/api/run-scrap`**

In `run_scrap()`, replace the existing before metadata read with payload read.

```python
before_payload = _read_latest_total_payload()
before_meta = (before_payload or {}).get("metadata")
consistency_probe = _build_consistency_probe((before_payload or {}).get("posts") or [])
```

- [x] **Step 6: Verify server tests**

Run:

```powershell
pytest tests/integration/test_run_scrap_stats.py -q
```

Expected: `5 passed`.

---

## Task 2: Viewer Quiet Consistency Check

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\web_viewer\script.js`
- Test: `D:\vibe-coding\scrap_sns\tests\unit\test_web_viewer_scrap_result_helpers.py`

- [x] **Step 1: Write failing test for hidden passed result**

Add a JS helper extraction test that passes `consistency_check.status = 'passed'` and expects no visible consistency rows.

```python
def test_build_scrap_result_view_model_hides_passed_consistency_steps():
    node_script = textwrap.dedent(
        r"""
        const fs = require('fs');
        const src = fs.readFileSync('web_viewer/script.js', 'utf8');

        function extractFunction(name) {
          const patterns = [`async function ${name}(`, `function ${name}(`];
          let start = -1;
          for (const pattern of patterns) {
            start = src.indexOf(pattern);
            if (start !== -1) break;
          }
          let depth = 0;
          let end = -1;
          for (let i = start; i < src.length; i += 1) {
            if (src[i] === '{') depth += 1;
            if (src[i] === '}') {
              depth -= 1;
              if (depth === 0) {
                end = i + 1;
                break;
              }
            }
          }
          return src.slice(start, end);
        }

        global.authPlatformLabels = { linkedin: 'LinkedIn', threads: 'Threads', x: 'X' };
        eval(extractFunction('normalizeAuthPlatform'));
        eval(extractFunction('getAuthRequiredPlatforms'));
        eval(extractFunction('getFailedPlatforms'));
        eval(extractFunction('getScrapStats'));
        eval(extractFunction('buildAuthRenewalPrompt'));
        eval(extractFunction('buildScrapResultViewModel'));

        const model = buildScrapResultViewModel({
          status: 'success',
          stats: { total: 2, threads: 1, linkedin: 1, twitter: 0, total_count: 1222, threads_count: 844, linkedin_count: 312, twitter_count: 66 },
          consistency_check: { status: 'passed', steps: [{ key: 'new_samples', label: '신규 샘플', status: 'passed', detail: '3개 확인' }] }
        }, 'update');

        console.log(JSON.stringify({
          resultTitle: model.title,
          title: model.consistencyTitle,
          status: model.consistencyStatus,
          rowCount: model.consistencyRows.length
        }));
        """
    )

    assert run_node_json(node_script) == {
        "resultTitle": "업데이트 완료",
        "title": "",
        "status": "passed",
        "rowCount": 0,
    }
```

- [x] **Step 2: Write failing test for missing sample**

Add a unit test that calls `verifyScrapConsistency()` with one LinkedIn sample and an empty posts list.

```python
def test_verify_scrap_consistency_reports_missing_new_sample():
    node_script = textwrap.dedent(
        r"""
        const fs = require('fs');
        const src = fs.readFileSync('web_viewer/script.js', 'utf8');
        function extractFunction(name) {
          const patterns = [`async function ${name}(`, `function ${name}(`];
          let start = -1;
          for (const pattern of patterns) {
            start = src.indexOf(pattern);
            if (start !== -1) break;
          }
          let depth = 0;
          let end = -1;
          for (let i = start; i < src.length; i += 1) {
            if (src[i] === '{') depth += 1;
            if (src[i] === '}') {
              depth -= 1;
              if (depth === 0) {
                end = i + 1;
                break;
              }
            }
          }
          return src.slice(start, end);
        }

        eval(extractFunction('resolvePostUrl'));
        eval(extractFunction('deriveConsistencyStatus'));
        eval(extractFunction('normalizeConsistencyPlatform'));
        eval(extractFunction('getConsistencyPostKey'));
        eval(extractFunction('verifyScrapConsistency'));

        (async () => {
          const result = await verifyScrapConsistency({
            new_samples: {
              linkedin: [{ platform_id: 'li-new-1', sns_platform: 'linkedin', url: 'https://www.linkedin.com/feed/update/urn:li:activity:1' }],
              threads: [],
              twitter: []
            }
          }, { ok: true, posts: [] });
          console.log(JSON.stringify(result));
        })();
        """
    )

    assert run_node_json(node_script) == {
        "status": "failed",
        "steps": [{"key": "new_samples_linkedin", "label": "LinkedIn 신규 샘플", "status": "failed", "detail": "1/1개 누락"}],
    }
```

- [x] **Step 3: Run failing unit tests**

Run:

```powershell
pytest tests/unit/test_web_viewer_scrap_result_helpers.py::test_build_scrap_result_view_model_hides_passed_consistency_steps -q
pytest tests/unit/test_web_viewer_scrap_result_helpers.py::test_verify_scrap_consistency_reports_missing_new_sample -q
```

Expected before implementation: first test shows the passed consistency section is still visible, second test fails because `verifyScrapConsistency` does not understand `new_samples`.

- [x] **Step 4: Implement quiet view model behavior**

In `buildScrapResultViewModel()`, only expose consistency rows for `failed`, `warning`, or `timeout`.

```javascript
const consistencyCheck = result?.consistency_check || null;
const consistencyStatus = consistencyCheck?.status || '';
const shouldShowConsistency = ['failed', 'warning', 'timeout'].includes(consistencyStatus);
const consistencyRows = shouldShowConsistency && Array.isArray(consistencyCheck?.steps)
    ? consistencyCheck.steps.map(step => ({
        label: step.label || step.key || '',
        status: step.status || 'skipped',
        detail: step.detail || ''
    }))
    : [];
const needsConsistencyReview = shouldShowConsistency;
```

- [x] **Step 5: Implement sample key comparison**

Add these functions near `deriveConsistencyStatus()`.

```javascript
function normalizeConsistencyPlatform(platform) {
    const value = String(platform || '').toLowerCase();
    if (value === 'x') return 'twitter';
    if (value === 'thread') return 'threads';
    return value;
}

function getConsistencyPostKey(post) {
    if (!post) return '';
    const platform = normalizeConsistencyPlatform(post.sns_platform);
    const postId = String(post.platform_id || post.code || '').trim();
    if (platform && postId) return `${platform}:id:${postId}`;

    const postUrl = resolvePostUrl(post);
    if (platform && postUrl) return `${platform}:url:${postUrl}`;
    return '';
}
```

- [x] **Step 6: Implement `verifyScrapConsistency()` for `new_samples`**

Use the already loaded posts. Do not call `/api/search`.

```javascript
async function verifyScrapConsistency(consistencyProbe, dataState) {
    const loadedPosts = Array.isArray(dataState?.posts) ? dataState.posts : allPosts;

    if (dataState?.ok === false) {
        return {
            status: 'failed',
            steps: [{ key: 'api_posts', label: '서버 API', status: 'failed', detail: '데이터 재조회 실패' }]
        };
    }

    if (!consistencyProbe) {
        return { status: 'skipped', steps: [] };
    }

    const samplesByPlatform = consistencyProbe.new_samples || {};
    const loadedKeys = new Set(loadedPosts.map(getConsistencyPostKey).filter(Boolean));
    const steps = ['linkedin', 'threads', 'twitter']
        .map(platform => {
            const samples = Array.isArray(samplesByPlatform[platform]) ? samplesByPlatform[platform] : [];
            if (samples.length === 0) return null;
            const missing = samples.filter(sample => {
                const sampleWithPlatform = Object.assign({}, sample, {
                    sns_platform: sample.sns_platform || platform
                });
                return !loadedKeys.has(getConsistencyPostKey(sampleWithPlatform));
            });
            const label = platform === 'linkedin' ? 'LinkedIn 신규 샘플' : (platform === 'threads' ? 'Threads 신규 샘플' : 'X 신규 샘플');
            return {
                key: `new_samples_${platform}`,
                label,
                status: missing.length === 0 ? 'passed' : 'failed',
                detail: missing.length === 0 ? `${samples.length}개 확인` : `${missing.length}/${samples.length}개 누락`
            };
        })
        .filter(Boolean);

    return { status: steps.length === 0 ? 'passed' : deriveConsistencyStatus(steps), steps };
}
```

- [x] **Step 7: Add 3-second timeout wrapper**

```javascript
async function verifyScrapConsistencyWithTimeout(consistencyProbe, dataState, timeoutMs = 3000) {
    let timeoutId;
    const timeoutResult = new Promise(resolve => {
        timeoutId = window.setTimeout(() => {
            resolve({
                status: 'timeout',
                steps: [{ key: 'new_samples_timeout', label: '신규 샘플', status: 'warning', detail: '3초 안에 확인하지 못했습니다.' }]
            });
        }, timeoutMs);
    });

    try {
        return await Promise.race([
            Promise.resolve().then(() => verifyScrapConsistency(consistencyProbe, dataState)),
            timeoutResult
        ]);
    } finally {
        window.clearTimeout(timeoutId);
    }
}
```

- [x] **Step 8: Use timeout wrapper in `executeScrap()`**

Replace the direct consistency call.

```javascript
const dataState = await fetchData();
const consistencyCheck = await verifyScrapConsistencyWithTimeout(result.consistency_probe, dataState, 3000);
showScrapResultModal(Object.assign({}, result, { consistency_check: consistencyCheck }), mode);
```

- [x] **Step 9: Verify viewer unit tests**

Run:

```powershell
pytest tests/unit/test_web_viewer_scrap_result_helpers.py -q
```

Expected: `6 passed`.

---

## Task 3: UI Terms And Documentation

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\index.html`
- Modify: `D:\vibe-coding\scrap_sns\README.md`
- Modify: `D:\vibe-coding\scrap_sns\BACKLOG.md`

- [x] **Step 1: Rename sort labels**

In `index.html`, replace user-facing sort labels only. Keep `data-sort` values unchanged.

```html
<span id="currentSortLabel">작성일순</span>
<button data-sort="date" class="block w-full text-left px-4 py-3 text-sm text-gray-300 hover:bg-white/5 hover:text-white transition-colors active-sort">작성일순</button>
<button data-sort="saved" class="block w-full text-left px-4 py-3 text-sm text-gray-300 hover:bg-white/5 hover:text-white transition-colors">로컬 수집순</button>
```

- [x] **Step 2: Add README time definitions**

Add this section to `README.md`.

```markdown
## 시간·정렬 기준

- `created_at`: 게시글이 플랫폼에 작성된 시간입니다. 웹 뷰어의 `작성일순` 기준입니다.
- `crawled_at`: 게시글이 이 프로젝트의 로컬 파일에 처음 수집된 시간입니다.
- `sequence_id`: 통합 결과 파일 안에서 다시 부여되는 로컬 순서 번호입니다.
- `platform_sequence_id`: 플랫폼별 수집 파일에서 보존한 순서 번호입니다.
- `platform_saved_at`: 실제 플랫폼에서 사용자가 저장한 시간입니다. 현재 스키마에는 없으며, 수집 가능성은 `BACKLOG.md`의 `BL-0504-01`에서 별도로 추적합니다.

현재 웹 뷰어의 `로컬 수집순`은 실제 플랫폼 저장시간순이 아니라 `crawled_at`과 플랫폼별 순서를 기준으로 만든 로컬 정렬입니다.
```

- [x] **Step 3: Add backlog entry for platform saved time**

Append one row to the root `BACKLOG.md`.

```markdown
| BL-0504-01 | 2026-05-04 | 실제 플랫폼 저장시간 수집 가능성 조사 | `linkedin_scrap.py`, `thread_scrap.py`, `tests/fixtures/snapshots/`, `utils/post_schema.py` | `██░░░░` | `████░░` | 현재 출력 파일과 원본 스냅샷에는 `saved_at`, `savedAt`, `bookmarked_at` 같은 실제 저장시간 필드가 없음. LinkedIn은 `saved: true`, Threads는 게시글 작성시간(`taken_at`)만 확인됨. 후속 조사에서 저장 목록 네트워크 응답을 더 깊게 추적해 실제 저장시간이 안정적으로 제공되는지 확인하고, 불가하면 `platform_sequence_id`를 "저장 목록 순서"로만 설명해야 함. 참고: `docs/specs/20260504_01_SNS-수집결과-정합성-검증.md` |
```

- [x] **Step 4: Verify label replacement**

Run:

```powershell
Select-String -Path index.html,README.md -Pattern "최근순|저장순"
```

Expected: no output for active UI and README wording. Historical spec documents may still mention old terms as context.

---

## Task 4: Headless Browser Verification

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\scripts\verify_auth_panel_headless.mjs`

- [x] **Step 1: Mock `new_samples` in `/api/run-scrap`**

Use a single LinkedIn post in both `/api/run-scrap` and `/api/posts` mock responses.

```javascript
const consistencyPost = {
  sequence_id: 999001,
  platform_id: 'li-consistency-20260504',
  sns_platform: 'linkedin',
  url: 'https://www.linkedin.com/feed/update/urn:li:activity:li-consistency-20260504',
  canonical_url: 'https://www.linkedin.com/feed/update/urn:li:activity:li-consistency-20260504',
  display_name: 'Consistency Tester',
  username: 'consistency-tester',
  created_at: '2026-05-04T09:00:00+09:00',
  crawled_at: '2026-05-04T09:05:00+09:00',
  full_text_preview: 'Unique Harness consistency probe'
};
```

In `/api/run-scrap`, include:

```javascript
consistency_probe: {
  source_file: 'output_total/sns_total_latest.json',
  updated_at: '2026-05-04T09:05:00+09:00',
  total_count: 1,
  platform_counts: { threads: 0, linkedin: 1, twitter: 0 },
  new_counts: { threads: 0, linkedin: 1, twitter: 0 },
  new_samples: {
    threads: [],
    linkedin: [{
      sequence_id: consistencyPost.sequence_id,
      platform_id: consistencyPost.platform_id,
      sns_platform: consistencyPost.sns_platform,
      url: consistencyPost.url,
      display_name: consistencyPost.display_name
    }],
    twitter: []
  }
}
```

- [x] **Step 2: Count search calls**

Add a counter around the `/api/search` route.

```javascript
let searchRequestCount = 0;

await page.route('**/api/search?**', async route => {
  searchRequestCount += 1;
  await route.fulfill({
    status: 200,
    contentType: 'application/json',
    body: JSON.stringify({ posts: [consistencyPost] })
  });
});
```

- [x] **Step 3: Assert quiet success**

After clicking update, wait until the run button is enabled again and assert:

```javascript
hasNoConsistencySection: !modalText.includes('정합성 확인'),
hasNoSearchProbe: searchRequestCount === 0,
```

- [x] **Step 4: Run headless verification**

Run:

```powershell
node scripts\verify_auth_panel_headless.mjs
```

Expected:

```json
{
  "ok": true,
  "hasNoConsistencySection": true,
  "hasNoSearchProbe": true
}
```

---

## Task 5: Full Verification And Performance Check

**Files:**
- Read only after implementation

- [x] **Step 1: Run unit tests**

```powershell
pytest tests/unit/test_web_viewer_scrap_result_helpers.py -q
```

Expected: `6 passed`.

- [x] **Step 2: Run run-scrap integration tests**

```powershell
pytest tests/integration/test_run_scrap_stats.py -q
```

Expected: `5 passed`.

- [x] **Step 3: Run posts/search API regression tests**

```powershell
pytest tests/integration/test_posts_api.py tests/integration/test_search_api.py -q
```

Expected: `16 passed`.

- [x] **Step 4: Run browser verification**

```powershell
node scripts\verify_auth_panel_headless.mjs
```

Expected: JSON output includes `"ok": true`.

- [x] **Step 5: Measure current browser timing**

Run a headless timing probe that clicks update against mocked `/api/run-scrap` and current `/api/posts`.

Expected current reference on 1,222 posts:

```json
{
  "elapsed_ms": 454,
  "post_count": 1222,
  "sample_count": 3,
  "search_requests": 0,
  "has_consistency_section": false
}
```

Acceptable threshold: under 3 seconds. Target: around 1 second or less.

---

## Task 6: Commit

**Files:**
- Stage only files changed by this plan.
- Do not stage unrelated snapshot deletions under `D:\vibe-coding\scrap_sns\tests\fixtures\snapshots\`.

- [x] **Step 1: Confirm staged scope**

```powershell
git diff --cached --name-status
```

Expected staged files:

```text
M	BACKLOG.md
M	CHANGELOG.md
M	README.md
M	docs/specs/20260504_01_SNS-수집결과-정합성-검증.md
M	index.html
M	scripts/verify_auth_panel_headless.mjs
M	server.py
M	tests/integration/test_run_scrap_stats.py
M	tests/unit/test_web_viewer_scrap_result_helpers.py
M	web_viewer/script.js
```

- [x] **Step 2: Run security gate**

```powershell
node C:\Users\ahnbu\.claude\skills\_shared\security-gate.mjs precommit --repo "D:\vibe-coding\scrap_sns" --staged
```

Expected: `Security gate passed precommit.`

- [x] **Step 3: Commit with cp skill workflow**

Use the project `cp` skill workflow. Commit message:

```text
feat(consistency): 신규 수집 샘플 정합성 확인 추가
```

Expected commit reference from current implementation: `83d2617`.

---

## Self-Review

- Spec coverage: R1, R4, R7 are covered by Tasks 1, 2, 4, 5. R3 and R8 are covered by Task 3. R2, R5, R6 are covered by the diagnostic boundaries and verification behavior.
- Placeholder scan: Explicit placeholder terms from the planning checklist are absent.
- Type consistency: Server uses `new_counts` and `new_samples`; viewer reads `consistencyProbe.new_samples`; tests assert the same keys.
- Migration check: No DB or output JSON migration is required because existing files are read-only inputs.

