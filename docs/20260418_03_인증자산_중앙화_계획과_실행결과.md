---
title: "Auth Asset Centralization Plan And Execution Result"
created: "2026-04-18 17:33"
tags:
  - plan
  - auth
  - x
  - scrap_sns
  - scrap-my
  - last30days
session_id: codex:019d9f9a-88cd-7432-9829-0a6fe9566145
session_path: C:/Users/ahnbu/.codex/sessions/2026/04/18/rollout-2026-04-18T16-59-58-019d9f9a-88cd-7432-9829-0a6fe9566145.jsonl
ai: codex
status: completed
---

# Auth Asset Centralization Plan And Execution Result

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `~/.config/auth`를 단일 런타임 정본으로 만들고 `scrap_sns`, `scrap-my`, `last30days-my`가 stale X 쿠키 없이 같은 인증 자산을 읽고 갱신하게 만든다.

**Architecture:** repo-tracked source는 `D:\vibe-coding\scrap_sns`에 두고, `utils/auth_paths.py`와 `scripts/auth_runtime/*`를 `scripts/sync_auth_runtime.ps1`로 `AUTH_HOME`에 배포한다. X 정본은 `x/user_data` 하나만 유지하고, `cookies.json`, `x_cookies_current.json`, `storage_state.json`, `x_storage_state_current.json`은 모두 같은 export 단계에서 파생한다. 성공 판정은 링크 존재 여부가 아니라 “이번 export가 만든 파일명”과 “필수 토큰 존재”를 함께 검증해 stale link를 차단한다.

**Tech Stack:** Python 3.13, PowerShell 7, Playwright, pytest, Node.js, Windows symbolic link/junction

---

## Execution Update

### 구현 반영

- `scrap_sns`에 `utils/auth_paths.py`, `scripts/auth_runtime/export_x_artifacts.py`, `scripts/auth_runtime/renew.py`, `scripts/sync_auth_runtime.ps1`, 관련 unit/integration test를 추가했다.
- `linkedin_scrap.py`, `thread_scrap.py`, `thread_scrap_single.py`, `twitter_scrap.py`, `renew_twitter_auth.py`, `inject_x_cookies.py`, `renew_auth.py`, adapter와 smoke test를 `AUTH_HOME` 기준으로 전환했다.
- `scrap-my`, `last30days-my`는 `AUTH_HOME` loader/fallback을 사용하도록 바꿨고 관련 테스트를 추가했다.

### 실행 중 수정한 이슈

- 초기 `sync_auth_runtime.ps1` rerun은 PowerShell `New-Item -ItemType SymbolicLink` 권한 오류로 실패했다. 최종 구현은 Python `os.replace()` 기반 원자적 link 교체로 바꿨고, rerun `SYNC_OK`를 확인했다.
- 초기 X runtime은 `x/user_data`가 guest profile 상태로 내려가 있어 smoke가 실패했다. `inject_x_cookies.py`로 dated cookie snapshot을 profile에 재주입한 뒤 X smoke와 export를 다시 통과시켰다.
- 초기 `export_x_artifacts.py`는 symlink 교체에 `trash`를 쓰다가 Windows에서 link가 남는 문제가 있었다. 최종 구현은 temp symlink + `os.replace()`로 수정했다.

### 검증 결과

- `python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_auth_paths.py D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py D:\vibe-coding\scrap_sns\tests\unit\test_twitter_cli_adapter.py D:\vibe-coding\scrap_sns\tests\unit\test_threads_http_adapter.py -q`
  - 결과: `26 passed`
- `python -m pytest D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py D:\vibe-coding\scrap_sns\tests\integration\test_twitter_scrap_single_cli.py D:\vibe-coding\scrap_sns\tests\integration\test_thread_scrap_single_browserless.py -q`
  - 결과: `8 passed`
- `python -m pytest D:\vibe-coding\scrap_sns\tests\smoke\test_linkedin_smoke.py D:\vibe-coding\scrap_sns\tests\smoke\test_threads_smoke.py D:\vibe-coding\scrap_sns\tests\smoke\test_twitter_smoke.py -v`
  - 결과: `4 passed`
- `python -m pytest C:\Users\ahnbu\.claude\skills\tests\test_auth_runtime_loader.py C:\Users\ahnbu\.claude\skills\tests\test_last30days_auth_home.py C:\Users\ahnbu\.claude\skills\scrap-my\tests\test_skool_auth_scrape.py C:\Users\ahnbu\.claude\skills\scrap-my\tests\test_download_images.py -q`
  - 결과: `10 passed`
- `node --test C:\Users\ahnbu\.claude\skills\scrap-my\tests\scrapling-command.test.mjs`
  - 결과: `5 passed`
- `powershell -ExecutionPolicy Bypass -File D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1 -SourceRoot D:\vibe-coding\scrap_sns -AuthHome C:\Users\ahnbu\.config\auth -LegacyAuthDir D:\vibe-coding\scrap_sns\auth`
  - 결과: `SYNC_OK`
- deployed `AUTH_HOME`에서 `export_x_artifacts()` 실행
  - 결과: `EXPORTED_COOKIE_FILE=cookies_20260418_9999.json`
  - 결과: `VALIDATED_COOKIE_TARGET=cookies_20260418_9999.json`
- runtime link 확인
  - 결과: `cookies.json -> cookies_20260418_9999.json`
  - 결과: `x_cookies_current.json -> cookies_20260418_9999.json`
  - 결과: `x_storage_state_current.json -> storage_state.json`

### 현재 상태

- `D:\vibe-coding\scrap_sns\auth`는 `C:\Users\ahnbu\.config\auth`를 가리키는 junction으로 동작한다.
- X canonical source는 `AUTH_HOME/x/user_data`, stable derived artifacts는 `AUTH_HOME/x/cookies.json`, `AUTH_HOME/x/storage_state.json`으로 정착했다.

### 후속 정리 반영

- `C:\Users\ahnbu\.config\last30days\.env`에서 수동 주입 상태였던 `AUTH_TOKEN`, `CT0`를 제거했다.
- `C:\Users\ahnbu\.config\auth\x\cookies_current.json` 잔존 symlink를 제거했다.
- 후속 검증 결과:
  - `.env`에는 `SETUP_COMPLETE`, `FROM_BROWSER`, `BROWSER_CONSENT`만 남아 있다.
  - `x/cookies_current.json`은 더 이상 존재하지 않는다.
  - `last30days-my`의 `get_config()`는 `AUTH_SOURCE auth_home`을 출력해 중앙 `AUTH_HOME` fallback을 실제 사용한다.

## Scope Lock

- In scope: `AUTH_HOME` 구조 배포, X export/validate 파이프라인, `scrap_sns` 경로 refactor, `scrap-my`/`last30days-my` fallback refactor, 운영 문서 현행화.
- Out of scope: 기존 output JSON backfill, X self-thread 병합 로직 변경, 새로운 인증 서비스 추가, 기존 브라우저 추출(`FROM_BROWSER`) 제거.
- Stop condition 1: Windows file symlink 생성이 현재 머신에서 실패하면 consumer 수정 전에 중단하고 Dev Mode/권한 상태를 먼저 해결한다.
- Stop condition 2: `renew.py x`가 새 cookie stamp를 만들지 못하거나 새 stamp와 `cookies.json` target이 일치하지 않으면 consumer rollout을 진행하지 않는다.

## Repo Map

- Repo 1: `D:\vibe-coding\scrap_sns`
  - auth runtime source, 배포 스크립트, runtime contract test, consumer refactor, 운영 문서.
- Repo 2: `C:\Users\ahnbu\.claude\skills`
  - `scrap-my`, `last30days-my`, 공용 loader, skill docs.
- Runtime path: `C:\Users\ahnbu\.config\auth`
  - git 추적 대상 아님. `sync_auth_runtime.ps1`만 이 경로를 쓰도록 유지한다.
- Dirty worktree note:
  - `C:\Users\ahnbu\.claude\skills`는 이미 unrelated 변경이 있으므로 `scrap-my/`, `last30days-my/`, `_shared/`, `CHANGELOG.md` 외 경로는 절대 stage/revert하지 않는다.

## File Structure

- Create: `D:\vibe-coding\scrap_sns\utils\auth_paths.py`
  - `AUTH_HOME` 경로 해석, X cookie target validation, flat compatibility path 계산.
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\export_x_artifacts.py`
  - `x/user_data`에서 `cookies_YYYYMMDD_HHmm.json` + `storage_state.json` 파생, nested/flat symlink 갱신.
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\renew.py`
  - LinkedIn/Threads/Skool storage_state 갱신, X persistent profile 로그인, export 후 stale validation.
- Create: `D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1`
  - runtime source를 `AUTH_HOME`로 복사, 디렉토리/flat link/junction 준비, one-time migration 수행.
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_auth_paths.py`
  - `AUTH_HOME` path resolution, stale link rejection.
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py`
  - 필수 토큰 추출, flat link 파일명 규칙.
- Create: `D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py`
  - temp `AUTH_HOME`/legacy auth dir에 sync script 실행 후 layout 검증.
- Modify: `D:\vibe-coding\scrap_sns\utils\twitter_cli_adapter.py`
  - `cookies.json` 우선, `x_cookies_current.json` 및 `cookies_*.json` fallback.
- Modify: `D:\vibe-coding\scrap_sns\utils\threads_http_adapter.py`
  - `auth_file=None`일 때 `AUTH_HOME/threads/storage_state.json` 기본 사용.
- Modify: `D:\vibe-coding\scrap_sns\linkedin_scrap.py`
- Modify: `D:\vibe-coding\scrap_sns\thread_scrap.py`
- Modify: `D:\vibe-coding\scrap_sns\thread_scrap_single.py`
- Modify: `D:\vibe-coding\scrap_sns\twitter_scrap.py`
- Modify: `D:\vibe-coding\scrap_sns\renew_twitter_auth.py`
- Modify: `D:\vibe-coding\scrap_sns\inject_x_cookies.py`
- Modify: `D:\vibe-coding\scrap_sns\renew_auth.py`
  - 직접 갱신 대신 `AUTH_HOME\renew.py` proxy/stub로 축소.
- Create: `C:\Users\ahnbu\.claude\skills\_shared\auth_runtime_loader.py`
  - `AUTH_HOME/auth_paths.py` 동적 import.
- Create: `C:\Users\ahnbu\.claude\skills\tests\test_auth_runtime_loader.py`
- Create: `C:\Users\ahnbu\.claude\skills\tests\test_last30days_auth_home.py`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\scripts\x_auth_scrape.py`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\scripts\skool_auth_scrape.py`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\scripts\x_twitter_cli_scrape.mjs`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\SKILL.md`
- Modify: `C:\Users\ahnbu\.claude\skills\last30days-my\scripts\lib\env.py`
- Modify: `C:\Users\ahnbu\.claude\skills\last30days-my\SKILL.md`
- Modify: `D:\vibe-coding\scrap_sns\README.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\development.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\crawling_logic.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\20260310_04_SNS 세션 인증정보(Auth) 갱신 가이드.md`
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`
- Modify: `C:\Users\ahnbu\.claude\skills\CHANGELOG.md`

## Persistence Surface And Migration Decision

### Affected persistent surfaces

- `C:\Users\ahnbu\.config\auth\x\user_data\`
  - X 유일 정본. one-time move 대상.
- `C:\Users\ahnbu\.config\auth\x\cookies_YYYYMMDD_HHmm.json`
  - 매 갱신마다 새 파일 생성.
- `C:\Users\ahnbu\.config\auth\x\cookies.json`
  - 최신 dated cookie 파일을 가리키는 stable symlink.
- `C:\Users\ahnbu\.config\auth\x\storage_state.json`
  - `x_auth_scrape.py`용 stable storage_state 파생본.
- `C:\Users\ahnbu\.config\auth\auth_linkedin.json`
- `C:\Users\ahnbu\.config\auth\auth_threads.json`
- `C:\Users\ahnbu\.config\auth\auth_skool.json`
  - nested storage_state를 flat path로 노출하는 compatibility symlink.
- `C:\Users\ahnbu\.config\auth\x_cookies_current.json`
- `C:\Users\ahnbu\.config\auth\x_storage_state_current.json`
  - flat X compatibility symlink. dynamic stamp 이름 대신 stable 이름을 쓴다.
- `D:\vibe-coding\scrap_sns\auth`
  - 최종적으로 `AUTH_HOME`를 가리키는 junction.
- `output_twitter/python/*.json`, `output_threads/python/*.json`, `output_linkedin/python/*.json`
  - 인증 경로만 바뀌고 schema는 유지된다. 데이터 마이그레이션 없음.

### Migration decision

- Existing output JSON migration: 하지 않는다.
- Reason 1: consumer output schema는 그대로 유지된다.
- Reason 2: auth asset layout만 바뀌고 게시물 payload field는 바뀌지 않는다.
- Reason 3: X stale 방지는 runtime export validation으로 해결되며 기존 게시물 재작성과 무관하다.

### One-time migration command/script path

1. `pwsh -NoProfile -File D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1 -AuthHome "$env:USERPROFILE\.config\auth" -LegacyAuthDir "D:\vibe-coding\scrap_sns\auth"`
2. `python "$env:USERPROFILE\.config\auth\renew.py" x`

### Migration result verification

1. `Get-Item "$env:USERPROFILE\.config\auth\cookies.json"` 을 실행하지 말고 아래 명령으로 target 확인
   - Run:

```powershell
@'
from pathlib import Path
home = Path.home() / ".config" / "auth"
print("cookie_link", (home / "x" / "cookies.json").resolve().name)
print("flat_cookie", (home / "x_cookies_current.json").resolve().name)
print("flat_storage", (home / "x_storage_state_current.json").resolve().name)
'@ | python -
```

   - Expected: `cookie_link cookies_YYYYMMDD_HHmm.json`, `flat_cookie cookies.json`, `flat_storage storage_state.json`
2. `python "$env:USERPROFILE\.config\auth\renew.py" x`
   - Expected: `EXPORTED_COOKIE_FILE=cookies_YYYYMMDD_HHmm.json`, `VALIDATED_COOKIE_TARGET=cookies_YYYYMMDD_HHmm.json`, exit code `0`

---

### Task 1: Add Shared Auth Path Resolver And Contract Tests

**Files:**
- Create: `D:\vibe-coding\scrap_sns\utils\auth_paths.py`
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_auth_paths.py`
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`

- [ ] **Step 1: Write the failing unit tests**

```python
import importlib
from pathlib import Path

import utils.auth_paths as auth_paths


def _write_cookie_file(path: Path, auth_token: str, ct0: str) -> None:
    path.write_text(
        f'[{{"name":"auth_token","value":"{auth_token}"}},{{"name":"ct0","value":"{ct0}"}}]',
        encoding="utf-8",
    )


def test_x_cookies_latest_prefers_nested_cookies_link(tmp_path, monkeypatch):
    x_dir = tmp_path / "x"
    x_dir.mkdir(parents=True)
    dated = x_dir / "cookies_20260418_1810.json"
    _write_cookie_file(dated, "new-token", "new-ct0")
    (x_dir / "cookies.json").symlink_to(dated.name)
    monkeypatch.setenv("AUTH_HOME", str(tmp_path))
    importlib.reload(auth_paths)

    assert auth_paths.x_cookies_latest() == x_dir / "cookies.json"


def test_validate_x_cookie_target_rejects_stale_link(tmp_path, monkeypatch):
    x_dir = tmp_path / "x"
    x_dir.mkdir(parents=True)
    old_dated = x_dir / "cookies_20260418_1800.json"
    new_dated = x_dir / "cookies_20260418_1810.json"
    _write_cookie_file(old_dated, "old-token", "old-ct0")
    _write_cookie_file(new_dated, "new-token", "new-ct0")
    (x_dir / "cookies.json").symlink_to(old_dated.name)
    monkeypatch.setenv("AUTH_HOME", str(tmp_path))
    importlib.reload(auth_paths)

    assert auth_paths.validate_x_cookie_target(new_dated.name) is False
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_auth_paths.py -q`

Expected: `ModuleNotFoundError: No module named 'utils.auth_paths'`

- [ ] **Step 3: Write the minimal path resolver**

```python
from __future__ import annotations

import os
from pathlib import Path

_DEFAULT_AUTH_HOME = Path.home() / ".config" / "auth"


def auth_home() -> Path:
    return Path(os.environ.get("AUTH_HOME", _DEFAULT_AUTH_HOME))


def auth_dir(domain: str) -> Path:
    override = os.environ.get(f"AUTH_HOME_{domain.upper()}")
    return Path(override) if override else auth_home() / domain


def linkedin_storage() -> Path:
    return auth_dir("linkedin") / "storage_state.json"


def threads_storage() -> Path:
    return auth_dir("threads") / "storage_state.json"


def skool_storage() -> Path:
    return auth_dir("skool") / "storage_state.json"


def x_storage() -> Path:
    return auth_dir("x") / "storage_state.json"


def x_user_data() -> Path:
    return auth_dir("x") / "user_data"


def x_cookie_link() -> Path:
    return auth_dir("x") / "cookies.json"


def flat_link(name: str) -> Path:
    return auth_home() / name


def x_flat_cookie() -> Path:
    return flat_link("x_cookies_current.json")


def x_flat_storage() -> Path:
    return flat_link("x_storage_state_current.json")


def x_cookies_latest() -> Path | None:
    link = x_cookie_link()
    if link.exists():
        return link
    files = sorted(auth_dir("x").glob("cookies_*.json"), reverse=True)
    return files[0] if files else None


def validate_x_cookie_target(expected_name: str) -> bool:
    link = x_cookie_link()
    if not link.exists() or not link.is_symlink():
        return False
    try:
        return link.resolve().name == expected_name
    except OSError:
        return False
```

- [ ] **Step 4: Run the tests to verify they pass**

Run: `python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_auth_paths.py -q`

Expected: `2 passed`

- [ ] **Step 5: Update `scrap_sns` changelog and commit with `cp` skill**

```text
Repo: D:\vibe-coding\scrap_sns
CHANGELOG scope: auth runtime
Commit message: feat(auth): add shared auth path resolver
Expected commit contents: utils/auth_paths.py, tests/unit/test_auth_paths.py, CHANGELOG.md
```

### Task 2: Implement X Export Pipeline, Renew Flow, And Runtime Sync Script

**Files:**
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\__init__.py`
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\export_x_artifacts.py`
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\renew.py`
- Create: `D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1`
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py`
- Create: `D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py`
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`

- [ ] **Step 1: Write the failing tests for token extraction and sync layout**

```python
from pathlib import Path

from scripts.auth_runtime.export_x_artifacts import (
    extract_token_pair,
    flat_cookie_link_name,
    flat_storage_link_name,
)


def test_extract_token_pair_returns_required_values():
    cookies = [
        {"name": "auth_token", "value": "aaa"},
        {"name": "ct0", "value": "bbb"},
        {"name": "lang", "value": "ko"},
    ]

    assert extract_token_pair(cookies) == {"auth_token": "aaa", "ct0": "bbb"}


def test_extract_token_pair_returns_none_when_ct0_missing():
    cookies = [{"name": "auth_token", "value": "aaa"}]

    assert extract_token_pair(cookies) is None


def test_flat_link_names_are_stable():
    assert flat_cookie_link_name() == "x_cookies_current.json"
    assert flat_storage_link_name() == "x_storage_state_current.json"
```

```python
import subprocess
from pathlib import Path


def test_sync_auth_runtime_creates_nested_and_flat_links(tmp_path):
    repo_root = Path(__file__).resolve().parents[2]
    auth_home = tmp_path / "auth-home"
    legacy_dir = tmp_path / "legacy-auth"
    legacy_dir.mkdir()

    result = subprocess.run(
        [
            "pwsh",
            "-NoProfile",
            "-File",
            str(repo_root / "scripts" / "sync_auth_runtime.ps1"),
            "-SourceRoot",
            str(repo_root),
            "-AuthHome",
            str(auth_home),
            "-LegacyAuthDir",
            str(legacy_dir),
        ],
        capture_output=True,
        text=True,
        check=False,
    )

    assert result.returncode == 0, result.stderr
    assert (auth_home / "auth_paths.py").exists()
    assert (auth_home / "auth_linkedin.json").is_symlink()
    assert (auth_home / "x_cookies_current.json").is_symlink()
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py -q`

Expected: import failure for `scripts.auth_runtime.export_x_artifacts` and missing `sync_auth_runtime.ps1`

- [ ] **Step 3: Write the runtime source and sync script**

```python
from __future__ import annotations

import json
import subprocess
import sys
from datetime import datetime
from pathlib import Path

from playwright.sync_api import sync_playwright

REPO_ROOT = Path(__file__).resolve().parents[2]
if str(REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(REPO_ROOT))

from utils.auth_paths import (
    validate_x_cookie_target,
    x_cookie_link,
    x_flat_cookie,
    x_flat_storage,
    x_storage,
    x_user_data,
)


def extract_token_pair(cookies: list[dict]) -> dict[str, str] | None:
    values = {item.get("name"): item.get("value") for item in cookies}
    if not values.get("auth_token") or not values.get("ct0"):
        return None
    return {"auth_token": values["auth_token"], "ct0": values["ct0"]}


def flat_cookie_link_name() -> str:
    return "x_cookies_current.json"


def flat_storage_link_name() -> str:
    return "x_storage_state_current.json"


def _refresh_link(link_path: Path, target_path: Path) -> None:
    if link_path.exists() or link_path.is_symlink():
        subprocess.run(["trash", str(link_path)], check=True)
    subprocess.run(
        [
            "pwsh",
            "-NoProfile",
            "-Command",
            f"New-Item -ItemType SymbolicLink -Path '{link_path}' -Target '{target_path}' | Out-Null",
        ],
        check=True,
        capture_output=True,
        text=True,
    )


def export_x_artifacts(stamp: str | None = None) -> tuple[Path, Path]:
    stamp = stamp or datetime.now().strftime("%Y%m%d_%H%M")
    x_dir = x_user_data().parent
    x_dir.mkdir(parents=True, exist_ok=True)
    storage_path = x_storage()
    dated_cookie_path = x_dir / f"cookies_{stamp}.json"

    with sync_playwright() as p:
        context = p.chromium.launch_persistent_context(
            user_data_dir=str(x_user_data()),
            channel="chrome",
            headless=True,
        )
        context.storage_state(path=str(storage_path))
        cookies = context.cookies("https://x.com")
        context.close()

    token_pair = extract_token_pair(cookies)
    if token_pair is None:
        raise RuntimeError("auth_token/ct0 missing after X export")

    dated_cookie_path.write_text(
        json.dumps(cookies, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    _refresh_link(x_cookie_link(), dated_cookie_path)
    _refresh_link(x_flat_cookie(), x_cookie_link())
    _refresh_link(x_flat_storage(), x_storage())
    if not validate_x_cookie_target(dated_cookie_path.name):
        raise RuntimeError("cookies.json still points to stale export target")

    print(f"EXPORTED_COOKIE_FILE={dated_cookie_path.name}")
    print(f"VALIDATED_COOKIE_TARGET={dated_cookie_path.name}")
    return dated_cookie_path, storage_path
```

```python
from __future__ import annotations

import subprocess
import sys
from pathlib import Path

from playwright.sync_api import sync_playwright

sys.path.insert(0, str(Path(__file__).resolve().parent))
from export_x_artifacts import export_x_artifacts

REPO_ROOT = Path(__file__).resolve().parents[2]
if str(REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(REPO_ROOT))

from utils.auth_paths import linkedin_storage, skool_storage, threads_storage, x_user_data


def renew_storage_state(name: str, url: str, target: Path) -> None:
    target.parent.mkdir(parents=True, exist_ok=True)
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=False)
        context_args = {}
        if target.exists() and target.stat().st_size > 10:
            context_args["storage_state"] = str(target)
        context = browser.new_context(**context_args)
        page = context.new_page()
        page.goto(url)
        input(f"[{name}] 로그인 완료 후 Enter: ")
        context.storage_state(path=str(target))
        browser.close()


def renew_x_profile() -> None:
    x_user_data().mkdir(parents=True, exist_ok=True)
    with sync_playwright() as p:
        context = p.chromium.launch_persistent_context(
            user_data_dir=str(x_user_data()),
            channel="chrome",
            headless=False,
        )
        page = context.pages[0] if context.pages else context.new_page()
        page.goto("https://x.com/i/flow/login")
        input("[X] 로그인 완료 후 Enter: ")
        context.close()


def main(argv: list[str]) -> int:
    targets = argv or ["linkedin", "threads", "skool", "x"]
    mapping = {
        "linkedin": ("LinkedIn", "https://www.linkedin.com/login", linkedin_storage()),
        "threads": ("Threads", "https://www.threads.com/login", threads_storage()),
        "skool": ("Skool", "https://www.skool.com/login", skool_storage()),
    }
    for target in targets:
        if target in mapping:
            name, url, path = mapping[target]
            renew_storage_state(name, url, path)
        elif target == "x":
            renew_x_profile()
            export_x_artifacts()
    return 0


if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))
```

```powershell
param(
  [string]$SourceRoot = "D:\vibe-coding\scrap_sns",
  [string]$AuthHome = "$env:USERPROFILE\.config\auth",
  [string]$LegacyAuthDir = "D:\vibe-coding\scrap_sns\auth"
)

$ErrorActionPreference = "Stop"
$dirs = @(
  $AuthHome,
  (Join-Path $AuthHome "x"),
  (Join-Path $AuthHome "linkedin"),
  (Join-Path $AuthHome "threads"),
  (Join-Path $AuthHome "skool"),
  (Join-Path $AuthHome "_backups\wave1_original")
)

foreach ($dir in $dirs) {
  New-Item -ItemType Directory -Path $dir -Force | Out-Null
}

Copy-Item (Join-Path $SourceRoot "utils\auth_paths.py") (Join-Path $AuthHome "auth_paths.py") -Force
Copy-Item (Join-Path $SourceRoot "scripts\auth_runtime\export_x_artifacts.py") (Join-Path $AuthHome "export_x_artifacts.py") -Force
Copy-Item (Join-Path $SourceRoot "scripts\auth_runtime\renew.py") (Join-Path $AuthHome "renew.py") -Force

function Set-CompatLink {
  param(
    [string]$Path,
    [string]$Target,
    [ValidateSet("SymbolicLink", "Junction")]
    [string]$Type = "SymbolicLink"
  )

  if (Test-Path $Path) {
    trash $Path
  }

  New-Item -ItemType $Type -Path $Path -Target $Target | Out-Null
}

Set-CompatLink -Path (Join-Path $AuthHome "auth_linkedin.json") -Target (Join-Path $AuthHome "linkedin\storage_state.json")
Set-CompatLink -Path (Join-Path $AuthHome "auth_threads.json") -Target (Join-Path $AuthHome "threads\storage_state.json")
Set-CompatLink -Path (Join-Path $AuthHome "auth_skool.json") -Target (Join-Path $AuthHome "skool\storage_state.json")
Set-CompatLink -Path (Join-Path $AuthHome "x_cookies_current.json") -Target (Join-Path $AuthHome "x\cookies.json")
Set-CompatLink -Path (Join-Path $AuthHome "x_storage_state_current.json") -Target (Join-Path $AuthHome "x\storage_state.json")

if (-not (Test-Path (Join-Path $AuthHome "x\user_data")) -and (Test-Path (Join-Path $LegacyAuthDir "x_user_data"))) {
  Move-Item (Join-Path $LegacyAuthDir "x_user_data") (Join-Path $AuthHome "x\user_data")
}

if (Test-Path $LegacyAuthDir) {
  Get-ChildItem $LegacyAuthDir -File -Filter "*.json" | ForEach-Object { trash $_.FullName }
  if (-not ((Get-Item $LegacyAuthDir) | Select-Object -ExpandProperty LinkType -ErrorAction SilentlyContinue)) {
    trash $LegacyAuthDir
  }
}

New-Item -ItemType Junction -Path $LegacyAuthDir -Target $AuthHome -Force | Out-Null
Write-Output "SYNC_OK"
```

- [ ] **Step 4: Run the tests to verify they pass**

Run:

```powershell
python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py -q
```

Expected: `4 passed`

- [ ] **Step 5: Update `scrap_sns` changelog and commit with `cp` skill**

```text
Repo: D:\vibe-coding\scrap_sns
CHANGELOG scope: auth runtime
Commit message: feat(auth): add auth runtime export and sync pipeline
Expected commit contents: scripts/auth_runtime/*, scripts/sync_auth_runtime.ps1, tests/unit/test_export_x_artifacts.py, tests/integration/test_auth_runtime_sync.py, CHANGELOG.md
```

### Task 3: Refactor `scrap_sns` Readers And Writers To The Shared Auth Runtime

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\utils\twitter_cli_adapter.py`
- Modify: `D:\vibe-coding\scrap_sns\utils\threads_http_adapter.py`
- Modify: `D:\vibe-coding\scrap_sns\linkedin_scrap.py`
- Modify: `D:\vibe-coding\scrap_sns\thread_scrap.py`
- Modify: `D:\vibe-coding\scrap_sns\thread_scrap_single.py`
- Modify: `D:\vibe-coding\scrap_sns\twitter_scrap.py`
- Modify: `D:\vibe-coding\scrap_sns\renew_twitter_auth.py`
- Modify: `D:\vibe-coding\scrap_sns\inject_x_cookies.py`
- Modify: `D:\vibe-coding\scrap_sns\renew_auth.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\unit\test_twitter_cli_adapter.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\unit\test_threads_http_adapter.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\smoke\test_linkedin_smoke.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\smoke\test_threads_smoke.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\smoke\test_twitter_smoke.py`
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`

- [ ] **Step 1: Extend unit tests so the current code fails on the new auth contract**

```python
def test_load_twitter_tokens_prefers_current_symlink(tmp_path):
    x_dir = tmp_path / "x"
    x_dir.mkdir(parents=True)
    current = x_dir / "x_cookies_current.json"
    actual = x_dir / "cookies_20260418_1900.json"
    _write_cookie_file(actual, auth_token="new-token", ct0="new-ct0")
    current.symlink_to(actual.name)

    assert load_twitter_tokens(auth_dir=x_dir) == {
        "auth_token": "new-token",
        "ct0": "new-ct0",
    }
```

```python
import importlib

import utils.threads_http_adapter as threads_http_adapter


def test_load_threads_cookies_uses_auth_home_default(monkeypatch, tmp_path):
    auth_file = tmp_path / "threads" / "storage_state.json"
    auth_file.parent.mkdir(parents=True)
    _write_storage_state(
        auth_file,
        [{"name": "sessionid", "value": "session", "domain": ".threads.com"}],
    )
    monkeypatch.setenv("AUTH_HOME", str(tmp_path))
    importlib.reload(threads_http_adapter)

    assert threads_http_adapter.load_threads_cookies() == {"sessionid": "session"}
```

- [ ] **Step 2: Run the targeted tests to verify they fail**

Run:

```powershell
python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_twitter_cli_adapter.py D:\vibe-coding\scrap_sns\tests\unit\test_threads_http_adapter.py -q
```

Expected: current code still searches `auth/x_cookies_*.json` and `auth/auth_threads.json`, so the two new tests fail

- [ ] **Step 3: Update the readers, writers, and smoke tests**

```python
from pathlib import Path

from utils.auth_paths import x_cookies_latest


def load_twitter_tokens(auth_dir=None):
    if auth_dir is None:
        cookie_path = x_cookies_latest()
    else:
        auth_dir = Path(auth_dir)
        candidates = []
        current = auth_dir / "x_cookies_current.json"
        nested = auth_dir / "cookies.json"
        if current.exists():
            candidates.append(current)
        if nested.exists():
            candidates.append(nested)
        candidates.extend(sorted(auth_dir.glob("x_cookies_*.json"), reverse=True))
        candidates.extend(sorted(auth_dir.glob("cookies_*.json"), reverse=True))
        cookie_path = candidates[0] if candidates else None
    if not cookie_path:
        return None
    with open(cookie_path, "r", encoding="utf-8") as file:
        cookies = json.load(file)
    values = {
        item.get("name"): item.get("value")
        for item in cookies
        if item.get("name") in {"auth_token", "ct0"}
    }
    if not values.get("auth_token") or not values.get("ct0"):
        return None
    return {"auth_token": values["auth_token"], "ct0": values["ct0"]}
```

```python
from pathlib import Path

from utils.auth_paths import threads_storage


def load_threads_cookies(auth_file: str | None = None) -> dict | None:
    resolved = Path(auth_file) if auth_file else threads_storage()
    with open(resolved, "r", encoding="utf-8") as file:
        storage_state = json.load(file)
    ...
```

```python
from utils.auth_paths import linkedin_storage, threads_storage, x_user_data, x_cookies_latest

AUTH_FILE = str(linkedin_storage())
# same replacement pattern:
# thread_scrap.py / thread_scrap_single.py -> str(threads_storage())
# twitter_scrap.py / renew_twitter_auth.py -> str(x_user_data())
# inject_x_cookies.py -> str(x_user_data()), str(x_cookies_latest())
```

```python
from pathlib import Path
import subprocess
import sys

RUNTIME_RENEW = Path.home() / ".config" / "auth" / "renew.py"


def main(argv: list[str]) -> int:
    if not RUNTIME_RENEW.exists():
        print(f"❌ 중앙 renew 스크립트가 없습니다: {RUNTIME_RENEW}")
        print("먼저 scripts/sync_auth_runtime.ps1 를 실행하세요.")
        return 1
    return subprocess.run([sys.executable, str(RUNTIME_RENEW), *argv], check=False).returncode


if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))
```

```python
from utils.auth_paths import linkedin_storage, threads_storage, x_user_data

auth_file = str(linkedin_storage())
# test_threads_smoke.py -> auth_file = str(threads_storage())
# test_twitter_smoke.py -> user_data_dir = str(x_user_data())
```

- [ ] **Step 4: Run unit, integration, and smoke tests**

Run:

```powershell
python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_twitter_cli_adapter.py D:\vibe-coding\scrap_sns\tests\unit\test_threads_http_adapter.py D:\vibe-coding\scrap_sns\tests\integration\test_twitter_scrap_single_cli.py D:\vibe-coding\scrap_sns\tests\integration\test_thread_scrap_single_browserless.py -q
python -m pytest D:\vibe-coding\scrap_sns\tests\smoke\test_linkedin_smoke.py D:\vibe-coding\scrap_sns\tests\smoke\test_threads_smoke.py D:\vibe-coding\scrap_sns\tests\smoke\test_twitter_smoke.py -v
```

Expected:
- first command: all tests pass
- second command: LinkedIn/Threads/X smoke tests pass when `AUTH_HOME` has been synced

- [ ] **Step 5: Update `scrap_sns` changelog and commit with `cp` skill**

```text
Repo: D:\vibe-coding\scrap_sns
CHANGELOG scope: auth consumers
Commit message: refactor(auth): route scrap_sns through auth home
Expected commit contents: reader/writer refactors, smoke tests, CHANGELOG.md
```

### Task 4: Switch `scrap-my` To `AUTH_HOME`

**Files:**
- Create: `C:\Users\ahnbu\.claude\skills\_shared\auth_runtime_loader.py`
- Create: `C:\Users\ahnbu\.claude\skills\tests\test_auth_runtime_loader.py`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\scripts\x_auth_scrape.py`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\scripts\skool_auth_scrape.py`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\scripts\x_twitter_cli_scrape.mjs`
- Modify: `C:\Users\ahnbu\.claude\skills\scrap-my\SKILL.md`
- Modify: `C:\Users\ahnbu\.claude\skills\CHANGELOG.md`

- [ ] **Step 1: Write the failing loader test**

```python
import sys
from pathlib import Path


SKILLS_ROOT = Path(__file__).resolve().parents[1]
if str(SKILLS_ROOT) not in sys.path:
    sys.path.insert(0, str(SKILLS_ROOT))

from _shared.auth_runtime_loader import load_auth_paths_module


def test_load_auth_paths_module_uses_auth_home_env(tmp_path, monkeypatch):
    auth_home = tmp_path / "auth"
    auth_home.mkdir()
    (auth_home / "auth_paths.py").write_text(
        "from pathlib import Path\n"
        "import os\n"
        "_HOME = Path(os.environ['AUTH_HOME'])\n"
        "def x_user_data(): return _HOME / 'x' / 'user_data'\n"
        "def x_flat_storage(): return _HOME / 'x_storage_state_current.json'\n"
        "def skool_storage(): return _HOME / 'skool' / 'storage_state.json'\n",
        encoding='utf-8',
    )
    monkeypatch.setenv("AUTH_HOME", str(auth_home))

    module = load_auth_paths_module()

    assert module.x_user_data() == auth_home / "x" / "user_data"
    assert module.x_flat_storage() == auth_home / "x_storage_state_current.json"
```

- [ ] **Step 2: Run the skills test to verify it fails**

Run: `python -m pytest C:\Users\ahnbu\.claude\skills\tests\test_auth_runtime_loader.py -q`

Expected: import failure for `_shared.auth_runtime_loader`

- [ ] **Step 3: Implement the loader and refactor `scrap-my` scripts**

```python
from __future__ import annotations

import importlib.util
import os
from pathlib import Path


def resolve_auth_home() -> Path:
    return Path(os.environ.get("AUTH_HOME", Path.home() / ".config" / "auth"))


def load_auth_paths_module():
    module_path = resolve_auth_home() / "auth_paths.py"
    if not module_path.exists():
        raise RuntimeError(f"auth_paths.py not found: {module_path}")
    spec = importlib.util.spec_from_file_location("auth_paths_runtime", module_path)
    if spec is None or spec.loader is None:
        raise RuntimeError(f"failed to load auth_paths.py: {module_path}")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module
```

```python
from pathlib import Path

SKILLS_ROOT = Path(__file__).resolve().parents[2]
if str(SKILLS_ROOT) not in sys.path:
    sys.path.insert(0, str(SKILLS_ROOT))

from _shared.auth_runtime_loader import load_auth_paths_module

AUTH_PATHS = load_auth_paths_module()
AUTH_DIR = str(AUTH_PATHS.x_user_data())
AUTH_STORAGE_STATE = str(AUTH_PATHS.x_flat_storage())
```

```python
from pathlib import Path

SKILLS_ROOT = Path(__file__).resolve().parents[2]
if str(SKILLS_ROOT) not in sys.path:
    sys.path.insert(0, str(SKILLS_ROOT))

from _shared.auth_runtime_loader import load_auth_paths_module

AUTH_PATHS = load_auth_paths_module()
AUTH_STORAGE_STATE = str(AUTH_PATHS.skool_storage())
```

```javascript
import { homedir } from "node:os";
import { join, resolve } from "node:path";

const defaultAuthHome = process.env.AUTH_HOME || resolve(homedir(), ".config", "auth");
const fallbackCookieFile = resolve(defaultAuthHome, "x", "cookies.json");

if (!process.env.TWITTER_AUTH_TOKEN || !process.env.TWITTER_CT0) {
  const cookieFile = process.env.X_COOKIES_FILE || fallbackCookieFile;
  ...
}
```

```markdown
- X cookie fallback order: `TWITTER_AUTH_TOKEN/TWITTER_CT0` -> `X_COOKIES_FILE` -> `AUTH_HOME/x/cookies.json`
- Skool state-save target: `AUTH_HOME/skool/storage_state.json`
- X storage_state target: `AUTH_HOME/x_storage_state_current.json`
```

- [ ] **Step 4: Run loader and direct script verification**

Run:

```powershell
python -m pytest C:\Users\ahnbu\.claude\skills\tests\test_auth_runtime_loader.py -q
node C:\Users\ahnbu\.claude\skills\scrap-my\scripts\x_twitter_cli_scrape.mjs "https://x.com/i/status/2034688795652816948"
python C:\Users\ahnbu\.claude\skills\scrap-my\scripts\skool_auth_scrape.py "https://www.skool.com/group/post?p=123"
```

Expected:
- pytest passes
- `x_twitter_cli_scrape.mjs` no longer exits with `TWITTER_AUTH_TOKEN and TWITTER_CT0 required` when `AUTH_HOME/x/cookies.json` exists
- Skool script no longer references `D:\vibe-coding\scrap_sns\auth\auth_skool.json`

- [ ] **Step 5: Update skills changelog and commit only the targeted files with `cp` skill**

```text
Repo: C:\Users\ahnbu\.claude\skills
CHANGELOG scope: scrap-my auth home
Commit message: refactor(scrap-my): resolve auth assets from auth home
Expected commit contents: _shared/auth_runtime_loader.py, tests/test_auth_runtime_loader.py, scrap-my/*, CHANGELOG.md
Do not stage unrelated deleted gstack-* files.
```

### Task 5: Add `AUTH_HOME` Fallback To `last30days-my`

**Files:**
- Create: `C:\Users\ahnbu\.claude\skills\tests\test_last30days_auth_home.py`
- Modify: `C:\Users\ahnbu\.claude\skills\last30days-my\scripts\lib\env.py`
- Modify: `C:\Users\ahnbu\.claude\skills\last30days-my\SKILL.md`
- Modify: `C:\Users\ahnbu\.claude\skills\CHANGELOG.md`

- [ ] **Step 1: Write the failing tests for auth-home fallback precedence**

```python
import importlib
import json
import sys
from pathlib import Path


SKILLS_ROOT = Path(__file__).resolve().parents[1]
if str(SKILLS_ROOT) not in sys.path:
    sys.path.insert(0, str(SKILLS_ROOT))

from last30days-my.scripts.lib import env as env_module


def test_get_config_loads_tokens_from_auth_home(tmp_path, monkeypatch):
    auth_home = tmp_path / "auth"
    x_dir = auth_home / "x"
    x_dir.mkdir(parents=True)
    (x_dir / "cookies.json").write_text(
        json.dumps(
            [
                {"name": "auth_token", "value": "aaa"},
                {"name": "ct0", "value": "bbb"},
            ]
        ),
        encoding="utf-8",
    )
    monkeypatch.setenv("AUTH_HOME", str(auth_home))
    monkeypatch.setenv("LAST30DAYS_CONFIG_DIR", "")
    monkeypatch.delenv("AUTH_TOKEN", raising=False)
    monkeypatch.delenv("CT0", raising=False)
    importlib.reload(env_module)

    config = env_module.get_config()

    assert config["AUTH_TOKEN"] == "aaa"
    assert config["CT0"] == "bbb"
    assert config["_AUTH_TOKEN_SOURCE"] == "auth_home"


def test_get_config_keeps_process_env_over_auth_home(tmp_path, monkeypatch):
    auth_home = tmp_path / "auth"
    x_dir = auth_home / "x"
    x_dir.mkdir(parents=True)
    (x_dir / "cookies.json").write_text(
        json.dumps(
            [
                {"name": "auth_token", "value": "aaa"},
                {"name": "ct0", "value": "bbb"},
            ]
        ),
        encoding="utf-8",
    )
    monkeypatch.setenv("AUTH_HOME", str(auth_home))
    monkeypatch.setenv("LAST30DAYS_CONFIG_DIR", "")
    monkeypatch.setenv("AUTH_TOKEN", "env-token")
    monkeypatch.setenv("CT0", "env-ct0")
    importlib.reload(env_module)

    config = env_module.get_config()

    assert config["AUTH_TOKEN"] == "env-token"
    assert config["CT0"] == "env-ct0"
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `python -m pytest C:\Users\ahnbu\.claude\skills\tests\test_last30days_auth_home.py -q`

Expected: first test fails because current `get_config()` never reads `AUTH_HOME/x/cookies.json`

- [ ] **Step 3: Implement the fallback before browser extraction and document it**

```python
def _load_auth_home_cookies(config: dict[str, Any]) -> dict[str, str]:
    if config.get("AUTH_TOKEN") and config.get("CT0"):
        return {}
    auth_home = Path(os.environ.get("AUTH_HOME", Path.home() / ".config" / "auth"))
    x_dir = Path(os.environ.get("AUTH_HOME_X", auth_home / "x"))
    link = x_dir / "cookies.json"
    candidates = [link] if link.exists() else sorted(x_dir.glob("cookies_*.json"), reverse=True)
    if not candidates:
        return {}
    try:
        data = json.loads(candidates[0].read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError):
        return {}
    out: dict[str, str] = {}
    for item in data:
        if item.get("name") == "auth_token":
            out["AUTH_TOKEN"] = item["value"]
        if item.get("name") == "ct0":
            out["CT0"] = item["value"]
    return out
```

```python
    auth_home_creds = _load_auth_home_cookies(config)
    for key, value in auth_home_creds.items():
        if not config.get(key):
            config[key] = value
            config[f"_{key}_SOURCE"] = "auth_home"

    browser_creds = extract_browser_credentials(config)
```

```markdown
- X unlock order: `AUTH_TOKEN/CT0` -> `AUTH_HOME/x/cookies.json` -> `FROM_BROWSER=auto` -> `XAI_API_KEY`
- `AUTH_HOME` fallback is silent and does not trigger browser prompts
```

- [ ] **Step 4: Run tests and direct config verification**

Run:

```powershell
python -m pytest C:\Users\ahnbu\.claude\skills\tests\test_last30days_auth_home.py -q
@'
import sys
from pathlib import Path
sys.path.insert(0, r"C:\Users\ahnbu\.claude\skills")
from last30days-my.scripts.lib.env import get_config
c = get_config()
print(c.get("_AUTH_TOKEN_SOURCE"))
'@ | python -
```

Expected:
- pytest passes
- direct check prints `auth_home` when process env lacks `AUTH_TOKEN`/`CT0` and `AUTH_HOME/x/cookies.json` exists

- [ ] **Step 5: Update skills changelog and commit only the targeted files with `cp` skill**

```text
Repo: C:\Users\ahnbu\.claude\skills
CHANGELOG scope: last30days auth home
Commit message: feat(last30days-my): add auth home x fallback
Expected commit contents: tests/test_last30days_auth_home.py, last30days-my/scripts/lib/env.py, last30days-my/SKILL.md, CHANGELOG.md
Do not stage unrelated deleted gstack-* files.
```

### Task 6: Deploy To `AUTH_HOME`, Update Runbooks, And Run Full Verification

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\README.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\development.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\crawling_logic.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\20260310_04_SNS 세션 인증정보(Auth) 갱신 가이드.md`
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`
- Runtime verify only: `C:\Users\ahnbu\.config\auth\*`
- Runtime verify only: `D:\vibe-coding\scrap_sns\auth`

- [ ] **Step 1: Run a stale-reference scan and confirm it still fails before docs update**

Run:

```powershell
rg -n "x_cookies_20260318|x_storage_state_20260318|auth/auth_twitter.json|D:\\vibe-coding\\scrap_sns\\auth\\\\auth_skool.json" `
  D:\vibe-coding\scrap_sns\README.md `
  D:\vibe-coding\scrap_sns\docs `
  C:\Users\ahnbu\.claude\skills\scrap-my\SKILL.md `
  C:\Users\ahnbu\.claude\skills\last30days-my\SKILL.md
```

Expected: at least one stale literal path match is still reported

- [ ] **Step 2: Deploy the runtime to the real auth home and perform the one-time migration**

Run:

```powershell
pwsh -NoProfile -File D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1 `
  -SourceRoot D:\vibe-coding\scrap_sns `
  -AuthHome "$env:USERPROFILE\.config\auth" `
  -LegacyAuthDir "D:\vibe-coding\scrap_sns\auth"

python "$env:USERPROFILE\.config\auth\renew.py" linkedin
python "$env:USERPROFILE\.config\auth\renew.py" threads
python "$env:USERPROFILE\.config\auth\renew.py" skool
python "$env:USERPROFILE\.config\auth\renew.py" x
```

Expected:
- sync step prints `SYNC_OK`
- X renew prints `EXPORTED_COOKIE_FILE=...` and `VALIDATED_COOKIE_TARGET=...`
- `D:\vibe-coding\scrap_sns\auth` becomes junction to `C:\Users\ahnbu\.config\auth`

- [ ] **Step 3: Update the runbooks and README to match the new runtime**

```markdown
- canonical runtime path: `C:\Users\ahnbu\.config\auth`
- deploy command: `pwsh -NoProfile -File .\scripts\sync_auth_runtime.ps1 ...`
- renew command: `python "$env:USERPROFILE\.config\auth\renew.py" <target>`
- flat compatibility files: `auth_linkedin.json`, `auth_threads.json`, `auth_skool.json`, `x_cookies_current.json`, `x_storage_state_current.json`
- X canonical source: `x/user_data`; derived artifacts: `cookies.json`, `storage_state.json`
```

- [ ] **Step 4: Run the full verification matrix**

Run:

```powershell
python -m pytest D:\vibe-coding\scrap_sns\tests\unit\test_auth_paths.py `
  D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py `
  D:\vibe-coding\scrap_sns\tests\unit\test_twitter_cli_adapter.py `
  D:\vibe-coding\scrap_sns\tests\unit\test_threads_http_adapter.py -q

python -m pytest D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py `
  D:\vibe-coding\scrap_sns\tests\integration\test_twitter_scrap_single_cli.py `
  D:\vibe-coding\scrap_sns\tests\integration\test_thread_scrap_single_browserless.py -q

python -m pytest D:\vibe-coding\scrap_sns\tests\smoke\test_linkedin_smoke.py `
  D:\vibe-coding\scrap_sns\tests\smoke\test_threads_smoke.py `
  D:\vibe-coding\scrap_sns\tests\smoke\test_twitter_smoke.py -v

node C:\Users\ahnbu\.claude\skills\scrap-my\scripts\x_twitter_cli_scrape.mjs "https://x.com/i/status/2034688795652816948"
python C:\Users\ahnbu\.claude\skills\scrap-my\scripts\skool_auth_scrape.py "https://www.skool.com/group/post?p=123"

@'
import sys
sys.path.insert(0, r"C:\Users\ahnbu\.claude\skills")
from last30days-my.scripts.lib.env import get_config
c = get_config()
print("AUTH_SOURCE", c.get("_AUTH_TOKEN_SOURCE"))
'@ | python -
```

Expected:
- all `scrap_sns` unit/integration/smoke tests pass
- `x_twitter_cli_scrape.mjs` reads `AUTH_HOME/x/cookies.json`
- `skool_auth_scrape.py` reads `AUTH_HOME/skool/storage_state.json`
- `get_config()` prints `AUTH_SOURCE auth_home` or `AUTH_SOURCE env` depending on explicit env presence

- [ ] **Step 5: Update `scrap_sns` changelog and commit the documentation/verification pass with `cp` skill**

```text
Repo: D:\vibe-coding\scrap_sns
CHANGELOG scope: auth runtime docs
Commit message: docs(auth): document auth home rollout
Expected commit contents: README.md, docs/*.md updates, CHANGELOG.md
```

## Self-Review

### Spec coverage

- stale `cookies.json` false-positive: Task 2에서 `VALIDATED_COOKIE_TARGET` 비교로 직접 해결했다.
- Windows 명령 불일치: 모든 실행 명령을 PowerShell/Python 기준으로 다시 작성했다.
- X `storage_state.json` 미갱신: Task 2 export 단계에서 `storage_state.json`도 같은 run에서 파생하도록 명시했다.
- flat compatibility: Task 2 sync script와 Task 6 deployment에서 stable flat symlink를 만든다.
- `scrap-my`, `last30days-my` fallback: Task 4, Task 5에 각각 분리했다.

### Placeholder scan

- `TBD`, `TODO`, “적절히”, “나중에” 같은 placeholder 없음.
- 각 task에 file path, test command, expected output, code block을 넣었다.

### Type and naming consistency

- stable flat names: `x_cookies_current.json`, `x_storage_state_current.json`
- nested stable names: `x/cookies.json`, `x/storage_state.json`
- canonical X source: `x/user_data`
- validation function name: `validate_x_cookie_target`
