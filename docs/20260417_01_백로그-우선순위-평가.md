---
created: 2026-04-17
session_id: 5ca19444-c5ad-4a50-9285-0a88ae5f1bcd
tags:
  - backlog
  - 우선순위
---

# 백로그 우선순위 평가

원본: [[BACKLOG]]

## 평가 기준

- **용이성** (5=쉬움, 1=어려움): 예상 작업 시간 + 의존성 복잡도
- **효과** (5=높음, 1=낮음): 사용자 체감 개선도 + 데이터 품질 영향

---

## 우선순위 평가표

|   순위   | ID             | 항목                        | 위치                                                 | 용이성 | 효과  |    Wave    | 문제 요약                                                                                                                                                  | 해결 방향                                                                                                        |     |     |
| :----: | -------------- | ------------------------- | -------------------------------------------------- | :-: | :-: | :--------: | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------ | --- | --- |
| ~~1~~  | ~~BL-0417-02~~ | ~~본문 클릭 비활성화~~            | `web_viewer/script.js`, `index.html`               |  5  |  4  | ~~Wave 1~~ | **완료 처리(2026-04-17)**: 본문 전체 click 제거, `.read-more-indicator`만 toggle, 텍스트 드래그 선택 가능                                                                   | `getReadMoreIndicatorHtml()` / `toggleExpandableText()` 추가 + `tests/unit/test_web_viewer_expandable_text.py` |     |     |
| ~~2~~  | ~~BL-0417-03~~ | ~~바로가기 터미널 숨기기~~          | `SNS허브_바로가기.lnk`                                   |  5  |  4  | ~~Wave 1~~ | **완료 처리(2026-04-17)**: `sns_hub.vbs` 기반 숨김 실행으로 전환. 작업표시줄의 레거시 `cmd.exe /c run_viewer.bat` 바로가기 교체 후 cmd 미노출 확인                                        | `.lnk` → `wscript.exe` + `sns_hub.vbs`, `README.md`/`package.json`/검증 스크립트 반영                                |     |     |
| ~~3~~  | ~~BL-0417-01~~ | ~~LinkedIn 복사 URN 노출~~    | `web_viewer/script.js` (copy-btn)                  |  4  |  4  | ~~Wave 1~~ | **완료 처리(2026-04-17)**: copy 메타가 `display_name` 우선으로 내려가도록 수정, URN 노출 제거                                                                                | `buildCopyText(post)` helper + `tests/unit/test_web_viewer_copy_string.py` 추가                                |     |     |
|   4    | BL-0417-04     | 업데이트 건수 0건 버그             | `server.py:96`, `web_viewer/script.js:176-177`     |  4  |  4  |   Wave 2   | 스크래핑 완료 후 "총 0건이 추가되었습니다" 메시지가 뜸. `server.py`가 `stats` 없이 응답하므로 JS fallback이 항상 0 반환                                                                   | `total_scrap.py`가 stdout 마지막에 `STATS:threads=N,linkedin=N,total=N` 출력 → `server.py`가 파싱해 응답에 포함              |     |     |
|   5    | BL-0412-03     | Threads 도메인 정규화 + 태그 키 통일 | 코드베이스 전체 (13개 파일)                                  |  4  |  5  |   Wave 2   | **분석완료**: 172건은 `threads.com`(현 공식 도메인) 키. 코드베이스가 `threads.net`(레거시) 하드코딩 중이라 태그 키 분산. 실측: `.net` 730건/`.com` 172건(exact중복155, username변경7, /t/CODE겸9) | Phase 1: 코드 `.net`→`.com` 전환 + `migrateLegacyTagKeys` 확장(code 기반 우선). Phase 2(BL-0417-05): 수집 데이터 URL 일괄 변환  |     |     |
|   6    | BL-0412-04     | 자동 태그 테스트 커버리지            | `tests/unit/test_web_viewer_auto_tagging.py`       |  4  |  4  |   Wave 2   | BL-0412-03 Phase 1에서 함께 해결. 필요 테스트 4개: `.net`→`.com` 정규화, `.net`+`.com` 공존 병합, username 변경 code 기반 병합, canonical 보존 케이스                                | BL-0412-03과 동시 실행                                                                                            |     |     |
|   7    | BL-0412-01     | Instagram CDN 403         | `server.py`, `web_viewer/script.js:handleImgError` |  2  |  5  |   Wave 3   | Instagram 이미지가 403으로 깨짐. wsrv.nl 프록시를 거치지 않는 케이스가 존재                                                                                                   | `server.py` 이미지 프록시 엔드포인트 또는 `local_images` 다운로드 파이프라인 확장                                                    |     |     |
|   8    | BL-0412-05     | Twitter 본문 오염             | `twitter_scrap.py`, `twitter_scrap_single.py`      |  2  |  5  |   Wave 3   | X 봇 차단으로 리다이렉트 → 다른 URL인데 같은 본문이 수집됨 (예: 두 트윗이 "몇년 전만 해도 아마존 본사에서…" 동일 본문). URL 기반 중복 제거로는 잡히지 않음                                                      | 본문 해시 기반 중복 감지 로직 추가 (1차). 근본 해결은 봇 차단 우회                                                                    |     |     |
|   9    | BL-0412-02     | Masonry 성능                | `web_viewer/script.js`                             |  2  |  3  |   Wave 4   | 975개 포스트 렌더 시 `setTimeout` 500ms+ 발생. 스크롤 시 버벅임                                                                                                        | 가상 스크롤 또는 페이지네이션 도입. 1000건 미만에서는 긴급도 낮음                                                                      |     |     |
| ~~10~~ | ~~BL-0413-01~~ | ~~Phase B: 태그 write~~     | —                                                  |  —  |  —  |     —      | **완료 처리(2026-04-17)**: 코드 탐색으로 태그 add/remove/서버동기화가 이미 구현되어 있음을 확인. 커밋 `042449e`                                                                       | —                                                                                                            |     |     |

---

## 의존관계

```
BL-0412-03 (Threads 도메인 정규화) ← BL-0412-04 (테스트 보강, 동시 실행)
BL-0412-03 Phase 1 완료 → BL-0417-05 (수집 데이터 URL 일괄 변환)
BL-0413-01: 완료 처리 (태그 write 이미 구현됨)
```

나머지 항목은 독립적.

---

## Wave 실행 계획

### Wave 1 — 완료 (2026-04-17)
| ID | 완료 내용 |
|---|---|
| BL-0417-02 | 본문 wrapper click 제거, `.read-more-indicator`만 toggle, 자동 테스트 추가 |
| BL-0417-03 | `sns_hub.vbs` 생성, `.lnk` 대상 변경, `README.md`/`package.json`/검증 스크립트 반영, 레거시 작업표시줄 바로가기 교체 후 cmd 미노출 확인 |
| BL-0417-01 | copy 메타를 `post.display_name \|\| post.username \|\| post.user` 우선순위로 수정하고 자동 테스트 추가 |

### Wave 2 — 단기
| ID | 핵심 작업 |
|---|---|
| BL-0417-04 | `total_scrap.py` stdout 마지막에 `STATS:` 출력 추가 → `server.py:96` 파싱 |
| BL-0412-03 | Threads 도메인 정규화: 코드 13개 파일 `.net`→`.com` 전환 + `migrateLegacyTagKeys` 확장(code 기반) + 태그 키 통일 |
| BL-0412-04 | 테스트 4개 추가 (BL-0412-03과 동시 실행) |

### Wave 3 — 중기
| ID | 핵심 작업 |
|---|---|
| BL-0412-01 | `server.py` 이미지 프록시 엔드포인트 또는 local_images 파이프라인 |
| BL-0412-05 | 본문 해시 중복 감지 로직 |

### Wave 4 — 후순위
| ID | 핵심 작업 |
|---|---|
| BL-0412-02 | 데이터 1000건 초과 시 가상 스크롤 도입 |
| BL-0417-05 | BL-0412-03 Phase 1 완료 후 수집 데이터 URL `.net`→`.com` 일괄 변환 |
