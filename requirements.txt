# Core
playwright==1.58.0
flask==3.1.1
flask-cors==6.0.1
requests==2.31.0
python-dotenv==1.2.1
beautifulsoup4==4.12.2
twitter-cli==0.8.5

# YouTube (자막 취득 — 버전 미고정: YouTube 대응상 최신 추종 필요)
yt-dlp
bgutil-ytdlp-pot-provider

# Dev
pytest==9.0.2
# playwright 본체와 별개 패키지다. `playwright`·`page` fixture 를 제공하며
# tests/e2e 8개와 tests/smoke 3개가 이것에 의존한다. 빠지면 그 테스트들이
# fixture 미발견으로 전부 error 가 난다.
pytest-playwright==0.7.2
