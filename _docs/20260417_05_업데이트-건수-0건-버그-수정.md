---
title: 업데이트 건수 0건 버그 수정 실행 기록
created: 2026-04-17 14:11

session_id: codex:019d99d4-36ed-7f10-8b33-a7167649c9f8
session_path: C:/Users/ahnbu/.codex/sessions/2026/04/17/rollout-2026-04-17T14-05-15-019d99d4-36ed-7f10-8b33-a7167649c9f8.jsonl

ai: codex
---

# 업데이트 건수 0건 버그 수정 실행 기록

## 발단: 사용자 요청

사용자는 `C:\Users\ahnbu\.claude\plans\serialized-gathering-sunrise.md` 실행을 요청했고, 이어서 worktree 없이 현재 workspace에서 처리하도록 지시했다. 대상 버그는 스크래핑 완료 후 뷰어 alert가 항상 `총 0건이 추가되었습니다`로 표시되는 문제였다.

## 작업 상세내역

1. 원인 확인
   `server.py`의 `/api/run-scrap` 응답에는 `stats` 필드가 없고, `web_viewer/script.js`는 `result.stats`가 비어 있으면 `0`으로 fallback하도록 되어 있었다.
2. TDD 적용
   `tests/integration/test_run_scrap_stats.py`를 새로 추가해 `stats` 필드 존재, before/after metadata delta 계산, 첫 실행 시 delta=전체건수 동작을 먼저 실패하도록 만들었다.
3. 최소 수정 구현
   `server.py`에 최신 `total_full_*.json`의 metadata를 읽는 `_read_latest_metadata()`를 추가하고, subprocess 실행 전후 count 차이를 `stats`로 계산해 응답에 포함했다.
4. UI 문구 정리
   `web_viewer/script.js`에서 `update` 모드는 신규 추가 건수와 전체 건수를 함께 보여주고, `all` 모드는 전체 재집계 결과만 보여주도록 분기했다. 이때 `X` 카운트도 함께 표시하도록 보완했다.
5. 검증 중 부산물 정리
   전체 테스트 실행 중 snapshot과 failure json이 갱신됐고, stale `index.lock`도 남았다. 실행 중 `git` 프로세스가 없음을 확인한 뒤 lock과 untracked snapshot을 제거하고 tracked fixture는 복구했다.

## 의사결정 기록

| 항목 | 파일 metadata 비교 | stdout 파싱 |
|------|--------------------|-------------|
| 안정성 | ✅ 기존 `total_full_*.json` 구조 재사용 | ❌ 이모지/한글 로그 포맷 변화에 취약 |
| 구현 범위 | ✅ `server.py`와 뷰어만 수정 | ⚠️ `total_scrap.py` 출력 형식 의존 |
| 유지보수 | ✅ count source가 저장 데이터와 일치 | ❌ 로그 문구 변경 시 함께 깨짐 |

<span style="color:#888">*정렬 기준: 사용자에게 바로 보이는 건수 정확도와 기존 저장 구조 재사용성을 가장 우선했다.*</span>

- 결정: `total_full_*.json` metadata를 before/after 비교해 `stats`를 계산하는 파일 기반 접근을 채택했다.
- 근거: `total_scrap.py`가 이미 `total_count`, `threads_count`, `linkedin_count`, `twitter_count`를 저장하고 있어 추가 수집 로직 수정이 필요 없었다.
- 트레이드오프: subprocess 실패 시 이전 metadata를 읽어 `delta=0`처럼 보일 수 있는 한계는 남아 있다. 이번 범위에서는 `returncode` 처리까지 확장하지 않았다.

## 검증계획과 실행결과

| 검증 항목 | 검증 방법 | 결과 | 비고 |
|-----------|-----------|------|------|
| 신규 stats 회귀 테스트 | `pytest tests/integration/test_run_scrap_stats.py -v` | ✅ 통과 | 3개 테스트 통과 |
| API 보안 회귀 | `pytest tests/e2e/test_api_security.py -v` | ✅ 통과 | 10개 테스트 통과 |
| 전체 테스트 스위트 | `pytest tests/ -v` | ❌ 실패 | 69개 중 9개 실패, 이번 변경 파일과 무관한 기존 실패 확인 |
| 브라우저 수동 확인 | 실제 스크래핑 실행 후 alert 확인 | ⏳ 미실행 | 자동화 범위 밖 |

## 리스크 및 미해결 이슈

- `server.py`는 여전히 subprocess `returncode`를 검사하지 않는다. 스크래핑 실패 시 이전 파일 기준 `stats`가 노출될 수 있다.
- 전체 테스트 실패 9건은 이번 변경 범위 밖이다. 확인된 유형은 기존 UI locator mismatch, Threads/LinkedIn/Twitter smoke에서 Playwright sync API 충돌, parser 기대값 불일치다.
- 사용자 변경으로 보이는 `BACKLOG.md` 수정은 건드리지 않았다.

## 다음 액션

- 필요하면 `run_scrap`에 `returncode` 검사와 실패 메시지 분기를 추가한다.
- 별도 작업으로 전체 테스트 실패 9건을 정리해 smoke/e2e baseline을 복구한다.
- 수동 브라우저 확인이 필요하면 실제 `update`/`all` 모드 alert 문구를 점검한다.

## 참고 자료

| 출처 | 용도 |
|------|------|
| `C:\Users\ahnbu\.claude\plans\serialized-gathering-sunrise.md` | 실행 기준 plan 원문 |
| `server.py`, `web_viewer/script.js`, `tests/integration/test_run_scrap_stats.py` | 실제 구현 및 검증 대상 파일 |
| `pytest tests/integration/test_run_scrap_stats.py -v` | 신규 동작 검증 근거 |
| `pytest tests/e2e/test_api_security.py -v` | 보안 회귀 없음 확인 |
| `pytest tests/ -v` | 전체 테스트 현황과 기존 실패 확인 |

---

> Plan 원문 — 원본: `C:\Users\ahnbu\.claude\plans\serialized-gathering-sunrise.md`

# (Plan) BL-0417-04: 업데이트 건수 0건 버그 수정

## Context

스크래핑 완료 후 뷰어에서 "총 0건이 추가되었습니다" 메시지가 항상 뜨는 버그. `server.py`가 응답에 `stats` 필드를 포함하지 않아 JS fallback이 항상 0을 반환한다.

**접근 방식: 파일 기반 (stdout 파싱 X)**
- `total_scrap.py`는 이미 `total_full_*.json`에 metadata(`total_count`, `threads_count`, `linkedin_count`, `twitter_count`)를 저장함
- `server.py`에 이미 `total_full_*.json`을 glob+sort로 읽는 패턴이 있음 (`get_latest_data`)
- stdout 파싱은 이모지/한국어 포함 텍스트라 취약. 파일 기반이 안정적이고 `total_scrap.py` 변경 불필요

**신규 건수 계산**: subprocess 실행 전/후 metadata를 비교하여 delta를 계산 → "N건 추가 (전체 M건)" 형태로 표시

---

## 수정 파일

### 1. `server.py` — 핵심 수정

**Step 1: `_read_latest_metadata()` 헬퍼 추가** (71줄, `ALLOWED_SCRAP_MODES` 바로 위)

```python
def _read_latest_metadata():
    """최신 total_full_*.json의 metadata를 읽는다. 파일이 없으면 None."""
    pattern = os.path.join(OUTPUT_TOTAL_DIR, "total_full_*.json")
    files = [
        path for path in glob.glob(pattern)
        if re.fullmatch(r"total_full_\d{8}\.json", os.path.basename(path))
    ]
    if not files:
        return None
    files.sort(reverse=True)
    with open(files[0], 'r', encoding='utf-8-sig') as f:
        data = json.load(f)
    return data.get('metadata')
```

> `get_latest_data()`의 glob+sort 패턴 재사용. 전체 JSON이 아닌 metadata만 반환하여 경량.

**Step 2: `run_scrap()` 수정** (74-104줄)

- subprocess 실행 전: `_read_latest_metadata()`로 before counts 캡처
- subprocess 완료 후: 다시 읽어 after counts 캡처
- delta 계산 후 `stats` 필드를 응답에 포함

```python
# Before subprocess (mode 검증 후, env 설정 전)
before_meta = _read_latest_metadata()
before_counts = {
    'total': (before_meta or {}).get('total_count', 0),
    'threads': (before_meta or {}).get('threads_count', 0),
    'linkedin': (before_meta or {}).get('linkedin_count', 0),
    'twitter': (before_meta or {}).get('twitter_count', 0),
}

# After process.wait() + stdout_val 구성 후
after_meta = _read_latest_metadata()
after_counts = {
    'total': (after_meta or {}).get('total_count', 0),
    'threads': (after_meta or {}).get('threads_count', 0),
    'linkedin': (after_meta or {}).get('linkedin_count', 0),
    'twitter': (after_meta or {}).get('twitter_count', 0),
}

stats = {
    'total': after_counts['total'] - before_counts['total'],
    'threads': after_counts['threads'] - before_counts['threads'],
    'linkedin': after_counts['linkedin'] - before_counts['linkedin'],
    'twitter': after_counts['twitter'] - before_counts['twitter'],
    'total_count': after_counts['total'],
    'threads_count': after_counts['threads'],
    'linkedin_count': after_counts['linkedin'],
    'twitter_count': after_counts['twitter'],
}

return jsonify({
    "status": "success",
    "message": "Scraping finished",
    "output": stdout_val,
    "stats": stats
})
```

### 2. `web_viewer/script.js` — 메시지 템플릿 수정 (175-178줄)

`mode`에 따라 메시지를 분기한다:
- **update 모드**: delta 기반 "N건 신규 추가" 표시
- **all 모드**: 전체 재집계 결과만 표시 (delta가 음수이거나 어색할 수 있으므로)

```javascript
if (result.status === 'success') {
    const stats = result.stats || { total: 0, threads: 0, linkedin: 0, twitter: 0, total_count: 0, threads_count: 0, linkedin_count: 0, twitter_count: 0 };
    let msg;
    if (mode === 'all') {
        msg = `전체 재수집 완료! (전체 ${stats.total_count}건)\n\n`
            + `쓰레드 — ${stats.threads_count}건\n`
            + `링크드인 — ${stats.linkedin_count}건\n`
            + `X — ${stats.twitter_count}건\n\n`
            + `데이터를 새로고침합니다.`;
    } else {
        msg = `스크래핑 완료! 총 ${stats.total}건 신규 추가 (전체 ${stats.total_count}건)\n\n`
            + `쓰레드 — ${stats.threads}건 추가 (전체 ${stats.threads_count}건)\n`
            + `링크드인 — ${stats.linkedin}건 추가 (전체 ${stats.linkedin_count}건)\n`
            + `X — ${stats.twitter}건 추가 (전체 ${stats.twitter_count}건)\n\n`
            + `데이터를 새로고침합니다.`;
    }
    alert(msg);
    fetchData();
}
```

변경점:
- twitter/x 카운트 추가 (기존에 누락)
- **all 모드 분기**: delta가 아닌 전체 건수만 표시 (linkedin_scrap.py의 all 모드가 데이터를 덮어쓸 수 있어 delta가 음수/무의미할 수 있음)
- update 모드: delta("N건 추가")와 absolute("전체 M건")를 함께 표시

### 3. `tests/integration/test_run_scrap_stats.py` — 새 파일

기존 `conftest.py`의 `client` fixture를 재사용. 3개 테스트 케이스:

1. **`test_stats_present_in_response`**: 응답에 `stats` 필드가 존재하고 필수 키(`total`, `threads`, `linkedin`, `twitter`, `*_count`) 포함
2. **`test_stats_delta_calculation`**: before 파일(100건) → subprocess가 after 파일(110건) 생성 → delta=10 검증
3. **`test_stats_first_run_no_prior_file`**: 사전 파일 없음 → delta=전체건수 검증

> `server.OUTPUT_TOTAL_DIR`을 `tmp_path`로 패치하고, `subprocess.Popen`을 mock하여 실제 스크래핑 없이 테스트.

---

## 수정하지 않는 파일

- `total_scrap.py`: 변경 불필요. metadata에 이미 필요한 count가 있음
- `index.html`: 스크래핑 UI 요소 변경 없음

---

## 리스크 (이번 범위 밖, 인지만)

1. **`process.returncode` 미검사**: `server.py:86`은 현재 subprocess 종료 코드를 확인하지 않고 항상 success를 반환함. 스크래핑이 실패해도 이전 파일 기준 stats(delta=0)가 반환될 수 있음. 별도 백로그 후보.
2. **`_read_latest_metadata()` 예외**: 파일 I/O 실패 시 전체 run_scrap이 500으로 빠짐. 현재 try-except 블록 안에 있으므로 치명적이진 않지만, 헬퍼 자체에 방어 코드를 넣을지는 구현 시 판단.

---

## 검증

1. `pytest tests/integration/test_run_scrap_stats.py -v` — 3개 신규 테스트 통과
2. `pytest tests/e2e/test_api_security.py -v` — 기존 보안 테스트 회귀 없음
3. `pytest tests/ -v` — 전체 테스트 통과
4. 수동: 브라우저에서 스크래핑 실행 → alert에 정확한 건수 표시 확인
