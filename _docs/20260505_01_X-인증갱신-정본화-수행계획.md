# X 인증 갱신 정본화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** X 인증 갱신 시 `AUTH_HOME/x/user_data`를 유일한 정본으로 갱신하고, `cookies.json`은 같은 로그인 context에서 파생된 산출물일 때만 성공으로 인정한다.

**Architecture:** 수동 로그인은 persistent Chrome profile인 `AUTH_HOME/x/user_data`에만 저장한다. 갱신 직후 같은 Playwright context에서 `storage_state.json`과 dated cookie snapshot을 만들고, 별도 비파괴 probe로 producer와 consumer 인증을 각각 검증한다. 개별 글 수집기(`twitter_scrap_single.py`)의 상세 수집 로직은 건드리지 않고, producer 인증 실패 오탐만 좁게 수정한다.

**Tech Stack:** Python 3.13, Playwright sync API, PowerShell, pytest, Windows symlink/junction, `twitter-cli`

---

## Execution Result

- Status: implemented and verified
- Executed at: 2026-05-05 00:50 KST
- Implementation summary:
  - `export_x_artifacts_from_context()`를 추가해 X 로그인 직후 같은 Playwright context에서 `storage_state.json`과 dated cookie snapshot을 파생하도록 변경했다.
  - `renew.py x` normal/web 흐름에서 context를 닫기 전에 export하고 `USER_DATA_OK`를 출력하도록 변경했다.
  - `scripts/auth_runtime/verify_x_auth.py`를 추가해 producer `user_data`와 consumer `cookies.json`을 비파괴로 검증한다.
  - Chrome channel headless persistent context가 `Browser window not found`로 흔들릴 때 bundled Chromium headless fallback을 사용하도록 probe를 안정화했다.
  - producer probe는 `Bookmarks?variables=` 응답을 최대 10초 명시적으로 기다린 뒤 판정하도록 보강했다.
  - `twitter_scrap.py`는 북마크 네트워크 응답을 이미 받았으면 DOM article 부재만으로 인증 실패 처리하지 않도록 수정했다.
  - `tests/smoke/test_twitter_smoke.py`는 새 비파괴 probe 경로를 사용하도록 갱신했다.
  - `twitter_scrap_single.py`의 상세 글/타래 수집 로직은 변경하지 않았다.
- TDD red checks:
  - `pytest tests/unit/test_export_x_artifacts.py -q` -> 새 context export 함수 부재로 2개 실패 확인
  - `pytest tests/unit/test_x_auth_probe.py -q` -> `verify_x_auth.py` 부재, transient launch helper 부재, fallback config 부재로 실패 확인
  - `pytest tests/integration/test_auth_runtime_sync.py -q` -> `verify_x_auth.py` runtime 배포 누락으로 실패 확인
  - `pytest tests/unit/test_twitter_auth_detection.py -q` -> `should_require_x_auth` 부재와 import-time stdout wrapping 문제 확인
- Verification:
  - `pytest tests/unit/test_export_x_artifacts.py tests/unit/test_x_auth_probe.py tests/unit/test_twitter_auth_detection.py tests/unit/test_auth_paths.py tests/unit/test_twitter_cli_adapter.py tests/integration/test_auth_runtime_sync.py -q` -> `30 passed`
  - `powershell -ExecutionPolicy Bypass -File .\scripts\sync_auth_runtime.ps1 -SourceRoot (Get-Location).Path -AuthHome "$env:USERPROFILE\.config\auth" -LegacyAuthDir ".\auth"` -> `SYNC_OK`
  - `python scripts\auth_runtime\verify_x_auth.py` -> `{"producer_ok":true,"consumer_ok":true}`
  - `pytest tests/smoke/test_twitter_smoke.py -q` -> `1 passed`
- Not executed:
  - `python "$env:USERPROFILE\.config\auth\renew.py" x`는 실제 X 로그인 완료 Enter가 필요한 대화형 명령이라 자동 실행하지 않았다. 현재 세션 유효성은 `verify_x_auth.py`로 producer/consumer 모두 통과 확인했다.
- Operational note:
  - 같은 `AUTH_HOME/x/user_data/`를 여는 producer probe와 smoke test는 병렬 실행하지 않는다. 병렬 실행 시 한쪽 검증이 `producer_ok:false` 또는 Chrome launch 오류를 낼 수 있다.
- Persistence and migration:
  - Existing output JSON migration: not required
  - Runtime auth files were synced to `C:\Users\ahnbu\.config\auth`
  - `output_twitter/*.json` and `output_total/*.json` schema unchanged

---

## Source Review

- 검수보고서: `D:\vibe-coding\scrap_sns\docs\plan-check\20260505_x-auth-renew-plan_검수보고서.md`
- 현재 export 문제 지점: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\export_x_artifacts.py`
  - `export_x_artifacts()`가 `AUTH_HOME/x/user_data`를 `headless=True`로 다시 연다.
- 현재 renew 문제 지점: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\renew.py`
  - `renew_x_profile()`은 로그인 후 context를 닫고, CLI 흐름에서 별도 단계로 export한다.
  - `renew_x_profile_web()`은 `context.storage_state()` 후 다시 `export_x_artifacts()`를 호출한다.
- 현재 producer 오탐 지점: `D:\vibe-coding\scrap_sns\twitter_scrap.py`
  - 북마크 네트워크 응답을 받았더라도 `article[data-testid="tweet"]`가 없으면 `SNS_AUTH_REQUIRED`로 빠질 수 있다.
- 현재 consumer 구조: `D:\vibe-coding\scrap_sns\utils\twitter_cli_adapter.py`
  - `AUTH_HOME/x/cookies.json` 또는 최신 dated cookie에서 `auth_token`, `ct0`를 읽어 `twitter-cli`에 전달한다.

---

## Plan-Check 반영표

| 중요도 | 검수 지적 | 반영 방식 |
|---|---|---|
| 1 | `export_x_artifacts.py`가 headless로 profile을 다시 열어 실패 가능성이 남음 | live context 기반 `export_x_artifacts_from_context()`를 먼저 추가 |
| 2 | 인증 검증에 실제 수집 명령을 쓰면 output JSON이 바뀔 수 있음 | 비파괴 `verify_x_auth.py` probe를 신규 추가 |
| 3 | 북마크 API 응답 후에도 DOM article 부재만으로 인증 실패 오탐 가능 | producer의 인증 실패 조건을 네트워크 응답 상태와 함께 판단 |
| 4 | 개별 글 수집 로직을 건드리면 타래/상세 수집 회귀 위험 | `twitter_scrap_single.py` 수집 로직은 scope out |
| 5 | README 성공 기준이 cookie 중심이라 운영 실수를 유발 | 성공 기준을 `user_data producer probe + cookie export + consumer token check`로 변경 |

> 정렬 기준: 같은 문제가 반복될 가능성과 실제 데이터 변경 위험을 먼저 배치했다.

---

## Scope Lock

### In Scope

- X 수동 인증 갱신 흐름
- `AUTH_HOME/x/user_data` 정본 원칙 강제
- `cookies_YYYYMMDD_HHmm.json`, `cookies.json`, `storage_state.json` 파생 생성
- producer용 북마크 접근 probe
- consumer용 cookie token probe
- `twitter_scrap.py`의 인증 실패 오탐 방지
- README와 관련 운영 문구 현행화

### Out of Scope

- `twitter_scrap_single.py`의 상세 글/타래 수집 로직 변경
- 기존 `output_twitter/*.json`, `output_total/*.json` 데이터 rewrite
- X self-thread 병합 정책 변경
- LinkedIn 인증 갱신 구현
- 앱의 로그인 열기 버튼 또는 자동화 브라우저 로그인 UX 재설계

---

## Persistence Surface

- DB tables: 없음
- Cache/index files: 없음
- Auth runtime files:
  - `C:\Users\ahnbu\.config\auth\x\user_data\`
  - `C:\Users\ahnbu\.config\auth\x\cookies_YYYYMMDD_HHmm.json`
  - `C:\Users\ahnbu\.config\auth\x\cookies.json`
  - `C:\Users\ahnbu\.config\auth\x\storage_state.json`
  - `C:\Users\ahnbu\.config\auth\x_cookies_current.json`
  - `C:\Users\ahnbu\.config\auth\x_storage_state_current.json`
- Output JSON schema: 변경 없음
- Existing data migration: 필요 없음
- 근거: 인증 runtime 산출물만 갱신하며 게시글 payload 필드, 통합 JSON 구조, viewer cache 구조를 바꾸지 않는다.
- Migration command: 없음
- Migration verification command:

```powershell
python "$env:USERPROFILE\.config\auth\renew.py" x
python D:\vibe-coding\scrap_sns\scripts\auth_runtime\verify_x_auth.py
```

Expected:

```text
USER_DATA_OK
EXPORTED_COOKIE_FILE=cookies_YYYYMMDD_HHmm.json
VALIDATED_COOKIE_TARGET=cookies_YYYYMMDD_HHmm.json
{"producer_ok":true,"consumer_ok":true}
```

---

## File Structure

- Modify: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\export_x_artifacts.py`
  - live context에서 X cookie/storage_state를 파생하는 함수를 추가한다.
  - 기존 headless 재오픈 함수는 fallback으로 유지한다.
- Modify: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\renew.py`
  - X 로그인 완료 후 context를 닫기 전에 artifact export를 수행한다.
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\verify_x_auth.py`
  - producer/consumer 인증 상태를 비파괴로 검증한다.
- Modify: `D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1`
  - `verify_x_auth.py`도 `AUTH_HOME`로 배포한다.
- Modify: `D:\vibe-coding\scrap_sns\twitter_scrap.py`
  - 북마크 네트워크 응답을 반영해 인증 실패 오탐을 줄인다.
- Modify: `D:\vibe-coding\scrap_sns\README.md`
  - X 인증 갱신 성공 기준을 user_data 정본 중심으로 바꾼다.
- Modify: `D:\vibe-coding\scrap_sns\docs\crawling_logic.md`
  - X producer/consumer 인증 자산 관계를 명확히 적는다.
- Modify: `D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py`
  - context 기반 export와 stale relink 방지를 검증한다.
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_x_auth_probe.py`
  - probe 판정 로직과 JSON 출력 계약을 검증한다.
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_twitter_auth_detection.py`
  - producer 인증 실패 판정 helper를 검증한다.
- Modify: `D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py`
  - sync script가 `verify_x_auth.py`를 배포하는지 확인한다.
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`
  - 커밋 전 변경 이력을 추가한다.

---

## Task 1: Context-Based X Artifact Export

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\export_x_artifacts.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py`

- [ ] **Step 1: Add failing tests for live context export**

Add these tests to `tests/unit/test_export_x_artifacts.py`.

```python
import json

import pytest

import scripts.auth_runtime.export_x_artifacts as export_mod


class FakeXContext:
    def __init__(self, cookies):
        self._cookies = cookies
        self.storage_state_paths = []
        self.cookies_urls = []

    def storage_state(self, path):
        self.storage_state_paths.append(path)
        with open(path, "w", encoding="utf-8") as file:
            json.dump({"cookies": [], "origins": []}, file)

    def cookies(self, url):
        self.cookies_urls.append(url)
        return self._cookies


def test_export_x_artifacts_from_context_writes_current_cookie_links(tmp_path, monkeypatch):
    monkeypatch.setenv("AUTH_HOME", str(tmp_path))

    context = FakeXContext([
        {"name": "auth_token", "value": "token"},
        {"name": "ct0", "value": "ct0"},
    ])

    cookie_path, storage_path = export_mod.export_x_artifacts_from_context(
        context,
        stamp="20260505_1200",
    )

    assert cookie_path.name == "cookies_20260505_1200.json"
    assert storage_path.name == "storage_state.json"
    assert context.cookies_urls == ["https://x.com"]
    assert (tmp_path / "x" / "cookies.json").resolve().name == "cookies_20260505_1200.json"
    assert (tmp_path / "x_cookies_current.json").is_symlink()


def test_export_x_artifacts_from_context_rejects_missing_tokens_without_relink(tmp_path, monkeypatch):
    monkeypatch.setenv("AUTH_HOME", str(tmp_path))
    x_dir = tmp_path / "x"
    x_dir.mkdir(parents=True)
    old_cookie = x_dir / "cookies_20260504_1800.json"
    old_cookie.write_text(
        json.dumps([
            {"name": "auth_token", "value": "old"},
            {"name": "ct0", "value": "old-ct0"},
        ]),
        encoding="utf-8",
    )
    (x_dir / "cookies.json").symlink_to(old_cookie.name)

    context = FakeXContext([{"name": "auth_token", "value": "new"}])

    with pytest.raises(RuntimeError, match="auth_token/ct0 missing"):
        export_mod.export_x_artifacts_from_context(context, stamp="20260505_1200")

    assert (x_dir / "cookies.json").resolve().name == "cookies_20260504_1800.json"
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```powershell
pytest tests/unit/test_export_x_artifacts.py -q
```

Expected before implementation:

```text
AttributeError: module 'scripts.auth_runtime.export_x_artifacts' has no attribute 'export_x_artifacts_from_context'
```

- [ ] **Step 3: Implement context export helper**

In `export_x_artifacts.py`, add a shared writer and a live context entrypoint.

```python
def _write_x_artifacts(cookies: list[dict], storage_path: Path, stamp: str) -> tuple[Path, Path]:
    token_pair = extract_token_pair(cookies)
    if token_pair is None:
        raise RuntimeError("auth_token/ct0 missing after X export")

    x_root = x_user_data().parent
    x_root.mkdir(parents=True, exist_ok=True)
    dated_cookie_path = x_root / f"cookies_{stamp}.json"
    dated_cookie_path.write_text(
        json.dumps(cookies, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )

    _refresh_link(x_cookie_link(), dated_cookie_path)
    _refresh_link(x_flat_cookie(), x_cookie_link())
    _refresh_link(x_flat_storage(), storage_path)

    if not validate_x_cookie_target(dated_cookie_path.name):
        raise RuntimeError("cookies.json still points to stale export target")

    print(f"EXPORTED_COOKIE_FILE={dated_cookie_path.name}")
    print(f"VALIDATED_COOKIE_TARGET={dated_cookie_path.name}")
    return dated_cookie_path, storage_path


def export_x_artifacts_from_context(context, stamp: str | None = None) -> tuple[Path, Path]:
    stamp = stamp or datetime.now().strftime("%Y%m%d_%H%M")
    x_user_data().mkdir(parents=True, exist_ok=True)
    storage_path = x_storage()
    storage_path.parent.mkdir(parents=True, exist_ok=True)
    context.storage_state(path=str(storage_path))
    cookies = context.cookies("https://x.com")
    return _write_x_artifacts(cookies, storage_path, stamp)
```

Then change existing `export_x_artifacts()` to call `_write_x_artifacts()` after it opens the fallback context and extracts cookies.

- [ ] **Step 4: Verify export tests**

Run:

```powershell
pytest tests/unit/test_export_x_artifacts.py -q
```

Expected:

```text
passed
```

---

## Task 2: Renew Flow Uses Same Login Context

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\renew.py`
- Modify: `D:\vibe-coding\scrap_sns\tests\unit\test_export_x_artifacts.py`

- [ ] **Step 1: Import the new helper**

Change the X export import in `renew.py` so both normal and web renewal can use the live context helper.

```python
try:
    from export_x_artifacts import export_x_artifacts, export_x_artifacts_from_context
except ImportError:
    from scripts.auth_runtime.export_x_artifacts import (
        export_x_artifacts,
        export_x_artifacts_from_context,
    )
```

- [ ] **Step 2: Export before closing the normal X context**

In `renew_x_profile()`, replace the current close-only ending:

```python
input("[X] 로그인 완료 후 Enter: ")
context.close()
```

with:

```python
input("[X] 로그인 완료 후 Enter: ")
export_x_artifacts_from_context(context)
print("USER_DATA_OK")
context.close()
```

- [ ] **Step 3: Export before closing the web X context**

In `renew_x_profile_web()`, replace:

```python
context.storage_state(path=str(x_storage()))
context.close()
export_x_artifacts()
```

with:

```python
export_x_artifacts_from_context(context)
print("USER_DATA_OK")
context.close()
```

- [ ] **Step 4: Remove the second export from the normal X target flow**

In the main target loop, replace:

```python
else:
    renew_x_profile()
    export_x_artifacts()
```

with:

```python
else:
    renew_x_profile()
```

Keep `export_x_artifacts()` available as a fallback function for future external manual Chrome recovery flows, but do not call it immediately after `renew_x_profile()` because that would reopen `user_data`.

- [ ] **Step 5: Run targeted verification**

Run:

```powershell
pytest tests/unit/test_export_x_artifacts.py -q
python "$env:USERPROFILE\.config\auth\renew.py" x
```

Expected:

```text
USER_DATA_OK
EXPORTED_COOKIE_FILE=cookies_YYYYMMDD_HHmm.json
VALIDATED_COOKIE_TARGET=cookies_YYYYMMDD_HHmm.json
```

---

## Task 3: Non-Destructive X Auth Probe

**Files:**
- Create: `D:\vibe-coding\scrap_sns\scripts\auth_runtime\verify_x_auth.py`
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_x_auth_probe.py`
- Modify: `D:\vibe-coding\scrap_sns\scripts\sync_auth_runtime.ps1`
- Modify: `D:\vibe-coding\scrap_sns\tests\integration\test_auth_runtime_sync.py`

- [ ] **Step 1: Add failing unit tests for probe classification**

Create `tests/unit/test_x_auth_probe.py`.

```python
from scripts.auth_runtime.verify_x_auth import classify_producer_probe, build_probe_report


def test_classify_producer_probe_accepts_bookmark_network_response():
    assert classify_producer_probe(
        current_url="https://x.com/i/bookmarks",
        bookmark_response_seen=True,
        parsed_bookmark_count=1,
        article_count=0,
    ) == (True, "bookmark_response")


def test_classify_producer_probe_rejects_login_url():
    assert classify_producer_probe(
        current_url="https://x.com/i/flow/login",
        bookmark_response_seen=False,
        parsed_bookmark_count=0,
        article_count=0,
    ) == (False, "login_required")


def test_build_probe_report_is_machine_readable():
    assert build_probe_report(producer_ok=True, consumer_ok=True) == {
        "producer_ok": True,
        "consumer_ok": True,
    }
```

- [ ] **Step 2: Run tests and confirm failure**

Run:

```powershell
pytest tests/unit/test_x_auth_probe.py -q
```

Expected before implementation:

```text
ModuleNotFoundError: No module named 'scripts.auth_runtime.verify_x_auth'
```

- [ ] **Step 3: Implement `verify_x_auth.py`**

Create the script with these public helpers and CLI behavior.

```python
from __future__ import annotations

import json
import sys
from pathlib import Path

from playwright.sync_api import sync_playwright

try:
    from auth_paths import read_x_cookie_tokens, x_cookie_link, x_user_data
except ImportError:
    REPO_ROOT = Path(__file__).resolve().parents[2]
    if str(REPO_ROOT) not in sys.path:
        sys.path.insert(0, str(REPO_ROOT))
    from utils.auth_paths import read_x_cookie_tokens, x_cookie_link, x_user_data


def classify_producer_probe(
    *,
    current_url: str,
    bookmark_response_seen: bool,
    parsed_bookmark_count: int,
    article_count: int,
) -> tuple[bool, str]:
    lowered = current_url.lower()
    if "login" in lowered or "signup" in lowered or "challenge" in lowered:
        return False, "login_required"
    if parsed_bookmark_count > 0:
        return True, "bookmark_response"
    if bookmark_response_seen or article_count > 0:
        return True, "bookmarks_loaded"
    return False, "no_bookmark_signal"


def build_probe_report(*, producer_ok: bool, consumer_ok: bool) -> dict[str, bool]:
    return {"producer_ok": producer_ok, "consumer_ok": consumer_ok}


def probe_consumer() -> bool:
    tokens = read_x_cookie_tokens(x_cookie_link())
    return bool(tokens and tokens.get("auth_token") and tokens.get("ct0"))


def probe_producer() -> tuple[bool, str]:
    state = {"bookmark_response_seen": False, "parsed_bookmark_count": 0}
    with sync_playwright() as playwright:
        context = playwright.chromium.launch_persistent_context(
            user_data_dir=str(x_user_data()),
            channel="chrome",
            headless=True,
        )
        page = context.pages[0] if context.pages else context.new_page()

        def handle_response(response):
            if "Bookmarks?variables=" not in response.url:
                return
            state["bookmark_response_seen"] = True
            if response.status != 200:
                return
            try:
                payload = response.json()
            except Exception:
                return
            state["parsed_bookmark_count"] += 1

        page.on("response", handle_response)
        page.goto("https://x.com/i/bookmarks", wait_until="domcontentloaded")
        page.wait_for_timeout(3000)
        article_count = page.locator('article[data-testid="tweet"]').count()
        result = classify_producer_probe(
            current_url=page.url,
            bookmark_response_seen=state["bookmark_response_seen"],
            parsed_bookmark_count=state["parsed_bookmark_count"],
            article_count=article_count,
        )
        context.close()
        return result


def main() -> int:
    producer_ok, _reason = probe_producer()
    consumer_ok = probe_consumer()
    print(json.dumps(build_probe_report(producer_ok=producer_ok, consumer_ok=consumer_ok), ensure_ascii=False))
    return 0 if producer_ok and consumer_ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

Note: `parsed_bookmark_count` is a signal count, not persisted data. The probe must not write `output_twitter` or `output_total`.

- [ ] **Step 4: Deploy probe through auth runtime sync**

In `scripts/sync_auth_runtime.ps1`, add:

```powershell
Copy-Item (Join-Path $SourceRoot "scripts\auth_runtime\verify_x_auth.py") (Join-Path $AuthHome "verify_x_auth.py") -Force
```

In `tests/integration/test_auth_runtime_sync.py`, assert:

```python
assert (auth_home / "verify_x_auth.py").exists()
```

- [ ] **Step 5: Verify probe and sync tests**

Run:

```powershell
pytest tests/unit/test_x_auth_probe.py tests/integration/test_auth_runtime_sync.py -q
python D:\vibe-coding\scrap_sns\scripts\auth_runtime\verify_x_auth.py
```

Expected:

```json
{"producer_ok":true,"consumer_ok":true}
```

---

## Task 4: Fix Producer Auth False Positive

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\twitter_scrap.py`
- Create: `D:\vibe-coding\scrap_sns\tests\unit\test_twitter_auth_detection.py`

- [ ] **Step 1: Add failing tests for producer auth decision**

Create `tests/unit/test_twitter_auth_detection.py`.

```python
from twitter_scrap import should_require_x_auth


def test_should_not_require_auth_when_bookmark_response_parsed_posts():
    assert should_require_x_auth(
        current_url="https://x.com/i/bookmarks",
        has_tweet_article=False,
        bookmark_response_seen=True,
        parsed_bookmark_count=3,
    ) is False


def test_should_require_auth_on_login_url():
    assert should_require_x_auth(
        current_url="https://x.com/i/flow/login",
        has_tweet_article=False,
        bookmark_response_seen=False,
        parsed_bookmark_count=0,
    ) is True


def test_should_require_auth_when_no_bookmark_signal_exists():
    assert should_require_x_auth(
        current_url="https://x.com/i/bookmarks",
        has_tweet_article=False,
        bookmark_response_seen=False,
        parsed_bookmark_count=0,
    ) is True
```

- [ ] **Step 2: Run test and confirm failure**

Run:

```powershell
pytest tests/unit/test_twitter_auth_detection.py -q
```

Expected before implementation:

```text
ImportError: cannot import name 'should_require_x_auth'
```

- [ ] **Step 3: Add decision helper to `twitter_scrap.py`**

Add near other small helper functions.

```python
def should_require_x_auth(
    *,
    current_url: str,
    has_tweet_article: bool,
    bookmark_response_seen: bool,
    parsed_bookmark_count: int,
) -> bool:
    url = (current_url or "").lower()
    if "login" in url or "signup" in url or "challenge" in url:
        return True
    if has_tweet_article:
        return False
    if parsed_bookmark_count > 0 or bookmark_response_seen:
        return False
    return True
```

- [ ] **Step 4: Track bookmark response state in producer**

Inside `main()` before `handle_response()`, add:

```python
bookmark_response_seen = False
parsed_bookmark_count = 0
```

Inside `handle_response(response)`, change:

```python
nonlocal new_count
if "Bookmarks?variables=" in response.url and response.status == 200:
```

to:

```python
nonlocal new_count, bookmark_response_seen, parsed_bookmark_count
if "Bookmarks?variables=" in response.url:
    bookmark_response_seen = True
if "Bookmarks?variables=" in response.url and response.status == 200:
```

After `new_posts = extract_from_json(response.json())`, add:

```python
parsed_bookmark_count += len(new_posts)
```

- [ ] **Step 5: Replace DOM-only auth check**

Replace:

```python
if not page.query_selector('article[data-testid="tweet"]'):
```

with:

```python
has_tweet_article = page.query_selector('article[data-testid="tweet"]') is not None
if should_require_x_auth(
    current_url=page.url,
    has_tweet_article=has_tweet_article,
    bookmark_response_seen=bookmark_response_seen,
    parsed_bookmark_count=parsed_bookmark_count,
):
```

If auth is not required but `has_tweet_article` is false, continue to the scroll stage without calling `wait_for_selector(..., timeout=0)`.

- [ ] **Step 6: Verify producer decision tests**

Run:

```powershell
pytest tests/unit/test_twitter_auth_detection.py -q
```

Expected:

```text
3 passed
```

---

## Task 5: Documentation And Runbook Update

**Files:**
- Modify: `D:\vibe-coding\scrap_sns\README.md`
- Modify: `D:\vibe-coding\scrap_sns\docs\crawling_logic.md`
- Modify: `D:\vibe-coding\scrap_sns\CHANGELOG.md`

- [ ] **Step 1: Update README X auth model**

Replace the current X auth description with this meaning:

```markdown
X(Twitter) 인증은 `AUTH_HOME/x/user_data/`를 유일한 수동 로그인 정본으로 사용합니다.
`AUTH_HOME/x/cookies.json`, `AUTH_HOME/x/storage_state.json`, `AUTH_HOME/x_cookies_current.json`은 `user_data`에서 파생된 산출물입니다.

- `twitter_scrap.py`: `AUTH_HOME/x/user_data/`로 북마크 목록을 수집합니다.
- `twitter_scrap_single.py`: `AUTH_HOME/x/cookies.json`에서 `auth_token`, `ct0`를 읽어 `twitter-cli` 상세 조회를 수행합니다.
- 쿠키만 갱신된 상태는 X 인증 갱신 완료로 보지 않습니다.
```

- [ ] **Step 2: Update README success criteria**

Replace the current “수동 갱신 성공 기준” with:

```markdown
수동 갱신 성공 기준:

1. `python "$env:USERPROFILE\.config\auth\renew.py" x` 출력에 `USER_DATA_OK`, `EXPORTED_COOKIE_FILE`, `VALIDATED_COOKIE_TARGET`가 모두 표시됩니다.
2. `python scripts\auth_runtime\verify_x_auth.py` 출력이 `{"producer_ok":true,"consumer_ok":true}`입니다.
3. `AUTH_HOME/x/cookies.json`은 최신 `cookies_YYYYMMDD_HHmm.json`을 가리키고, 해당 쿠키에 `auth_token`, `ct0`가 모두 존재합니다.

`twitter-cli` 상세 조회 성공만으로는 producer 인증 성공을 보장하지 않습니다.
```

- [ ] **Step 3: Update crawling logic docs**

In `docs/crawling_logic.md`, add the X auth relationship:

```markdown
X 인증 정본은 `AUTH_HOME/x/user_data/` 하나입니다.
producer는 이 profile을 직접 사용하고, consumer는 같은 profile에서 export된 `cookies.json`을 사용합니다.
따라서 인증 갱신 완료 판정은 producer probe와 consumer token probe가 모두 통과해야 합니다.
```

- [ ] **Step 4: Update changelog**

Add an entry under the current unreleased or latest section using the existing format:

```text
Scope: auth runtime
Change: X 인증 갱신을 user_data 정본과 context 기반 cookie export로 정비
Verification: pytest targeted auth tests, verify_x_auth.py
```

- [ ] **Step 5: Verify docs have no cookie-only success wording**

Run:

```powershell
rg -n "쿠키만|twitter-cli 상세 조회 성공만|producer_ok|consumer_ok|user_data" README.md docs\crawling_logic.md
```

Expected:

```text
README.md:... 쿠키만 갱신된 상태는 X 인증 갱신 완료로 보지 않습니다.
README.md:... {"producer_ok":true,"consumer_ok":true}
docs\crawling_logic.md:... producer probe와 consumer token probe가 모두 통과해야 합니다.
```

---

## Task 6: Full Verification

**Files:**
- No implementation changes in this task.

- [ ] **Step 1: Run unit and integration tests**

Run:

```powershell
pytest tests/unit/test_export_x_artifacts.py `
  tests/unit/test_x_auth_probe.py `
  tests/unit/test_twitter_auth_detection.py `
  tests/unit/test_auth_paths.py `
  tests/unit/test_twitter_cli_adapter.py `
  tests/integration/test_auth_runtime_sync.py -q
```

Expected:

```text
passed
```

- [ ] **Step 2: Sync runtime scripts**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\sync_auth_runtime.ps1 `
  -SourceRoot (Get-Location).Path `
  -AuthHome "$env:USERPROFILE\.config\auth" `
  -LegacyAuthDir ".\auth"
```

Expected:

```text
SYNC_OK
```

- [ ] **Step 3: Renew X auth manually**

Run:

```powershell
python "$env:USERPROFILE\.config\auth\renew.py" x
```

Expected:

```text
USER_DATA_OK
EXPORTED_COOKIE_FILE=cookies_YYYYMMDD_HHmm.json
VALIDATED_COOKIE_TARGET=cookies_YYYYMMDD_HHmm.json
```

- [ ] **Step 4: Run non-destructive auth probe**

Run:

```powershell
python scripts\auth_runtime\verify_x_auth.py
```

Expected:

```json
{"producer_ok":true,"consumer_ok":true}
```

- [ ] **Step 5: Optional producer smoke after probe success**

Run only after Step 4 passes:

```powershell
pytest tests/smoke/test_twitter_smoke.py -q
```

Expected:

```text
passed
```

Do not use `python twitter_scrap.py --mode update` as the auth renewal verification command. That command performs real collection and may change output files.

---

## Commit Plan

Use the project `cp` skill workflow if the user asks to commit. Do not run raw `git add` or `git commit` directly.

Recommended commit split:

1. `fix(auth): X 인증 갱신을 user_data 정본으로 검증`
   - export helper, renew flow, probe, sync script, tests
2. `fix(x): 북마크 producer 인증 오탐 방지`
   - `twitter_scrap.py`, producer auth detection test
3. `docs(auth): X 인증 갱신 성공 기준 정리`
   - README, crawling docs, CHANGELOG

---

## Self-Review

### Requirement Coverage

- `user_data` 하나만 수동 갱신한다: Task 1, Task 2, Task 5에서 반영.
- consumer는 `user_data`에서 파생된 `cookies.json`만 쓴다: Task 1, Task 3, Task 5에서 반영.
- producer는 `user_data`를 쓴다: Task 3, Task 4에서 검증.
- 개별 글 수집 로직은 건드리지 않는다: Scope Lock에 명시.
- 실제 수집 명령으로 인증 검증하지 않는다: Task 3, Task 6에서 비파괴 probe로 대체.

### Placeholder Scan

- 비어 있는 placeholder 표현 없음.
- 각 task에 파일 경로, 변경 위치, 실행 명령, 기대 출력 포함.

### Type And Naming Consistency

- Canonical profile: `AUTH_HOME/x/user_data`
- Nested cookie link: `AUTH_HOME/x/cookies.json`
- Flat cookie link: `AUTH_HOME/x_cookies_current.json`
- Probe output keys: `producer_ok`, `consumer_ok`
- New helper names: `export_x_artifacts_from_context`, `should_require_x_auth`, `classify_producer_probe`

### Residual Risk

- X가 headless producer probe를 차단할 경우 `verify_x_auth.py`가 실패할 수 있다. 이 경우 인증이 실제로 만료됐는지, 또는 X가 headless 접근만 차단했는지를 로그 reason으로 분리해 다음 작업에서 headed probe fallback을 검토한다.
- `parsed_bookmark_count`를 단순 signal로 쓰기 때문에 실제 게시글 수와 일치하지 않을 수 있다. 이 계획에서는 인증 오탐 방지 신호로만 사용하고, 수집 건수 계산에는 사용하지 않는다.
