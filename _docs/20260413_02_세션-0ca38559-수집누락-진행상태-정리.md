---
title: SNS 스크래퍼 수집 누락 세션 진행 상태 정리
created: 2026-04-13 09:07

session_id: 0ca38559-5818-41f1-ba30-f02f2fbe7d8d
session_path: C:\Users\ahnbu\.claude\projects\D--vibe-coding-scrap-sns\0ca38559-5818-41f1-ba30-f02f2fbe7d8d.jsonl
plan: C:\Users\ahnbu\.claude\plans\compressed-plotting-forest.md
spec: D:\vibe-coding\scrap_sns\docs\specs\20260412_01_SNS-스크래퍼-수집누락-개선.md
ai: claude
---

# SNS 스크래퍼 수집 누락 세션 진행 상태 정리

## 발단: 사용자 요청

웹 뷰어 `index.html`의 업데이트 버튼을 눌렀는데 실제로 SNS 업데이트가 안 된 것처럼 보인다는 문제 제기에서 시작했다. 첫 요청은 버튼 클릭이 실제 스크래핑을 실행했는지, 그리고 그 실행 이력이 로그에 남았는지 확인하는 것이었다.

이번 문서는 `plan만 작성된 세션`이 아니라, `진단 -> 일부 수정 -> plan 작성 -> ExitPlanMode 후 R2 구현 시도 -> 직접 검증 실패`까지 진행된 세션이라는 점을 정확히 남기기 위한 기록이다.

## 작업 상세내역

### 1. 초기 진단과 로그 확인

로그 3종과 `output_total/total_full_20260412.json` 생성 시각을 대조한 결과, 2026-04-12 16:32~16:34 KST 실행분이 서로 정합했다. 즉 업데이트 버튼 클릭은 실제 스크래핑 실행으로 이어졌고, 로그도 남고 있었다.

다만 로그 파일이 `"w"` 모드로 열려 이전 실행 이력이 매번 사라지는 문제가 확인됐다. 이 부분은 추적성 측면에서 결함으로 판단되어 즉시 수정 범위에 포함됐다.

### 2. 플랫폼별 분석 결과

Threads, LinkedIn, Twitter를 분리해서 원인을 확인한 결과 플랫폼별 상태가 달랐다. LinkedIn과 Twitter는 처음에는 "누락처럼 보이는 사례"였지만, 실제 데이터 존재 여부와 저장된 본문을 다시 대조하면서 성격이 갈렸다.

초기 실행 로그 기준 플랫폼별 결과는 Threads 신규 0건, LinkedIn 신규 1건, Twitter 신규 0건이었다. 이 수치만 보면 단순히 "새 글이 없었다"처럼 보였지만, 이후 샘플 교차 확인으로 Threads 누락과 Twitter 본문 오염이 분리됐다.

- Threads: 진짜 누락. 지정한 3건은 플랫폼 파일, update 결과, 로그, 실패 목록 어디에도 없었다.
- LinkedIn: 누락 아님. 플랫폼 파일, total_full, data.js, 뷰어 기본 정렬 기준에서 모두 정상으로 확인됐다.
- Twitter: platform_id는 존재하지만 본문이 다른 게시물 텍스트로 오염된 사례가 확인됐다. 동일 본문 경고가 다수 발생한 로그와도 정합했다.

### 3. 실제로 완료된 작업

아래 항목은 세션 중 실제 수정 또는 확정이 끝난 항목이다.

- `D:\vibe-coding\scrap_sns\total_scrap.py`
  로그 open 모드를 `"w"`에서 `"a"`로 변경했다. 실행 시작/종료 구분선이 이미 있어 append만으로도 실행 이력 식별이 가능하다고 판단했다.
- `D:\vibe-coding\scrap_sns\BACKLOG.md`
  Twitter 본문 오염 문제를 후속 과제로 등록했다.
- `D:\vibe-coding\scrap_sns\docs\specs\20260412_01_SNS-스크래퍼-수집누락-개선.md`
  성공 기준을 누락 3건에서 누락 8건으로 확대하고, LinkedIn은 이슈 없음으로 닫는 방향으로 정리했다.

### 4. Threads R2 진행 경과

실측 확인 결과, Threads 저장 목록은 저장 시점순이 아니라 게시물 작성일순에 가까운 배치로 보였다. 그래서 최근 저장한 게시물이 기존 기준점 아래에 위치할 수 있고, 기존 `stop_codes` 기반 즉시 중단 로직은 이런 게시물을 영구적으로 놓칠 수 있었다.

그 결과 plan에서는 `stop_codes 즉시 중단 -> 기수집 연속 카운터 N=20` 방식으로 바꾸기로 결정했다. 이 계획은 `C:\Users\ahnbu\.claude\plans\compressed-plotting-forest.md`에 저장됐다.

하지만 여기서 끝난 것이 아니다. ExitPlanMode 이후 실제로 `thread_scrap.py` 수정과 `python thread_scrap.py --mode update` 직접 검증까지 진행됐다. 첫 구현은 `기수집 연속 카운터` 방식이었고, 검증 결과 신규 0건으로 실패했다. 이후 세션 후반에는 `기수집 skip + 기존 no_new_data_count 활용` 쪽으로 로직이 다시 바뀌었지만, 재검증에서도 신규 0건으로 끝났다.

따라서 세션 종료 시점의 R2 상태는 `원인과 1차 접근법은 특정했지만, 구현은 실패했고 미해결로 남음`이다. `R2 해결 완료`로 기록하면 사실과 다르다.

## 의사결정 기록

- 결정: 로그 보존 방식은 append로 바꾼다.
  근거: `"w"` 모드는 최근 실행만 남겨 원인 추적을 어렵게 만든다.
  트레이드오프: 로그 파일이 커질 수 있지만, 기존 구분선을 그대로 재사용할 수 있어 구현 비용이 가장 낮다.

- 결정: LinkedIn은 이번 범위에서 코드 수정 대상이 아니다.
  근거: 플랫폼 파일, total_full, data.js, 뷰어 기본 정렬 기준까지 확인했을 때 데이터와 표시 모두 정상으로 확인됐다.
  트레이드오프: 사용자가 처음 체감한 "누락" 경험 자체는 설명이 필요하지만, 스크래퍼 변경으로 이어질 근거는 없었다.

- 결정: Twitter 본문 오염은 이번 범위 밖으로 분리한다.
  근거: bot 차단 또는 리다이렉트에 가까운 현상으로 보여 즉시 수습 범위를 넘어섰다.
  트레이드오프: 이번 세션에서 해결되지는 않지만, BACKLOG로 옮겨 추적성은 확보했다.

- 결정: Threads R2의 설계 초안은 `기수집 연속 카운터 N=20`로 둔다.
  근거: 실측 저장 목록에서 신규 5건 뒤에 기수집 약 16건 구간이 확인됐기 때문이다.
  트레이드오프: 세션 후반 실제 검증에서 이 접근이 그대로 성공하지 않았으므로, plan/spec와 종료 시점 code를 동일시하면 안 된다.

## 검증계획과 실행결과

| 검증 항목 | 검증 방법 | 결과 | 비고 |
|-----------|-----------|------|------|
| 로그 시각 정합성 | `logs/*.log`와 `output_total/total_full_20260412.json` 시각 대조 | ✅ 확인 | 16:32~16:34 KST 흐름 일치 |
| 로그 append 변경 | `total_scrap.py` open 모드 확인 | ✅ 완료 | `"w"` -> `"a"` 변경 |
| LinkedIn 누락 여부 | 플랫폼 파일, total_full, data.js, 뷰어 정렬 기준 교차 확인 | ✅ 이슈 없음 | 코드 변경 불필요 |
| Twitter 저장 본문 교차 확인 | platform_id와 본문 첫 줄 비교 | ⚠️ 오염 확인 | BACKLOG 이관 |
| Threads 저장 목록 실측 | 실제 저장 목록과 기준점 위치 확인 | ✅ 원인 파악 | 신규 5건과 누락 3건이 기준점 아래에 위치 |
| Threads R2 직접 실행 검증 | `python thread_scrap.py --mode update` | ❌ 실패 | 신규 0건, 미해결 |

## 리스크 및 미해결 이슈

- 가장 큰 미해결은 R2다. 신규 5건이 왜 `collected_data`까지 도달하지 못하는지 세션 내에서 확정하지 못했다.
- 원인 후보는 세션 종료 시점 기준으로 세 가지다.
  `root_user_pk` 필터에서 탈락하는 경우
  네트워크 응답에 신규 5건 code가 실제로 포함되지 않는 경우
  `existing_codes` 또는 그에 준하는 비교 지점에서 신규가 오인되는 경우
- 따라서 `plan`, `spec`, `thread_scrap.py` working tree는 완전히 같은 상태가 아니다. 다음 세션은 구현보다 먼저 이 정합성부터 다시 맞춰야 한다.

## 다음 액션

- `thread_scrap.py`에 디버그 로깅을 넣어 신규 5건 code가 네트워크 응답에 들어오는지 먼저 확인한다.
- 들어온다면 어느 필터에서 탈락하는지 추적한다.
- 그 결과를 기준으로 `plan/spec/code` 중 무엇을 source of truth로 둘지 다시 정렬한다.
- 정렬이 끝난 뒤에만 R2 수정안을 재구현하고, 누락 8건 수집 여부를 다시 검증한다.

## 참고 자료

| 출처 | 용도 |
|------|------|
| `C:\Users\ahnbu\.claude\projects\D--vibe-coding-scrap-sns\0ca38559-5818-41f1-ba30-f02f2fbe7d8d.jsonl` | 원 세션 도구 이력과 실제 진행 범위 확인 |
| `C:\Users\ahnbu\.claude\exports\0ca38559-5818-41f1-ba30-f02f2fbe7d8d\export_20260413_085202.md` | 세션 복원용 정리본과 발화 순서 확인 |
| `C:\Users\ahnbu\.claude\plans\compressed-plotting-forest.md` | R2 1차 구현 계획 원문 |
| `D:\vibe-coding\scrap_sns\docs\specs\20260412_01_SNS-스크래퍼-수집누락-개선.md` | 세션 중 반영된 성공 기준과 범위 판단 근거 |

---

> Plan 원문 — 원본: `C:\Users\ahnbu\.claude\plans\compressed-plotting-forest.md`

# (Plan) Threads update 모드 기수집 연속 카운터 방식으로 교체

## Context

Threads 스크래퍼의 update 모드가 stop_codes(최근 저장 5건) 발견 시 즉시 중단하는데, Threads 저장 목록이 게시물 작성일순으로 정렬되어 최근 저장했지만 오래된 게시물이 기준점 아래에 위치 → 영원히 수집 불가. 실측에서 신규 5건 + 누락 3건 확인. SPEC: `docs/specs/20260412_01_SNS-스크래퍼-수집누락-개선.md`

## 변경 파일

`thread_scrap.py` (1개 파일, 5개 지점)

## Step 1: 변수 초기화 변경 (라인 226-227)

**Before:**
```python
stop_codes = []  # 중단 기준 code 리스트
stop_code_found = False  # 중단 플래그
```

**After:**
```python
existing_codes = set()  # 기수집 code set (update 모드 중단 판단용)
consecutive_dup_count = 0  # 기수집 연속 카운터
CONSECUTIVE_DUP_LIMIT = 20  # 연속 N개 기수집이면 중단
```

## Step 2: 기존 데이터 로드 시 existing_codes 구축 + stop_codes 설정 블록 교체 (라인 238-253)

**Before** (라인 248-253):
```python
# "update only" 모드: 최신 Simple 파일의 최상단 code 가져오기
if CRAWL_MODE == "update only":
    if posts:
        # 최신순 5개 추출 (삭제 방지용 징검다리 전략)
        stop_codes = [p.get('platform_id') or p.get('code') for p in posts[:5]]
        print(f"🔄 UPDATE ONLY 모드: {stop_codes} 중 하나라도 발견 시 수집을 중단합니다.")
```

**After:**
```python
# "update only" 모드: 기수집 code set 구축
if CRAWL_MODE == "update only":
    existing_codes = set(all_posts_map.keys())
    print(f"🔄 UPDATE ONLY 모드: 기존 {len(existing_codes)}건과 대조, 연속 {CONSECUTIVE_DUP_LIMIT}건 기수집 시 중단합니다.")
```

## Step 3: 네트워크 응답 처리 — stop_codes 체크 -> 기수집 카운터로 교체 (라인 367-371)

**Before:**
```python
# ⛔ UPDATE ONLY 모드: stop_codes 중 하나 발견 시 중단
if stop_codes and code in stop_codes:
    print(f"✋ 기준 게시물 발견! (code: {code}) - 크롤링 중단")
    stop_code_found = True
    return # 함수 종료
```

**After:**
```python
# ⛔ UPDATE ONLY 모드: 기수집 연속 카운터
if existing_codes and code in existing_codes:
    consecutive_dup_count += 1
    if consecutive_dup_count >= CONSECUTIVE_DUP_LIMIT:
        print(f"✋ 기수집 {CONSECUTIVE_DUP_LIMIT}건 연속 → 수집 중단")
        stop_code_found = True
        return
    continue
elif existing_codes:
    consecutive_dup_count = 0
```

주의: `continue`를 넣어 기수집 게시물은 collected_data에 추가하지 않고 건너뜀. 신규 게시물만 아래 로직으로 내려가 수집됨. `stop_code_found`는 기존 스크롤 중단 플래그를 재활용.

## Step 4: DOM 스캔 — stop_codes 체크 -> 기수집 카운터로 교체 (라인 503-507)

**Before:**
```python
# ⛔ UPDATE ONLY 모드: stop_codes 중 하나 발견 시 중단
if stop_codes and code in stop_codes:
    print(f"✋ 기준 게시물 발견! (code: {code}) - DOM 스캔 중단")
    stop_code_found = True
    break
```

**After:**
```python
# ⛔ UPDATE ONLY 모드: 기수집 연속 카운터
if existing_codes and code in existing_codes:
    consecutive_dup_count += 1
    if consecutive_dup_count >= CONSECUTIVE_DUP_LIMIT:
        print(f"✋ 기수집 {CONSECUTIVE_DUP_LIMIT}건 연속 → DOM 스캔 중단")
        stop_code_found = True
        break
    continue
elif existing_codes:
    consecutive_dup_count = 0
```

## Step 5: 스크롤 루프 — 변경 불필요 (라인 584-587)

기존 `stop_code_found` 플래그를 그대로 재활용하므로 이 블록은 변경 없음:
```python
if stop_code_found:
    print(f"\n✋ 기준 게시물 발견! (스크롤 중단)")
    break
```

단, 로그 메시지를 더 정확하게 변경:
```python
if stop_code_found:
    print(f"\n✋ 기수집 연속 한도 도달 (스크롤 중단)")
    break
```

## 검증

1. `python thread_scrap.py --mode update` 실행
2. 로그에서 `🔄 UPDATE ONLY 모드: 기존 665건과 대조` 출력 확인
3. 기존 게시물 스킵, 신규 게시물 수집 확인
4. 연속 기수집 20건 도달 시 정상 중단 확인
5. 수집 후 누락 8건(`DQ8mqRaEtUL`, `DW7wRVUD82s`, `DW8k05MEj9D`, `DVVRaH3kYkF`, `DWq9TgCGcp3`, `DWn9J1mEpb4`, `DWROO2NDQ2i`, `DWeNl73jxfT`) 포함 여부 확인: `grep -c "DQ8mqRaEtUL\|DWn9J1mEpb4" output_threads/python/threads_py_full_*.json`
6. `--mode all`일 때 `existing_codes`가 빈 set이므로 기존 동작(최대 50회 스크롤) 유지 확인
