---
created: 2026-04-17
session_id: codex:019d9914-aeeb-7e82-b911-af1ea64f6770
tags:
  - wave1
  - web-viewer
  - launcher
---

# Wave 1 뷰어-런처 실행계획과 결과

## 요청 요약

`BACKLOG.md`와 `docs/20260417_01_백로그-우선순위-평가.md` 기준 Wave 1 항목 3개를 구현하고, 실행 결과와 검증 근거를 한 문서로 정리한다.

## 연결 문서

- Claude 계획문서: `C:\Users\ahnbu\.claude\plans\ticklish-wandering-simon.md`
- 백로그 평가 문서: `D:\vibe-coding\scrap_sns\docs\20260417_01_백로그-우선순위-평가.md`
- 백로그 원본: `D:\vibe-coding\scrap_sns\BACKLOG.md`

## 범위

- 포함:
  - `BL-0417-01` LinkedIn copy 메타의 URN 노출 제거
  - `BL-0417-02` 본문 전체 click 제거, `.read-more-indicator`만 toggle
  - `BL-0417-03` `sns_hub.vbs` 기반 숨김 런처 전환
- 제외:
  - `BL-0417-04` 이후 backlog 항목
  - 영구화 surface 변경 및 마이그레이션
  - `run_viewer.bat`, `stop_viewer.bat` 코드 수정

## 핵심 결정

- `web_viewer/script.js`의 인라인 로직은 테스트 가능한 helper로 분리한다.
- 자동 검증은 기존 Node extraction 패턴을 재사용하는 `pytest` 기반 unit test로 고정한다.
- 런처는 `.lnk -> wscript.exe -> sns_hub.vbs` 진입점으로 전환하되, 서버 readiness는 `/api/status`로 확인한다.
- `scripts/validate_sns_hub_launcher.ps1`를 추적하기 위해 `.gitignore`는 파일 단위 예외만 연다.

## 구현 결과 요약

- `BL-0417-01`
  - `buildCopyText(post)` helper 추가
  - copy 메타 우선순위를 `display_name || username || user`로 변경
  - `tests/unit/test_web_viewer_copy_string.py` 추가
- `BL-0417-02`
  - `getReadMoreIndicatorHtml()` / `toggleExpandableText()` helper 추가
  - 본문 wrapper click 제거, `.read-more-indicator` click만 유지
  - `tests/unit/test_web_viewer_expandable_text.py` 추가
- `BL-0417-03`
  - `sns_hub.vbs`, `scripts/validate_sns_hub_launcher.ps1` 추가
  - `SNS허브_바로가기.lnk`, `README.md`, `package.json`, `.gitignore` 갱신

## 검증 요약

- 자동 검증:
  - `pytest tests/unit/test_linkedin_parser.py tests/unit/test_web_viewer_resolve_post_url.py tests/unit/test_web_viewer_copy_string.py tests/unit/test_web_viewer_expandable_text.py -v`
  - 결과: `8 passed`
  - `powershell -ExecutionPolicy Bypass -File .\scripts\validate_sns_hub_launcher.ps1`
  - 결과: `SNS hub launcher validation passed.`
- 수동 검증:
  - 새 `SNS허브_바로가기.lnk` 실행 시 cmd 창이 뜨지 않음을 확인
  - 작업표시줄에 고정된 레거시 `cmd.exe /c "D:\vibe-coding\scrap_sns\run_viewer.bat"` 바로가기 때문에 1차 false negative가 있었고, 새 바로가기로 교체 후 증상이 사라짐

## 리스크 및 후속

- 아직 미확인:
  - 새 바로가기 기준 Live Mode 로드
  - 긴 카드 본문 드래그 선택
  - Read more / Show less 실동작
  - `stop_viewer.bat` 기반 프로세스 정리
- 관련 backlog:
  - `BL-0417-04`, `BL-0417-05`, `BL-0412-03`, `BL-0412-04`는 후속 wave 범위

---

## 부록 A. 원본 실행 상세 계획

**Goal:** Wave 1 범위의 3개 backlog 항목(BL-0417-01, BL-0417-02, BL-0417-03)을 최소 변경으로 구현하고, 자동 테스트와 런처 검증 스크립트까지 포함해 회귀를 막는다.

**Architecture:** `web_viewer/script.js` 안에서 테스트가 불가능한 인라인 로직을 작은 helper function으로 분리해 Node 추출 기반 unit test로 검증한다. 런처는 `sns_hub.vbs`를 새 기본 진입점으로 두고, `.lnk`는 `wscript.exe`를 호출하도록 전환하며, `.lnk`/문서/`package.json` 상태를 PowerShell 검증 스크립트로 확인한다.

**Tech Stack:** Vanilla JavaScript, Python `pytest`, Node.js eval-based function extraction, Windows Script Host (`wscript`, `.vbs`), PowerShell COM (`WScript.Shell`)

---

## File Structure

- `web_viewer/script.js`
  - `buildCopyText(post)` helper 추가
  - `getReadMoreIndicatorHtml(isExpanded)` helper 추가
  - `toggleExpandableText(paragraph, indicator)` helper 추가
  - copy 버튼 핸들러와 긴 본문 toggle 바인딩을 helper 기반으로 정리
- `tests/unit/test_web_viewer_copy_string.py`
  - `resolvePostUrl()` + `buildCopyText()`를 Node로 추출해 display_name 우선 규칙 검증
- `tests/unit/test_web_viewer_expandable_text.py`
  - toggle helper를 Node로 추출해 `line-clamp-4` 전환과 indicator HTML 변경 검증
- `sns_hub.vbs`
  - Flask 서버를 숨김 실행하고 `/api/status` 응답을 기다린 뒤 `index.html` 오픈
- `scripts/validate_sns_hub_launcher.ps1`
  - `.lnk` target/arguments, `sns_hub.vbs` 핵심 문자열, `README.md`, `package.json`을 점검
- `SNS허브_바로가기.lnk`
  - target을 `wscript.exe`, arguments를 `sns_hub.vbs`로 갱신
- `README.md`
  - 뷰어 기본 실행 진입점을 `.lnk`/`wscript sns_hub.vbs`로 변경
- `package.json`
  - `npm run view`가 `wscript sns_hub.vbs`를 실행하도록 변경

---

### Task 1: LinkedIn 복사 문자열을 helper로 분리하고 자동 테스트 추가

**Files:**
- Modify: `web_viewer/script.js` (copy 버튼 핸들러 주변, 현재 `resolvePostUrl()` 아래 helper 추가 + copy handler 교체)
- Create: `tests/unit/test_web_viewer_copy_string.py`
- Verify existing: `tests/unit/test_web_viewer_resolve_post_url.py`

- [ ] **Step 1: 실패하는 unit test를 먼저 추가한다**

`tests/unit/test_web_viewer_copy_string.py`를 아래 내용으로 만든다.

```python
import json
import subprocess
import textwrap


def run_node_json(node_script: str):
    completed = subprocess.run(
        ["node", "-e", node_script],
        capture_output=True,
        text=True,
        encoding="utf-8",
        cwd=".",
    )

    assert completed.returncode == 0, completed.stderr
    stdout = completed.stdout.strip()
    return json.loads(stdout.splitlines()[-1])


def test_build_copy_text_prefers_display_name_and_falls_back():
    node_script = textwrap.dedent(
        """
        const fs = require('fs');
        const src = fs.readFileSync('web_viewer/script.js', 'utf8');

        function extractFunction(name) {
          const patterns = [`async function ${name}(`, `function ${name}(`];
          let start = -1;
          for (const pattern of patterns) {
            start = src.indexOf(pattern);
            if (start !== -1) break;
          }
          if (start === -1) {
            console.error(`${name} missing`);
            process.exit(1);
          }

          let depth = 0;
          let end = -1;
          for (let i = start; i < src.length; i += 1) {
            const ch = src[i];
            if (ch === '{') depth += 1;
            if (ch === '}') {
              depth -= 1;
              if (depth === 0) {
                end = i + 1;
                break;
              }
            }
          }

          if (end === -1) {
            console.error(`${name} parse failure`);
            process.exit(1);
          }

          return src.slice(start, end);
        }

        eval(extractFunction('resolvePostUrl'));
        eval(extractFunction('buildCopyText'));

        const payload = {
          displayName: buildCopyText({
            full_text: '첫 번째 본문',
            url: 'https://www.linkedin.com/posts/example-1',
            display_name: '홍길동',
            username: 'urn:li:member:123',
            user: 'legacy-user',
            created_at: '2026-04-17T09:00:00Z',
            sns_platform: 'linkedin'
          }),
          usernameFallback: buildCopyText({
            full_text: '두 번째 본문',
            url: 'https://www.linkedin.com/posts/example-2',
            username: 'fallback-user',
            created_at: '2026-04-16T09:00:00Z',
            sns_platform: 'linkedin'
          }),
          userFallback: buildCopyText({
            full_text: '세 번째 본문',
            post_url: 'https://www.linkedin.com/posts/example-3',
            user: 'legacy-user',
            created_at: '2026-04-15T09:00:00Z',
            sns_platform: 'linkedin'
          })
        };

        console.log(JSON.stringify(payload));
        """
    )

    payload = run_node_json(node_script)
    assert payload["displayName"] == (
        "첫 번째 본문\\n\\n"
        "*출처: https://www.linkedin.com/posts/example-1\\n"
        "*홍길동 / 2026-04-17 / linkedin"
    )
    assert payload["usernameFallback"] == (
        "두 번째 본문\\n\\n"
        "*출처: https://www.linkedin.com/posts/example-2\\n"
        "*fallback-user / 2026-04-16 / linkedin"
    )
    assert payload["userFallback"] == (
        "세 번째 본문\\n\\n"
        "*출처: https://www.linkedin.com/posts/example-3\\n"
        "*legacy-user / 2026-04-15 / linkedin"
    )
```

- [ ] **Step 2: 테스트가 helper 부재로 실패하는지 확인한다**

Run:

```powershell
pytest tests/unit/test_web_viewer_copy_string.py -v
```

Expected:

```text
FAILED tests/unit/test_web_viewer_copy_string.py::test_build_copy_text_prefers_display_name_and_falls_back
...
AssertionError: buildCopyText missing
```

- [ ] **Step 3: `buildCopyText(post)` helper를 추가하고 copy handler가 이를 사용하게 바꾼다**

`web_viewer/script.js`에서 `resolvePostUrl()` 바로 아래에 helper를 추가한다.

```javascript
function buildCopyText(post) {
    const fullText = post.full_text || '';
    const postUrl = resolvePostUrl(post);
    const author = post.display_name || post.username || post.user || '';
    const createdAt = (post.created_at || '').slice(0, 10);
    const platform = post.sns_platform || '';
    return `${fullText}\n\n*출처: ${postUrl}\n*${author} / ${createdAt} / ${platform}`;
}
```

copy 버튼 핸들러는 인라인 문자열 조합을 제거하고 helper 호출만 남긴다.

```javascript
const copyBtn = header.querySelector('.copy-btn');
copyBtn.addEventListener('click', async (e) => {
    e.stopPropagation();
    const textToCopy = buildCopyText(post);
    const icon = copyBtn.querySelector('span');

    try {
        await navigator.clipboard.writeText(textToCopy);
        const originalIcon = icon.textContent;
        icon.textContent = 'check';
        icon.classList.add('text-green-400');

        setTimeout(() => {
            icon.textContent = originalIcon;
            icon.classList.remove('text-green-400');
        }, 2000);
    } catch (err) {
        console.error('Failed to copy: ', err);
        alert('복사에 실패했습니다.');
    }
});
```

- [ ] **Step 4: 신규 테스트와 기존 URL 해석 테스트를 함께 돌려 회귀를 확인한다**

Run:

```powershell
pytest tests/unit/test_web_viewer_copy_string.py tests/unit/test_web_viewer_resolve_post_url.py -v
```

Expected:

```text
PASSED tests/unit/test_web_viewer_copy_string.py::test_build_copy_text_prefers_display_name_and_falls_back
PASSED tests/unit/test_web_viewer_resolve_post_url.py::test_resolve_post_url_handles_threads_legacy_and_standard_shapes
```

- [ ] **Step 5: 이 task만 별도 커밋한다**

이 레포에서는 raw `git add/commit` 대신 `cp` 스킬을 사용한다.

```text
Suggested commit: fix(viewer): 링크드인 복사 메타에 display_name 우선 적용
Files in scope:
- web_viewer/script.js
- tests/unit/test_web_viewer_copy_string.py
- CHANGELOG.md
```

---

### Task 2: 긴 본문 toggle을 indicator 클릭으로 제한하고 자동 테스트 추가

**Files:**
- Modify: `web_viewer/script.js` (긴 본문 렌더링 블록, 현재 `content.innerHTML`과 `content.addEventListener('click', ...)` 구간)
- Create: `tests/unit/test_web_viewer_expandable_text.py`

- [ ] **Step 1: toggle helper 기준의 실패 테스트를 추가한다**

`tests/unit/test_web_viewer_expandable_text.py`를 아래 내용으로 만든다.

```python
import json
import subprocess
import textwrap


def run_node_json(node_script: str):
    completed = subprocess.run(
        ["node", "-e", node_script],
        capture_output=True,
        text=True,
        encoding="utf-8",
        cwd=".",
    )

    assert completed.returncode == 0, completed.stderr
    stdout = completed.stdout.strip()
    return json.loads(stdout.splitlines()[-1])


def test_toggle_expandable_text_switches_class_and_indicator_markup():
    node_script = textwrap.dedent(
        """
        const fs = require('fs');
        const src = fs.readFileSync('web_viewer/script.js', 'utf8');

        function extractFunction(name) {
          const patterns = [`async function ${name}(`, `function ${name}(`];
          let start = -1;
          for (const pattern of patterns) {
            start = src.indexOf(pattern);
            if (start !== -1) break;
          }
          if (start === -1) {
            console.error(`${name} missing`);
            process.exit(1);
          }

          let depth = 0;
          let end = -1;
          for (let i = start; i < src.length; i += 1) {
            const ch = src[i];
            if (ch === '{') depth += 1;
            if (ch === '}') {
              depth -= 1;
              if (depth === 0) {
                end = i + 1;
                break;
              }
            }
          }

          if (end === -1) {
            console.error(`${name} parse failure`);
            process.exit(1);
          }

          return src.slice(start, end);
        }

        eval(extractFunction('getReadMoreIndicatorHtml'));
        eval(extractFunction('toggleExpandableText'));

        const classes = new Set(['line-clamp-4']);
        const paragraph = {
          classList: {
            contains(name) { return classes.has(name); },
            add(name) { classes.add(name); },
            remove(name) { classes.delete(name); }
          }
        };
        const indicator = { innerHTML: '' };

        const firstState = toggleExpandableText(paragraph, indicator);
        const expandedHtml = indicator.innerHTML;
        const hasClampAfterExpand = classes.has('line-clamp-4');

        const secondState = toggleExpandableText(paragraph, indicator);
        const collapsedHtml = indicator.innerHTML;
        const hasClampAfterCollapse = classes.has('line-clamp-4');

        console.log(JSON.stringify({
          firstState,
          expandedHtml,
          hasClampAfterExpand,
          secondState,
          collapsedHtml,
          hasClampAfterCollapse
        }));
        """
    )

    payload = run_node_json(node_script)
    assert payload["firstState"] == "expanded"
    assert payload["hasClampAfterExpand"] is False
    assert "Show less" in payload["expandedHtml"]
    assert "expand_less" in payload["expandedHtml"]
    assert payload["secondState"] == "collapsed"
    assert payload["hasClampAfterCollapse"] is True
    assert "Read more" in payload["collapsedHtml"]
    assert "expand_more" in payload["collapsedHtml"]
```

- [ ] **Step 2: helper가 아직 없어서 테스트가 실패하는지 확인한다**

Run:

```powershell
pytest tests/unit/test_web_viewer_expandable_text.py -v
```

Expected:

```text
FAILED tests/unit/test_web_viewer_expandable_text.py::test_toggle_expandable_text_switches_class_and_indicator_markup
...
AssertionError: getReadMoreIndicatorHtml missing
```

- [ ] **Step 3: helper 2개를 추가하고 DOM 바인딩을 indicator 전용으로 바꾼다**

`web_viewer/script.js`에서 `buildCopyText()` 아래에 helper 2개를 추가한다.

```javascript
function getReadMoreIndicatorHtml(isExpanded) {
    if (isExpanded) {
        return `<span>Show less</span><span class="material-symbols-outlined text-[14px]">expand_less</span>`;
    }
    return `<span>Read more</span><span class="material-symbols-outlined text-[14px]">expand_more</span>`;
}

function toggleExpandableText(paragraph, indicator) {
    const isCollapsed = paragraph.classList.contains('line-clamp-4');
    if (isCollapsed) {
        paragraph.classList.remove('line-clamp-4');
        if (indicator) indicator.innerHTML = getReadMoreIndicatorHtml(true);
        return 'expanded';
    }

    paragraph.classList.add('line-clamp-4');
    if (indicator) indicator.innerHTML = getReadMoreIndicatorHtml(false);
    return 'collapsed';
}
```

긴 본문 렌더링 블록은 wrapper click을 제거하고 indicator click만 연결한다. `select-none`도 삭제한다.

```javascript
content.innerHTML = `
    <div class="relative">
        <p class="text-sm text-gray-200 leading-relaxed font-light ${isLongText ? 'line-clamp-4' : ''} transition-all" id="text-${escapeHtml(postUrl)}">
            ${cleanText}
        </p>
        ${isLongText ? `
        <div class="mt-2 text-xs font-medium text-gray-500 transition-colors flex items-center gap-1 read-more-indicator cursor-pointer">
            ${getReadMoreIndicatorHtml(false)}
        </div>` : ''}
    </div>
`;

if (isLongText) {
    const paragraph = content.querySelector('p');
    const indicator = content.querySelector('.read-more-indicator');
    if (indicator) {
        indicator.addEventListener('click', (e) => {
            e.stopPropagation();
            toggleExpandableText(paragraph, indicator);
        });
    }
}
```

- [ ] **Step 4: 자동 테스트와 최소 수동 확인을 함께 수행한다**

Run:

```powershell
pytest tests/unit/test_web_viewer_expandable_text.py -v
```

Expected:

```text
PASSED tests/unit/test_web_viewer_expandable_text.py::test_toggle_expandable_text_switches_class_and_indicator_markup
```

브라우저 수동 확인:

```text
1. 긴 카드 본문을 드래그하면 텍스트 선택이 된다.
2. 본문 영역을 클릭해도 접힘 상태가 바뀌지 않는다.
3. Read more / Show less 영역만 눌렀을 때 clamp가 바뀐다.
```

- [ ] **Step 5: 이 task만 별도 커밋한다**

```text
Suggested commit: fix(viewer): 본문 토글을 인디케이터 클릭으로 제한
Files in scope:
- web_viewer/script.js
- tests/unit/test_web_viewer_expandable_text.py
- CHANGELOG.md
```

---

### Task 3: `sns_hub.vbs` 기반 숨김 런처와 검증 스크립트를 추가하고 진입점 문서를 갱신한다

**Files:**
- Create: `sns_hub.vbs`
- Create: `scripts/validate_sns_hub_launcher.ps1`
- Modify: `SNS허브_바로가기.lnk`
- Modify: `README.md`
- Modify: `package.json`
- Verify existing: `run_viewer.bat`, `stop_viewer.bat`, `server.py`

- [ ] **Step 1: 미래 상태를 검증하는 PowerShell 스크립트를 먼저 추가한다**

`scripts/validate_sns_hub_launcher.ps1`를 아래 내용으로 만든다.

```powershell
param(
    [string]$RepoRoot = (Get-Location).Path
)

$errors = New-Object System.Collections.Generic.List[string]
$shortcutPath = Join-Path $RepoRoot 'SNS허브_바로가기.lnk'
$vbsPath = Join-Path $RepoRoot 'sns_hub.vbs'
$readmePath = Join-Path $RepoRoot 'README.md'
$packageJsonPath = Join-Path $RepoRoot 'package.json'

if (-not (Test-Path $shortcutPath)) {
    $errors.Add("shortcut missing: $shortcutPath")
} else {
    $shell = New-Object -ComObject WScript.Shell
    $shortcut = $shell.CreateShortcut($shortcutPath)
    $expectedTarget = Join-Path $env:WINDIR 'System32\wscript.exe'
    $expectedArguments = '"' + $vbsPath + '"'

    if ($shortcut.TargetPath -ne $expectedTarget) {
        $errors.Add("shortcut target mismatch: $($shortcut.TargetPath)")
    }
    if ($shortcut.Arguments -ne $expectedArguments) {
        $errors.Add("shortcut arguments mismatch: $($shortcut.Arguments)")
    }
}

if (-not (Test-Path $vbsPath)) {
    $errors.Add("sns_hub.vbs missing")
} else {
    $vbsContent = Get-Content $vbsPath -Raw
    if ($vbsContent -notmatch 'http://localhost:5000/api/status') {
        $errors.Add('sns_hub.vbs missing /api/status readiness check')
    }
    if ($vbsContent -notmatch 'start "" index\.html') {
        $errors.Add('sns_hub.vbs missing browser launch command')
    }
}

$packageJson = Get-Content $packageJsonPath -Raw | ConvertFrom-Json
if ($packageJson.scripts.view -ne 'wscript sns_hub.vbs') {
    $errors.Add("package.json scripts.view mismatch: $($packageJson.scripts.view)")
}

$readmeContent = Get-Content $readmePath -Raw
if ($readmeContent -notmatch 'SNS허브_바로가기\.lnk') {
    $errors.Add('README.md missing SNS허브_바로가기.lnk guidance')
}
if ($readmeContent -notmatch 'wscript sns_hub\.vbs') {
    $errors.Add('README.md missing wscript sns_hub.vbs guidance')
}

if ($errors.Count -gt 0) {
    $errors | ForEach-Object { Write-Error $_ }
    exit 1
}

Write-Host 'Launcher validation passed'
```

- [ ] **Step 2: 현재 상태에서 검증 스크립트가 실패하는지 확인한다**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\validate_sns_hub_launcher.ps1
```

Expected:

```text
sns_hub.vbs missing
shortcut target mismatch: ...
package.json scripts.view mismatch: run_viewer.bat
README.md missing SNS허브_바로가기.lnk guidance
```

- [ ] **Step 3: `sns_hub.vbs`를 추가한다**

`sns_hub.vbs`는 아래 내용으로 만든다.

```vbscript
Set objShell = CreateObject("WScript.Shell")
Set fso = CreateObject("Scripting.FileSystemObject")

strPath = fso.GetParentFolderName(WScript.ScriptFullName)

' Flask 서버를 숨김 실행한다.
objShell.Run "cmd /c cd /d """ & strPath & """ && python server.py", 0, False

' /api/status 가 200을 반환할 때까지 최대 10초 대기한다.
ready = False
For i = 1 To 10
    WScript.Sleep 1000
    On Error Resume Next
    Set http = CreateObject("MSXML2.XMLHTTP")
    http.Open "GET", "http://localhost:5000/api/status", False
    http.Send
    If Err.Number = 0 Then
        If http.Status = 200 Then
            ready = True
        End If
    End If
    Set http = Nothing
    Err.Clear
    On Error GoTo 0
    If ready Then Exit For
Next

' 브라우저를 연다. 타임아웃이어도 기존 UX와 동일하게 index.html 은 연다.
objShell.Run "cmd /c cd /d """ & strPath & """ && start """" index.html", 0, False
```

- [ ] **Step 4: `.lnk`, `README.md`, `package.json`을 함께 갱신한다**

`.lnk`는 PowerShell COM으로 갱신한다.

```powershell
$repoRoot = (Resolve-Path '.').Path
$shortcutPath = Join-Path $repoRoot 'SNS허브_바로가기.lnk'
$vbsPath = Join-Path $repoRoot 'sns_hub.vbs'
$shell = New-Object -ComObject WScript.Shell
$shortcut = $shell.CreateShortcut($shortcutPath)
$shortcut.TargetPath = Join-Path $env:WINDIR 'System32\wscript.exe'
$shortcut.Arguments = '"' + $vbsPath + '"'
$shortcut.WorkingDirectory = $repoRoot
$shortcut.Save()
```

`README.md`의 뷰어 실행 안내를 아래로 교체한다.

````markdown
### 웹 뷰어 실행
기본 진입점은 `SNS허브_바로가기.lnk`이며, 내부적으로 `sns_hub.vbs`가 Flask 서버를 숨김 실행한 뒤 브라우저를 엽니다.
```powershell
# 권장: Windows Explorer에서 SNS허브_바로가기.lnk 더블클릭
wscript sns_hub.vbs
```
````

`package.json`의 `view` script는 아래 한 줄로 바꾼다.

```json
"view": "wscript sns_hub.vbs"
```

- [ ] **Step 5: 자동 검증과 수동 런처 확인을 수행한다**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\validate_sns_hub_launcher.ps1
```

Expected:

```text
Launcher validation passed
```

수동 확인:

```text
1. SNS허브_바로가기.lnk 실행 시 cmd 창이 보이지 않는다.
2. 브라우저가 열리고 Live Mode 로 로드된다.
3. stop_viewer.bat 실행 후 5000 포트 점유 프로세스와 server.py 프로세스가 정리된다.
```

- [ ] **Step 6: 이 task만 별도 커밋한다**

```text
Suggested commit: fix(launcher): 바로가기를 wscript 진입점으로 전환
Files in scope:
- sns_hub.vbs
- scripts/validate_sns_hub_launcher.ps1
- SNS허브_바로가기.lnk
- README.md
- package.json
- CHANGELOG.md
```

---

## Final Verification

- [ ] **Step 1: Wave 1 전체 자동 검증을 한 번에 실행한다**

Run:

```powershell
pytest tests/unit/test_linkedin_parser.py tests/unit/test_web_viewer_resolve_post_url.py tests/unit/test_web_viewer_copy_string.py tests/unit/test_web_viewer_expandable_text.py -v
```

Expected:

```text
6 passed
```

- [ ] **Step 2: 런처 검증 스크립트를 다시 실행한다**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\validate_sns_hub_launcher.ps1
```

Expected:

```text
Launcher validation passed
```

- [ ] **Step 3: 수동 시나리오를 체크리스트로 닫는다**

```text
- LinkedIn copy 메타 줄이 display_name 우선 규칙을 따른다.
- 긴 본문 드래그 선택이 된다.
- 본문 클릭은 접힘 상태를 바꾸지 않는다.
- Read more / Show less 만 토글된다.
- SNS허브_바로가기.lnk 는 cmd 없이 브라우저만 연다.
- stop_viewer.bat 로 숨김 실행 프로세스가 정리된다.
```

---

## Notes

- 영구화 surface 변경은 없다. `output_total/*`, `web_viewer/sns_tags.json`, 서버 API contract, DB/캐시/인덱스 모두 유지한다.
- 마이그레이션은 없다. 따라서 마이그레이션 스크립트/실행 명령/결과 검증 명령도 불필요하다.
- `tests/ui_verification.py`와 기존 E2E 정비는 이번 wave 범위에서 제외한다.
- `run_viewer.bat`와 `stop_viewer.bat`는 코드 수정 범위에서 제외한다. 다만 문서상 기본 진입점은 `.lnk`/`sns_hub.vbs`로 바뀐다.

---

## Execution Result (2026-04-17)

### Task 1 Result

- 상태: 완료
- 변경 파일:
  - `web_viewer/script.js`
  - `tests/unit/test_web_viewer_copy_string.py`
- 구현 결과:
  - `buildCopyText(post)` helper 추가
  - copy 버튼 핸들러가 helper를 사용하도록 변경
  - 자동 테스트는 `display_name` 우선, `username` fallback, `user` fallback 3개 케이스로 보강
- 리뷰 메모:
  - 1차 spec review에서 테스트가 1개 happy path만 덮는 문제를 발견
  - 같은 task 안에서 fallback 3케이스를 모두 추가해 재검증 완료

### Task 2 Result

- 상태: 완료
- 변경 파일:
  - `web_viewer/script.js`
  - `tests/unit/test_web_viewer_expandable_text.py`
- 구현 결과:
  - `getReadMoreIndicatorHtml(isExpanded)` helper 추가
  - `toggleExpandableText(paragraph, indicator)` helper 추가
  - wrapper click 제거, `.read-more-indicator` click만 토글되도록 변경
  - `cursor-pointer group/text`, `select-none` 제거
  - `.read-more-indicator`는 `<div>` 유지 + `cursor-pointer` 추가
- 리뷰 메모:
  - spec review 통과
  - code quality review에서 추가 수정 필요사항 없음

### Task 3 Result

- 상태: 완료
- 변경 파일:
  - `sns_hub.vbs`
  - `scripts/validate_sns_hub_launcher.ps1`
  - `SNS허브_바로가기.lnk`
  - `README.md`
  - `package.json`
  - `.gitignore`
- 구현 결과:
  - `sns_hub.vbs` 추가
  - `.lnk` 타깃을 `wscript.exe`, 인수를 절대경로 `sns_hub.vbs`로 갱신
  - `README.md`와 `package.json`의 기본 진입점을 `sns_hub.vbs`로 변경
  - 런처 검증 스크립트 추가
  - `.gitignore`는 `scripts/*` 유지 상태에서 `!scripts/validate_sns_hub_launcher.ps1`만 예외로 열어 최소 범위로 조정
- 리뷰 메모:
  - 1차 review에서 `sns_hub.vbs`가 cwd 의존적이라는 점을 발견
  - `cd /d repoRoot && python server.py` / `cd /d repoRoot && start "" index.html` 형태로 수정
  - validation script에 shortcut `WorkingDirectory` 검증도 추가
  - 재검토 후 spec / quality 모두 통과

### Automatic Verification

- 실행:

```powershell
pytest tests/unit/test_linkedin_parser.py tests/unit/test_web_viewer_resolve_post_url.py tests/unit/test_web_viewer_copy_string.py tests/unit/test_web_viewer_expandable_text.py -v
powershell -ExecutionPolicy Bypass -File .\scripts\validate_sns_hub_launcher.ps1
```

- 결과:

```text
8 passed
SNS hub launcher validation passed.
```

### Manual Verification

- 완료 항목:
  - 새 `SNS허브_바로가기.lnk` 실행 시 cmd 창 미노출 확인
- 확인된 이슈:
  - 작업표시줄에 고정된 레거시 `cmd.exe /c "D:\vibe-coding\scrap_sns\run_viewer.bat"` 바로가기가 1차 false negative 원인이었음
  - 새 바로가기로 교체한 뒤 cmd 창 노출 증상은 사라짐
- 미실행 항목:
  - 브라우저 Live Mode 로드 확인
  - 긴 카드 본문 드래그 선택 확인
  - Read more / Show less 실동작 확인
  - `stop_viewer.bat` 기반 프로세스 정리 확인
- 메모:
  - 런처 수동 검증은 일부 완료됐지만, 브라우저 동작과 정리 동작까지는 아직 후속 점검이 남아 있다.
