---
title: 프로젝트 문서 현행화 실행 기록
created: 2026-04-17 13:02

session_id: codex:019d991a-5181-71b0-bbed-13bd709ff9d7
session_path: C:/Users/ahnbu/.codex/sessions/2026/04/17/rollout-2026-04-17T10-42-12-019d991a-5181-71b0-bbed-13bd709ff9d7.jsonl


ai: codex
---

# 프로젝트 문서 현행화 실행 기록

## 발단: 사용자 요청

사용자는 현재 프로젝트 상태에 맞게 문서를 전면 재검토하고, `README.md`뿐 아니라 프로젝트 규칙 문서와 참조 문서까지 함께 현행화하라고 요청했다.

중간 산출물로는 아래 문서들이 제공되었고, 최종 코드와 일치하지 않을 수 있으므로 작업 범위 참고용으로만 사용하라고 명시되었다.

- `C:\Users\ahnbu\.claude\plans\ticklish-wandering-simon.md`
- `docs\plans\20260417_02_wave1-뷰어-런처-상세실행계획.md`
- `C:\Users\ahnbu\.claude\plans\nested-wandering-lake.md`
- `docs\20260417_03_Threads-도메인-정규화-구현.md`

## 작업 상세내역

- 문서 범위를 `README.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `docs/development.md`, `docs/crawling_logic.md` 6개로 고정했다.
- plan 파일은 세션 DB 기준으로 연결되지 않아 별도 원문 첨부 없이 진행했다.
- 현재 워킹트리와 실제 코드 파일을 기준으로 실행 진입점, 활성 스크립트, Threads canonical 도메인, 테스트 구조, 참조 문서 상태를 다시 확인했다.
- 기존 문서 변경 이력도 확인했다. 특히 `ab1082c docs: 프로젝트 규칙 문서 현행화 — Threads 도메인·참조 문서 추가` 커밋이 이미 있어, 이번 작업은 그 후속 재감사 성격으로 진행한다.
- 문서에서 stale 가능성이 높은 항목으로 `linkedin_scrap_by_user.py`, `substack_scrap_by_user.py`, `threads.net` canonical, `run_viewer.bat` 기본 진입점, 예전 output schema 설명을 우선 검토 대상으로 잡았다.

## 의사결정 기록

| 항목 | 최소 수정 | 사실 감사 후 재정리 | 전면 재작성 |
|------|-----------|---------------------|-------------|
| 현재 코드 반영 정확도 | ❌ 낮음 | ✅ 가장 유리 | ⚠️ 높음 |
| 기존 사용자 수정 보존 | ✅ 가장 유리 | ✅ 유리 | ❌ 위험 |
| 작업 비용 | ✅ 낮음 | 보통 | ❌ 높음 |
| 문서 역할 정리 | ❌ 부족 | ✅ 충분 | ✅ 충분 |

<span style="color:#888">*정렬 기준: 현재 워킹트리에 이미 문서 수정이 존재하므로, 기존 변경을 덮지 않으면서 코드 기준 사실성만 복구하는 방식을 우선했다.*</span>

- 결정: 기존 문서를 기반으로 한 사실 감사 후 재정리
- 근거: 이미 문서 변경 커밋과 미커밋 수정이 존재해 전면 재작성은 충돌 위험이 크고, 최소 수정은 현재 불일치를 충분히 해소하지 못한다.
- 트레이드오프: 문서별 구조는 크게 유지하되, 오래된 설명과 현재 코드가 충돌하는 부분은 과감히 교체해야 한다.

## 검증계획과 실행결과

> compare-table 스킬 이모지 포맷 적용 (✅❌⚠️⏳)

| 검증 항목 | 검증 방법 | 결과 | 비고 |
|-----------|-----------|------|------|
| 대상 문서 6개 현행화 | 파일 diff와 본문 대조 | ✅ 완료 | `README.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `docs/development.md`, `docs/crawling_logic.md` 본문 갱신 완료 |
| 실행 명령 정합성 | `package.json`, `requirements.txt`, `pytest.ini` 대조 | ✅ 완료 | `npm run view -> wscript sns_hub.vbs`, `pip install -r requirements.txt`, `pytest` entrypoint 일치 |
| Threads canonical 일관성 | 문서 내 `threads.net`/`.com` 검색 | ✅ 완료 | canonical은 모두 `www.threads.com`; `threads.net`은 레거시 alias 설명으로만 잔존 |
| stale 스크립트 제거 | 문서 내 사용자별 LinkedIn/Substack 스크립트 검색 | ✅ 완료 | `rg` 무매치 확인 |
| 유틸리티/테스트 검증 | `node utils/query-sns.mjs --help`, `pytest --collect-only` | ✅ 완료 | `query-sns` help 출력 정상, `pytest` 66개 수집, `migrate_threads_domain.py --dry-run` pending 0 |

## 리스크 및 미해결 이슈

- `server.py`는 `/`에서 `web_viewer/index.html`을 서빙하도록 되어 있으나 실제 HTML 진입점은 루트 `index.html`이다. 이번 작업은 문서 정합성만 다루므로 코드 수정 없이 운영 진입점 설명만 보수적으로 적는다.
- 워킹트리에 사용자 변경이 많아 문서 수정은 요청 범위 6개 파일로만 제한해야 한다.

## 다음 액션

- 대상 문서 6개를 코드 기준으로 수정한다.
- 검증 명령을 실행해 결과를 표에 반영한다.
- 모든 검증이 `✅ 완료`일 때만 커밋 여부를 판단한다.

## 참고 자료

| 출처 | 용도 |
|------|------|
| `README.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md` | 현재 문서 상태와 stale 서술 확인 |
| `docs/development.md`, `docs/crawling_logic.md` | 참조 문서의 현행성 점검 대상 |
| `package.json`, `requirements.txt`, `pytest.ini` | 실행 명령과 설치/테스트 진입점 검증 |
| `server.py`, `web_viewer/script.js`, `utils/post_schema.py`, `utils/query-sns.mjs`, `migrate_threads_domain.py` | 문서가 따라야 할 실제 동작 기준 |
| `C:\Users\ahnbu\.claude\plans\ticklish-wandering-simon.md` | 작업 범위 참고용 중간 산출물 |
| `C:\Users\ahnbu\.claude\plans\nested-wandering-lake.md` | 작업 범위 참고용 중간 산출물 |
| `docs\20260417_03_Threads-도메인-정규화-구현.md` | Threads `.com` 정규화 작업 참고 |

---
