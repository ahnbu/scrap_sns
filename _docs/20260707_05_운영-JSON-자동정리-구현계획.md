---
title: 운영-JSON-자동정리-구현계획
created: "2026-07-07 16:37"
tags:
  - plan
session_id: codex:019f3b7c-dc60-7de0-9814-ff9a2e2050de
session_path: C:/Users/ahnbu/.codex/sessions/2026/07/07/rollout-2026-07-07T16-31-11-019f3b7c-dc60-7de0-9814-ff9a2e2050de.jsonl
ai: codex
status: planned
---

# Old Output JSON Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 수집 성공 후 오래된 운영 JSON을 자동 정리하고, 필요할 때 같은 정책을 수동 dry-run/apply로 실행할 수 있게 한다.

**Architecture:** 날짜형 운영 JSON만 대상으로 하는 독립 Node.js 스크립트 `scripts/cleanup_old_output_json.mjs`를 만든다. `total_scrap.py`는 `save_total()`이 예외 없이 끝난 뒤 이 스크립트를 `--apply`로 호출한다. 삭제는 직접 삭제가 아니라 로컬 `safe-trash` 명령을 통해 수행하며, 기본 CLI 실행은 dry-run이다.

**Tech Stack:** Node.js ESM, Python `subprocess`, `node:test`, pytest, `safe-trash`, PowerShell on Windows.

---

## 범위

### 포함

- 오래된 운영 JSON 정리 스크립트 추가
- 수집 성공 후 자동 정리 연결
- 정리 정책 단위 테스트 추가
- `.gitignore` 예외 추가
- `package.json` 수동 실행 명령 추가
- 정리 실패가 수집 결과 저장을 되돌리지 않도록 경고 로그 처리

### 제외

- OpenCLI 전환
- 웹 뷰어 UI 변경
- 서버 API 변경
- 기존 JSON 스키마 변경
- `.bak`, `update`, `raw`, `debug`, `.md` 정리
- 과거 히스토리 문서 수정

## 정리 대상

정리 대상은 아래 정규식에 정확히 맞는 파일만이다.

| 그룹 | 디렉터리 | 파일명 정규식 |
|---|---|---|
| LinkedIn full | `output_linkedin/python/` | `^linkedin_py_full_\d{8}\.json$` |
| Threads full | `output_threads/python/` | `^threads_py_full_\d{8}\.json$` |
| Threads simple | `output_threads/python/` | `^threads_py_simple_\d{8}\.json$` |
| X full | `output_twitter/python/` | `^twitter_py_full_\d{8}\.json$` |
| X simple | `output_twitter/python/` | `^twitter_py_simple_\d{8}\.json$` |
| Total full | `output_total/` | `^total_full_\d{8}\.json$` |

명시적으로 제외한다.

- `*.bak`
- `*.bak.json`
- `*_update_*.json`
- `*.md`
- `output_*/*/update/`
- `output_linkedin/opencli_shadow/`
- `output_linkedin/debug/`
- `tests/fixtures/snapshots/`

## 보존 정책

각 그룹별로 독립 적용한다.

1. 오늘 날짜 파일은 항상 보존한다.
2. 최신 5개 파일은 날짜와 무관하게 항상 보존한다.
3. 최근 30일 파일은 모두 보존한다.
4. 30일 이전 파일은 월별 최신 1개만 보존한다.
5. 위 조건에 하나도 해당하지 않는 날짜형 운영 JSON만 정리 후보가 된다.

기준 날짜는 KST(UTC+9)다. 테스트에서는 `--today YYYYMMDD`로 기준 날짜를 고정할 수 있어야 한다.

## 파일 구조

- Create: `D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs`
  - 운영 JSON 파일을 스캔하고 보존/정리 후보를 계산한다.
  - 기본 실행은 dry-run이다.
  - `--apply`일 때만 `safe-trash`를 호출한다.
  - 테스트를 위해 핵심 함수들을 export한다.
- Create: `D:/vibe-coding/scrap_sns/tests/unit/test_cleanup_old_output_json.mjs`
  - 보존 정책, 제외 규칙, dry-run/apply 동작을 검증한다.
- Modify: `D:/vibe-coding/scrap_sns/total_scrap.py`
  - `save_total()` 성공 직후 자동 정리를 호출한다.
  - 정리 실패는 경고로 출력하되 이미 저장된 수집 결과를 실패 처리하지 않는다.
- Modify: `D:/vibe-coding/scrap_sns/package.json`
  - 수동 dry-run/apply 명령을 추가한다.
- Modify: `D:/vibe-coding/scrap_sns/.gitignore`
  - `scripts/cleanup_old_output_json.mjs`를 추적 가능하게 예외 추가한다.

## Task 1: 정리 정책 테스트 추가

**Files:**
- Create: `D:/vibe-coding/scrap_sns/tests/unit/test_cleanup_old_output_json.mjs`
- Create later: `D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs`

- [ ] **Step 1: 실패하는 Node 테스트를 작성한다**

Create `tests/unit/test_cleanup_old_output_json.mjs` with these cases:

```javascript
import assert from "node:assert/strict";
import { mkdir, writeFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import test from "node:test";

import {
  TARGETS,
  getKstTodayYmd,
  planCleanup,
  parseArgs,
} from "../../scripts/cleanup_old_output_json.mjs";

async function touchJson(root, relativePath) {
  const filePath = path.join(root, relativePath);
  await mkdir(path.dirname(filePath), { recursive: true });
  await writeFile(filePath, "{\"posts\":[]}\n", "utf8");
  return filePath;
}

test("parseArgs defaults to dry-run and rejects apply plus dry-run together", () => {
  assert.deepEqual(parseArgs([]).mode, "dry-run");
  assert.throws(() => parseArgs(["--apply", "--dry-run"]), /Choose only one/);
});

test("getKstTodayYmd formats the current day in KST", () => {
  assert.equal(getKstTodayYmd(new Date("2026-07-06T16:00:00.000Z")), "20260707");
});

test("planCleanup keeps today, latest 5, recent 30 days, and monthly latest", async () => {
  const root = await os.promises.mkdtemp(path.join(os.tmpdir(), "sns-cleanup-"));
  const dates = [
    "20260201",
    "20260213",
    "20260310",
    "20260318",
    "20260411",
    "20260425",
    "20260501",
    "20260529",
    "20260603",
    "20260614",
    "20260623",
    "20260707",
  ];

  for (const ymd of dates) {
    await touchJson(root, `output_total/total_full_${ymd}.json`);
  }

  const plan = await planCleanup({
    root,
    todayYmd: "20260707",
    keepDays: 30,
    keepLatest: 5,
    targets: [TARGETS.find((target) => target.id === "total_full")],
  });

  const kept = plan.groups[0].keep.map((item) => path.basename(item.path)).sort();
  const deleted = plan.groups[0].delete.map((item) => path.basename(item.path)).sort();

  assert.deepEqual(kept, [
    "total_full_20260213.json",
    "total_full_20260318.json",
    "total_full_20260425.json",
    "total_full_20260529.json",
    "total_full_20260603.json",
    "total_full_20260614.json",
    "total_full_20260623.json",
    "total_full_20260707.json",
  ]);
  assert.deepEqual(deleted, [
    "total_full_20260201.json",
    "total_full_20260310.json",
    "total_full_20260411.json",
    "total_full_20260501.json",
  ]);
});

test("planCleanup ignores backups, markdown, update files, and non-target directories", async () => {
  const root = await os.promises.mkdtemp(path.join(os.tmpdir(), "sns-cleanup-ignore-"));
  await touchJson(root, "output_threads/python/threads_py_full_20260411.json");
  await touchJson(root, "output_threads/python/threads_py_full_20260411.json.bak");
  await touchJson(root, "output_threads/python/threads_py_full_20260411_20260417_122609.bak.json");
  await touchJson(root, "output_threads/python/update/threads_py_full_update_20260411_120000.json");
  await touchJson(root, "output_total/total_full_20260411.md");

  const plan = await planCleanup({
    root,
    todayYmd: "20260707",
    keepDays: 30,
    keepLatest: 0,
    targets: [TARGETS.find((target) => target.id === "threads_full")],
  });

  assert.equal(plan.groups[0].files.length, 1);
  assert.equal(path.basename(plan.groups[0].files[0].path), "threads_py_full_20260411.json");
});
```

- [ ] **Step 2: 테스트가 구현 전 실패하는지 확인한다**

Run:

```powershell
node --test D:/vibe-coding/scrap_sns/tests/unit/test_cleanup_old_output_json.mjs
```

Expected:

```markdown
FAIL
Cannot find module '../../scripts/cleanup_old_output_json.mjs'
```

## Task 2: 독립 정리 스크립트 구현

**Files:**
- Create: `D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs`

- [ ] **Step 1: CLI 옵션과 대상 그룹을 구현한다**

Create the script with these exported names:

```javascript
export const TARGETS = [
  {
    id: "linkedin_full",
    label: "LinkedIn full",
    dir: "output_linkedin/python",
    regex: /^linkedin_py_full_(\d{8})\.json$/,
  },
  {
    id: "threads_full",
    label: "Threads full",
    dir: "output_threads/python",
    regex: /^threads_py_full_(\d{8})\.json$/,
  },
  {
    id: "threads_simple",
    label: "Threads simple",
    dir: "output_threads/python",
    regex: /^threads_py_simple_(\d{8})\.json$/,
  },
  {
    id: "twitter_full",
    label: "X full",
    dir: "output_twitter/python",
    regex: /^twitter_py_full_(\d{8})\.json$/,
  },
  {
    id: "twitter_simple",
    label: "X simple",
    dir: "output_twitter/python",
    regex: /^twitter_py_simple_(\d{8})\.json$/,
  },
  {
    id: "total_full",
    label: "Total full",
    dir: "output_total",
    regex: /^total_full_(\d{8})\.json$/,
  },
];
```

`parseArgs(argv)` must support:

```markdown
--dry-run        기본값. 정리 후보만 출력한다.
--apply          safe-trash로 실제 정리한다.
--root <path>    테스트용 루트. 기본값은 레포 루트.
--today YYYYMMDD 테스트용 기준 날짜. 생략 시 KST 오늘.
--keep-days N    기본 30.
--keep-latest N  기본 5.
--json           사람이 읽는 출력 대신 JSON summary 출력.
--help           사용법 출력.
```

- [ ] **Step 2: KST 날짜 계산을 구현한다**

Use a deterministic formatter:

```javascript
export function getKstTodayYmd(now = new Date()) {
  const parts = new Intl.DateTimeFormat("en-CA", {
    timeZone: "Asia/Seoul",
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
  }).formatToParts(now);
  const values = Object.fromEntries(parts.map((part) => [part.type, part.value]));
  return `${values.year}${values.month}${values.day}`;
}
```

Date difference helper:

```javascript
function ymdToUtcDay(ymd) {
  const year = Number(ymd.slice(0, 4));
  const month = Number(ymd.slice(4, 6));
  const day = Number(ymd.slice(6, 8));
  return Date.UTC(year, month - 1, day) / 86400000;
}
```

- [ ] **Step 3: 보존/정리 후보 계산을 구현한다**

Add `listTargetFiles()` before `planCleanup()`:

```javascript
import { readdir } from "node:fs/promises";
import path from "node:path";

export async function listTargetFiles(root, target) {
  const dirPath = path.join(root, target.dir);
  let entries = [];
  try {
    entries = await readdir(dirPath, { withFileTypes: true });
  } catch (error) {
    if (error && error.code === "ENOENT") return [];
    throw error;
  }

  return entries
    .filter((entry) => entry.isFile())
    .map((entry) => {
      const match = target.regex.exec(entry.name);
      if (!match) return null;
      return {
        path: path.join(dirPath, entry.name),
        relativePath: path.join(target.dir, entry.name).replace(/\\/g, "/"),
        name: entry.name,
        ymd: match[1],
      };
    })
    .filter(Boolean)
    .sort((a, b) => a.ymd.localeCompare(b.ymd) || a.name.localeCompare(b.name));
}
```

Then implement `planCleanup()`:

```javascript
export async function planCleanup({
  root,
  todayYmd,
  keepDays = 30,
  keepLatest = 5,
  targets = TARGETS,
} = {}) {
  const groups = [];
  for (const target of targets) {
    const files = await listTargetFiles(root, target);
    const keepPaths = new Set();
    const todayDay = ymdToUtcDay(todayYmd);

    for (const file of files) {
      if (file.ymd === todayYmd) keepPaths.add(file.path);
      if (todayDay - ymdToUtcDay(file.ymd) <= keepDays) keepPaths.add(file.path);
    }

    for (const file of [...files].sort((a, b) => b.ymd.localeCompare(a.ymd)).slice(0, keepLatest)) {
      keepPaths.add(file.path);
    }

    const monthlyLatest = new Map();
    for (const file of files) {
      if (file.ymd === todayYmd) continue;
      if (todayDay - ymdToUtcDay(file.ymd) <= keepDays) continue;
      const month = file.ymd.slice(0, 6);
      const current = monthlyLatest.get(month);
      if (!current || file.ymd > current.ymd) monthlyLatest.set(month, file);
    }
    for (const file of monthlyLatest.values()) keepPaths.add(file.path);

    const keep = files.filter((file) => keepPaths.has(file.path));
    const deleteItems = files.filter((file) => !keepPaths.has(file.path));
    groups.push({ target, files, keep, delete: deleteItems });
  }

  return {
    todayYmd,
    keepDays,
    keepLatest,
    groups,
    delete: groups.flatMap((group) => group.delete),
    keep: groups.flatMap((group) => group.keep),
  };
}
```

- [ ] **Step 4: safe-trash 실행을 구현한다**

Requirements:

```markdown
- `--dry-run`에서는 safe-trash를 호출하지 않는다.
- `--apply`에서는 정리 후보 경로만 safe-trash로 보낸다.
- 후보가 0개면 exit code 0.
- safe-trash 명령을 찾지 못하면 exit code 1과 명확한 오류 메시지를 출력한다.
```

Implementation shape:

```javascript
import { spawnSync } from "node:child_process";

function resolveSafeTrashCommand() {
  const where = spawnSync("where.exe", ["safe-trash"], { encoding: "utf8" });
  if (where.status !== 0) return null;
  return where.stdout.split(/\r?\n/).map((line) => line.trim()).find(Boolean) || null;
}

export function runSafeTrash(paths) {
  const command = resolveSafeTrashCommand();
  if (!command) {
    throw new Error("safe-trash command was not found on PATH.");
  }

  for (const targetPath of paths) {
    const result = spawnSync(
      "powershell",
      ["-NoProfile", "-ExecutionPolicy", "Bypass", "-File", command, targetPath],
      { stdio: "inherit" },
    );
    if (result.status !== 0) {
      throw new Error(`safe-trash failed for ${targetPath}`);
    }
  }
}
```

- [ ] **Step 5: 사람이 읽는 출력과 JSON 출력을 구현한다**

Dry-run output must include:

```markdown
Mode: dry-run
Today(KST): 20260707
Policy: keep latest 5, keep recent 30 days, keep monthly latest before that
[Total full] files=34 keep=8 cleanup=26
  cleanup output_total/total_full_20260211.json
Summary: cleanup candidates 26, kept 8
```

Apply output must include:

```markdown
Mode: apply
[Total full] files=34 keep=8 cleanup=26
  cleanup output_total/total_full_20260211.json
Moved to trash: 26 files
```

JSON output shape:

```json
{
  "mode": "dry-run",
  "todayYmd": "20260707",
  "keepDays": 30,
  "keepLatest": 5,
  "deleteCount": 26,
  "keepCount": 8,
  "groups": [
    {
      "id": "total_full",
      "label": "Total full",
      "fileCount": 34,
      "keepCount": 8,
      "deleteCount": 26
    }
  ]
}
```

- [ ] **Step 6: Node 테스트가 통과하는지 확인한다**

Run:

```powershell
node --test D:/vibe-coding/scrap_sns/tests/unit/test_cleanup_old_output_json.mjs
```

Expected:

```markdown
PASS
```

## Task 3: 수집 성공 후 자동 정리 연결

**Files:**
- Modify: `D:/vibe-coding/scrap_sns/total_scrap.py`
- Modify: `D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_orchestration.py`

- [ ] **Step 1: 자동 정리 호출 테스트를 추가한다**

Add a test to `tests/unit/test_total_scrap_orchestration.py`:

```python
def test_run_calls_cleanup_after_save_total_success(tmp_path):
    script = r"""
import json
import total_scrap

events = []

def fake_run_scrapers_in_parallel(mode="update"):
    events.append(["run_scrapers", mode])
    return {"threads": {"status": "ok"}}

def fake_merge_results():
    events.append(["merge_results"])
    return ([{"platform_id": "1", "media": []}], 1, 0, 0)

def fake_download_images(posts):
    events.append(["download_images", len(posts)])

def fake_save_total(posts, threads_count, linkedin_count, twitter_count):
    events.append(["save_total", len(posts), threads_count, linkedin_count, twitter_count])

def fake_cleanup():
    events.append(["cleanup"])
    return True

total_scrap.run_scrapers_in_parallel = fake_run_scrapers_in_parallel
total_scrap.merge_results = fake_merge_results
total_scrap.download_images = fake_download_images
total_scrap.save_total = fake_save_total
total_scrap.cleanup_old_output_json_after_success = fake_cleanup

total_scrap.run(mode="update")
print(json.dumps(events, ensure_ascii=False))
"""

    completed = _run_total_scrap_probe(script, tmp_path)

    assert completed.returncode == 0, completed.stderr
    assert json.loads(completed.stdout.strip().splitlines()[-2]) == [
        ["run_scrapers", "update"],
        ["merge_results"],
        ["download_images", 1],
        ["save_total", 1, 1, 0, 0],
        ["cleanup"],
    ]
```

Add a failure guard test:

```python
def test_run_does_not_cleanup_when_save_total_fails(tmp_path):
    script = r"""
import json
import total_scrap

events = []

total_scrap.run_scrapers_in_parallel = lambda mode="update": {}
total_scrap.merge_results = lambda: ([{"platform_id": "1", "media": []}], 1, 0, 0)
total_scrap.download_images = lambda posts: events.append(["download_images"])

def fake_save_total(*args):
    events.append(["save_total"])
    raise RuntimeError("save failed")

def fake_cleanup():
    events.append(["cleanup"])

total_scrap.save_total = fake_save_total
total_scrap.cleanup_old_output_json_after_success = fake_cleanup

try:
    total_scrap.run(mode="update")
except RuntimeError as exc:
    print(json.dumps({"error": str(exc), "events": events}, ensure_ascii=False))
"""

    completed = _run_total_scrap_probe(script, tmp_path)

    assert completed.returncode == 0, completed.stderr
    payload = json.loads(completed.stdout.strip().splitlines()[-2])
    assert payload == {
        "error": "save failed",
        "events": [["download_images"], ["save_total"]],
    }
```

- [ ] **Step 2: 테스트가 구현 전 실패하는지 확인한다**

Run:

```powershell
pytest D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_orchestration.py -q
```

Expected before implementation:

```markdown
FAIL
AttributeError: module 'total_scrap' has no attribute 'cleanup_old_output_json_after_success'
```

- [ ] **Step 3: `total_scrap.py`에 자동 정리 함수를 추가한다**

Add near the other helper functions:

```python
def cleanup_old_output_json_after_success():
    repo_root = os.path.dirname(os.path.abspath(__file__))
    script_path = os.path.join(repo_root, "scripts", "cleanup_old_output_json.mjs")
    if not os.path.exists(script_path):
        print(f"⚠️ 오래된 운영 JSON 정리 스크립트를 찾을 수 없습니다: {script_path}")
        return False

    completed = subprocess.run(
        ["node", script_path, "--apply"],
        cwd=repo_root,
        text=True,
        encoding="utf-8",
        errors="replace",
        capture_output=True,
        check=False,
    )

    if completed.stdout:
        print(completed.stdout.strip())
    if completed.stderr:
        print(completed.stderr.strip())

    if completed.returncode != 0:
        print(f"⚠️ 오래된 운영 JSON 자동 정리에 실패했습니다. exit={completed.returncode}")
        return False

    return True
```

- [ ] **Step 4: `save_total()` 성공 직후에만 호출한다**

Change:

```python
save_total(posts, threads_count, linkedin_count, twitter_count)
```

to:

```python
save_total(posts, threads_count, linkedin_count, twitter_count)
cleanup_old_output_json_after_success()
```

Do not call cleanup before:

```python
merge_results()
download_images(posts)
validate_local_image_links(new_posts)
save_total(posts, threads_count, linkedin_count, twitter_count)
```

- [ ] **Step 5: Python 테스트가 통과하는지 확인한다**

Run:

```powershell
pytest D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_orchestration.py -q
```

Expected:

```markdown
all passed
```

## Task 4: 수동 실행 명령과 Git 추적 예외 추가

**Files:**
- Modify: `D:/vibe-coding/scrap_sns/package.json`
- Modify: `D:/vibe-coding/scrap_sns/.gitignore`

- [ ] **Step 1: package scripts를 추가한다**

Modify `package.json` scripts:

```json
{
  "scripts": {
    "start": "python scrap_sns_server.py",
    "view": "wscript sns_hub.vbs",
    "stop": "stop_viewer.bat",
    "scrap:threads": "python thread_scrap.py",
    "scrap:linkedin": "python linkedin_scrap.py",
    "scrap:all": "python total_scrap.py",
    "cleanup:outputs": "node scripts/cleanup_old_output_json.mjs --dry-run",
    "cleanup:outputs:apply": "node scripts/cleanup_old_output_json.mjs --apply"
  }
}
```

- [ ] **Step 2: `.gitignore` 예외를 추가한다**

Under the existing `scripts/*` exceptions, add:

```gitignore
!scripts/cleanup_old_output_json.mjs
```

- [ ] **Step 3: 추적 상태를 확인한다**

Run:

```powershell
git check-ignore -v scripts/cleanup_old_output_json.mjs
```

Expected:

```markdown
no output
```

Run:

```powershell
git status --short scripts/cleanup_old_output_json.mjs tests/unit/test_cleanup_old_output_json.mjs .gitignore package.json
```

Expected:

```markdown
?? scripts/cleanup_old_output_json.mjs
?? tests/unit/test_cleanup_old_output_json.mjs
 M .gitignore
 M package.json
```

## Task 5: Dry-run과 실제 정리 경로 검증

**Files:**
- No additional edits.

- [ ] **Step 1: 실제 데이터 기준 dry-run을 실행한다**

Run:

```powershell
node D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs --dry-run
```

Expected:

```markdown
Mode: dry-run
Today(KST): 20260707
Summary: cleanup candidates 0 or more, kept 0 or more
```

Must confirm:

```markdown
- 출력에 `.bak`, `.bak.json`, `.md`, `update` 경로가 없다.
- 오늘 파일 `*_20260707.json`이 cleanup 후보에 없다.
- 각 그룹별 최신 5개가 cleanup 후보에 없다.
```

- [ ] **Step 2: JSON summary를 확인한다**

Run:

```powershell
node D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs --dry-run --json
```

Expected:

```markdown
valid JSON
mode is `dry-run`
deleteCount is a number
groups contains 6 items
```

- [ ] **Step 3: safe-trash 존재를 확인한다**

Run:

```powershell
Get-Command safe-trash
```

Expected:

```markdown
Source points to C:\Users\ahnbu\scripts\safe-trash.ps1 or another valid safe-trash executable.
```

- [ ] **Step 4: apply는 사용자 승인 후에만 실행한다**

Run only after reviewing dry-run output:

```powershell
node D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs --apply
```

Expected:

```markdown
Mode: apply
Moved to trash: N files
```

- [ ] **Step 5: apply 후 대상 파일 수가 정책과 일치하는지 확인한다**

Run:

```powershell
node D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs --dry-run
```

Expected:

```markdown
Summary: cleanup candidates 0
```

## Task 6: 전체 테스트와 완료 검증

**Files:**
- No additional edits.

- [ ] **Step 1: 변경 범위 테스트를 실행한다**

Run:

```powershell
node --test D:/vibe-coding/scrap_sns/tests/unit/test_cleanup_old_output_json.mjs
pytest D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_orchestration.py -q
pytest D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_local_images.py D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_redirect_alias.py -q
```

Expected:

```markdown
all passed
```

- [ ] **Step 2: CLI help와 package scripts를 확인한다**

Run:

```powershell
node D:/vibe-coding/scrap_sns/scripts/cleanup_old_output_json.mjs --help
npm run cleanup:outputs
```

Expected:

```markdown
--help output includes --dry-run, --apply, --keep-days, --keep-latest, --today, --json.
`npm run cleanup:outputs` runs dry-run and exits 0.
```

- [ ] **Step 3: 자동 정리 연결을 실제 수집 없이 함수 수준에서 확인한다**

Run:

```powershell
pytest D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_orchestration.py::test_run_calls_cleanup_after_save_total_success D:/vibe-coding/scrap_sns/tests/unit/test_total_scrap_orchestration.py::test_run_does_not_cleanup_when_save_total_fails -q
```

Expected:

```markdown
2 passed
```

- [ ] **Step 4: 보안 게이트를 실행한다**

Run:

```powershell
node C:/Users/ahnbu/.claude/skills/_shared/security-gate.mjs precommit --repo D:/vibe-coding/scrap_sns --staged
```

Expected:

```markdown
secret, credential, external-transfer 의심 패턴 없음.
```

이 명령은 실제 커밋 직전 staged 상태에서 다시 실행한다.

## 커밋 분리 기준

이 작업은 한 관심사로 묶을 수 있다.

```markdown
feat(output-cleanup): 오래된 운영 JSON 자동 정리 추가 — 날짜별 수집 산출물 누적 방지
```

단, 실제 apply 실행으로 운영 JSON 삭제가 발생하면 커밋을 분리한다.

1. `feat(output-cleanup)`: 스크립트, 테스트, 자동 연결, 문서/설정
2. `chore(output-data)`: 정책 적용으로 정리된 운영 JSON 삭제 diff

커밋은 직접 `git add/commit`하지 않고 cp 스킬로 처리한다.

## 자체 검수

- 요구 범위 확인: 오래된 JSON 정리와 수집 성공 후 자동 정리만 포함했다.
- OpenCLI 전환: 범위에서 제외했다.
- 자동 실행: `save_total()` 성공 직후 호출로 반영했다.
- 안전 삭제: 직접 삭제가 아니라 `safe-trash`로 제한했다.
- 파일 대상: 날짜형 운영 JSON 정규식으로 제한해 `.bak`, `update`, `raw`, `debug`, `.md`를 제외했다.
- 테스트: Node 정책 테스트와 Python 자동 연결 테스트를 모두 포함했다.
- 영구 데이터 영향: 운영 JSON 파일 개수가 줄어든다. 기존 데이터 재처리는 필요 없고, 정리된 파일은 휴지통에서 복구 가능해야 한다.

## Implementation Results

실행 시각: 2026-07-07 16:50 KST

### Plan Check

| 항목 | 결과 |
|---|---|
| 1차 plan-check-lite | `수정 필요` |
| 1차 보고서 | `D:/vibe-coding/scrap_sns/docs/plan-check/20260707_05_운영-JSON-자동정리-구현계획_경량계획검수보고서.md` |
| 반영 내용 | `listTargetFiles()` 구현 명세 추가, Python 자동 정리 함수의 `repo_root` 계산 명시 |
| 2차 plan-check-lite | `실행 가능` |
| 2차 보고서 | `D:/vibe-coding/scrap_sns/docs/plan-check/20260707_05_운영-JSON-자동정리-구현계획_경량계획검수보고서_1644.md` |

### 변경 내역

| 파일 | 변경 |
|---|---|
| `scripts/cleanup_old_output_json.mjs` | 운영 JSON 날짜 파일만 스캔하는 정리 스크립트 추가. 기본 dry-run, `--apply` 시 `safe-trash` 사용 |
| `tests/unit/test_cleanup_old_output_json.mjs` | 보존 정책, KST 날짜, 대상 파일 필터링 테스트 추가 |
| `total_scrap.py` | `save_total()` 성공 직후 `cleanup_old_output_json_after_success()` 호출 추가 |
| `tests/unit/test_total_scrap_orchestration.py` | 저장 성공 후 자동 정리 호출, 저장 실패 시 미호출 테스트 추가 |
| `package.json` | `cleanup:outputs`, `cleanup:outputs:apply` 명령 추가 |
| `.gitignore` | `scripts/cleanup_old_output_json.mjs` 추적 예외 추가 |

### 검증 결과

| 검증 | 결과 |
|---|---|
| `node --test tests/unit/test_cleanup_old_output_json.mjs` | PASS, 4 tests |
| `pytest tests/unit/test_total_scrap_orchestration.py -q` | PASS, 5 tests |
| `pytest tests/unit/test_total_scrap_local_images.py tests/unit/test_total_scrap_redirect_alias.py -q` | PASS, 2 tests |
| `python -m py_compile total_scrap.py` | PASS |
| `node scripts/cleanup_old_output_json.mjs --help` | PASS, 주요 옵션 표시 확인 |
| `npm run cleanup:outputs` | PASS, dry-run 정상 실행 |
| `node scripts/cleanup_old_output_json.mjs --dry-run --json` | PASS, `mode=dry-run`, `groups=6`, `deleteCount=142`, `keepCount=51` |
| `Get-Command safe-trash` | PASS, `C:\Users\ahnbu\scripts\safe-trash.ps1` 확인 |
| 제외 규칙 확인 | PASS, dry-run 후보 142개 중 `.bak`, `.md`, `update`, `20260707` 포함 0개 |

### 운영 데이터 영향

이번 구현 중 실제 운영 JSON 정리는 실행하지 않았다. 현재 dry-run 기준 정리 후보는 142개이며, 다음 성공 수집 후 자동으로 `safe-trash`를 통해 휴지통으로 이동된다.

### 남은 리스크

- 다음 실제 수집 성공 시 운영 JSON 삭제 diff가 크게 발생한다. 커밋은 코드 변경과 데이터 정리 diff를 분리해야 한다.
- `safe-trash`가 PATH에서 사라지면 자동 정리는 실패하지만, 수집 결과 저장은 유지되고 경고만 출력된다.
