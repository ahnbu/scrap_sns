---
title: "링크드인-이미지-복구와-외부요약-자동화-아이콘-개선-계획"
created: "2026-09-01 11:30"
session_id: "931037b9-008c-4bef-b378-a8e38ac862fd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/931037b9-008c-4bef-b378-a8e38ac862fd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-01 11:30:34"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

## Blocking Issues
**이 계획은 링크드인 누락 이미지의 96%에 해당하는 기존 만료분(453건) 복구를 '범위 밖'으로 미루고 있어, "링크드인 이미지가 전혀 보이지 않는다"는 사용자의 당초 목적을 온전히 달성할 가능성이 매우 낮습니다.**

- 요구사항 누락에 따른 승인 필요 / `7. 범위 밖` / 계획 문서에서 기존 만료 이미지 453건의 복구를 "별도 계획으로 판단"한다며 후속 과제로 분리(미룸)했습니다. 이대로 실행하면 대부분의 과거 링크드인 글에서 여전히 이미지가 보이지 않게 되므로, 미뤄진 항목을 이번 계획에 포함할지 사용자의 명시적 승인이 필요합니다.
- 사람 육안(GUI) 의존 검증 불가 / `5. 검증 기준`의 웨이브 1-6, 웨이브 2-5, 웨이브 3-2, 웨이브 3-3 / '화면 캡처', '뷰어에서 확인', '레이아웃 깨짐 확인' 등 모두 브라우저 수동 확인 항목입니다. `done-check` 에이전트가 종료코드나 로그 증거만으로 자동 판정할 수 있도록 헤드리스(Headless, 화면 없이 백그라운드에서 브라우저를 실행하는 방식) 스크립트 실행을 명시하거나 스크립트 기반 판정으로 대체해야 합니다.

## Required Fixes
- 코드 플레이스홀더 제거: 웨이브 1-4의 `save_total(...)` 및 웨이브 2-1의 `subprocess.run(["node", ...])` 설명에 사용된 `...` 생략 기호를 구체적인 인자나 확정된 코드로 명시해야 합니다.
- 캡처 산출물 처리 방침 명시: 검증 과정에서 생성되는 '화면 캡처' 파일(산출물)을 깃(Git) 커밋에 포함할지, `.gitignore`에 넣어 명시적으로 제외할지, 아니면 로컬 전용(local-only)으로 둘지 산출물 처리 방침을 명시해야 합니다.
- 실행 웨이브 블록 명시: 웨이브 단위의 실행 계획임에도 `## 실행 웨이브` 또는 `## 웨이브 구조` 헤딩이 없습니다. 자연어로 작성된 `### 웨이브 1` 등을 명시적인 웨이브 블록 헤딩 아래에 배치하여 파일 단위 작업 시 선후관계 충돌을 막아야 합니다.
- 영구 데이터 백업 추가: 웨이브 1에서 데이터 갱신 시 기존 `output_total` 내 데이터가 오염될 위험이 있습니다. 실행 전 기존 데이터를 복사해두는 롤백(Rollback, 실패 시 되돌리기)용 로컬 백업 단계를 추가해야 합니다.

## 참고 제안 (판정에 미반영)
- 트레이드오프 명시: 새로운 이미지를 다운로드하기 위해 과거 게시글에 재접근(웨이브 1-3)하는 방식은 최신 URL을 얻는 이점이 있지만, 그만큼 크롤링 소요 시간과 네트워크 트래픽이 늘어나는 단점(트레이드오프)이 있습니다. 이를 문서에 명시하는 것을 권장합니다.