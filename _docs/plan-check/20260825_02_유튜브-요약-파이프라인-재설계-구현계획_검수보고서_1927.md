---
title: "유튜브-요약-파이프라인-재설계-구현계획"
created: "2026-08-25 19:27"
session_id: "8726f5e3-015c-4a6c-bd4b-aa0f7130c887"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/8726f5e3-015c-4a6c-bd4b-aa0f7130c887.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-25 19:27:46"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- 지적: `youtube_scrap.py`에 `--no-bulk-summary` CLI 인자 정의 누락
- 근거: 계획서 3.4항 b ("total_scrap.py가 부르는 YouTube 명령에 --no-bulk-summary 가드를 넣어") vs 2-F-1항 "신설 CLI 인자 (정본)" 및 4장 변경 파일 목록
- 실행 전 막히는 이유: 계획대로 `total_scrap.py`에서 `--no-bulk-summary` 플래그를 넘겨 실행하면, 정작 `youtube_scrap.py`의 `argparse`에는 해당 인자가 정의되지 않아 `ArgumentError (unrecognized arguments)` 예외가 발생하며 백그라운드 수집 프로세스(일상 업데이트 포함) 전체가 강제 종료된다.

## Required Fixes
- 2-E항의 쿼터 실측 프로토콜 수치 불일치 수정: 2-E항은 "20건 배치 실행" 후 "델타 × (448/20)"으로 잔량을 판정한다고 기재되어 있으나, 2-F항에 명시된 실제 웨이브 설계는 W0 게이트가 10건이고 전체 대상이 76건이다. 과거 초안의 잔재이므로, 수치를 W0 기준인 10건 실행 및 76/10 배수로 맞춰 2-F항의 설계와 모순되지 않도록 정정해야 한다.
- S2 검증 스크립트(`verify_youtube_viewer_headless.mjs`)의 `sequence_id` 조회 방식 명시: 3.3항 S2 절은 검증 스크립트가 `/api/post/{sequence_id}`를 호출해 본문을 받는다고 서술하였으나, 2-F-3 실행 명령에서 스크립트에 전달되는 `--ids "$IDS"`는 `sequence_id`가 아닌 `video_id`의 집합이다. 헤드리스 스크립트 내부에서 `/api/posts`를 조회하는 등의 방식으로 `video_id`를 `sequence_id`로 매핑한 뒤 상세를 조회하도록 논리를 추가 명시해야 404 오류(실패) 없이 검증이 작동한다.

## 참고 제안 (판정에 미반영)
- 직전(5차) 검수에서 지적된 2-D 절의 `$ndjsonPath` 제거와 S14 검증 명령 신설(3.7.4장) 건은 코드를 확인한 결과 올바르게 반영되어 실질적으로 닫혔음.
