---
title: "수집데이터-본문결측-중복-오염-수정계획"
created: "2026-08-24 11:52"
session_id: "4efc85ee-1841-4417-bd43-ced95062bdc2"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/4efc85ee-1841-4417-bd43-ced95062bdc2.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 11:52:12"
---

이 계획서는 기존 코드를 깊이 분석하고 엣지 케이스와 롤백 계획까지 세심하게 고려한 매우 높은 수준의 계획서입니다. 사용자께서 특별히 지시하신 5대 리스크도 모두 계획서 내에서 방어 로직이 완비되어 있습니다. 

다만, 세부 실행 스크립트의 검증 단언(Assertion)에서 기존 파이썬 코드의 동작 방식을 잘못 예측하여 **성공적으로 실행해도 검증에서 무조건 실패하여 파이프라인이 막히는(Blocking) 논리적 모순 2건**이 발견되었습니다.

아래는 요청하신 포맷에 맞춘 검수 보고서입니다.

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
1. **단계 C-2-c(X 데이터 리셋 스크립트 작성): `date` 필드 동기화 로직 누락**
   - 근거: 계획서 C-2-e의 검증 단언 3번은 `date != created_at[:10]` 인 X 레코드 수가 0이 될 것을 기대한다. 그러나 `twitter_scrap_single.py:121-124` 코드를 확인하면 `created_at` 필드가 없을 때만 `date`를 덮어쓴다. C-2-c 리셋 요건에는 `created_at`을 보존한다고 되어 있어 재수집 시 `date`가 갱신되지 않는다.
   - 실행 전 막히는 이유: 오염된 `date` 값이 수정되지 않고 그대로 남아, C-2-e 검증 스크립트가 무조건 실패(`exit 1`)하여 파이프라인이 차단된다. 리셋 스크립트(C-2-c) 요구사항에 `date`를 `created_at[:10]`으로 명시적으로 맞추는 로직을 추가해야 한다.

2. **단계 B-2-d(Threads 중복 정리 검증): 단언 3번 기대값의 수학적 모순**
   - 근거: B-2-b 단계에서 `pk` 중복 9개 그룹의 레코드에 삭제 대신 `detail_status="duplicate_of_canonical"`을 부여한다. `total_scrap.py:514-518`의 `merge_results()` 로직은 이 상태값을 가진 레코드를 통합본에서 원천 제외한다.
   - 실행 전 막히는 이유: 중복 9건이 병합 시 제외되므로 `total_full_*.json`의 최종 Threads 건수는 기존 1,247건에서 1,238건으로 **정확히 9건 감소**해야 정상이다. 그러나 단언 3번이 `1,247 이상(감소하면 안 됨)`을 기대하고 있어, 코드가 정상 동작하더라도 검증에서 무조건 실패(`exit 1`)한다. 기대값을 1,238로 수정해야 한다.

## Required Fixes
없음

## 참고 제안 (판정에 미반영)
- **추가 지시 5개 항목 집중 검토 결과**:
  1. **A-2/B-0 fail_count 리스크**: 계획서는 백업뿐 아니라 실패 시 카운트를 올리지 않는 우회 스크립트(`recover_threads_detail_failures.py`) 사용을 명시했으며, B-0-e에서 복구 여부를 이중 검증하므로 리스크가 완전히 닫혀 있습니다.
  2. **C-2, B-2 리셋 작업 구체성**: 모호한 서술이 아닙니다. 대상 파일, 변경/보존 필드, dry-run 옵션 등 구체적 요구사항을 담은 스크립트 신규 작성 지시와 셸 명령이 명확히 제시되어 즉시 구현·실행 가능한 수준입니다.
  3. **3.3항 범위 축소 여부**: 범위 축소에 해당하지 않습니다. 사용자 원 요청("이미지 본문 누락 + 중복 진단")의 스코프를 준수한 것이며, 진단 중 우연히 발견된 3건의 별개 사안을 덜어낸 것은 무단 범위 축소가 아닌 정당한 경계 설정입니다.
  4. **미확정 사항의 실행 차단 여부**: 차단 리스크가 없습니다. 핵심인 CLI URL 호환성은 이미 계획서 내에서 '해소'로 기재되었으며, 만일의 사태(CLI 실패)를 대비해 C-2-b 단계에 데이터를 건드리기 전 테스트하는 사전 게이트를 두어 안전하게 닫혀 있습니다.
  5. **토큰 만료 및 부분 실패 처리**: C-2-d 단계에 상세한 부분 실패 룰이 있습니다. 일부 실패 시 성공분 유지 및 1회 재실행, 토큰 만료 시 전체 롤백 없이 리셋 상태로 일시 정지 후 사용자에게 갱신을 요청하도록 합리적으로 계획되어 있습니다.
- **웨이브 실행 중단 범위**: C-2-b(CLI 사전 확인 게이트) 실패 시 "W3으로 넘어가지 않는다"고 기재되어 있습니다. X 데이터 처리(C-2)와 Threads 데이터 처리(W3)는 기술적으로 완전히 독립적이므로 C-2만 중단하고 W3을 이어가도 무방하나, 보수적인 전체 파이프라인 중단 결정이라고 간주하면 문제되지 않습니다.