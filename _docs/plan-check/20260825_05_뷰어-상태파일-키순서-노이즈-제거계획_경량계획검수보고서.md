---
title: "뷰어-상태파일-키순서-노이즈-제거계획"
created: "2026-08-25 10:05"
session_id: "2c31ce5e-cb60-4972-8155-4b6f34c09e2c"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/2c31ce5e-cb60-4972-8155-4b6f34c09e2c.jsonl"
ai: "claude"
model: "independent-lite-reviewer"
performed_at: "2026-08-25 10:05:26"
---

# Plan Check Lite Reviewer Report

## Verdict
실행 가능

계획서가 인용한 핵심 코드 사실(S2-A/S2-B 대상 행 번호, `write_json` 호출부 2곳, S3 테스트 파일 존재, S4 API 엔드포인트)은 실제 코드와 전부 일치했다. 완료 기준은 전부 명령 종료코드·로그로 판정 가능하고, `write_json` 스키마 파괴 위험은 파라미터화(기본값 `False`)와 S2-C grep 게이트로 실행 시점에 자동 차단된다. 서술 부분(2.3절)의 코드 인용 2곳에 행 번호 오차가 있으나 실행 단계(S2)에는 영향이 없어 Blocking으로 분류하지 않았다.

## Blocking Issues
없음

## Required Fixes
1. **2.3절 `script.js` 행 번호 오류**: 계획서는 "자동태그 적용(`script.js:1414` `postTags[url] = [...merged]`)"이라 썼으나, 실제 `postTags[url] = [...merged]`는 1463행에 있다. 1414행은 무관한 코드다. 실행 단계에는 영향 없지만 근거 서술의 정확성을 위해 수정 권장.
2. **S3의 `test_tag_catalog_api.py:59` 인용 부정확**: `stored == payload` 형태의 순서 비의존 비교는 33-34행(`test_save_and_get_tag_catalog_roundtrip`)에 있다. 59행은 다른 테스트(`test_save_tag_catalog_accepts_empty_object`)의 `== {}` 비교다. 결론(순서 비의존)은 두 테스트 모두에서 성립하므로 판정에는 영향 없으나 인용 행 번호를 33-34로 정정 권장.
3. **S3 테스트 범위에 `tests/unit/test_migrate_threads_domain.py` 누락**: 이 계획은 `migrate_threads_domain.py`의 `write_json`을 수정하는데, S3은 `tests/integration/*`와 `tests/e2e/test_api_security.py`만 돌린다. 해당 unit 테스트는 `write_json`을 직접 테스트하지 않아 회귀 위험은 낮지만, `AGENTS.md`의 "변경 범위에 맞는 현재 테스트로 수행" 원칙에 맞춰 `pytest tests/unit/test_migrate_threads_domain.py -q`를 S3에 추가하는 편이 안전하다.

## 코드 대조 결과

| 계획서 서술 | 실제 확인 | 일치 |
|---|---|---|
| `scrap_sns_server.py:833` `save_tags`의 `json.dump` | 833행 `json.dump(data, f, indent=4, ensure_ascii=False)` (821행 `def save_tags`) | 일치 |
| `scrap_sns_server.py:866` `save_tag_catalog`의 `json.dump` | 866행 동일 패턴 (855행 `def save_tag_catalog`) | 일치 |
| `scrap_sns_server.py:878` `_atomic_write_json`의 `json.dump` | 878행 동일 패턴 (873행 정의), 호출부는 907행(`save_user_metadata`) 1곳뿐 | 일치 |
| `migrate_threads_domain.py:101-102` `write_json` 정의부 | 101행 `def write_json(path: Path, payload: Any) -> None:`, 102행 `path.write_text(json.dumps(...))` | 일치 |
| `write_json` 호출부 2곳 — `apply_file_rewrites`(212행), `apply_tag_rewrite`(226행) | 212행·226행에 각각 위치, 다른 호출부 없음 | 일치 |
| S3 테스트 파일 4개 존재 | 모두 존재 | 일치 |
| S4 API 엔드포인트 — `/api/get-tags`(808행), `/api/save-tags`(821행) | 확인 | 일치 |
| S5 참조 엔드포인트 — `/api/save-tag-catalog`(854행), `/api/save-user-metadata`(898행) | 확인 | 일치 |
| `web_viewer/script.js:1915-1921` 인용 코드 블록 | 인용과 동일 | 일치 |
| `script.js:1414` `postTags[url] = [...merged]` | 실제 위치는 1463행 | **불일치** (서술부, 실행 영향 없음) |
| `test_tag_catalog_api.py:59` 순서 비의존 비교 근거 | 실제는 33-34행. 59행은 다른 테스트의 `== {}` | **부분 불일치** (결론은 유효) |
| `AGENTS.md:82`(서버 재시작)·`:83`(화면 캡처) 인용 | 실제 내용과 일치, S4-B 예외 논리와 부합 | 일치 |

---

## 계획 작성자(claude) 재대조

reviewer가 지적한 행 번호 3건을 직접 확인했다.

| 지적 | 확인 결과 |
|---|---|
| RF1 — `script.js:1414` | `grep -n "postTags\[url\] = \[\.\.\.merged\]"` → **1463행**. 1414행은 `}` 한 줄. reviewer 지적이 맞다 |
| RF2 — `test_tag_catalog_api.py:59` | 33-34행 `stored = json.loads(...)` / `assert stored == payload`, 59행 `assert json.loads(...) == {}`. reviewer 지적이 맞다 |
| RF3 — `tests/unit/test_migrate_threads_domain.py` | 파일 존재 확인. S3 범위에 추가하는 것이 `AGENTS.md` 검증 원칙에 부합 |

세 건 모두 확증됐으므로 계획서에 반영한다.
