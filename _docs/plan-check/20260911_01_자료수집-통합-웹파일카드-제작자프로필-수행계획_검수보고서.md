---
title: "자료수집-통합-웹파일카드-제작자프로필-수행계획"
created: "2026-09-11 09:41"
session_id: "c20181fd-7bd2-4a3f-b641-85a90051a435"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/c20181fd-7bd2-4a3f-b641-85a90051a435.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-11 09:41:47"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- `is_saved` 속성 하드코딩으로 인한 요구사항(R6) 충돌 / 근거: W2 T2-a 필드 변환 / 실행 전 막히는 이유: 모든 자료 노트를 `is_saved = True`로 고정하면 "저장" 필터 노출은 보장되나, SPEC R6("자료 카드에도 똑같이 별표가 동작한다")와 정면으로 충돌한다. 사용자가 별표를 해제하더라도 서버가 항상 `True`를 반환하게 되어 상태 변경이 무시되고 사용자 메타데이터(`sns_user_metadata.json`)와 동기화가 깨진다. 실제 데이터와 연동되도록 필드 매핑 로직을 수정해야 한다.

## Required Fixes
- **T2-a Obsidian URI 경로 플레이스홀더 구체화**: `url` 필드 생성 시 명시된 `obsidian://open?path=…`에서 `…` 자리에 볼트 이름과 파일 경로를 어떻게 URL 인코딩하여 조합할지 구체적인 포맷을 확정하라.
- **T2-g http/https 렌더러 허용 정책의 범위 명확화**: 렌더러가 `http(s)`만 허용할 경우 볼트 노트 본문 안에 포함된 `[[위키링크]]`나 `obsidian://` 등 로컬 프로토콜 링크가 차단되어 텍스트로 깨질 위험이 있다. 이 보안 규칙이 내부 링크까지 차단하는 것인지 예외를 두는지 명시하라.
- **W5 T5-b 헤드리스 브라우저 구동 도구 명시**: 링크 모음 페이지 렌더링에 사용할 구체적인 실행 도구(예: Playwright, Scrapling 등)나 스크립트 환경을 명시하라.
- **W5 게이트 수치 기록 위치 정정**: T5-c에서 파일럿 결과를 "§8에 적는다"라고 했으나 실제 §8은 '범위 밖'이므로 '§9 실행 결과'로 정정하라.
- **W0 baseline.log 산출물 처리 닫기**: `_docs/evidence/20260911_baseline.log` 파일의 git 커밋 포함/제외 여부가 명시되지 않았으므로 이를 확정하라.

## 참고 제안 (판정에 미반영)
- W2 T2-a의 자료 노트 선별 규칙 중 "최상위 폴더의 md"라는 표현이 하위 폴더를 모두 순회(recursive)하는 것인지 모호할 수 있으므로, 구현 단계에서 파일 탐색 시 사용할 glob 패턴(예: `**/*.md`) 등을 명확히 기재해 두면 누락을 방지할 수 있습니다.