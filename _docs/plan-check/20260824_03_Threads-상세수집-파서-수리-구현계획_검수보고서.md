---
title: "Threads-상세수집-파서-수리-구현계획"
created: "2026-08-24 14:38"
session_id: "b8c63cc4-3441-4a0f-af9b-a2d3d3c23134"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/b8c63cc4-3441-4a0f-af9b-a2d3d3c23134.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 14:38:52"
---

검수 대상: `_docs/20260824_03_Threads-상세수집-파서-수리-구현계획.md`
검수 회차: 2회 (1차 지적 반영 후 2차 재검수)
reviewer: agy / Gemini 3.1 Pro (High)

---

# 1차 검수

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- **수동 UI 검증 의존 (`done-check` 자동 검증 불가)** / 문서 4-4절 / 뷰어 확인(9~12번 항목) 과정이 `wscript sns_hub.vbs`를 실행해 사용자가 뷰어에서 육안으로 본문과 저자를 확인하고 캡처하는 것에 전적으로 의존하고 있습니다. 규정에 따라 UI·브라우저 검증은 `headless` 실행 여부와 기계적 판정이 가능한 로그/종료코드 등 증거가 남아 `done-check`가 사람 눈 없이 판정할 수 있어야 합니다. Playwright 등을 통한 자동 돔(DOM) 스냅샷 검증 스크립트를 추가하거나, 수동 개입과 창 활성화가 불가피한 예외라면 그 사유를 계획에 명시해야 합니다.
- **영구 캐시 데이터 롤백 및 백업 대책 누락** / 문서 3-3절(S6 단계) / 실패 이력인 `scrap_failures_threads.json`은 `.bak` 백업 절차(3-2 (3))가 마련되어 있으나, 정작 파싱과 병합 결과가 저장되어 기존 데이터가 덮어써지는 `output_threads/` 내 원본 JSON 파일들의 백업 절차가 없습니다. `thread_scrap_single.py`는 기존 데이터를 메모리에 올렸다가 같은 파일에 `json.dump`로 덮어쓰므로(in-place update), 실행 중 실패하거나 데이터가 오염될 경우 복구가 불가능한 영구 데이터 손실 위험이 있습니다. 실행 단계(S6 이전)에 해당 디렉토리에 대한 백업 방안을 추가해야 합니다.

## Required Fixes
- **축약본 생성 스크립트 커밋 여부 확정**: 문서 7장(작업량)에 "골든 축약본 생성 스크립트 | 30~40줄 (일회성, 커밋 여부 판단)"이라고 서술되어 있습니다. "판단"과 같은 실행 불가 placeholder가 남아 있으므로, 이 일회성 스크립트를 작업 후 `scripts/` 하위에 커밋할 것인지 아니면 임시 스크래치로 사용 후 삭제할 것인지 계획에서 기본값을 확정하여 서술해야 합니다.

## 참고 제안 (판정에 미반영)
- **4-2절 S2 로컬 스크립트 지칭 명확화**: 4-2절 자동화 검증과 3-3절 S2 단계에 "로컬 스크립트로 스냅샷 42건 전수 재현"이라고 언급되어 있으나 어떤 스크립트(`scripts/recover_threads_detail_failures.py` 인지 별도 테스트용 스크래치 스크립트인지)를 지칭하는지 불명확합니다. 헷갈리지 않게 실행할 스크립트의 경로를 명시해두면 좋습니다.

---

# 2차 검수 (1차 지적 반영 후)

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- `명령어 내 실행 불가 placeholder` / 4-2절, 4-5절(1) / `python "<세션 스크래치패드>/verify_threads_snapshots.py"` 및 `--shot "<세션 스크래치패드>/threads_parser_total_count.png"`와 같이 쉘 실행 코드 블록에 리터럴 `<세션 스크래치패드>`가 남아 있다. AI 에이전트가 이 명령을 그대로 실행하면 시스템 경로를 찾지 못해 반드시 실패한다. `scratch/` 등 실제 존재하는 구체적인 디렉토리 경로로 명시해야 한다.

## Required Fixes
- `서술 내 placeholder 제거` / 3-2 (3), 3-2 (4) / `scrap_failures_threads.json.<타임스탬프>.bak`의 `<타임스탬프>`를 쉘에서 실행할 수 있는 변수 구문(예: `$(date +%Y%m%d)` 또는 PowerShell `$(Get-Date -f yyyyMMdd)`)이나 구체적인 포맷(예: `YYYYMMDDHHMMSS`)으로 대체하고, 서술에 쓰인 `<세션 스크래치패드>` 역시 구체적인 경로(예: `scratch/`)로 확정하여 기재하라.
- `S9 정리 대상 누락 보완` / 3-3절 실행 순서 / 7-1절 산출물 처리에는 `scrap_failures_threads.json.*.bak` 파일도 S9에서 `safe-trash`로 정리한다고 명시되어 있으나, 3-3절의 S9 작업 항목(작업란)에는 `.prefix.bak` 정리만 적혀 있다. S9 단계에서 실패 이력 백업본 삭제도 함께 처리되도록 문구를 일치시켜라.

## 참고 제안 (판정에 미반영)
- 3-2 (3) 단계에서 `git checkout -- scrap_failures_threads.json` 실행 시 현재 작업트리의 미커밋된 최신 실패 이력이 버려지게 되는데, 어차피 직후 단계에서 본문을 `{}`로 초기화하므로 최종 결과에는 영향이 없다. 단, 불필요한 Git 명령을 거치는 셈이므로 `checkout` 없이 바로 현재 파일을 백업하고 `{}`로 덮어쓰는 편이 더 직관적일 수 있다.
- 1차 검수에서 지적된 뷰어 자동화/headless 분리(4-4, 4-5절) 및 백업/롤백 절차(3-2(5))는 프로젝트 규칙(`AGENTS.md`)에 부합하도록 매우 완성도 높게 잘 반영되었다. 파일:라인 번호 인용과 분석 내용 또한 실제 코드 베이스와 정확히 일치한다.

---

# 코드베이스 검증 결과 (Claude, 반영 여부)

## 1차 지적

| 지적 | 판정 | 근거 | 반영 |
|---|---|---|---|
| Blocking — 수동 UI 검증 의존 | 확증 | `scripts/verify_viewer_total_count_headless.mjs:1-10` 이 headless·exit 0/1·`--shot` 지원으로 이미 존재하는데 계획이 쓰지 않았다. 단 `AGENTS.md:84` 가 "실제 뷰어 화면에서 대상 게시글이 확인되는 것"을 완료 기준에 명시하므로 화면 확인 자체는 제거 불가 — 사유 명시가 필요한 예외 | 2-9절 신설, 4-4·4-5절로 분리 |
| Blocking — `output_threads/` 백업 누락 | 확증 | `thread_scrap_single.py:259-260` 이 `open(latest_full_path, "w")` 로 full DB를 제자리 덮어씀. `:162-163` 도 simple DB 동일. `scripts/merge_total_only.py` 에 백업 코드 없음(`bak|backup|shutil` grep 0건) | 2-8절 신설, 3-2 (5) 백업·롤백, S5.5·S9 추가 |
| Required — 축약 스크립트 커밋 여부 | 확증 | 7장 "커밋 여부 판단"은 실행 불가 placeholder | 커밋하지 않음으로 확정 |

추가 발견(검수가 지적하지 않은 계획 누락): `AGENTS.md:84` 가 요구하는 `merge_results()` → `download_images()` → `validate_local_image_links()` 3종을 계획이 명시하지 않았다. 실제로는 `scripts/merge_total_only.py:49`·`:59` 와 `save_total()` 내부 `total_scrap.py:578` 이 셋을 모두 호출한다. 4-4절에 호출 경로 표로 명시했다.

## 2차 지적

| 지적 | 판정 | 근거 | 반영 |
|---|---|---|---|
| Blocking — 명령어 내 `<세션 스크래치패드>` | 확증 | 실행할 세션은 다른 스크래치패드를 가지므로 그대로 실행하면 실패한다. 다만 reviewer 제안 `scratch/` 대신 `docs/temp/` 를 채택 — `.gitignore:12` 로 이미 무시 대상이고 실제 존재한다(`git check-ignore docs/temp/BL-0824-01/x.py` → ignored) | `docs/temp/BL-0824-01/` 로 전면 치환 |
| Required — `<타임스탬프>` | 확증 | 추가 발견: `.gitignore:131` 이 `output_threads/python/*.bak_*` 를 이미 무시한다. 레포 기존 규약을 따라 `.prefix.bak` → `.bak_YYYYMMDDHHMMSS` 로 변경 | 실행 가능한 python 1줄 명령으로 대체 |
| Required — S9 문구 불일치 | 확증 | 3-3 S9는 `.prefix.bak` 만, 7-1은 둘 다 기재 | S9를 `output_threads/python/*.bak_*` + `docs/temp/BL-0824-01/` 로 통일 |
| 참고 — `git checkout` 불필요 | 부분 반박 | 최종값이 같다는 지적은 맞으나, 파서 수리를 중도 포기할 때 작업트리에 오염된 80건이 남는 것과 알려진 기준선 36건이 남는 것은 다르다. 유지하되 사유를 계획에 1줄 명시 | 사유 명시 후 유지 |

검수가 지적하지 않은 위험 2건을 추가로 확인해 반영했다.

1. `scrap_failures_threads.json.bak_*` 는 레포 루트라 gitignore에 걸리지 않는다(`git check-ignore` → TRACKED). 실수 커밋 위험이 있어 백업 위치를 `docs/temp/BL-0824-01/` 로 옮겼다.
2. **병행 세션이 `output_threads/` 를 이미 수정 중이다.** `git diff .gitignore` 미커밋 추가분에 `# Threads display_name 백필 산출물`, 실파일로 `threads_py_full_20260824.json.bak_20260824_134449`(13:44) 존재, `display_name == username` 수치가 702 → 700으로 변동. 계획 6-3의 파일 분담이 이미 깨져 있어 6-4절에 실측 경고와 S0-a 사전 확인 단계를 신설했다. 또한 계획이 근거로 삼은 `.gitignore:131` 이 미커밋 줄이므로 S5.5 직후 `git check-ignore` 재확인을 넣었다.
