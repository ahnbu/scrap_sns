---
title: "내-글-정렬-두-플랫폼-병합-계획"
created: "2026-08-27 19:26"
session_id: "9b1bfbc7-be92-4850-8340-6d48fc059bbd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/9b1bfbc7-be92-4850-8340-6d48fc059bbd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 19:26:23"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
없음

## Required Fixes
1. **명시적 웨이브 블록 누락**: 계획 문서 내에서 'W3', 'W6' 등 특정 웨이브를 언급(예: 도입부의 "W6 에서...", V26의 "W3 착수 직전")하고 있으나, 정작 전체 실행 순서를 정의한 `## 실행 웨이브` 섹션이 없습니다. 자연어로만 선후관계를 적어두면 실행 에이전트(exec-plan)가 파일 충돌 기준으로 잘못 재분류하여 의도한 선후관계가 깨질 위험이 있으므로, 각 작업(T1~T6)을 할당한 명확한 `## 실행 웨이브` 또는 `## 웨이브 구조` 블록을 추가해야 합니다.
2. **`parse_dt()` 타임존 절단 버그 방치 위험**: 3.6절 T6-0에서 `parse_dt()`의 `strptime` 절단 버그를 '관찰 항목'으로 미루겠다고 선언했습니다. 그러나 해당 버그(`text[:len(fmt)+6]` 절단)는 문자열이 6자리 마이크로초를 가졌을 경우 정확히 타임존 부분(e.g., `+00:00`)만 잘라내어 naive로 성공적으로 파싱하게 만듭니다. 이로 인해 UTC 등 타임존을 포함하는 다른 수집원(예: Threads)의 시각이 유입되면, T6-0의 `if parsed.tzinfo is not None:` 검사를 우회하게 되어 `days_since()`가 9시간 오작동하는 논리적 구멍이 발생합니다. 관찰 항목으로 미루지 말고 이번 작업 범위 내에서 `parse_dt()`가 타임존을 유실하지 않도록(`fromisoformat()`을 먼저 시도하거나 절단 로직을 수정) 보완해야 합니다.
3. **`assign_own_post_order()` 내 임시 필드 삭제(pop) 금지 명시**: T1·T2 로직을 `assign_own_post_order()` 함수로 분리할 때, 함수 내부에서 임시 필드 `_own_batch_key`를 섣불리 `pop()`하지 않도록 명시해야 합니다. 이 필드는 분리된 함수가 끝난 후 `total_scrap.py`의 `save_total()` 내부 `sort_key`(`_saved_at_key`)에서 1차 키로 참조되므로, 여기서 먼저 지워버리면 후속 정렬이 실패합니다. (기존처럼 `save_total()`이 정렬 후 직접 삭제하도록 두어야 합니다.)

## 참고 제안 (판정에 미반영)
- **T5-b 의 `cmd /c A && B` 체이닝 호환성**: `my_posts_scrap.py`가 인증 오류(`EXIT_CODE 3`)로 실패하면 `cmd /c` 구조상 후속 명령어는 실행되지 않으며 최종 종료코드가 `3`으로 전달되므로, `total_scrap.py`의 Auth 시그널 처리와 정상 호환됩니다. 
- 단, `total_scrap.py`에서 타임아웃 등으로 `Popen` 프로세스를 강제 종료할 경우, Windows 환경에서는 부모인 `cmd.exe`만 종료되고 실제 실행 중인 자식 프로세스가 고아(orphan) 프로세스로 백그라운드에 남을 수 있는 구조적 취약점이 기존 코드베이스에 존재합니다. 이 계획의 범위를 넘어서므로 판정에는 반영하지 않으나, 장기적인 관찰과 개선이 필요합니다.