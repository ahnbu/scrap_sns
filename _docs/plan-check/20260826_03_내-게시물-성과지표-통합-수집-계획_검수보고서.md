---
title: "내-게시물-성과지표-통합-수집-계획"
created: "2026-08-26 12:11"
session_id: "bd25318e-1540-4c9c-8c65-2b611cf6d512"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/bd25318e-1540-4c9c-8c65-2b611cf6d512.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-26 12:11:43"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

## Blocking Issues
- **W5 의존성 깨짐 대비 대안 누락** / `_docs/20260826_03_내-게시물-성과지표-통합-수집-계획.md` 3.7절 및 4절 / 병행 계획의 W5(`utils/metric_refresh.py` 생성) 완료 전까지 착수 불가로 묶어두었으나, W5가 실패하거나 지연될 경우의 대안(예: 해당 로직을 `linkedin_metrics.py`에 임시 구현 후 통합 등)이 전혀 없어 실행이 완전히 막힘.
- **새 필드 `is_own_post` 파급 식별 누락** / `utils/query-sns.mjs` 306라인 `postToResult` 및 `tests/unit/test_post_schema.py` / 스키마 필드를 추가(T2)하더라도 CLI 툴인 `query-sns.mjs`가 이를 명시적으로 반환 객체에 매핑하지 않으면 조회 시 새 필드가 유실됨. 또한 `test_post_schema.py`가 새 필드의 기본값을 처리하도록 갱신되지 않으면 테스트가 실패할 수 있음.
- **수동 GUI 검증(V7) 의존** / `_docs/20260826_03_내-게시물-성과지표-통합-수집-계획.md` 5절 V7 / "화면 캡처를 증거로 남긴다"며 사람 눈 확인에 의존함. "종료코드·로그로 증거가 남아 done-check가 사람 눈 없이 판정할 수 있는가. '브라우저에서 확인한다' 수준이면 실행 불가"라는 지침을 명백히 위반함. Playwright 기반 자동화 e2e 테스트 신설로 검증을 대체해야 함.

## Required Fixes
- **Headless 실행 여부 명시 누락:** T1의 `my_posts_scrap.py`가 호출할 `sns_insight_update` 내 `collect_linkedin_posts`의 인자 `headed` 기본값이 `False`(headless)이긴 하나, 계획 문서 자체에 "headless 실행"임이 명시되지 않음. 문서에 명시적으로 적어야 함.
- **"전체 소요 증가 ≈ 0초" 추론 정정:** 3.2절에서 Threads 실측 95초에 숨어 소요 증가가 0초라 단정했으나, Threads 신규 글이 없는 날에는 병목이 MyPosts(39.2초)로 옮겨가 전체 웨이브 소요 시간이 순증가함. 이 예외(반례)를 인정하고 설계 논거를 정정해야 함.
- **웨이브 구조 명시:** 복잡한 선후관계가 있음에도 자연어로 된 `9. 착수 순서`만 있고 `## 실행 웨이브` 또는 `## 웨이브 구조` 블록이 없음. exec-plan이 선후관계를 오인할 수 있으므로 명시적 웨이브 블록을 추가해야 함.
- **증거 산출물의 커밋 처리 여부:** V7 등에서 남기는 캡처 증거물에 대해 커밋 포함, 명시 제외, local-only 중 어떤 처리 의도인지 문서에 닫아두어야 함.
- **통합본 덮어쓰기 복구 방안 (Rollback):** 2,112건 전체 재생성 중 중단/실패 시 `output_total`의 이전 상태 복구(예: git checkout 등) 방안과 부분 실패 시 데이터 재처리 방법을 명시해야 함.

## 참고 제안 (판정에 미반영)
- **접근법 트레이드오프 명시:** 노출수를 얻기 위해 비로그인 스크래핑 대신 별도의 39.2초 실행 비용을 치르는 점이 숨겨져 있습니다. "39.2초의 고정 실행 시간을 추가하는 대신 100%의 노출수를 확보한다"는 트레이드오프를 계획에 명시적으로 추가하는 것이 좋습니다.
- **T5 뷰어 MY 필터 구현 구체화:** `web_viewer/script.js`에서 MY 필터 동작을 위해 `is_own_post`를 판별하는 로직이 T5에 구체적으로 명시되지 않았습니다. DOM 필터링 로직에 대한 접근법을 덧붙이면 파일 충돌 위험을 줄일 수 있습니다.
