---
title: "X-북마크-커서-페이지네이션-전환-계획"
created: "2026-08-26 10:48"
session_id: "dfdff5f4-bdf2-48aa-b72f-ca022d5f92ca"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/dfdff5f4-bdf2-48aa-b72f-ca022d5f92ca.jsonl"
ai: "claude"
model: "independent-lite-reviewer"
performed_at: "2026-08-26 10:48:01"
---

# Plan Check Lite Reviewer Report

## Verdict
실행 가능

## Blocking Issues

없음. 라운드 1 Blocking 2건을 코드로 재확인한 결과 모두 실제로 닫혔다.

- **결함 1(simple→full 전파 누락)**: `total_scrap.py:499`에서 `merge_results()`가 `twitter_py_full_*.json`을 읽는 것을 확인했다. 계획 3.2 S3의 4단계 "`python twitter_scrap_single.py` 실행"이 추가됐고, `twitter_scrap_single.py`의 `for post in posts: if post.get("is_detail_collected"): ... full_map[pid].update(post)` 블록이 `if targets:` 블록 **밖**에 있어 신규 상세수집 대상이 0건이어도 무조건 실행되는 구조임을 코드로 확인했다. 즉 3단계에서 simple에 새 지표를 써넣은 뒤 4단계를 돌리면, 네트워크 재수집 없이도 simple의 지표가 full로 정상 전파된다. 전파 메커니즘이 문서상 주장이 아니라 실제 코드 구조로 성립한다.
- **결함 2(백업·롤백 경로 부재)**: 3.2 S3-3 "simple과 full을 둘 다 백업한 뒤 simple 갱신"과 3.4 롤백 절이 추가됐다. `twitter_scrap_single.py`가 함수 끝에서 `full_file`과 `latest_simple` 둘 다 덮어쓰는 것을 확인했으므로, "4단계 실패 시 full 백업본만 복원", "5단계 실패 시 두 백업본 복원 후 재병합"이라는 롤백 절의 단계 구분이 코드 동작과 어긋나지 않는다. 백업 시점이 full 수정 시점(4단계)보다 앞서 배치돼 있어 순서도 맞다.

## Required Fixes

1. **줄번호 드리프트**: 계획서가 인용한 `twitter_scrap_single.py:159`(`full_map[pid].update(post)`)는 실제로는 163행이다. 나머지 인용 줄번호(`twitter_scrap.py:362/373/449`, `verify_x_auth.py:106/108`, `total_scrap.py:499`, `twitter_scrap_single.py:85`, `utils/post_meta.py:71`)는 전부 실제 코드와 정확히 일치함을 대조 확인했다.

2. **V7 테스트가 S2(커서 페이지네이션) 본체를 커버하지 않음**: `tests/`에 `extract_from_json`이나 페이지네이션 루프·종료 가드를 직접 검증하는 단위 테스트가 없다. V7이 지정한 3개는 S1(인증 판정) 회귀 방지 대상이다. V1~V6이 실행 결과로 이를 대신 검증하는 설계이므로 승인을 막을 정도는 아니지만, "S2 로직은 자동 테스트 없이 V1~V6 실측만으로 검증한다"는 점을 계획서에 명시해 의도된 공백임을 밝혀 두는 편이 낫다.

3. **백업 메커니즘의 구체적 방식 부재**: S3 3단계는 "백업한 뒤"라고만 쓰여 있어 저장 위치·명명 규칙이 없다. 레포에 이미 `migrate_threads_domain.py:156` `create_backup()`(동일 디렉터리, 타임스탬프 `.bak` 접미사) 패턴이 있으므로 이를 재사용하거나 동등한 규칙을 한 줄 추가할 것.

4. **5절(후속) 테스트 건수 오기재**: 계획서는 `tests/e2e/test_metric_layout_ui.py 관련 2건 제거`라고 적었으나, 실제로 `#metricsOnlyBtn`을 참조하는 것은 `test_v9_metrics_only_filter_shows_only_carded_posts` 단일 함수(323·334행에서 두 번 클릭)뿐이다. 후속 착수 전 "1건"으로 정정해 혼선을 막아야 한다.

## 추가 확인 사항 (참고, Required Fix 아님)

- 라운드 1 Required Fixes 3건 모두 반영 확인: `twitter_scrap.py:449` 줄번호 정정(실제 `if consecutive_no_new >= 5:`와 일치), `expect_response`가 `goto()` 자체를 감싸야 한다는 설명과 예시 코드 명시, V7 테스트 파일 3개 모두 레포에 실존.
- `utils/post_meta.py:71` `build_post_key()`가 `{platform}:{identifier}` 형식이며 username을 키로 쓰지 않는다는 계획서 주장, `index.html:147`·`script.js:182`·`script.js:2089` 인용 모두 코드와 일치.
- AGENTS.md의 SNS 수집·병합 완료 기준(`merge_results()` → `download_images()` → `validate_local_image_links()` 통과, 뷰어 총건수 일치, 실제 화면 확인)을 S3 5~6단계와 V6이 충족한다.

## 반영 결과 (검수 후 조치)

Required Fixes 4건 모두 계획서에 반영 완료: 줄번호 163 정정, S2 자동 테스트 공백 명시, 백업 규칙(`create_backup()` 패턴) 추가, 후속 테스트 1건으로 정정.
