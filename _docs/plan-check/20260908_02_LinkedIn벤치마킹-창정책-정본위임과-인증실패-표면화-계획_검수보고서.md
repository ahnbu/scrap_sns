---
title: "LinkedIn벤치마킹-창정책-정본위임과-인증실패-표면화-계획"
created: "2026-09-08 22:20"
session_id: "3cce6ea2-c366-4085-ab8f-ad50f861771d"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/3cce6ea2-c366-4085-ab8f-ad50f861771d.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-08 22:20:22"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
1. W3 `scrap_sns_server.py` 매핑 시 `platform_results` 병합 발생 / `scrap_sns_server.py:640` / 계획(W3)은 `_canonical_auth_platform`에 `bench_linkedin -> linkedin` 매핑을 추가하되 `platform_results`의 상태 표시는 지금대로 둔다고 명시했다. 그러나 640번 줄의 `platform_results` 정규화 루프 역시 동일한 `_canonical_auth_platform` 함수를 호출하므로, 계획대로 해당 함수에 매핑을 추가하면 `bench_linkedin`의 결과가 `linkedin` 키로 덮어써지며 결국 벤치마킹과 저장글의 실패 원인이 뭉뚱그려지는 버그(계획서 3.W3에서 피하고자 한 바로 그 결과)가 필연적으로 발생한다.

## Required Fixes
1. W3 매핑 추가 위치를 수정하라. `_canonical_auth_platform` 함수 자체를 수정하지 말고, `_normalize_scrap_summary` 함수 내의 `auth_required` 판정 수집 루프(678번 줄 이하) 혹은 최종 반환 배열 생성부(690번 줄 부근)에서만 독자적으로 `bench_` 접두사를 처리하도록 매핑 적용 범위를 제한해야 한다.

## 참고 제안 (판정에 미반영)
1. **접근법의 트레이드오프**: 이 계획은 브라우저 구동 정책을 `_shared` 정본으로 위임하여 중복 창 생성을 막고 인증 실패 투명성을 **얻는 대신**, 단독 스크립트였던 `linkedin_scrap_by_user.py`가 저장소 외부(`~/.claude/skills/_shared`)로 강한 런타임 의존성을 가지게 되어 환경 이식성을 **잃는다**. 다만 계획서 내 리스크(R4)에서 이를 인지하고 수용했으므로 판단은 타당하다.
2. W2 구현 시 `linkedin_scrap_by_user.py`에 `utils.auth_paths`, `utils.auth_status`, `utils.benchmark_store` 모듈의 함수 import가 누락 없이 들어가야 한다.
3. W2-3의 세션 저장 로직은 기존 Playwright의 `page.context.storage_state(path=...)`에서 `path` 인자를 제거해 dict 객체를 반환받은 뒤, 그 결과를 `atomic_save_json()`에 넘겨 저장하는 방식으로 구현됨을 명시해 두면 작업 중 혼선을 줄일 수 있다.