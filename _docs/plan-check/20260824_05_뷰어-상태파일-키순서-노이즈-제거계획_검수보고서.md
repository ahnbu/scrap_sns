---
title: "뷰어-상태파일-키순서-노이즈-제거계획"
created: "2026-08-24 20:04"
session_id: "bfefcedd-ef06-458b-8320-a1376cd4b23c"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/bfefcedd-ef06-458b-8320-a1376cd4b23c.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 20:04:00"
saved_at: "2026-08-25 (지연 저장 — 검수 직후 세션 중단으로 미저장분을 세션 2c31ce5e에서 복원 저장)"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- **`migrate_threads_domain.py:102` 수정 지시 모순 및 스키마 파괴 위험**
  - **근거**: `[migrate\_threads\_domain](file:///D:/vibe-coding/scrap_sns/migrate_threads_domain.py#L202-L214)`의 `apply_file_rewrites` 함수, `[20260824\_05\_뷰어-상태파일-키순서-노이즈-제거계획](file:///D:/vibe-coding/scrap_sns/_docs/20260824_05_뷰어-상태파일-키순서-노이즈-제거계획.md#L154)`
  - **실행 전 막히는 이유**: S2 단계에서 `write_json` 함수 내 `json.dumps` 호출부에 무조건 `sort_keys=True`를 추가하도록 지시했으나, 이 함수는 수집 산출물(`output_total/` 등) 저장에도 쓰인다. 계획 6번 항목에서 "분기하거나 별도 함수로 처리"라고 에이전트의 실행 시 판단으로 결정을 미뤄두었다. 이대로 실행하면 산출물의 필수 필드 순서(`STANDARD_FIELD_ORDER`)가 파괴되거나, 실행 에이전트가 코드를 스스로 확정하지 못해 작업이 중단된다.

- **GUI 검증의 Headless 및 완료 증거(evidence) 기록 부재**
  - **근거**: `[20260824\_05\_뷰어-상태파일-키순서-노이즈-제거계획](file:///D:/vibe-coding/scrap_sns/_docs/20260824_05_뷰어-상태파일-키순서-노이즈-제거계획.md#L171-L179)` (S4 단계)
  - **실행 전 막히는 이유**: "브라우저에서 확인한다" 수준의 수동 검증 지시(`http://localhost:5000/`에서 태그 1개 추가 등)가 명시되어 있다. 사용자 포커스를 뺏지 않는 Headless 브라우저 실행이나 API 자동화 스크립트 작성이 구체적으로 지시되지 않았고, 성공 여부를 증명할 종료코드·로그 산출 방법이 누락되어 추후 완료 검수(`done-check`)가 증거 기반으로 자동 판정할 수 없다.

## Required Fixes
- S2 단계의 `[migrate\_threads\_domain](file:///D:/vibe-coding/scrap_sns/migrate_threads_domain.py)` 수정 계획을 구체적인 코드로 확정하라. (예: `write_json`에 `sort_keys: bool = False` 파라미터를 추가하고, 태그 관련 호출부에서만 `True`를 넘기도록 설계 변경)
- S4 단계의 검증 과정을 로컬 API(`/api/save-tags`) 호출 자동화 스크립트 또는 Headless Playwright 스크립트로 교체하고, 테스트 성공 시 `$SCRATCH` 디렉토리에 결과 로그를 남기도록 명시하라.
- 6번 항목 "위험과 대응"에서 에이전트에게 실행 중 판단을 미루는 서술("S2에서 사용처를 먼저 확인하고...")을 제거하라.

## 참고 제안 (판정에 미반영)
- S5 단계에서 `sns_tag_catalog.json`과 `sns_user_metadata.json`의 정렬본을 생성하기 위해 "태그 카탈로그 편집 1회, 별표 토글 1회 후 원복" 등 뷰어 UI 조작을 지시하고 있다. UI 조작보다, 단순히 기존 파일을 읽어 정렬 옵션(`sort_keys=True`)으로 덮어쓰는 1회성 마이그레이션 스크립트를 작성해 일괄 적용하는 방식이 훨씬 안정적이고 빠르다.
- 클라이언트 `localStorage`와 서버 상태가 다를 때 서버에서 `sort_keys=True`를 적용해 노이즈를 억제하는 것은 타당하다. 이 접근의 트레이드오프는 diff 노이즈를 억제하는 대신 JSON 파일 내 키 배열이 시간순(생성/적용 순서)에서 알파벳순으로 영구 변경된다는 점이다. 코드 소비처(소팅 무관) 분석이 끝났으므로 이 트레이드오프는 수용할 만하다.

---

## 계획 작성자(claude) 코드베이스 재검증

reviewer 지적을 계획 작성자가 코드로 직접 대조한 결과다.

| 지적 | 판정 | 확인 근거 |
|---|---|---|
| **B1 — `write_json` 스키마 파괴** | **Confirmed** | `migrate_threads_domain.py:101-102`는 파라미터 없는 단일 함수. 호출부 2곳의 성격이 다름 — 212행은 `collect_json_files()`가 모은 `output_total/total_full_*.json`·`output_threads/python/**/*.json`(수집 산출물), 226행은 `TAGS_PATH`. 무조건 `sort_keys=True`면 산출물 필드가 알파벳 정렬되어 `utils/post_schema.py:6`의 `STANDARD_FIELD_ORDER` 규약과 충돌 |
| **B2 — headless·증거 부재** | **Confirmed** | 계획서 S4가 `wscript sns_hub.vbs` 실행 후 "`http://localhost:5000/`에서 태그 1개 추가"라는 수동 조작만 기술. headless 언급 없고 종료코드·로그 산출 없음. 태그 저장은 `scrap_sns_server.py:821` `/api/save-tags` POST로 브라우저 없이 재현 가능하므로 자동화가 실제로 가능 |
| **RF3 — 실행 중 판단 미루기** | **Confirmed** | 계획서 6장에 "S2에서 사용처를 먼저 확인하고, 태그 파일 전용이 아니면 호출부에서 분기하거나 해당 파일만 별도 함수로 처리"라고 적혀 있어 실행 시점 판단으로 미뤘음. B1과 동일 사안 |

세 지적 모두 확증됐다. 반박할 항목은 없다.

한 가지 덧붙이면, B2에 대해 `AGENTS.md:83`은 "웹 뷰어 **사용자 경험이 바뀌는** 변경"에 화면 캡처를 요구하는데 이번 변경은 저장 포맷만 바뀌고 UX는 동일하다. 다만 `AGENTS.md:82`의 서버 재시작 규약은 그대로 적용되므로, **재시작은 유지하되 검증은 API 호출로 자동화**하는 방향이 규약과 reviewer 지적을 모두 만족한다.

## 반영 결과

계획서 `_docs/20260824_05_뷰어-상태파일-키순서-노이즈-제거계획.md`에 다음과 같이 반영했다.

| 지적 | 반영 위치 | 처리 |
|---|---|---|
| B1 / RF1 | 3.2 S2 | `write_json(path, payload, *, sort_keys: bool = False)`로 파라미터화하고 `apply_tag_rewrite`의 호출부(226행)만 `sort_keys=True`를 넘기도록 확정 코드 명시 |
| B2 / RF2 | 3.2 S4 | 수동 브라우저 조작을 `/api/save-tags` POST 자동화 스크립트로 교체. 종료코드 판정과 `$SCRATCH/s4_verify.log` 증거 산출 명시 |
| RF3 | 6장 | "S2에서 사용처를 먼저 확인하고…" 서술 삭제, 위험 항목을 "S2 확정 코드로 이미 차단됨"으로 대체 |
| 참고 제안 1 | 3.2 S5 | UI 조작(카탈로그 편집·별표 토글) 지시를 1회성 정렬 마이그레이션 스크립트로 교체 |
| 참고 제안 2 | 2.4 | 키 순서가 시간순→알파벳순으로 영구 변경되는 트레이드오프를 명시적으로 수용 기록 |
