---
title: "수집데이터-본문결측-중복-오염-수정계획"
created: "2026-08-24 11:30"
session_id: "4efc85ee-1841-4417-bd43-ced95062bdc2"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/4efc85ee-1841-4417-bd43-ced95062bdc2.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 11:30:45"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

(판정 기준: Blocking Issues가 다수 존재하여 당장 '수정 필요' 상태이나, 3.3절의 후속 분리 사항과 단계 D 등 사용자 판단 및 승인이 필요한 선택이 포함되어 있으므로 판정 우선순위 규칙(`승인 필요` > `수정 필요`)에 따라 '승인 필요'로 판정함.)

## Blocking Issues
1. **Threads 실패 원인 규명 시 데이터 영구 제외 유발 (단계 A-2/B-0)**
   - 근거: 계획서 내 "주의: 실행 자체가 `fail_count`를 3으로 올릴 수 있다. 실행 전 `scrap_failures_threads.json` 백업 필수" 명시
   - 차단 이유: 백업만 지시할 뿐 "테스트 완료 직후 백업본을 복원한다"거나 "실행 시 fail_count 증가 로직을 임시로 차단한다" 등의 구체적인 복구 조치가 없습니다. 명시된 대로만 실행하면 대상 4건이 향후 수집에서 영구 제외되는 데이터 손실로 직결됩니다.
2. **JSON 리셋/정리 작업의 구체적 실행 수단 누락 (단계 C-2, B-2)**
   - 근거: 단계 C-2("리셋 필드: is_detail_collected → False..."), 단계 B-2("대상: platform_id 완전중복 21건...")
   - 차단 이유: 대량의 JSON 파일 데이터(61건 및 중복 21건 등)를 리셋하고 오염을 정리해야 하지만, "리셋한다"는 서술만 있을 뿐 이를 수행할 1회성 Python 스크립트 작성 계획이나 실행 명령어가 전혀 없습니다. AI 에이전트가 코드를 임의 작성하지 않고는 즉시 실행이 불가능합니다.
3. **토큰 만료 및 부분 실패(Partial Failure) 대응 누락 (단계 C-2)**
   - 근거: 단계 C-2의 61건 라이브 재수집 계획
   - 차단 이유: 인증 토큰이 필요한 라이브 수집 중 세션 만료, API 제한 등으로 61건 중 일부만 성공할 수 있습니다. 성공분만 저장할지, 전체를 롤백할지, 실패 건수만 재처리할지 등 부분 실패에 대한 복구 로직이 없어 데이터 정합성이 깨질 위험이 열려 있습니다.
4. **GUI/사람의 눈에 의존하는 수동 검증 방식 (단계 C-2, B-2)**
   - 근거: 검증 단계의 "실제 http://localhost:5000/ 화면에서 확인 후 캡처 보관"
   - 차단 이유: 브라우저 창을 띄워 확인하는 방식은 사용자 포커스를 빼앗고 병행 작업을 끊습니다. 또한 `done-check`가 종료코드와 로그만으로 판정할 수 없는 방식입니다. headless 실행 여부를 명시하고, JSON 데이터를 쿼리해 검증하는 스크립트 기반의 단언(assertion) 조건이 검증 방법에 추가되어야 합니다.
5. **실행 명령에 실행 불가 Placeholder 잔존 (단계 C-2)**
   - 근거: "사전 확인: `<위 python> -m twitter_cli.cli tweet --help`"
   - 차단 이유: 실제 셸에서 실행해야 하는 명령어에 `<위 python>`이라는 Placeholder가 그대로 남아있습니다. 실행 주체가 이 텍스트를 그대로 실행하려다 에러가 발생하므로, 실행 가능한 구체적 절대 경로 변수나 온전한 명령어로 수정해야 합니다.

## Required Fixes
1. **명시적 웨이브(Wave) 구조 블록 추가**
   - 계획이 선후관계(C-1 후 C-2, B-1 후 B-2 등)를 요구함에도 단순 "단계"로만 나열되어 있습니다. 에이전트가 파일 충돌 기준으로 임의 병렬 실행하여 의도한 선후관계가 깨지는 것을 막으려면 명시적인 `## 실행 웨이브` 블록을 구성해야 합니다.
2. **C-2 선행 테스트 실패 시 대비책 명시**
   - 계획서 4절(미확정 사항)에서 X API의 ID 수용 여부를 CLI 도움말(`--help`)로만 해소했다고 판단하고 있습니다. 그러나 실제 재수집 시 1건의 선행 테스트가 인증이나 파싱 에러로 실패할 수 있으며, 이 경우의 롤백 방안이나 전체 실행 중단 기준이 명시되어야 합니다.

## 참고 제안 (판정에 미반영)
- **3.3 범위 밖 분리 사항 평가 (승인 필요 사유)**: 분리된 3건(Threads display_name 705건 단순 표시 오류, twitter_parser 죽은 코드, twitter_scrap 길이 비교 덮어쓰기)은 당초 사용자가 요구한 '본문 결측 및 중복 진단' 목적과 무관한 별개의 이슈입니다. 이를 본 계획의 범위에서 덜어내 후속 과제로 분리한 것은 임의적인 범위 축소가 아니라 **과최적화와 스코프 크립(Scope Creep)을 방지하기 위한 타당하고 훌륭한 조치**입니다.
- **단계 D 실행 권장**: 중복 키 삭제 건수를 로깅하는 작업이 사용자 선택(선택 사항)으로 남겨져 있으나, 21건의 데이터가 조용히 무시되는(Swallow) 현상을 가시화하는 중요한 작업이므로 가급적 웨이브에 포함해 실행하는 것을 권장합니다.