---
title: "LinkedIn벤치마킹-창정책-정본위임과-인증실패-표면화-계획"
created: "2026-09-08 22:29"
session_id: "3cce6ea2-c366-4085-ab8f-ad50f861771d"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/3cce6ea2-c366-4085-ab8f-ad50f861771d.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-08 22:29:27"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- `linkedin_scrap_benchmark.py`가 인증 실패 종료코드(86)를 삼킴 / `D:/vibe-coding/scrap_sns/linkedin_scrap_benchmark.py`의 48-52번 줄 / 실행 전 막히는 이유: W2-2 계획에 따라 `linkedin_scrap_by_user.py`가 인증 실패로 86(AUTH_REQUIRED) 코드를 반환해도, 부모 프로세스인 `linkedin_scrap_benchmark.py`는 오류 메시지만 찍고 `result.returncode != 0` 분기를 지나쳐 최종적으로 0(성공)을 반환한다. 이로 인해 최상위 호출자인 `total_scrap.py`가 86 코드를 전달받지 못해 `auth_required` 목록에 `bench_linkedin`을 추가하지 않는다. 결국 W3의 서버 수정이 무의미해지며 "인증 실패가 화면에 뜨게 한다"는 목적(P2) 달성에 반드시 실패한다. 수집기가 86으로 종료되면 `linkedin_scrap_benchmark.py`도 86으로 즉각 종료하도록 W2 계획에 추가되어야 한다.

## Required Fixes
- `storage_state.json` 덮어쓰기 전 백업 로직 추가: W2-3에서 `atomic_save_json`을 통해 파일 쓰기의 원자성은 확보했으나, 획득한 세션 정보 자체가 비정상일 경우를 대비한 롤백 수단이 없다. 이 파일은 레포지토리 외부(`~/.config/auth/...`)에 위치해 git 복구가 불가능하므로, 영구 데이터 손실을 막기 위해 덮어쓰기 전에 기존 파일의 백업(예: `.bak`)을 생성하는 로직을 W2-3에 명시해야 한다.
- 실행 불가 placeholder 제거: W2-4 내용 중 `_shared.browser_launch.decide(headless_reason=...)`에 있는 `...`가 실제 코드에 그대로 남지 않도록, 예시로 든 문자열(`"2026-09-08 실측: 로그인 세션 + headless 로 활동목록 13건·866KB 정상"`)을 실제 인자로 확정하여 기입하도록 계획을 수정해야 한다.

## 참고 제안 (판정에 미반영)
- W2-4에서 사용하는 `linkedin_scrap_by_user.py`는 `chromium.launch()` 기반의 임시(ephemeral) 세션을 사용합니다. 하지만 `browser_launch.decide()`는 `engine`을 별도로 지정하지 않으면 기본값인 `persistent`를 가정하여 `LAUNCH: ... engine=persistent` 로그를 출력합니다. 당장 동작을 깨뜨리지는 않으나 로그의 정확성이 떨어지므로, 추후 정본의 `ENGINES` 상수에 `ephemeral`을 추가하는 것을 고려할 만합니다.