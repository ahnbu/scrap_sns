---
title: "유튜브-저장영상-수집-반영-계획"
created: "2026-08-25 10:19"
session_id: "553fe88a-d9f1-4d9a-8577-e63635ba667e"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/553fe88a-d9f1-4d9a-8577-e63635ba667e.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-25 10:19:04"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

**판정 사유:**
본 계획은 최대 477건에 대해 유튜브 자막 다운로드 및 Gemini Flash API를 통한 요약 생성을 수행한다. 이는 명시적인 API 비용을 발생시키며, 오류 발생 시 병렬 처리로도 최소 10분 이상의 시간과 API 요금이 다시 소요되므로 **'코드 수정만으로 되돌릴 수 없는 비용 발생 및 재실행'** 항목에 해당하여 최종 승인이 필요하다. 또한 원활한 구현과 검증을 위해 아래 서술된 Blocking Issues가 선행 해결되어야 한다.

## Blocking Issues

1. **지적:** 4단계 검증 과정이 '수동 브라우저 확인 및 캡처'에만 의존하여 시스템적 판정이 불가하다.
   - **근거:** 5절 4단계 검증 항목 ("`http://localhost:5000/`에서 업데이트 버튼 클릭 ... 실제 화면 확인 및 캡처")
   - **실행 전 막히는 이유:** `done-check`가 사람 눈 없이 로그나 종료 코드만으로 GUI 작동을 확정할 수 없다. `AGENTS.md`가 캡처 증거를 요구하더라도, headless 기반의 E2E 테스트(예: Playwright) 등을 추가하여 UI 클릭 및 렌더링 검증 결과를 로그나 종료 코드로 남기는 절차가 계획에 명시되어야 한다. 그렇지 않으면 자동 판정이 불가능하다.

2. **지적:** 수집 대상 규모와 파일럿 실행 여부의 서술이 상호 충돌한다.
   - **근거:** 2.1절 ("수집 대상 (확정) ... 합계 628") vs 6절 ("파일럿(drive7 81건)으로 실제 소요를 측정한 뒤 전량 착수") 및 7절 ("대상 재생목록 확대 여부 ... 파일럿 성공 후").
   - **실행 전 막히는 이유:** 하드코딩될 `youtube_scrap.py`의 `PLAYLISTS` 변수에 3개 목록(628건) 전체를 넣을지, 1개 목록(81건)만 넣어 파일럿으로 제한할지 기준이 없다. 이를 확정하지 않고 실행하면 에이전트가 임의 판단하여 초과 비용이 발생하거나 의도치 않게 수집 범위가 좁아질 리스크가 있다.

## Required Fixes

1. **요약 결과 캐시의 물리적 위치 명시:** 5절 2단계 6번의 "결과 캐시"가 구체적으로 어디에 어떤 형식으로 저장되는지(예: 별도 JSON, 로컬 변수 등) 명시하라. 파일로 영구 저장된다면 `.gitignore` 처리 방안과 재수집 시의 캐시 활용 규칙도 함께 정의해야 한다.
2. **미결정 사항 확정:** 7절에 미결정 상태로 남은 `Gemini Flash 요약의 형식·길이`, `provider 서버 상시 실행 여부`, `requirements.txt 등재 여부`는 구현 에이전트의 실행을 중단시키거나 자의적 구현을 낳는다. 실행 전 계획 단계에서 단일 지침으로 확정해야 한다.
3. **서술형 Placeholder 대체 지침:** 5절 2단계의 `PLAYLISTS = [{"id": "PLed50f9DS0U_...", "name": "drive7"}, ...]` 서술에 있는 `...` 기호가 실제 코드에 삽입되지 않도록, "확정된 재생목록 전체를 하드코딩할 것"이라는 지침을 추가하라.

## 참고 제안 (판정에 미반영)

1. **API Rate Limit 대비 로직 신설:** 수백 건의 Gemini API 요청이 병렬로 들어갈 경우 `429 Too Many Requests`가 발생하기 쉽다. 스크래퍼 내부에 Retry 및 지수 백오프(Exponential Backoff) 처리 로직을 명시적으로 추가하는 것을 권장한다.
2. **실행 웨이브 명시:** 5절의 개발 단계들이 의존성을 갖는 순차적 과제들이라면, 자동 실행 시 의도치 않은 병렬 충돌을 막을 수 있도록 `## 실행 웨이브` 블록을 통해 작업 선후관계를 명확히 구조화하는 것이 좋다.