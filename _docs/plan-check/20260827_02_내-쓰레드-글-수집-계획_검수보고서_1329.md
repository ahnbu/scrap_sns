---
title: "내-쓰레드-글-수집-계획"
created: "2026-08-27 13:29"
session_id: "c31b5cef-2f02-4063-8cd7-75139cfe5bfd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/c31b5cef-2f02-4063-8cd7-75139cfe5bfd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 13:29:12"
---

# Plan Check Reviewer Report

## Verdict
실행 가능

## Blocking Issues
없음

## Required Fixes
- 1.1절 #21 및 3.7절의 구조 선례 줄 수(189/128/226) 정정: 1라운드 검수자가 파일 줄 수가 일치한다고 한 것은 거짓이다. 실제 코드 확인 결과 `my_posts_scrap.py`는 143줄, `utils/my_posts_adapter.py`는 102줄, `tests/unit/test_my_posts_adapter.py`는 160줄이다. 구현 결과에 영향을 주지 않는 참조용 수치이나 명백히 틀린 인용이므로, 문서의 값을 실제 측정값(143/102/160)으로 바로잡아야 한다.

## 참고 제안 (판정에 미반영)
- **1라운드 지적사항(Blocking/Required) 반영 확인**:
  - **V7 토큰 무효화 검증 위험 해소**: 계획서 5절에서 V7 검증 방식을 실제 파일 조작이 아닌 "단위 테스트 + 예외 모킹"으로 명확히 교체했다. 실제 `~/.claude/settings.json`을 전혀 건드리지 않으므로, 1라운드 리뷰어가 제안한 `try/finally` 기반의 파일 조작 스크립트보다 원천적으로 위험이 낮고 훨씬 안전한 조치다. 매우 타당하다.
  - **3.7절 T1 불일치 해소**: 3.7절 T1 항목에 `.gitignore` 예외 추가(5.1)가 정상적으로 명시되어 산출물 처리 계획과 일치하게 되었다.
  - **4절 placeholder 해소**: `exit_auth_required("threads", reason="login_required")`로 대상 시그니처에 맞게 확정되어 바로 실행 가능한 상태다.
- **1.1절 실측표 검증 (LOC 제외)**: 설정 파일 내 `mcpServers.threads` 존재, `utils/common.py:109` 시간대 처리(naive 변환), `utils/auth_status.py:20` 페이로드(`platform` 포함), `my_posts_scrap.py:126` 인자, E2E 테스트 headless 고정, 저장글 `is_merged_thread` 비율(1187/1273, 93%), `threads_mcp_runner.py`의 `subprocess.Popen` 사용 등 코드와 설정으로 확인 가능한 항목 모두가 사실과 정확히 일치한다.
- **4절 BL-0826-01 기존 착수 조건 폐기 타당성**: 기존 조건(링크드인 성공 2회)은 인증 실패 시나리오를 전혀 유발하지 않으므로 원인 분리 테스트로서 무의미했다. 이미 `emit_auth_required`가 플랫폼을 명시하고 있으므로, 이를 강제하는 단위 테스트 요건으로 대체하고 기존 착수 조건을 폐기한 것은 논리적으로 타당한 결정이다.
- **3.3절 30분 판별 규칙 리스크 및 롤백 경로**: 병합 과정에 대한 로그 보완책이 적절히 수립되어 있다. 또한, 30분 규칙의 허점으로 본문이 잘못 병합되어 로컬 영구 데이터에 쓰이더라도 쓰레드 API 측의 원본 데이터가 훼손되는 것이 아니다. 규칙을 보완한 후 파이프라인을 1회 재실행하면 Git으로 추적 중인 로컬 파일 결과물이 그대로 덮어씌워지므로 롤백 경로는 안전하고 확실하게 보장되어 있다.
- **3.1절 sns_insight_update 수정 불필요 확인**: 외부 레포인 `sns_insight_update/collectors/threads.py` 내 `collect_threads_posts()` 코드를 직접 확인한 결과, 내부적으로 MCP를 구동하며 예외 발생 시 `ThreadsAuthRequired`를 던지도록 완벽히 구현되어 있다. scrap_sns 측에서는 해당 모듈 호출 후 예외만 잡아주면 되므로 레포 수정 없이 100% 실행 가능하다.