---
title: "내-게시물-성과지표-통합-수집-계획"
created: "2026-08-26 12:20"
session_id: "bd25318e-1540-4c9c-8c65-2b611cf6d512"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/bd25318e-1540-4c9c-8c65-2b611cf6d512.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-26 12:20:34"
---

> 2회차 검수. reviewer가 응답 대신 파일로 산출해, 본문을 스크립트 산출 경로에 병합함.

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
1. **API 반환 화이트리스트(`META_FIELDS`) 누락으로 인한 뷰어 연동 실패**
   - 근거: `utils/post_meta.py:20-25`, `scrap_sns_server.py:800`의 `get_posts()` 내부 `build_post_meta()` 호출 로직
   - 실행 전 막히는 이유: T7에서 CLI(`query-sns.mjs`) 노출은 추가했으나, 웹 뷰어 API가 게시글을 반환할 때 사용하는 화이트리스트(`utils/post_meta.py`의 `META_FIELDS`)에 `is_own_post`를 추가하는 계획이 빠져 있습니다. 서버 API(`scrap_sns_server.py`)는 `build_post_meta()`를 호출해 `META_FIELDS`에 명시된 필드만 뷰어로 내려줍니다. 따라서 계획대로 실행하면 뷰어로 `is_own_post` 데이터가 아예 전달되지 않으며, W5 뷰어 구현 시 T5의 필터 로직(`post.is_own_post === true`)이 항상 `undefined === true`로 평가되어 "MY" 필터가 완전히 먹통이 됩니다.

2. **Consumer 웨이브 내 `my_posts_scrap.py`의 파일 쓰기 경합(Race Condition) 또는 병합 누락**
   - 근거: `total_scrap.py:432`의 Consumer 웨이브 병렬 실행 로직(`subprocess.Popen`), `total_scrap.py:498` `merge_results()`
   - 실행 전 막히는 이유: 3.4절에서 "동시 로그인 세션 회피"를 명분으로 `my_posts_scrap.py`를 Consumer 웨이브로 옮겼습니다. 하지만 `total_scrap.py`의 Consumer 웨이브는 내부 스크립트들을 완전히 동시 실행합니다. 
     - 만약 `my_posts_scrap.py`가 기존 데이터와 함께 `linkedin_py_full_*.json`에 직접 쓴다면, 동일 웨이브에서 도는 `linkedin_metric_single.py`와 동시 쓰기 경합이 발생해 파일이 깨질 위험이 큽니다. 
     - 만약 별도의 출력 파일(`my_posts_py_full_*.json`)을 쓴다면, `total_scrap.py`의 `merge_results()`가 이 새로운 파일을 읽도록 로직을 추가해야 하는데 계획에 누락되어 있어 최종 통합본(`total_full_*.json`)에 내 글 데이터가 영구 누락됩니다.

## Required Fixes
1. T7 또는 T2에 `utils/post_meta.py`의 `META_FIELDS` 리스트에 `is_own_post`를 추가하는 작업을 반드시 명시하십시오.
2. `my_posts_scrap.py`의 출력 저장 방식을 명확히 확정하고, 이에 따른 동시성/병합 문제를 해결하십시오. 
   - 별도 파일(`my_posts_py_full_*.json`)에 저장한다면: `total_scrap.py`의 `merge_results()`에 해당 파일을 병합하는 로직을 추가하는 작업을 포함하십시오.
   - 기존 파일(`linkedin_py_full_*.json`)에 저장한다면: Consumer 웨이브 내에서 `linkedin_metric_single.py`와 `my_posts_scrap.py`가 동시에 실행되지 않도록 스크립트 간 실행 순서를 제어(배리어 적용 등)하십시오.

## 참고 제안 (판정에 미반영)
- **테스트 스키마 기본값 갱신**: CLI나 뷰어 동작을 막지는 않지만, `tests/unit/test_post_schema.py`도 `is_own_post` 기본값 처리를 검증하도록 보완하는 것이 T8 목표인 데이터 일관성에 더 부합합니다.

---

### 사용자 질문에 대한 답변
- **W5 의존성 대안 설계 생략 및 '병행 세션 커밋 후 착수' 대기 판단**: 타당합니다. `utils/metric_refresh.py`가 이미 구현되어 파일로 존재하므로 의존성 단절 시나리오가 아니며, 남은 것은 미커밋 파일 충돌뿐이므로 억지 대안 설계보다 병행 세션 커밋을 대기하는 것이 정석입니다.
- **`is_own_post` 파급 식별 충분성**: 불충분합니다. CLI 단은 해소되었으나 위 Blocking Issue 1번에 지적한 대로 가장 중요한 뷰어 API 단독 화이트리스트가 누락되었습니다.
- **수동 GUI 검증 대체 후 done-check 가능 여부**: 가능합니다. V8~V12에서 headless 모드와 판정 조건(종료코드, UI 상태 캡처)을 구체적으로 제시해 done-check 봇이 증거 기반으로 충분히 검증할 수 있습니다.
- **소요 증가 추론**: 타당합니다. 웨이브 내 프로세스가 병렬이므로 하한선과 소요 시간을 기반으로 최악의 상한(+10초)을 도출한 논리가 수학적으로 맞습니다.
- **MyPosts를 consumer 웨이브로 옮긴 근거와 부작용 검토**: "동시 로그인 세션 회피"라는 근거 자체는 합리적입니다. 그러나 옮김으로써 발생하는 치명적인 부작용(Consumer 웨이브 내 병렬 쓰기 경합 또는 병합 누락)을 전혀 검토하지 못했습니다. (Blocking Issue 2번 참조)
- **`PLATFORM_POLICIES`의 `fresh_post_days: None` 추가**: 현재 코드(`utils/metric_refresh.py:115-117`)에서는 `None`이 들어오면 `TypeError`가 발생하지만, 계획 3.7절에 "`fresh_post_days=None`을 `classify_target`이 '신선도 검사 생략'으로 해석하도록 분기를 추가한다"고 필요한 분기 수정 사항을 명시했으므로 문제없이 식별되었습니다.
- **`utils/common.py` 중복 사본 "값만 동기화" 절충의 안전성**: 안전합니다. `utils/common.py`의 `reorder_post()`는 `defaults` 처리를 하지 않으므로 타 플랫폼 글에는 `is_own_post` 키가 아예 생성되지 않습니다. 그러나 자바스크립트에서는 `post.is_own_post === true` 검사 시 `undefined === true`가 되어 `false`와 동일하게 작동하므로 필터 로직에 부작용을 일으키지 않습니다.
