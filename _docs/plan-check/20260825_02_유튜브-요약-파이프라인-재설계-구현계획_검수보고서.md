---
title: "유튜브-요약-파이프라인-재설계-구현계획"
created: "2026-08-25 18:47"
session_id: "8726f5e3-015c-4a6c-bd4b-aa0f7130c887"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/8726f5e3-015c-4a6c-bd4b-aa0f7130c887.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-25 18:47:49"
---

## 1차 검수 (agy / Gemini 3.1 Pro (High))

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- 프롬프트 내용 누락 / 계획서 2-D절 / `(agy)\n<형식 지시 + 규칙>...` 부분에 정확한 프롬프트 내용이 문자열 형태로 확정되지 않아, 코드 구현 시 AI가 임의로 프롬프트를 창작해야 하므로 출력 형식을 통제할 수 없게 됨.
- 실행 명령에 Placeholder 잔존 / 계획서 3.5.2절 f항목 / `query-sessions.js get <각 세션ID>`는 그대로 터미널에 실행할 수 없어 작업이 막힘. 3.5.1에서 식별된 대상 세션 ID 목록을 활용하여 구체적 명령어나 루프문으로 풀어써야 함.
- QA 검증 기준에 Placeholder 잔존 / 계획서 5장 S2, S8 / `/api/search?q=<제품명>&platform=youtube` 및 `query-sessions.js get <ID>`는 미확정 값을 포함하고 있어 done-check 단계에서 사람 개입 없이 기계적으로 판정할 수 없음. 테스트에 사용할 실제 단어(예: `툭`)와 실제 세션 ID로 치환해야 함.
- 수동 GUI 검증 요구로 인한 병행 작업 차단 / 계획서 3장 3-3절 / `뷰어 재시작 (wscript sns_hub.vbs) 후 상단 총건수 = 최신 JSON 게시글 수 확인`은 창을 띄워 사용자 포커스를 빼앗고 육안 확인을 강제함. 수동 확인이 필수적인 예외 사유가 없으므로, 해당 검증 로직을 `verify_youtube_viewer_headless.mjs`에 통합하여 headless 환경에서 판정할 수 있도록 수정해야 함.

## Required Fixes
없음

## 참고 제안 (판정에 미반영)
- 3-3절의 수동 확인을 스크립트로 자동화할 때, 5장 S7(저장·통합·화면 3자 일치)의 검증 방법 또한 육안 확인에서 `verify_youtube_viewer_headless.mjs` 실행으로 변경하면, S1·S5와 함께 한 번의 스크립트 실행으로 QA를 효율적으로 종결할 수 있습니다.

---

## 2차 검수 (agy / Gemini 3.1 Pro (High))

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- 뷰어 및 검증 스크립트 코드 수정 작업의 실행 웨이브(Wave) 배정 누락 / `## 실행 웨이브` 절 및 `4. 변경 파일 목록` / STEP 3의 `scripts/verify_youtube_viewer_headless.mjs` 검사 로직 추가(3-3) 및 뷰어 UI 변경(`script.js`, `style.css`), `total_scrap.py` 가드 추가 작업이 웨이브 실행 계획(A~E) 어디에도 선후관계로 배정되지 않았습니다. 계획된 웨이브 순서대로 코드를 실행할 경우, 뷰어와 검증 코드가 수정되지 않은 채로 E 웨이브(headless 검증)에 진입하게 되어 반드시 검증에 실패하게 됩니다.
- S1 검증 조건과 웨이브 E(부분 실행 후 검증)의 논리적 충돌 / `STEP 3` S1 판정식 및 `## 실행 웨이브` E / S1 검증은 "유튜브 카드 전수에 대해" 새 형식(`────` 및 `[요약]`)을 요구하나, 웨이브 E는 W0(10건) 등 부분 처리 직후마다 실행되도록 규정되어 있습니다. W0 10건만 처리된 상태에서 웨이브 E가 돌면, 나머지 66건의 미처리 카드가 S1 조건을 만족하지 못하므로 반드시 검사 스크립트가 실패하고 후속 작업이 차단됩니다.

## Required Fixes
- `STEP 1` 및 `STEP 2`는 git 추적 대상인 기존 데이터를 덮어쓰므로, 문제 발생 시 명시적으로 어떻게 롤백할 것인지(`git checkout`, `git restore` 등)를 계획에 명확히 서술하십시오.
- 문서 내 서술이나 예시에 쓰인 placeholder(`[상세] \n … \n [설명]`, `00:00 …`, `/api/post/<id>`)를 구체적인 예시 값이나 정규식 등의 확정된 표기로 치환하십시오.
- `STEP 0-2`에서 원장 파일(`logs/youtube_summary_ledger.jsonl`)을 `_docs/evidence/`로 복사한다고 했으나 실행할 복사 명령이 명시되지 않았고, `4. 변경 파일 목록`에도 해당 원장 복사본과 검증 스크립트 캡처 산출물(`20260825_youtube_summary_card.png`)이 커밋 대상인지 여부가 누락되어 있으니 이를 보완하십시오.

## 참고 제안 (판정에 미반영)
- W0 게이트 g3에서 "요약 10건을 사람이 직접 읽는다"고 되어 있으나 이 육안 검수 결과를 어디에 어떻게 기록할지(예: 별도 리뷰 문서 작성 여부) 구체화하면 추후 검수 이력이 명확해질 것입니다.
