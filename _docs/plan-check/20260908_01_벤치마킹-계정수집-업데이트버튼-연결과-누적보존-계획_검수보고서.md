---
title: "벤치마킹-계정수집-업데이트버튼-연결과-누적보존-계획"
created: "2026-09-08 19:26"
session_id: "7e1884d6-79c3-43b9-a6c2-c4b9edb34ccd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/7e1884d6-79c3-43b9-a6c2-c4b9edb34ccd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-08 19:26:01"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- **완료 기준과 구현 내용의 모순** / `D:/vibe-coding/scrap_sns/_docs/20260908_01_벤치마킹-계정수집-업데이트버튼-연결과-누적보존-계획.md:118,176` / 계획대로 실행하면 검증 단계에서 반드시 실패한다. W1은 계정당 보관 상한을 30편으로 두고 초과분은 오래된 순으로 잘라낸다고 했으나, 완료 기준 2는 "9/6에 모은 글이 한 건도 사라지지 않는다(이전 집합이 이후 집합의 부분집합)"고 규정했다. 새로 수집된 글로 인해 상한 30편을 넘겨 기존 글이 잘려나가면 완료 기준을 결코 통과할 수 없다.
- **GUI 검증 자동화(done-check) 불가** / `D:/vibe-coding/scrap_sns/_docs/20260908_01_벤치마킹-계정수집-업데이트버튼-연결과-누적보존-계획.md:178` / `done-check`가 사람 눈 없이 판정할 수 없어 자동화 실행이 불가능하다. "창을 띄우지 않고 캡처를 남긴다"고만 되어 있는데, 에이전트는 이미지만으로 UI 갱신 성공 여부를 단정하기 어렵다. 검증 스크립트가 DOM에서 날짜 텍스트를 파싱하여 기대값과 일치할 때 종료 코드 0을 반환하도록 로직을 명시해야 한다.

## Required Fixes
- **명시적 `## 실행 웨이브` 블록 추가:** "W1 완료 후에 적용한다"는 자연어 서술만으로는 AI 병렬 실행 시 파일 충돌 기준으로 작업이 재분류되어 의도한 선후관계가 깨질 수 있다. 문서 내에 `## 실행 웨이브` 블록을 생성해 작업 간 의존성(W1 → W2 → W3)을 명확히 지정하라.
- **검증 산출물 처리 방침 명시:** 완료 기준 4에서 생성되는 스크린샷 캡처 파일의 생애주기(커밋 포함 여부, 확인 후 즉시 삭제, 또는 `.gitignore` 처리 등)를 명시하여 커밋 오염을 방지해야 한다.

## 참고 제안 (판정에 미반영)
- **접근법의 트레이드오프 명시:** 벤치마킹이 `total_scrap.py`의 `업데이트` 버튼 동작 하나로 통합되어 관리가 매우 편해지지만(얻는 것), 그만큼 동기 호출(blocking) 시간이 길어져 UI 화면 대기가 크게 늘어난다(잃는 것)는 점을 문서에 명시해두면 좋다.
- **빈 데이터 예외 처리:** W1에서 `*_user_full_*.json`을 읽어 병합할 때, 기존 파일이 아예 존재하지 않는 최초 수집 환경(예: 갓 클론한 환경)에서도 에러 없이 빈 상태로 병합을 시작할 수 있도록 초기화 로직을 신경써야 한다.