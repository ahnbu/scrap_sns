---
title: "참조문서-통합과-API목록-테스트고정-구현계획"
created: "2026-07-26 18:26"
session_id: "2f066359-7a18-4159-80ca-f0a7ed77144c"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/2f066359-7a18-4159-80ca-f0a7ed77144c.jsonl"
ai: "claude"
model: "independent-lite-reviewer"
performed_at: "2026-07-26 18:26:22"
---

---
title: "Plan Check Lite Reviewer Report"
created: "2026-07-26 18:25"
session_id: "2f066359-7a18-4159-80ca-f0a7ed77144c"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/2f066359-7a18-4159-80ca-f0a7ed77144c.jsonl"
ai: "claude"
---

# Plan Check Lite Reviewer Report

대상: `docs/20260726_03_참조문서-통합과-API목록-테스트고정-구현계획.md`

검수 이력: 1차 `수정 필요` (Blocking 5건 + 추가 Fix 2건) → 수정 반영 → 2차 `실행 가능`

## Verdict

실행 가능

## Blocking Issues

없음

1차 검수 Blocking 5건과 추가 Fix 2건이 모두 실제로 반영됐음을 실행 근거로 확인했다.

### 1. BL-0726-02 → BL-0726-03 교체 (해결)

- plan Step 4-3이 `BL-0726-03` 부여로 교체되고, 재사용 금지 근거를 `BACKLOG.md:25`·`CHANGELOG.md:7`로 명시
- `git log --oneline -S "BL-0726-02" -- BACKLOG.md` → `55adc31`(삭제) / `602a4bf`(추가) 2건. 삭제 이력 실재 확인
- `BACKLOG.md:25` 원문: "한 번 부여된 ID는 변경·재사용하지 않는다 (삭제해도 번호 유지)"

### 2. `.omo/` gitignore 무효 서술 (해결)

- plan이 "84개 파일 전부 git 추적 중", "이미 추적 중인 파일에는 gitignore가 적용되지 않아 무효", "`git rm --cached` 필요"로 정정
- `git ls-files .omo/ | wc -l` → `84` (plans 3 / drafts 2 / evidence 77) — plan 기재와 완전 일치
- `.gitignore:73-75` 등재 확인, 무효 판정 근거 성립

### 3. `_doc_routes()` 백틱 한정 정규식 (해결 — 실행 검증)

Step 1-2가 지시한 문서 블록을 그대로 파일로 만들어 교체된 정규식 실행:

```
raw found count: 17
doc set size: 17
missing (code-doc): []
phantom (doc-code): []
count test: True
```

`- \`GET /api/status\``, `- \`GET /api/get-tags\` / \`POST /api/save-tags\`` 두 형식 모두 정상 파싱. 산문 `/api/auth/*`는 백틱이 없어 미포착 — phantom 0건. Step 1-2에 표기 제약도 추가돼 지시-테스트 연동이 닫혔다.

### 4. `DOC_PATH` 절대경로화 (해결)

- `os.path.dirname` 3회 결과: `tests/contract/test_api_surface.py` → `D:\vibe-coding\scrap_sns` (레포 루트 정확)
- `cd tests && pytest contract/...` 실행 시 `rootdir`가 레포 루트로 해석되고 `conftest.py:5` 패턴이 `sys.path`를 채워 import 성공. 실행 위치 비종속 성립

### 5. Step 4-1 grep `BACKLOG.md` 제외 + 순서 의존 명시 (해결)

- 순서 의존 주의 명시, `grep -v "^./BACKLOG.md"` 추가, Step 4-3 후 최종 재실행 절차 명시
- 현재 상태 grep 실행 → 9건 히트(AGENTS 36/37/59, GEMINI 74/75, linkedin_scrap 425, README 32/289/290). Step 4-1 수정 대상 표의 6개 항목·라인번호와 정확히 일치

### 추가 Fix 2건 (해결)

- `/tmp` → scratchpad: 세션 scratchpad 절대경로 사용, 레포 밖이라 커밋 범위 비오염 근거 명시
- `grep -c || true`: 실제 실행 → `1 / 0 / 0`, `exit=0`. 3연쇄 중단 없이 완주

### 신규 결함 검증 (없음)

| 점검 | 결과 |
| --- | --- |
| `not m.endswith("/")` 필터가 정상 라우트 오배제 | 17개 중 `/`로 끝나는 라우트 `[]` — 오배제 0건 |
| `_normalize()` 충돌 | raw 17 → normalized-unique 17, collisions `[]` |
| `EXPECTED_ROUTE_COUNT = 17` | 실측 17 일치 |
| Step 2-3 키워드 26종 | 원본 합본 대조 결과 미존재 키워드 `[]` — 달성 가능 |
| `tests/contract/__init__.py` | 존재 — 신규 파일 패키지 인식 문제 없음 |

## Required Fixes

없음

## 실행 시 참고 (blocking 아님)

- Step 1-2 대상 라인 중 `crawling_logic.md:129-141`은 실제로 `130-140`행에 있어 1~2행 오차가 있다. 편집 시 육안 확인으로 흡수되는 수준
- `README.md`의 API 언급은 지목된 `199-207` 외에 `206`, `208`행에도 산문 형태로 있다. Step 3-1 테스트는 `docs/architecture.md`만 대상이므로 `README.md` 표기가 테스트를 깨뜨리지는 않는다
