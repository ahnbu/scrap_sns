---
title: "뷰어정리-유튜브확대-지표갱신-웨이브계획"
created: "2026-08-26 10:59"
session_id: "7d3daa6a-72b3-4ae5-8431-95717010b657"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/7d3daa6a-72b3-4ae5-8431-95717010b657.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-26 10:59:06"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

## Blocking Issues
1. **실행 명령에 placeholder 존재**
   - 근거: 3절 W2-b (명령어: `python youtube_scrap.py --mode update --playlists all --max-summaries <배치크기>`)
   - 실행 전 막히는 이유: `<배치크기>`가 실행할 터미널 명령에 그대로 남아 있어, 이대로 실행하면 구문 오류가 발생해 진행이 불가능하다. 터미널 웨이브 처리를 위한 구체적인 숫자(예: `187`)로 확정해야 한다.

2. **수동 GUI 확인 위주의 검증 (완료 기준 미달)**
   - 근거: 3절 W1 검증 ("콘솔에 prefetch 관련 error 0건", "네트워크 탭에서 script.js?v= 확인"), W2-d 검증 ("뷰어 화면에서 일치 확인"), W5 검증 ("실제 뷰어 화면에서 확인")
   - 실행 전 막히는 이유: 모든 검증이 브라우저에서 사람이 눈으로 확인하는 방식이다. `done-check`가 증거 기반(종료코드, 로그, 캡처 파일 등)으로 사람 눈 없이 판정할 수 없어 계획대로라면 검증 단계에서 막힌다. e2e 자동 테스트, 스냅샷 캡처, DOM 조회 스크립트 등 자동화된 방식으로 대체해야 한다.

3. **영구 데이터 변경 및 비용 발생 시나리오의 실패 복구/재처리 방안 누락**
   - 근거: 3절 W2-b (유튜브 187건 병렬 요약 처리) 및 W5 (지표 덮어쓰기)
   - 실행 전 막히는 이유: W2-b는 과금되는 Gemini API를 대량 호출하는데, 실행 중단(타임아웃/에러) 시 이미 생성된 요약 캐시를 기반으로 스킵하여 재시작할 수 있는지(부분 실패 복구 및 재처리 방법) 명시되지 않았다. W5는 수집된 데이터를 덮어쓰는데 롤백 방안(예: git 활용한 `output_*` 원복)이 누락되었다. 두 항목 모두 되돌리기 어려운 결정이므로 실행 전 안전망 서술이 필수다.

## Required Fixes
1. **인용 라인 번호 오류 정정**
   - `web_viewer/script.js:2450` (1.1절 #3)은 잘못된 인용이다. 실제 `if (aHas !== bHas) return aHas ? -1 : 1` 분기 로직은 `web_viewer/script.js:2464`에 위치한다.
2. **W1-T (T3 테스트) 수동 검증 선언 수정**
   - "되돌리기가 확실하지 않으면 T3는 수동 검증으로만 확인한다"는 서술은 테스트 자동화 및 clean worktree 원칙과 충돌한다. `sns_tags.json`을 테스트 종료 시 원복하는 스크립트를 추가하거나, 샌드박스용 데이터를 지정해 자동화 검증으로 확정해야 한다.
3. **캐시버스터 식별자 명시 누락**
   - 3.1절에서 "각 웨이브를 닫을 때마다 갱신한다"고 했으나 W1의 `20260826-w1` 외에 W1-T, W3 종료 시 어떤 문자열(예: `w1-t`, `w3`)로 덮어쓸지 계획서 내 구체적으로 명시해야 한다.

## 참고 제안 (판정에 미반영)
- **병렬 예외 처리 명확화**: W2-a 유튜브 요약 병렬화 시 `append_ledger`에 `threading.Lock`을 거는 것 외에도, `Future.result()` 반환 시 타임아웃이나 예외가 발생할 때 메인 프로세스를 중단할지 계속 진행할지 명확히 해두면 더 단단한 설계가 됩니다.
- **Threads 계정 차단 리스크**: W5에서 Threads의 갱신 주기를 짧게 가져가는(14일/5일) 트레이드오프는 빠른 데이터 축적을 얻는 대신 빈번한 로그인 세션 노출에 따른 계정 차단 리스크(잃는 것)를 감수하는 것입니다. 운영 로그로 200건 관측 후 주기를 신속히 늘리는 등 주의가 필요합니다.
- **캐시버스터 근본 해결**: 향후 Vite, Webpack 같은 번들러를 도입하면 `script.js?v=` 해시 갱신이 자동화되어 이런 갱신 누락으로 인한 뷰어 미반영 문제를 근본적으로 해결할 수 있습니다.
- **이전 판단 정정의 타당성**: 5절의 조사 중 정정한 판단(스냅샷 값 복사와 실제 읽은 값의 오인)은 타당합니다. W3로 갱신 시점을 먼저 기록(W5 선행)하는 구조를 취함으로써 같은 오류를 반복하지 않게 잘 설계되었습니다.