---
title: "SNS벤치마킹-계정수집관리-수행계획"
created: "2026-09-06 15:48"
session_id: "c6c88b11-598d-4f27-8cda-60aa9281eb24"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/c6c88b11-598d-4f27-8cda-60aa9281eb24.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-06 15:48:14"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- 수동 시각 검증에 의존하여 자동화 AI가 판정할 수 없음 / `_docs/20260906_01_SNS벤치마킹-계정수집관리-수행계획.md`의 P3, P6, P7, P8 완료 판정 / `done-check` AI는 브라우저를 띄워 화면을 직접 눈으로 보고 판정할 수 없다. 계획서에 "화면에서 직접 확인한다", "눈으로 봐야 하는 것"이라고 명시되어 있는데, 이는 사람이 직접 확인해야 한다는 뜻이다. 이를 Playwright(브라우저 자동화 도구) 등을 써서 성공 시 종료 코드 0을 반환하도록 스크립트로 교체하거나, 사람의 눈 검수가 반드시 필요하다면 AI가 실행을 멈추고 사용자의 승인을 기다리도록 계획을 고쳐야 실행 단계에서 막히지 않는다.
- 유튜브 채널 확인용 백엔드 API 라우트 누락 / `_docs/20260906_01_SNS벤치마킹-계정수집관리-수행계획.md`의 P1 및 P4 / P4에서 사용자가 주소를 입력하면 즉시 유튜브 채널을 조회해 정보를 채운다고 했으나, 프론트엔드에서 API 키 없이 직접 조회할 수 없다. 백엔드인 `scrap_sns_server.py`에서 이를 대신 호출해주는 검증용 API 라우트(예: `/api/verify-youtube-channel`)가 P1에 명시되지 않아, 설계 구조상 클라이언트가 이를 수행할 방법이 없다.
- 영구 데이터 일괄 수정 스크립트의 백업 절차 부재 / `_docs/20260906_01_SNS벤치마킹-계정수집관리-수행계획.md`의 P0 작업 (`scripts/backfill_youtube_channel_id.py`) / 기존에 수집된 505건의 데이터를 스크립트로 직접 수정하는데 백업 생성이나 롤백(되돌리기) 절차가 없다. 스크립트 실행 중 일부만 성공하거나 에러가 나면 원본 데이터가 영구적으로 망가질 위험이 있다.
- 스키마 점검 명령어 오류 및 대상 누락 / `_docs/20260906_01_SNS벤치마킹-계정수집관리-수행계획.md`의 P0 완료 판정 / `python migrate_schema.py` 명령어는 처리할 파일을 지정하는 `--target` 인자가 필수인데 적혀있지 않다. 또한 코드를 보면 현재 쓰레드(Threads) 데이터만 검사하도록 굳어져 있어, 정작 검사해야 할 유튜브 데이터는 아무런 에러 없이 점검에서 제외되므로 완료 기준을 증명할 수 없다.

## Required Fixes
- **조건부 실행 서술(Placeholder) 확정**: 중간 이슈 M2 처리 방안에 있는 "필요하면 서버가 필터를 먼저 적용"이라는 모호한 문구를 지우고, 검색 결과 500건 제한으로 인해 저장글이 누락되는 문제를 막기 위해 서버 필터를 무조건 적용할지 여부를 명확히 결정해서 적어야 한다.
- **스키마 마이그레이션 스크립트 수정 명시**: P0 단계에 `migrate_schema.py`가 유튜브(YouTube) 플랫폼 데이터도 정상적으로 읽고 검사할 수 있도록 코드를 먼저 고친다는 내용을 포함해야 한다.
- **검증 산출물(캡처)의 형상관리 의도 명시**: P3, P6, P7, P8에서 생성되는 증거용 캡처 이미지들을 Git 커밋에 포함할지, `.gitignore`로 제외할지, 아니면 로컬에만 둘지 산출물 처리 방식을 명시해야 한다.

## 참고 제안 (판정에 미반영)
- **트레이드오프 (데이터 기본값 설계)**: 기존 데이터가 화면에서 사라지는 것을 막기 위해 `is_saved` 필드의 기본값을 무조건 `True`로 두는 결정(D14)은 화면 표출 누락을 막는 확실한 이점이 있지만, 이후 벤치마킹 데이터와 내가 저장한 북마크가 만날 때마다 양쪽의 상태를 꼼꼼하게 합쳐야 하는 복잡한 코드를 영구적으로 관리해야 하는 비용(잃는 것)이 발생한다.
- **과최적화 주의 (디자인 검수 스크립트)**: P7에서 배지 3개가 겹치는 단 2건의 디자인을 확인하기 위해, 4가지 화면 크기별로 자동 검수하는 기계용 스크립트(`verify_benchmark_ui_design.mjs`)를 새로 짜는 것은 문제 크기에 비해 해결책이 너무 무겁다(과최적화). 이 부분은 복잡한 스크립트를 짜기보다는 사람이 직접 눈으로 한 번 확인하고 끝내는 것이 훨씬 경제적이다.