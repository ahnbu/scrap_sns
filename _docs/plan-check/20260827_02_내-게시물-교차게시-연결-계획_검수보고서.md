---
title: "내-게시물-교차게시-연결-계획"
created: "2026-08-27 12:20"
session_id: "c31b5cef-2f02-4063-8cd7-75139cfe5bfd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/c31b5cef-2f02-4063-8cd7-75139cfe5bfd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 12:20:56"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
1. **선행 조건과 현재 실행 의도의 충돌 (실행 불가)** / 1.2절 및 4절 / 1.2절은 "결정 비용이 지금 가장 싸다"며 현재 시점의 당장 실행을 정당화하지만, 4절은 선행 작업(W1)의 착수 조건("LinkedIn 내 글 수집 2회차 정상 확인", 현재 1회)을 '유지한다'고 명시했습니다. 계획대로면 선행 조건 미달로 현재 시점에서 실행이 막히므로, 당장 착수하기 위해 선행 조건을 면제(override)할지, 아니면 계획 전체를 다음 업데이트 주기로 미룰지 확정해야 합니다.
2. **수동 조작 및 사람 눈에 의존하는 UI 검증** / 5절 V9 / V9의 검증 방법이 `실제 화면 조작 + 캡처`로 명시되어 있습니다. 이는 실행 중 브라우저 창이 떠 사용자 포커스를 빼앗고, 자동화된 `done-check`가 종료 코드 기반으로 성공 여부를 판정할 수 없게 만듭니다. 다른 UI 검증처럼 사람 눈에 의존하지 않는 `headless e2e + 캡처` 방식으로 수정해야 합니다.

## Required Fixes
1. **V10 산출물 처리 방식 명시**: 5절의 V10 검증에서 일회성 스크립트로 생성되는 "유사도 리포트" 산출물을 어떻게 처리할지 닫혀 있지 않습니다. 리포트를 커밋 증거에 포함할지, `.gitignore`에 추가할지, 또는 확인 후 즉시 폐기하는 로컬 전용(local-only) 파일인지 명시해야 합니다.
2. **영구 데이터(통합본) 롤백 절차 누락**: 9절 W4(통합본 재생성)에서 핵심 영구 데이터인 `total_full_*.json`을 전량 재생성합니다. 중간에 병합이 실패하거나 파일이 손상될 경우를 대비한 롤백 절차(예: `git checkout -- output_total/...` 복구 등)를 계획에 명시해야 합니다.
3. **증거 저장 경로 오타 수정**: 5절 V9의 증거 캡처 저장 경로가 현재 계획 문서 ID(`20260827_02`)와 다른 `_docs/evidence/20260827_01/`로 기재되어 있습니다. 일관성을 위해 경로를 수정해야 합니다.

## 참고 제안 (판정에 미반영)
- W3(짝짓기)에서 `total_scrap.py`의 병합 파이프라인(T5)을 연결할 때, Threads 어댑터가 임시로 생성할 KST 변환 데이터나 내 게시물 경로를 병합기의 glob 패턴이 정확히 순회하고 읽을 수 있는지 디렉터리 경로를 추가로 명시해 두면 더 안전합니다.
- 데이터와 코드 인용부(1.1절 실측치 31쌍 및 0일, `web_viewer/script.js:214` 등)는 실제 파일 및 JSON과 100% 일치함을 확인했습니다. 구조적 설계(레코드 병합 회피 등)는 트레이드오프가 훌륭하게 고려되어 있습니다.