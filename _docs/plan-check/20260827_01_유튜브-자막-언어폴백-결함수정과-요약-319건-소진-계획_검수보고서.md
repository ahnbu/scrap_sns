---
title: "유튜브-자막-언어폴백-결함수정과-요약-319건-소진-계획"
created: "2026-08-27 12:15"
session_id: "baae7242-ac6b-43ec-8a42-3d238feb877f"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/baae7242-ac6b-43ec-8a42-3d238feb877f.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 12:15:28"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

## 승인 필요 사유
319건의 요약 작업 소진(S8)은 Gemini 주간 쿼터의 약 52~76%를 한 번에 소모하는 대규모 **비용 발생** 작업입니다. 만약 자막 다운로드 폴백 로직이 잘못되어 엉뚱한 자막이 대량 수집되거나 요약 품질에 결함이 발생할 경우, 이를 되돌리기 위해 319건 전체를 다시 재실행하며 쿼터를 중복 과금해야 합니다. 이는 '코드 수정만으로 되돌릴 수 없는 상태 변경'에 해당하므로 S7(24건 표본 테스트) 결과 확인과 별개로 대규모 소진 실행 전 명시적인 사용자 승인이 필요합니다.

## 분석 (트레이드오프 및 과최적화)
- **트레이드오프**: 2단계 언어 폴백(S1) 적용 시, 429 차단을 원천 방지하고 고품질의 원본 자막을 안전하게 얻을 수 있는 장점이 매우 큽니다. 반면 1차 실패 시 2차 yt-dlp 호출이 추가로 발생하여 개별 실패 건의 처리 시간이 소폭(2~4초) 늘어나는 단점이 있으나, 현재 병렬 처리 구조 덕분에 체감 실행 시간 증가는 미미하여 얻는 것이 훨씬 큰 합리적인 선택입니다.
- **과최적화 방어**: 기존에 멤버 전용 재질의 비용을 줄이려던 최적화 시도가 '실측 결과 과장'임을 인정하고, 비용 절감이 아닌 '진단 목적'으로만 상태를 분리하기로 한 판단(3.4절)은 과최적화를 스스로 경계한 훌륭한 접근입니다.

## Blocking Issues
- 지적: 완료 판정 기준 6번(뷰어 화면에서 표본 요약 표시 확인)이 수동 GUI 검증 수준에 머물러 있음.
  - 근거: 7절 6번 "뷰어 화면에서 회수분 표본(예: rsSUIvAkjvk, Qt50DVNJcvw)의 요약 표시 확인"
  - 실행 전 막히는 이유: GUI 검증 시 headless 실행과 종료코드/로그 기반 증거 수집 방법이 누락되어 있습니다. 향후 `done-check`가 사람 눈 없이 판정할 수 없어 검증 자동화가 막힙니다. Playwright 등을 이용한 headless 검증 스크립트를 작성하여 증거를 남기거나, 명시적인 수동 검증 및 사용자 승인 단계를 계획에 추가해야 합니다.

## Required Fixes
1. **명령어 누락 보완**: S7(자막 24건 회수)을 실제로 실행하기 위한 구체적인 CLI 명령어(예: `python youtube_scrap.py --mode update --skip-summaries` 등)가 문서에 누락되어 있습니다. 실행 주체가 혼동하지 않도록 명시해야 합니다.
2. **명시적 웨이브 블록 추가**: 8절 '실행 순서 요약'을 `## 실행 웨이브` 또는 `## 웨이브 구조` 제목의 블록으로 변경하여 웨이브를 명확히 분리해야 합니다. (exec-plan이 자연어 서술을 무시하고 파일 충돌 기준으로 임의로 선후관계를 재분류하는 것을 방지)
3. **Placeholder 제거**: 3.3절 예시 서술에 실행 불가 placeholder(`<id>`)가 남아 있습니다. 실행 명령이 아닌 단순 출력 예시이지만, 규칙에 따라 임의의 구체적 예시값으로 수정해야 합니다.
4. **산출물 처리 방침 명시**: S9 뷰어 검증 시 캡처 이미지나 테스트 리포트 등의 새 증거 파일이 생길 경우, 이를 커밋에 포함할지 아니면 local-only(`.gitignore` 제외 등)로 유지할지 산출물 처리 방침을 명시해야 합니다.

## 참고 제안 (판정에 미반영)
- S1의 2단계 폴백 구현 시, `youtube_scrap.py`의 `download_transcript()` 내부에서 `subprocess.run`을 순차적으로 2회(1차 `.*-orig`, 실패 시 2차 `ko,en`) 호출하는 로직임이 분명하므로, 구현 단계에서 혼선이 없도록 1차와 2차의 분기를 주석으로 명확히 구분하는 것을 권장합니다.
- `SUBTITLE_LANG_PRIORITY`에 `en-orig`를 추가(S3)할 때, `ko-orig`, `ko`, `en-orig`, `en` 순서로 일관되게 배치하여 기존 자막 우선순위 로직의 일관성을 유지하는 것이 좋습니다.