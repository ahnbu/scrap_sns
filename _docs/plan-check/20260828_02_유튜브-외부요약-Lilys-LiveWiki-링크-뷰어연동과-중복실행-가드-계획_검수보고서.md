---
title: "유튜브-외부요약-Lilys-LiveWiki-링크-뷰어연동과-중복실행-가드-계획"
created: "2026-08-28 18:10"
session_id: "8b4aba7b-6afe-4a84-a957-1013186633dc"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/8b4aba7b-6afe-4a84-a957-1013186633dc.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-28 18:10:35"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- 지적: 수동 브라우저 조작 및 눈 검사에 의존하는 검증 방식으로 인한 자동화 판정 불가
  - 근거: `4.4 사용자 시나리오 검증` ("API/CLI 결과만으로 완료 판단하지 않는다", "http://localhost:5000/에서 유튜브 필터를 켠다", "각각 클릭해 올바른 페이지가 열리는지 본다")
  - 실행 전 막히는 이유: '브라우저에서 직접 확인한다' 수준의 수동 GUI 검증은 사용자 포커스를 빼앗아 병행 작업을 끊습니다. 또한 테스트 종료 코드나 자동화된 로그가 생성되지 않으므로, 이후 `done-check`가 사람 개입 없이 증거 기반으로 완료를 판정하는 파이프라인을 원천적으로 막습니다. Playwright 등을 이용한 headless 자동화 검증으로 교체하거나, 수동 창 띄움이 불가피한 경우 그 사유가 명시되어야 합니다.

## Required Fixes
- 검증 산출물의 git 처리 기준 명시: `4.4`에서 화면 캡처를 `_docs/evidence/20260828_02/` 경로에 남기도록 지시했으나, 이 파일들을 커밋에 포함할지, `.gitignore`로 제외할지, 혹은 local-only로 남길지 의도가 닫혀 있지 않습니다.
- 실행 웨이브(Wave) 블록 추가: `6. 실행 순서`가 번호 목록으로만 적혀 있습니다. 자연어 서술만 있으면 `exec-plan`이 파일 충돌 기준으로 임의 병렬화할 때 의도한 선후관계가 깨질 수 있으므로, `## 실행 웨이브` 또는 `## 웨이브 구조` 블록을 추가하여 병렬/순차 단위를 강제해야 합니다.
- 실행 조건의 판단 유보(Placeholder) 확정: `T4`와 `5. 남은 리스크`에서 stale 판정 시간을 "예: 3시간", "로그에서 확인한 뒤 값을 정한다"와 같이 실행 시점에 판단하도록 미루고 있습니다. `done-check`가 판정할 수 있도록 실행 전 구체적인 타임아웃 기본값을 계획 문서에 확정해야 합니다.

## 참고 제안 (판정에 미반영)
- 접근법의 트레이드오프(얻는 것/잃는 것): 매핑이 존재하는 28.3%의 카드에만 아이콘을 표시하는 전략은 오작동(죽은 링크)을 방지하여 신뢰도를 지키는(얻는 것) 장점이 있습니다. 반면, 나머지 71.7%의 유튜브 영상에는 여전히 확장 기능이 제공되지 않으므로, 사용자가 보기에 UI의 일관성이 떨어지거나 기능이 파편화되어 보일 수 있는 단점(잃는 것)이 따릅니다.
- 과최적화 주의: `T4`의 중복 실행 방지 가드는 동시 병합으로 인한 파일 오염을 막기 위한 합리적 조치이나, UI 기능 추가라는 원래 목적 대비 백엔드 보강 작업에 가깝습니다. 작은 잠재적 오류를 막으려다 핵심 파이프라인(스크래퍼) 자체를 중단시키는 더 큰 문제를 낳지 않도록, 검사와 락 로직을 최대한 가볍게 유지해야 합니다.