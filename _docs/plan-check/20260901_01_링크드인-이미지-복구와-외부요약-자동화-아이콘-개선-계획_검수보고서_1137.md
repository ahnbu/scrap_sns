---
title: "링크드인-이미지-복구와-외부요약-자동화-아이콘-개선-계획"
created: "2026-09-01 11:37"
session_id: "931037b9-008c-4bef-b378-a8e38ac862fd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/931037b9-008c-4bef-b378-a8e38ac862fd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-01 11:37:33"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
외부 요약 자동 갱신 설계 오류 / `scripts/build_external_summaries.mjs:13-16` / 계획 2-1에서 "앞 두 단계 중 하나라도 실패하면 세 번째(`build_external_summaries.mjs`)는 실행하지 않는다"며 그 사유를 "반쪽 소스로 매핑을 덮어쓰면 기존 링크가 사라진다"고 서술했다. 그러나 해당 대상 스크립트는 수집이 실패해도 기존에 저장된 성공본 JSON 파일을 읽어 병합하도록 이미 견고하게 설계되어 있다. 계획대로 단일 수집 실패 시 전체 병합을 중단하게 되면, Lilys 인증 만료 등으로 한쪽만 실패했을 때 성공적으로 수집 완료된 LiveWiki 요약까지 뷰어에 반영되지 못하는 심각한 설계 오류(복원력 훼손)를 낳는다.

## Required Fixes
- 계획 2-1의 실행 중단 조건을 수정하라. 앞의 `lilys`, `livewiki` 수집 중 하나가 실패하더라도(비정상 종료코드를 반환하더라도) `build_external_summaries.mjs`를 항상 호출하도록 변경해야 한다. (스크립트 자체가 이전 성공본을 활용하므로 안전하다)

## 참고 제안 (판정에 미반영)
- 계획 1-4에서 `python scripts/rebuild_total.py`를 실행할 때, 해당 스크립트 내부에서 `download_images`가 호출되어 실제 이미지 로컬 다운로드가 이루어진다는 점을 명시하면, 이후 완료 검수(done-check) 시 데이터 흐름 추적이 더 명확해질 것이다.