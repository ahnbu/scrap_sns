---
title: "내-쓰레드-글-수집-계획"
created: "2026-08-27 13:22"
session_id: "c31b5cef-2f02-4063-8cd7-75139cfe5bfd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/c31b5cef-2f02-4063-8cd7-75139cfe5bfd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 13:22:48"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
- V7(토큰 무효화 검증)의 백업·복구 절차 불충분 / 5절 V7 / `~/.claude/settings.json`은 Claude AI 환경을 구성하는 핵심 설정 파일이다. 검증을 위해 토큰을 조작했다가 스크립트 크래시 등으로 복구에 실패할 경우 AI 시스템 환경 자체가 망가지는 치명적 리스크가 있다. 계획에 서술된 "원본 값을 먼저 백업하고... 되돌린다"는 방법은 안전장치가 없어 복구 실패 리스크를 방어하지 못한다. 실제 설정 파일을 건드리지 않도록 단위 테스트에서 `mock.patch`를 이용해 토큰 무효화 상황을 모킹(mocking)하거나, 부득이하게 실제 파일을 수정해야 한다면 예외 발생 시에도 롤백이 강제되는 `try...finally` 구조의 안전 스크립트를 사용하도록 계획을 변경해야 한다.

## Required Fixes
- 3.7절 작업 단위 표와 5.1절 간의 불일치 해소: 5.1절에서는 `.gitignore` 예외 추가를 T1 범위로 지정했으나 3.7절 작업 표에는 누락되어 있다. 3.7절 표에 해당 내용을 명시적으로 추가해야 한다.
- 4절의 `emit_auth_required("threads", ...)`에서 `...` placeholder 제거: `utils/auth_status.py:20`의 시그니처에 따라 필수 파라미터인 `reason` 값(예: `"login_required"` 또는 `"token_expired"`)을 명시하여 코드가 즉시 실행 가능한 형태로 확정되어야 한다.

## 참고 제안 (판정에 미반영)
- **1.1절 실측표 검증 결과**: 코드 및 설정 대조 결과 모두 사실과 정확히 부합한다. (`~/.claude/settings.json`에 `mcpServers.threads` 존재, `utils/common.py:109` 시간대 인자 없이 로컬 처리, `utils/auth_status.py:20` 플랫폼 정보 payload 포함, `my_posts_scrap.py:126` 호출 인자 일치, E2E 테스트 headless=True 고정, 저장글 `is_merged_thread` 비율 93%(1187/1273) 일치, `threads_mcp_runner.py`의 `subprocess.Popen` 사용 확인). 파일 줄 수만 실측 189/128/226줄로 계획서와 1줄씩 오차가 있으나 무의미한 수준이다.
- **4절 기존 착수 조건 폐기 타당성**: 매우 타당하다. `utils/auth_status.py`의 `emit_auth_required()`가 이미 플랫폼을 구분하여 실패 원인을 명확히 출력하므로, "실패 모드 분리가 어렵다"는 기존 조건의 논거는 해소된 상태다.
- **3.3절 30분 판별 규칙 롤백 경로**: 충분하다. 30분 기준이 틀려 잘못 합쳐지더라도 수집 산출물과 통합본이 모두 Git 추적 하에 있고, 원본 데이터는 API 쪽에 그대로 유지되므로, 코드를 수정한 뒤 다시 1회 수집을 돌리면 완벽하게 덮어쓰기로 복구할 수 있다.
- **3.1절 `sns_insight_update` 수정 불필요성**: 확인 완료. `sns_insight_update.collectors.threads` 모듈의 `collect_threads_posts()`가 내부적으로 MCP 구동과 파싱을 모두 처리하며 인증 실패 시 `ThreadsAuthRequired` 예외를 던지므로, scrap_sns에서는 호출 후 해당 예외만 잡아주면 레포지토리 수정 없이 구현할 수 있다.