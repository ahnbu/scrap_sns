---
title: "X-북마크-커서-페이지네이션-전환-계획"
created: "2026-08-26 10:41"
session_id: "dfdff5f4-bdf2-48aa-b72f-ca022d5f92ca"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/dfdff5f4-bdf2-48aa-b72f-ca022d5f92ca.jsonl"
ai: "claude"
model: "independent-lite-reviewer"
performed_at: "2026-08-26 10:41:45"
---

# Plan Check Lite Reviewer Report

## Verdict
수정 필요

## Blocking Issues

1. **`twitter_py_full_*.json` 재생성 단계가 실행 절차(3.2 S3)에서 빠져 있다.** `total_scrap.py:499`의 `merge_results()`는 `find_latest_full_file(OUTPUT_TWITTER_DIR, "twitter_py_full_*.json")`로 **full 파일만** 읽어 `output_total`에 병합한다. 그런데 이번 계획이 고치는 `twitter_scrap.py`는 `twitter_py_simple_*.json`만 쓴다(`twitter_scrap.py:36` `OUTPUT_FILE_PATTERN = "twitter_py_simple_{date}.json"`). simple → full 변환은 별도 컨슈머 `twitter_scrap_single.py`가 담당하며, 이 스크립트는 `is_detail_collected=True`인 게시물만 골라 `full_map`에 병합해 full 파일을 다시 쓴다(`twitter_scrap_single.py:159-165`). 계획서 S3(126-136행)는 "① simple 백업·갱신 → ② `merge_results()`"만 명시하고, 그 사이에 `twitter_scrap_single.py`(또는 동등한 consumer) 실행을 포함하지 않았다. 이 순서대로면 새로 확보한 74건 지표는 simple 파일에만 남고 `output_total`에는 반영되지 않아, 계획 자신이 세운 완료 기준 V1(지표 보유 94건 이상)과 V5(통합본 커버리지 97% 이상)를 통과할 수 없다. AGENTS.md의 "LinkedIn 본문·메타데이터를 durable하게 바꾸려면 최신 `linkedin_py_full_*.json`도 함께 맞춘 뒤 통합본을 재생성한다"는 규칙과 동일한 구조가 X에도 성립하는데, 계획서에 이 단계가 누락돼 있다.

2. **롤백(3.4)이 같은 이유로 닫혀 있지 않다.** 위 1번을 고치기 위해 `twitter_scrap_single.py` 실행을 절차에 추가하면 full 파일이 실제로 덮어써지는데, 3.4는 "simple 백업본 복원 → `merge_results()` 재실행"만 명시한다. full 파일의 백업·복원 경로가 없으므로, S2/S3 진행 중 실패 시 consumer가 이미 덮어쓴 `twitter_py_full_*.json`은 되돌릴 방법이 계획에 없다. "기존 95건은 어느 단계에서 실패하든 그대로 남는다"는 계획서 문장은 full 파일까지 포함하면 성립하지 않는다.

## Required Fixes

1. `twitter_scrap.py:445` 인용 오류. 실제 파일에서 445행은 `else:`이고, 계획서가 인용한 `if consecutive_no_new >= 5: break`는 449행이다. 인용 코드 내용 자체는 정확하나 줄번호가 4행 어긋나 있다 — 실행 시 참조하는 사람이 잘못된 줄을 편집할 수 있으므로 445 → 449로 정정할 것. (나머지 인용 `twitter_scrap.py:362`, `scripts/auth_runtime/verify_x_auth.py:108`, `utils/post_meta.py:71`은 모두 실제 파일 내용과 일치함을 확인했다.)

2. S1 "`wait_for_response` → `expect_response` 교체"의 구현 방식이 명시돼 있지 않다. 설치본 Playwright 1.58.0의 sync `Page`에는 `wait_for_response`가 없음은 실측으로 확인했다(`dir(Page)`에 `expect_response`만 존재). 다만 `expect_response`는 컨텍스트매니저로, 응답을 유발하는 동작(`page.goto`)을 감싸야 레이스 컨디션 없이 동작한다. 현재 코드는 `page.goto()`가 이미 완료된 뒤(twitter_scrap.py:373, verify_x_auth.py:106) 별도 함수 `wait_for_bookmark_signal`에서 대기하는 구조이므로, 단순 이름 치환만 하면 응답이 이미 도착한 뒤 리스닝을 시작해 여전히 5초 blind wait(`except: page.wait_for_timeout(5000)`)로 떨어지는 동일 증상이 재현될 위험이 있다. `with page.expect_response(predicate, timeout=10000) as info: page.goto(...)` 형태로 goto 호출 자체를 감싸도록 재구조화가 필요하다는 점을 계획에 명시해야, S2가 전제하는 "결정적 첫 응답 캡처"가 실제로 보장된다.

3. V7 "변경 범위 관련 pytest 통과"가 대상 테스트를 구체적으로 지정하지 않았다. 레포에 이미 관련 테스트 `tests/unit/test_twitter_auth_detection.py`, `tests/unit/test_x_auth_probe.py`, `tests/integration/test_auth_runtime_sync.py`가 존재한다. AGENTS.md는 "검증은 변경 범위에 맞는 현재 테스트로 수행한다"고 명시하므로, V7에 이 파일들을 명시적으로 못박는 것이 실행 가능성을 높인다.

4. (참고, 승인 근거 확인됨) 5절 "후속" 분리는 검수 대상이 아니다 — 제공된 컨텍스트에 따르면 버튼 제거·회귀 테스트 전환은 사용자가 앞선 대화에서 이미 "X 작업 이후에 처리"하기로 합의한 항목이므로, 승인 없는 축소가 아니라 사용자 승인이 선행된 순서 조정으로 판단했다.
