---
title: "유튜브-요약-파이프라인-재설계-구현계획"
created: "2026-08-25 19:06"
session_id: "8726f5e3-015c-4a6c-bd4b-aa0f7130c887"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/8726f5e3-015c-4a6c-bd4b-aa0f7130c887.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-25 19:06:45"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

## Blocking Issues
1. 중간 뷰어 검증(g4) 시점과 통합본 생성 순서 모순
   - 근거: 계획서 2-F 표, 2-F-3 검증 명령, 3.0 실행 순서
   - 실행 전 막히는 이유: 요약(Wave D) 각 웨이브 직후 게이트 g4에서 뷰어 카드를 확인하도록 요구하나, 정작 뷰어에 최신 데이터를 반영하는 `total_scrap.py --mode update` 및 서버 재시작은 맨 마지막 단계인 STEP 3(Wave E)에 몰려 있습니다. 따라서 W0 종료 직후 2-F-3의 검증 스크립트를 돌리면 뷰어가 업데이트되지 않은 과거 데이터를 서빙하므로 새 요약 형식이 보이지 않아 검증이 반드시 실패합니다. 웨이브 종료마다 통합본 재생성을 추가하거나, g4 검증 시점을 STEP 3으로 통일하여 모순을 풀어야 합니다.
2. 명령 실행 블록의 PowerShell 실행 불가 (Bash 전용 구문 사용)
   - 근거: 계획서 2-F-3 (`WAVE=W0`, `IDS=$(...)`), 3.5.2 (`for id in ...; do`, `grep -E`)
   - 실행 전 막히는 이유: 사용자의 기본 셸은 Windows PowerShell이나, 제공된 명령어들이 Bash 전용 변수 대입, 명령어 치환(`$()`), 반복문(`for ... do`), 그리고 윈도우에 없는 `grep` 명령어로 작성되어 있어 그대로 복사해 실행할 경우 구문 오류로 차단됩니다. PowerShell 호환 문법(`$WAVE="W0"`, `$IDS = python ...`, `ForEach-Object`, `Select-String` 등)으로 변환해야 합니다.

## Required Fixes
1. **승인 필요 사유 (R4 백로그 이관)**: SPEC 요구사항 R4(신규 영상 수집이 뷰어 업데이트 버튼을 오래 묶지 않아야 한다)를 "일상 수집에서는 이미 막히지 않는다"는 실측을 근거로 구현 범위에서 제외하고 백로그로 이관(3.4절)했습니다. 원 요청에 포함된 사항을 후속 과제로 분리했으므로 진행 전 사용자 승인이 필요합니다.
2. 3.5.2절의 `MANUAL_SUBTASK_SESSION_IDS` 세션 5건 추가 지시(a항)가 사용자 수동 작업임을 명시해야 합니다. 후속 스크립트(`backfill-ai-cli-subtasks.mjs`)가 이 배열을 자동으로 채워주지 않으므로, 스크립트 실행 전 사용자가 `session-subtask-classifier.js`를 직접 편집해야 한다는 선행 지시가 누락되어 있습니다.

## 참고 제안 (판정에 미반영)
- 2-F-2의 날짜 필터(`--added-min-days 30`, `--added-max-days 30`)를 구현할 때, '30일 이내'와 '30일 초과' 조건 사이에서 30일째 되는 데이터가 누락되거나 중복 처리되지 않도록 코드 수준의 부등호(`<`, `>=`, `<=`) 경계 처리에 유의해야 합니다. (실제 데이터에 해당 명령을 대입해 본 결과, 76건의 갯수는 빈틈없이 정확하게 분리되는 것을 확인했습니다.)
- 웨이브 커밋 단계(A1, A2, A3 등)가 3.0-R에 명시되어 있으나, 정작 STEP 0~2의 작업 목록에는 어느 시점에 어떤 코드를 커밋할지 구체적 매핑이 없어 혼동의 여지가 있습니다.
