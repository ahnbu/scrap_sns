---
title: "뷰어-저자필터-구현계획"
created: "2026-08-24 13:40"
session_id: "97519bd7-9dd4-47eb-82f1-c5f5643d57a1"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/97519bd7-9dd4-47eb-82f1-c5f5643d57a1.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 13:40:44"
---

# Plan Check Reviewer Report

## Verdict
실행 가능

## Blocking Issues
없음

## Required Fixes
1. **Threads 백필 스크립트 Rate Limit 방어 누락**: 3-7에서 407명의 프로필을 조회할 때 단기간에 요청이 몰리면 Threads(Instagram) WAF에 의해 IP 차단을 당할 리스크가 큽니다. 구현 시 스크립트에 반드시 `time.sleep(2)` 등의 딜레이(지연 시간) 로직을 추가하여 속도를 조절하도록 명시해야 합니다.
2. **빈 `username` 클릭 비활성화 로직 부재**: 3-2 (3) 텍스트에서는 "`username`이 빈 문자열인 게시글은 필터 키를 만들 수 없으므로 클릭 비활성화한다"고 올바르게 명시했으나, 함께 제공된 코드 스니펫에는 빈 `username`일 때 리스너를 달지 않거나 반환하는 방어 로직이 빠져 있습니다. 구현 시 `if (!authorKey.username) return;` 혹은 이벤트 바인딩 제외 처리를 스크립트에 반영해야 합니다.

## 참고 제안 (판정에 미반영)
- **백필 로그의 식별자**: 3-7의 백필 로그 작성 시 단순히 성공/실패 횟수만 기록하지 말고, 어떤 `username` 조회가 실패했는지 로그 파일에 구체적으로 남기면 향후 수동 검증이나 재처리에 큰 도움이 됩니다. (계획서의 "실패 목록"이 이를 의미하는 것으로 보이나, 로그 내용에 명시할 것을 권장합니다.)

---
**확인 요청하신 항목에 대한 답변:**
- (a) 카드 텍스트에 `full_name`이 없다는 근거는 타당합니다. `inner_text()`는 화면에 보이는 텍스트만 긁어오므로 UI상 계정 아이디만 렌더링된다면 DOM 경로에서 `full_name`을 추출할 수 없습니다.
- (b) 백필 일원화 방식은 타당하며 실질적으로 신규 발생 차단을 대체합니다. 수집기를 무리하게 건드리지 않고, 정기 수집 이후 백필 스크립트를 주기적으로 실행해 보완하는 운용 방식이 더 안전합니다.
- (c) `thread_scrap.py`를 변경하지 않음으로써 오히려 레거시 코드나 다른 DOM 수집 로직이 깨지는 리스크를 피했으므로, 남겨진 결함은 없으며 올바른 결정입니다.
- 추가된 12개의 Playwright 시나리오는 DOM 셀렉터, 상태 카운트 계산, CSS 좌표 비교(`getBoundingClientRect()`) 등 객관적 assertion이 가능하므로 `done-check`의 증거 기반 판정에 충분합니다. 빠진 핵심 항목은 없습니다.