---
title: "참여지표-커버리지-확대-웨이브계획"
created: "2026-08-24 19:41"
session_id: "8fdf5458-a4b1-43eb-a5ef-4d722ba553b4"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/8fdf5458-a4b1-43eb-a5ef-4d722ba553b4.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 19:41:58"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

이 계획은 기존 데이터의 지표 누락 원인을 정확히 짚었고, 웨이브별 롤백 단위와 데이터 소실 방어(W3-A 보존 가드)까지 고려되어 있어 **목적을 성공적으로 달성할 가능성이 매우 높습니다.** 
트레이드오프 측면에서도 소수 리포스트 지표 10건(BL-0824-07)과 인증 실패 UI 구현(BL-0504-02)을 포기하는 대신, 핵심 지표 수집 커버리지를 5%에서 66%로 안전하고 빠르게 끌어올리는 현실적인 접근을 취했습니다. 과최적화를 피하고 발견성(W1)을 먼저 확보한 점도 훌륭합니다.

다만, UI 검증 자동화가 누락되어 있고 실행 불가능한 명령어가 포함되어 있어 실행 전 일부 수정이 필요합니다.

## Blocking Issues
1. **사람 눈에 의존하는 UI 검증** / W1, W2, W3-B, W4 검증 기준 / "브라우저에서 성과순 선택", "실화면 캡처를 증거로 남긴다"라고만 서술되어 있어 AI(`done-check`)가 사람 눈 없이 코드로 검증할 수 없습니다. "사용자 화면 기준 조작"이라는 요구사항(AGENTS.md)을 충족하면서도 포커스를 뺏지 않으려면, **Headless Playwright 검증 스크립트**를 작성하여 화면 접근, 정렬 클릭, 스크린샷 캡처, 그리고 DOM상의 배지 개수를 읽어 종료코드와 로그로 출력하도록 검증 방식을 명시해야 합니다.
2. **명령어 Placeholder (`...`) 방치** / W3-B 작업 ① 파일럿 2번 항목 / 명령어 예시로 `scripts/recover_threads_detail_failures.py --codes ...`를 기재했습니다. 에이전트가 이를 그대로 셸에 입력하면 문법 오류로 막히게 됩니다. JSON에서 대상 50건의 code를 파싱해 인자로 넘기는 셸 파이프라인(`xargs` 등)이나 래퍼 스크립트를 구체적으로 명시해야 합니다.

## Required Fixes
- W4의 "181명을 20~30명 배치로 나눠 순회" 작업 시, `linkedin_scrap_by_user.py`가 자체적으로 배치 인자를 지원하지 않는다면 분할 처리를 위한 스크립트 실행 방식을 어떻게 할 것인지 확정해 적어야 합니다.
- 수집 스크립트(W2, W3-B, W4) 구동 시 브라우저 창이 떠서 사용자 포커스를 빼앗지 않도록 Headless 모드 실행 여부를 점검하거나 강제하는 절차를 추가해야 합니다.
- 자동 UI 검증을 위해 생성되는 Playwright 코드와 결과 스크린샷 원본의 처리 방향(공통 규칙에 따라 스크래치패드에 저장하고 커밋에서는 제외할 것인지 등)을 명확히 명시해야 합니다.

## 참고 제안 (판정에 미반영)
- W3-C 마이그레이션 직전, W3-B 실행 결과로 인해 `-1` 잔존 건수가 26건에서 변동되었을 상황을 대비해 `jq` 등을 활용한 건수 확인 명령어(dry-run 보조 수단)를 계획에 미리 적어두면 실행 단계의 불확실성을 줄일 수 있습니다.
- 대규모 백필 작업(W3-B, W4) 중 레이트리밋이나 차단이 발생할 경우의 쿨다운(대기 시간) 정책을 추가해두면, 에이전트가 예기치 않은 에러 로그에 당황해 멈추는 것을 막을 수 있습니다.