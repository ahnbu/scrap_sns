---
title: "뷰어정리-유튜브확대-지표갱신-웨이브계획_검수보고서_2회차"
created: "2026-08-26 11:10"
session_id: "7d3daa6a-72b3-4ae5-8431-95717010b657"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/7d3daa6a-72b3-4ae5-8431-95717010b657.jsonl"
ai: "agy"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

## 1회차 지적 해소 확인
- **B1. W2-b 실행 명령 placeholder (`<배치크기>`)**
  - **해소**: 3절 W2-b에서 명령어를 `--max-summaries 50`으로 확정하고, 50으로 자른 구체적인 사유도 명시했다.
- **B2. 수동 GUI 위주의 검증**
  - **해소**: W1, W2, W5에 '자동 증거 기준' 검증표를 신설하여, e2e 테스트, grep 조회, pytest 결과(종료코드 0), API 및 파일 대조 방식 등으로 검증 방법을 자동화했다.
- **B3. 실패 복구/재처리 방안 누락**
  - **해소**: W2-b에 타임아웃/중단 시 `load_cached_summary()`의 3중 캐시 판정으로 재개됨을 명시했고, W2-b와 W5 시작 전 `git status` 커밋 여부 확인을 필수 조건으로 달아 롤백(git checkout) 안전망을 확보했다.
- **RF1. 인용 라인 번호 오류 정정**
  - **해소**: 1.1절 3번 항목에서 `web_viewer/script.js:2464`로 올바르게 인용 및 정정되었다.
- **RF2. W1-T 수동 검증 선언 수정**
  - **해소**: W1-T 신규 테스트에 setup/teardown fixture를 도입하고 `byte` 단위 동일성 검사(assert)를 명시하여 자동 원복 구조를 확정했다.
- **RF3. 캐시버스터 식별자 명시 누락**
  - **해소**: 3.1절에 웨이브별로 기입할 식별자 값(`20260826-w1`, `20260826-w1t`, `20260826-w3`)을 표로 확정했다.

## Blocking Issues
1. **W1 V4 자동 검증 불가능 (`scrapRunInProgress` 캡슐화)**
   - 근거: `web_viewer/script.js:435`, W1 검증표 V4
   - 실행 전 막히는 이유: V4는 e2e 환경(Playwright)에서 `scrapRunInProgress` 플래그를 참으로 설정할 것을 요구한다. 하지만 해당 변수는 `DOMContentLoaded` 내부 스코프에 묶여 있어 `window` 객체를 통한 외부 조작이 불가능하다. 앱의 실행 버튼(UI)을 눌러 플래그를 세우면 실제 `/api/run-scrap` API가 호출되어 "실제 수집을 검증에 끼우지 않는다"는 전제 조건과 충돌한다. 변수 노출이나 네트워크 Mocking 같은 구체적 우회 방안이 계획에 명시되어야 한다.

2. **W5 V6 완료 팝업 노출 목표 및 로그 검증 불가 (`scrap_sns_server.py`의 로그 필터링)**
   - 근거: 3절 W5 "운영 로그" 내용, `scrap_sns_server.py`의 `_scrap_progress_message_from_line` 함수 구조
   - 실행 전 막히는 이유: W5는 `total_scrap.py`의 stdout에 "지표 갱신 12건 · 값이 바뀐 글 3건" 패턴의 로그를 출력하고, 이를 `logs/scrap_progress.log`에 남기거나 뷰어 팝업에 노출시키려 한다. 하지만 현재 서버는 `_scrap_progress_message_from_line`에 미리 정의된 정규식(화이트리스트)에 맞지 않는 모든 로그를 `None`으로 버린다. W5에서 새 패턴의 로그를 추가해도 서버 코드를 수정하지 않으면 팝업과 로그 파일에 절대 도달할 수 없어 V6 검증을 통과할 수 없다. 계획서에 서버의 로직 수정 내용이 추가되어야 한다.

## Required Fixes
1. **W1-T T3 서버 API 엔드포인트 명칭 정정**
   - 내용: 계획서에서는 테스트 검증 시 `GET /api/tags` 와 `PUT/POST`를 호출한다고 명시했으나, 실제 `scrap_sns_server.py`의 엔드포인트는 `GET /api/get-tags`와 `POST /api/save-tags`이다. 이를 실제 라우트에 맞게 정정해야 한다.
2. **W5 롤백 대상 경로에 `output_linkedin/` 추가**
   - 내용: 3절 W5의 부분 실패 처리에서 롤백 명령어로 `git checkout -- output_threads/ output_youtube/`를 명시했으나, W5의 정책 승격 결과 LinkedIn 데이터 역시 영향을 받아 갱신될 수 있다. 롤백 대상 경로 및 착수 전 필수 커밋 확인 항목에 `output_linkedin/`도 명시해야 완전한 안전망이 된다.

## 참고 제안 (판정에 미반영)
- 없음
