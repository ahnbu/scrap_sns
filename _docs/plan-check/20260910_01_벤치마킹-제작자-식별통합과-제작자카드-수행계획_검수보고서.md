---
title: "벤치마킹-제작자-식별통합과-제작자카드-수행계획"
created: "2026-09-10 13:48"
session_id: "4b0993d8-e622-4654-9cd7-61b93dafdb74"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/4b0993d8-e622-4654-9cd7-61b93dafdb74.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-10 13:48:36"
---

# Plan Check Reviewer Report

## Verdict
승인 필요

**승인 필요 사유:**
사용자의 원 요청이었던 "뷰어 안에서 게시글(SNS·파일·URL)을 한눈에 보고 싶다"와 "스레드 프로필 등을 통한 채널 자동보강" 요구사항을 계획 문서(§3.1, §3.3, §7)가 리스크와 효율성을 이유로 명시적으로 기각하고 후속 과제 및 옵시디언 링크(B안)로 대체했습니다. 사용자가 최초 요구한 핵심 기능을 계획이 배제하거나 미뤘으므로 승인이 필요합니다.

## Blocking Issues
1. 영구 데이터 마이그레이션 시 백업/롤백 대책 누락 / `scripts/migrate_linkedin_author_key.py` 신규 생성 (W2 T2-b) / `output_linkedin_user` 하위의 JSON 파일은 git으로 추적되지 않는 영구 원천 데이터입니다. 마이그레이션 스크립트 실행 중 실패하거나 오류가 발생할 경우 데이터가 영구 손실되므로, 실행 전 원본의 사전 백업(backup)과 롤백 로직이 반드시 계획에 포함되어야 합니다.
2. 마이그레이션 대상 파일 경로 오류 / `total_scrap.py:46, 679` 및 `W2 T2-b` / 계획서(T2-b)는 마이그레이션 대상 파일을 `output_linkedin_user/linkedin_user_full_*.json`으로 명시했으나, 코드 베이스 상 실제 파일 위치는 `output_linkedin_user/python/linkedin_user_full_*.json`입니다. 계획된 잘못된 경로를 그대로 사용하면 스크립트가 파일을 찾지 못해 반드시 실패합니다.

## Required Fixes
1. 실행 불가 placeholder 제거: T1-e 완료 기준의 `ACoAAAzvikMB…`, T3-a의 API 경로 `GET /api/creator-profile?account_id=…`, T3-c의 URI 스킴 `obsidian://open?vault=…&file=…` 등 서술과 예시에 `…` 형태의 placeholder가 남아있습니다. 실행 에이전트의 혼선을 막기 위해 기본값이나 실제 사용할 명확한 형태(패턴)로 대체해야 합니다.
2. 검증 증거 산출물 처리 방침 명시 누락: §5 검증 방법에서 "각 단계 캡처를 `_docs/evidence/`에 남긴다"고 명시했으나, 생성된 캡처 이미지를 커밋에 포함할 것인지, `.gitignore`를 통해 로컬에만 남길 것인지 의도가 닫혀 있지 않아 확정해야 합니다.
3. 명시적 웨이브 블록 부재: 선후관계가 있는 병렬/웨이브 실행 계획임에도 불구하고, `## 실행 웨이브` 또는 `## 웨이브 구조`라는 명시적인 헤더 블록이 없습니다. 문서 내 `### W1` 형태의 일반 헤더만 사용하면, 실행 단계에서 에이전트가 파일 충돌 기준으로 태스크를 임의 재분류하여 의도한 선후관계가 깨질 위험이 있습니다.

## 참고 제안 (판정에 미반영)
- **채택된 접근법의 트레이드오프**:
  - **T1-b 표식 재계산**: 1회성 스크립트에 그치지 않고 계정/채널 추가 시마다 표식이 자동 갱신되는 유지보수상의 큰 이점(얻는 것)이 있으나, 매 통합마다 전체 레코드 대조 연산이 추가되는 연산 비용(잃는 것)이 발생합니다. (문서에서 사전 인덱싱으로 이를 O(n)으로 제어하고 있어 훌륭한 방어 설계입니다.)
  - **제작자 카드 외부 연동(B안)**: 뷰어 내장형 구조(C안)를 포기해 구현 복잡도를 대폭 줄이고 기존 옵시디언 뷰를 재활용하는 실용성(얻는 것)을 취했지만, 사용자가 파일 목록을 열람할 때 브라우저 뷰어에서 데스크탑 앱으로 컨텍스트(화면) 전환이 강제되는 단점(잃는 것)이 있습니다.
- **런타임 정규화 위치 일원화 검토**: T2-a에서 `to_standard()`를 통해 `username`을 후처리 정규화하는 방식도 좋으나, 근본적으로 `linkedin_scrap_benchmark.py` 수집기 자체가 데이터 추출 시점부터 표시명 대신 내부 ID를 사용하도록 로직을 일원화하는 편이 파이프라인 전반의 데이터 일관성 유지에 더 유리할 수 있습니다.