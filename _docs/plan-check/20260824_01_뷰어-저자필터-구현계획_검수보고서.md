---
title: "뷰어-저자필터-구현계획"
created: "2026-08-24 13:33"
session_id: "97519bd7-9dd4-47eb-82f1-c5f5643d57a1"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/97519bd7-9dd4-47eb-82f1-c5f5643d57a1.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-24 13:33:26"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
1. **DOM 경로 데이터 추출 방식의 모순 (`thread_scrap.py`)**
   - **지적**: DOM 기반 수집(`initial_dom`, `scroll_dom`)에서 `full_name`을 얻기 위해 "페이지 JSON 페이로드"를 파싱한다는 계획은 작동하지 않습니다.
   - **근거**: `thread_scrap.py:664` 라인을 보면 현재 DOM 수집은 화면 요소의 `innerText`(`raw_text = element.inner_text()`)를 읽는 방식입니다. 특히 스크롤로 동적 로드되는 `scroll_dom` 게시글은 초기 페이지 JSON 페이로드에 아예 존재하지 않습니다.
   - **실행 전 막히는 이유**: 계획서 지시대로 구현 에이전트가 JSON 페이로드를 뒤지기 시작하면 `scroll_dom` 등에서 데이터를 찾지 못해 실패합니다. 해당 하드코딩 2줄(`:307`, `:711`)을 제거할 때, `innerText`를 줄바꿈으로 나눈 `raw_text.split('\n')`의 첫 번째 줄(`lines[0]`)을 활용하여 `display_name`을 추출하도록 계획을 구체적으로 정정해야 합니다.

2. **UI 검증의 자동화 누락 및 육안 판정 의존**
   - **지적**: 4장 검증 계획이 `done-check` 도구가 사람 눈 없이 로그와 종료코드만으로 판정해야 한다는 원칙에 위배됩니다.
   - **근거**: 4장 검증 계획 본문 ("`wscript sns_hub.vbs`로 ... 사용자 조작 순서대로 확인하고 화면 캡처를 증거로 남긴다.")
   - **실행 전 막히는 이유**: 브라우저 화면 캡처와 수동 조작으로는 후속 `done-check` 에이전트가 증거 기반 완료 판정을 내릴 수 없습니다. Playwright 등을 이용한 자동화된 헤드리스(headless) E2E UI 테스트 스크립트를 작성하여, 1~11번 시나리오의 DOM 변화와 필터 동작을 프로그램 코드(assertion)로 검증하고 성공/실패 로그 및 종료코드를 남기도록 계획을 전면 수정해야 합니다.

## Required Fixes
1. **자동 검증 산출물 처리 방침 명시**: 새로 추가될 Playwright 등 UI 자동화 검증 스크립트 파일과 그 실행 결과 로그(증거)를 레포지토리에 커밋할지, 혹은 `.gitignore` 등에 따라 일회성(local-only)으로 폐기할지 명시해야 합니다.
2. **백필 실행 로그 산출물 처리 명시**: `scripts/backfill_threads_display_name.py` 실행 시 발생하는 성공/부분 실패 내역 등의 로그 리포트를 커밋에 포함할지 여부가 누락되어 있으므로 추가 바랍니다.

## 참고 제안 (판정에 미반영)
- **키보드 접근성 개선**: `script.js`의 `authorLink` 키보드 이벤트 리스너에서 Space키(` `)를 누를 때 기본 스크롤 동작이 발생할 수 있습니다. `e.preventDefault();` 처리를 추가하는 것을 권장합니다.
- **배지 텍스트 렌더링**: `updateGlobalTags()`에서 상단 배지에 `currentAuthor.label`을 표시할 때, HTML 인젝션 등을 방지하기 위해 뷰어에 이미 정의된 `escapeHtml()` 처리를 씌우는 것이 안전합니다.