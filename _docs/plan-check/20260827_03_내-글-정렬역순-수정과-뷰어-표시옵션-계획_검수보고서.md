---
title: "내-글-정렬역순-수정과-뷰어-표시옵션-계획"
created: "2026-08-27 13:58"
session_id: "3e2e2056-1ccd-4988-a6f0-a055490ef000"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/3e2e2056-1ccd-4988-a6f0-a055490ef000.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 13:58:19"
---

# Plan Check Reviewer Report

## Verdict
수정 필요

## Blocking Issues
1. 지적: 수동 GUI 검증(V7, V8, V9, V10, V13) 불가
   근거: 7절 검증 계획 표 내부 ("뷰어 화면에서 확인", "최종 육안", "실제 화면에서 MY 필터 켜고 스크롤" 등)
   실행 전 막히는 이유: 향후 작업 완료 검수(`done-check`)를 수행할 AI 에이전트는 Headless 환경에서 동작하므로, 사용자의 시각적 브라우저 조작이나 "육안 확인"을 요구하는 단계를 수행할 수 없으며 이로 인해 검증 단계에서 반드시 실패한다.

## Required Fixes
1. 7절의 V7, V8, V9, V10, V13 수동 검증 항목을 폐기하고, 기존 E2E 테스트(`tests/e2e/test_my_posts_filter_ui.py`)에 해당 UI 요소 렌더링 및 정렬 검증 케이스를 추가하여 자동화(Headless 환경 실행 및 종료 코드 기반 검증)하도록 계획을 수정해야 한다.
2. T1(3.2절)의 `own_batch_keys` 별도 맵(`id(p)` 의존 방식) 분리 설계를 폐기하고, 게시물 데이터에 직접 `_own_batch_key` 속성을 임시 주입하는 방식으로 변경해야 한다(과최적화). 별도 맵을 유지하면 `_saved_at_key` 함수가 해당 맵에 접근할 스코프가 없어 파이썬 실행 에러가 발생한다. 대신 객체에 임시 필드를 추가하여 정렬을 마친 뒤, 파일 저장 전 단계(`total_scrap.py`의 `save_total()` 내부 `reorder_post()` 호출 직전)에서 해당 키를 `pop()`하여 제거하면 스키마 오염을 깔끔하게 막을 수 있다.

## 참고 제안 (판정에 미반영)
1. **T3-b 라벨 형태의 트레이드오프**: 5절 D4 권장안에 따르면 ALL 목록에서 내 글이 숨겨진 전체 수(2454건)가 분모가 되며, MY 버튼을 켰을 때는 `68 / 2454 건`처럼 표시되어 직관적 수학과 다소 어긋난다. 이는 기존 E2E 테스트의 라벨 파싱 정규식(`숫자 / 숫자`) 구조를 깨지 않고 개발 범위를 좁히기 위한 타협점임을 명시적으로 인지하고 진행하면 된다.
2. **`check_platform_order_regression.py` 상태 점검**: V5에서 해당 스크립트를 사용해 저장글의 정렬 순서 훼손을 검증한다고 명시되어 있다. 실측 결과 해당 스크립트가 디렉토리에 실재하는 것은 확인되었으나, W3 웨이브 착수 전에 스크립트가 정상 동작하는지 테스트 런을 가볍게 수행해보는 것이 안전하다.