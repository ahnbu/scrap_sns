---
title: "링크드인-이미지-복구와-외부요약-자동화-아이콘-개선-계획"
created: "2026-09-01 12:08"
session_id: "931037b9-008c-4bef-b378-a8e38ac862fd"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/931037b9-008c-4bef-b378-a8e38ac862fd.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-09-01 12:08:14"
---

---
title: Done Check Reviewer Report
created: 2026-09-01 12:05
session_id: 931037b9-008c-4bef-b378-a8e38ac862fd
session_path: C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/931037b9-008c-4bef-b378-a8e38ac862fd.jsonl
ai: claude
---

# Done Check Reviewer Report

## Verdict
미완료

## Verification Audit

| 단계 | 검증 항목 | 상태 | 근거 및 비고 |
|---|---|---|---|
| 웨이브 1 | 1-V1 (pytest) | 통과 | `python -m pytest tests/unit -q` (447 passed) |
| | 1-V2 (만료 판정 테스트) | 통과 | `test_media_expiry.py` 실행 완료 (22 passed) |
| | 1-V3 (대상 선정 테스트) | 통과 | `test_select_image_download_posts.py` 실행 완료 |
| | 1-V4 (재수집 완주) | 통과 | `scripts/rebuild_total.py` 완료 로그 (`rebuild_total.log`) |
| | 1-V5 (로컬 확보 건수) | 통과 | LinkedIn 471/472 (99.8%) 확보 |
| | 1-V6 (헤드리스 검증) | 통과 | `verify_linkedin_images_headless.mjs` 통과 (100% 로드) |
| 웨이브 2 | 2-V1 (빌더 dry-run) | **확인 불가** | `--dry-run` 실행 증거 없음 |
| | 2-V2 (빌더 단위테스트) | **확인 불가** | `node --test tests/unit/test_build_external_summaries.mjs` 실행 증거 없음 |
| | 2-V3 (실패 내성) | 통과 | 몽키패치 테스트(`False` -> `True`) 완주 확인 |
| | 2-V4 (JSON 갱신) | 통과 | `sns_external_summaries.json` 당일 갱신 및 카운트 증가 |
| | 2-V5 (요약 링크 검증) | **미수행** | `verify_external_summary_links_headless.mjs` 신규 작성 및 실행 누락 |
| 웨이브 3 | 3-V1~V3 (아이콘 검증) | 통과 | `verify_external_summary_icons.mjs` 확장 후 9/9 통과 |

## Blocking Findings

- Claim: 계획에 명시된 2-V5 신규 헤드리스 검증 스크립트가 작성되지 않고 검증이 누락되었다. (요구사항 누락)
- Evidence: `ls scripts/verify_external_summary_links_headless.mjs` 실행 결과 해당 파일이 작업 디렉토리에 존재하지 않음 (os error 2). 사용자 요약 10번 항목은 육안 확인(주장)일 뿐 자동화 스크립트 수행이 아님.
- Impact: 유튜브 카드에 외부 요약 링크가 올바르게 삽입되는지 보장하는 자동화된 회귀 검증 체계가 구성되지 않음.
- Severity: Important
- Fix: `scripts/verify_external_summary_links_headless.mjs` 스크립트를 구현하고 실행하여 exit 0을 확인하라.

## Advisory Findings

- Claim: 신규 검증 스크립트 `scripts/verify_linkedin_images_headless.mjs`가 `.gitignore`에 의해 커밋 대상에서 제외되어 있다.
- Evidence: `git check-ignore -v scripts/verify_linkedin_images_headless.mjs` 명령 결과 `.gitignore`의 44번째 줄 `scripts/*` 규칙에 의해 무시됨.
- Impact: 작성한 검증 스크립트가 커밋에 포함되지 않아 영구 유실된다.
- Severity: Advisory
- Fix: `.gitignore`에 `!scripts/verify_linkedin_images_headless.mjs` 예외를 추가하라.

- Claim: 실행 과정에서 생성된 로그 파일들이 커밋 대상(untracked)으로 남아 있다.
- Evidence: `git status` 결과 `_docs/evidence/linkedin_all_scan.log`와 `_docs/evidence/rebuild_total.log`가 untracked 파일(`??`)로 나타남.
- Impact: 일회성 디버깅/실행 기록이 저장소 히스토리를 오염시킬 수 있다.
- Severity: Advisory
- Fix: 해당 로그 파일들을 삭제하거나, `.gitignore`에 `_docs/evidence/*.log` 규칙을 추가하여 무시하라.

## Unverified Concerns

- Claim: 2-V1과 2-V2의 실행 증거가 누락되었다.
- Evidence: 제공된 실행 명령 목록에 `node scripts/build_external_summaries.mjs --dry-run` 및 `node --test tests/unit/test_build_external_summaries.mjs`의 수행 내역이 없음 (파이썬 `pytest`만 보고됨).
- Impact: 외부 요약 빌더의 dry-run 및 Node.js 단위 테스트 무결성이 보장되지 않음.
- Severity: Important
- Fix: 해당 Node.js 테스트 및 dry-run 명령을 실행하고 로그(exit 0)를 확보하라.

## Rationale

- **11번 판단(script.js) 타당성**: `web_viewer/script.js`의 diff를 직접 분석한 결과, `getBestImageSource`는 이미지 URL 문자열을 반환하는 데만 사용되며, `EXTERNAL_SUMMARY_SERVICES` 등은 HTML 배지 렌더링에만 국한됩니다. 전역 데이터(array) 필터링이나 뷰어 카운트 갱신 로직에는 전혀 영향을 미치지 않습니다. 따라서 `2604건 vs 2533건` 오류를 본 작업 범위 외(BACKLOG)로 분리한 판단은 코드 기반으로 볼 때 완전히 타당하며 Blocking 사유가 아닙니다.
- **1-3 조건 정정 타당성**: `linkedin_scrap.py:325-345`를 확인했습니다. 이미지가 없는 텍스트 전용 포스트(`media`가 빈 배열)의 경우 `has_live_media_url`이 `False`를 반환합니다. 계획 초안대로 `not has_live_media_url(media)`만 썼다면 수많은 텍스트 포스트에서 조기종료 카운터가 불필요하게 0으로 리셋되어 무한 루프(크롤링) 상태에 빠졌을 것입니다. `bool(existing_media)`를 추가하여 "이미지가 존재하면서 만료된 경우"로 제한한 것은 정확하고 훌륭한 엔지니어링 픽스입니다.
- **영구 데이터 surface 확인**: `output_total/total_full_20260901.json` 등의 데이터 산출물은 `.gitignore`의 예외 패턴(`!output_total/total_full_*.json`) 덕분에 정상적으로 tracked(커밋 대상) 상태입니다. 즉 데이터 영구화 처리는 닫혀 있습니다. 단, 로그 파일과 신규 스크립트 누락 문제는 Advisory Findings로 안내했습니다.
- **최종 판정**: 요구사항 2-V5(신규 헤드리스 검증 스크립트 작성 및 실행)가 이행되지 않아 명백한 "요구사항 누락"에 해당하므로, 판정 기준에 따라 `미완료` 판정합니다.
