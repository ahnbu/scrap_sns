---
title: "유튜브-자막-언어폴백-결함수정과-요약-319건-소진-계획(실행완료)"
created: "2026-08-27 14:06"
session_id: "baae7242-ac6b-43ec-8a42-3d238feb877f"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/baae7242-ac6b-43ec-8a42-3d238feb877f.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 14:06:16"
---

# Done Check Reviewer Report

## Verdict
미완료

## Verification Audit
- `pytest tests/unit -q` (391 passed) 및 `tests/e2e/test_youtube_summary_recovery_ui.py` (6 passed)가 실행되었고 증거가 확인됨.
- 오염 잔존 확인(grep 결과 0건) 및 뷰어 검색(0건) 수행됨.
- `output_youtube/summaries/` 3건 및 `youtube_py_full_20260827.json` 파일의 데이터 정정이 대상 커밋(`e7c9b9d`) 내역과 일치함.
- `youtube_scrap.py`의 핵심 구현과 `tests/unit/test_youtube_transcript.py`에 추가된 7건의 테스트 코드 반영을 확인함.

## Blocking Findings
- Claim: 계획 문서 10.5절의 명시적 제외 서술과 달리, 대상 커밋에 통합본과 타 세션 작업물이 모두 포함되어 커밋됨
- Evidence: `git -C D:/vibe-coding/scrap_sns show --stat e7c9b9d` 명령 출력 (제외했다던 `output_total/total_full_20260827.json` 및 `my_threads_scrap.py`, `threads_own_full_20260827.json` 등 타 세션 관련 파일 30개가 포함됨)
- Impact: 타 세션의 미검증/미승인 작업물(Threads 관련)과 파생 산출물이 의도치 않게 커밋되어 리포지토리 형상과 계획 문서(10.5절)의 통제 의도가 명백히 충돌함.
- Severity: Important
- Fix: 커밋 `e7c9b9d`를 리셋하고 10절에서 의도한 범위의 파일만 분리하여 커밋하거나, 10.5절의 서술을 실제 커밋 형상에 맞게 정정해야 함.

## Advisory Findings
- Claim: `sanitize_summary()`의 HTML 주석 일괄 제거로 인한 정상 텍스트 유실 가능성
- Evidence: `youtube_scrap.py` 596행 `HTML_COMMENT_RE = re.compile(r"<!--.*?-->", re.DOTALL)`
- Impact: 영상 자체가 HTML 주석 문법을 설명하는 등 정당하게 생성된 주석 텍스트가 요약문에 포함될 경우 유실될 수 있음. (단, 이 트레이드오프는 10.3절에서 "요약문에 주석이 있을 이유가 없다"고 검토·수용되었음)
- Severity: Advisory
- Fix: 향후 실제 유실 문제가 보고되면, agy 완료 마커(`<!-- GOAL_COMPLETE -->`) 등 특정 패턴만 제거하도록 정규식을 좁히는 방안 고려.

- Claim: 닫히지 않은 HTML 주석(`<!--` 만 있는 경우)에 대한 테스트 케이스 누락
- Evidence: `tests/unit/test_youtube_transcript.py` 의 신규 테스트 7건
- Impact: 정규식 동작상 `-->`가 없으면 삭제되지 않으므로 텍스트가 통째로 삭제되는 위험은 없어 안전하나, 이 방어 동작이 테스트 코드에 명시적으로 검증되어 있지 않음.
- Severity: Advisory
- Fix: `test_sanitize_handles_unclosed_comment` 등의 단위 테스트를 보강하여 안전한 동작을 명문화할 것.

## Unverified Concerns
없음

## Rationale
- **정상 요약 훼손 가능성**: HTML 주석 문법 강의 등에서 텍스트 일부가 유실될 가능성이 기술적으로 존재합니다. 하지만 이 트레이드오프는 계획 10.3절에 기록되고 의사결정 되었습니다.
- **닫히지 않은 주석의 위험성**: 정규식 `<!--.*?-->`는 닫히는 `-->`가 없으면 매칭을 포기하므로, 문장 끝까지 잘려나가는 데이터 손실 위험은 없습니다. 동작 자체는 안전합니다.
- **캐시 무결성 문제**: `load_cached_summary()`의 반환값 변경은 캐시 적중 여부를 판단하는 해시 검증(`transcript_sha1`, `prompt_sha1`)에 영향을 주지 않습니다. 캐시를 읽을 때만 필터링하므로 무결성을 깨지 않으며, 정제 결과가 빈 문자열이 될 경우 `None`을 반환해 재요약을 유도하므로 캐시 설계의 전제와 안전하게 결합됩니다.
- **회귀 테스트 7건의 완전성**: 기본 마커 제거, 멀티라인, 여백 처리, None 값 입력 등 주요 사례를 훌륭히 방어하고 있으나, 위에서 언급한 닫히지 않은 주석에 대한 안전 동작 검증만이 누락되었습니다.
- **통합본 미커밋의 데이터 정합성**: 통합본은 개별 플랫폼별 원장(`youtube_py_full_*.json` 등)에서 언제든 100% 재생성 가능한 파생 데이터이므로 미커밋으로 두는 것 자체는 정합성에 문제를 일으키지 않습니다. 다른 세션의 작업을 섞지 않기 위한 합리적인 판단이었습니다.
- **계획 서술과 실제의 불일치**: 그러나 10.5절의 "제외했다"는 명시적 단언과 달리, 실제 대상 커밋(`e7c9b9d`)에는 배제하려 했던 통합본 및 타 세션 파일들이 통째로 섞여 들어갔습니다. 이는 커밋 범위 통제(실제 실패)에 해당하므로 미완료로 판정합니다.