---
title: "내-게시물-성과지표-통합-수집-계획"
created: "2026-08-26 13:27"
session_id: "bd25318e-1540-4c9c-8c65-2b611cf6d512"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/bd25318e-1540-4c9c-8c65-2b611cf6d512.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-26 13:27:04"
---

# Done Check Reviewer Report

## Verdict
완료

## Verification Audit
- **V1~V5 (어댑터 및 가드)**: 통과. `pytest tests/unit/test_my_posts_adapter.py -q` 실행으로 16건 통과가 확인되었습니다.
- **V6 (CLI 노출)**: 통과. `node utils/query-sns.mjs by-user 안병욱` 출력 결과 및 코드(`utils/query-sns.mjs` 내 `is_own_post: post.is_own_post === true` 매핑 추가)를 통해 화이트리스트 노출이 확인되었습니다.
- **V6b (뷰어 API 노출)**: 통과. `curl /api/posts` 결과 `is_own_post: true` 36건 확인 및 `utils/post_meta.py`의 `META_FIELDS`에 해당 필드가 등재된 것을 확인했습니다.
- **V6c (파일 경합 방지)**: 통과. `my_posts_scrap.py` 실행 증거 및 전용 파일 `linkedin_own_full_20260826.json` 생성이 확인되었습니다.
- **V6d (병합 소스 연결)**: 통과. `total_scrap.py`의 `merge_results()` 코드(line 623 부근)에서 내 게시물 소스(`OUTPUT_LINKEDIN_OWN_DIR`)를 병합하고 로그(`🙋 내 게시물 36건 병합`)를 출력하는 로직이 추가되었습니다.
- **V7 (통합본 정합)**: 통과. `output_total/total_full_20260826.json` 파일을 직접 검증(`grep -c '"is_own_post": true'`)하여 통합본 내에 36건의 내 글 데이터가 정확히 병합되어 있음을 확인했습니다.
- **V8~V11 (MY 필터 e2e)**: 통과. `tests/e2e/test_my_posts_filter_ui.py` 7건 통과 로그 및 캡처 증거 4장 확인되었습니다. `web_viewer/script.js` 내 필터링(`isOwnPost`) 및 상태 영속 로직(`localStorage.getItem(OWN_POSTS_ONLY_STORAGE_KEY)`)도 코드 레벨에서 확인했습니다.
- **V12 (인증 실패 경로 검증)**: 미수행 (Unverified Concerns 참고).
- **V13 (소요시간 회귀 검증)**: 미수행 (Unverified Concerns 참고).

## Blocking Findings
없음

## Advisory Findings
- **Claim: 계획과 달라진 점 - `merge_results()` 반환 arity 유지 및 `own_posts` 선순위 배치**
  - Evidence: `total_scrap.py` 코드(line 615, 627) 확인. 반환 시 `len(linkedin_posts) + len(own_posts)`로 합산하여 기존 호출부의 호환성을 지켰고, `all_posts` 병합 시 `own_posts`를 리스트 선두에 두었습니다.
  - Impact: 기존 인터페이스 하위 호환성을 유지하고 중복 제거 로직(먼저 발견된 것을 우선)을 역이용하여 `is_own_post` 등 더 풍부한 데이터가 보존되도록 한 정당한 구현 디테일입니다.
  - Severity: Advisory
  - Fix: 수정 불필요. 잘 판단된 결정입니다.
- **Claim: 기존 결함 - Contract 및 E2E 테스트 에러 (`test_latest_total_links_existing_local_image_files`, Playwright Sync API 에러)**
  - Evidence: `pytest tests/contract/test_schemas.py -q -k "test_latest_total_links_existing_local_image_files"` 직접 실행 결과, 내 게시물과 무관한 이미지 링크 유실 오류(Instagram/Threads 등으로 추정) 확인. `tests/e2e` 동시 실행 오류 또한 asyncio 루프 문제로 기존 결함임을 확인했습니다.
  - Impact: 본 구현과 무관한 기존 부채입니다.
  - Severity: Advisory
  - Fix: 추후 별도 백로그(이슈)로 분리해 해결을 권장합니다.

## Unverified Concerns
- **Claim: V12 인증 실패 경로 검증 미수행**
  - Evidence: 계획서 10.4 미수행 자기보고 확인. `my_posts_scrap.py` 내 `AuthRequired` 예외 처리 및 `exit_auth_required` 호출 코드는 정상적으로 작성되었으나(`except AuthRequired as exc:` 분기) 실제 실패 유발 테스트는 수행하지 않았습니다.
  - Impact: 코드 구조상 올바르게 동작할 것으로 보이나 인증 만료 시 기댓값대로 작동하는지 물리적 증거는 없습니다. 데이터 손실이나 호환성 파괴, 요구사항 자체의 누락이 아니므로 완료를 막는 Blocking Issue에 해당하지 않습니다.
- **Claim: V13 소요시간 회귀 검증 미수행**
  - Evidence: 계획서 10.4 미수행 자기보고. 개별 수집(39초)만 계측했고 오케스트레이션 실행 관점의 로그 실측치가 없습니다.
  - Impact: 개별 계측치가 계획(39초)과 일치해 큰 지연은 없을 것으로 판단됩니다. 이 역시 완료를 막을 수준의 결함이 아닙니다.

## Rationale
계획된 핵심 요구사항(내 글 수집 연동, 어댑터 변환, 정책 모듈 분기, 통합본 병합, 뷰어 내 MY 필터 적용)이 모두 올바르게 구현되었습니다.
- `utils/post_schema.py`, `utils/common.py`, `utils/post_meta.py` 및 `utils/query-sns.mjs` 등 데이터가 통과하는 관문들 전반에 걸쳐 `is_own_post` 필드가 적절히 화이트리스트 처리된 것을 코드로 확인했습니다.
- `metric_refresh.py`의 내 게시물 전용 정책(`fresh_post_days=None`, `run_limit=20`)이 정상 등록되었고, 해당 조건에서 신선도 검사를 올바르게 건너뜀을 확인했습니다.
- `my_posts_scrap.py`의 회귀 가드 코드(`REGRESSION_RATIO = 0.5`)도 설계 의도대로 포함되어 데이터 부분 덮어쓰기를 방지하고 있습니다.
- 영구 데이터 파급 검증 차원에서 `output_total/total_full_20260826.json`을 직접 검사해 36건의 내 글 데이터가 실제 통합본에 무사히 안착했음을 확인했습니다.
- V12, V13 항목의 검증이 누락되었으나, 두 항목 모두 블로킹 이슈 기준(데이터 손실, 보안 위험, 요구사항 누락 등)에 해당하지 않으므로 전체 판정을 `완료`로 부여합니다.
