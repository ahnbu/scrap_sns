---
title: "내-글-정렬역순-수정과-뷰어-표시옵션-계획"
created: "2026-08-27 14:24"
session_id: "3e2e2056-1ccd-4988-a6f0-a055490ef000"
session_path: "C:/Users/ahnbu/.claude/projects/D--vibe-coding-scrap-sns/3e2e2056-1ccd-4988-a6f0-a055490ef000.jsonl"
ai: "claude"
model: "Gemini 3.1 Pro (High)"
performed_at: "2026-08-27 14:24:28"
---

# Done Check Reviewer Report

## Verdict
완료

## Verification Audit
- **T1 (내 글 정렬 순서 수정):** `total_scrap.py`의 `merge_results()`에서 `crawled_at` 최솟값을 추출하여 임시 필드 `_own_batch_key`로 묶음 대표값을 부여하고, `platform_sequence_id`를 작성일 오름차순으로 재정렬함. `save_total()` 저장 직전 해당 임시 필드를 `pop()`하여 스키마 오염을 방지함. (V2-V4, V6 증거 기반)
- **T2 (MY 버튼 아이콘 제거):** `index.html`에서 MY 버튼의 `person` 아이콘(span)과 `gap-1.5` 여백이 제거됨.
- **T3 (표시 옵션 추가):** `index.html` 설정 모달에 '표시' 탭과 내 글 숨기기 토글(`<input type="checkbox">`)이 추가됨. `web_viewer/script.js`에 `sns_hide_own_posts` localStorage 저장과 MY 버튼 ON 시 숨김 토글이 켜져 있어도 내 글이 노출되는 상호작용 우선순위 로직이 구현됨.
- **T3-b (라벨 모집단 분기):** `script.js`에 `getPopulationTotal()`을 추가하여 숨김 토글이 활성화된 경우 상단 건수 라벨의 전체 수(분모)에서 내 글 수를 정상적으로 차감함. (V8, V9, V11 테스트 통과)
- **T4 (통합본 재생성 툴):** 전체 수집과 이미지 재다운로드 과정을 생략한 `scripts/rebuild_total.py` 스크립트가 신규 작성됨. `--dry-run` 옵션으로 수정된 정렬 적용 결과를 콘솔에서 먼저 확인할 수 있도록 구현됨.
- **검증 실행 상태:** E2E 테스트(`test_my_posts_filter_ui.py`)에 V7~V12 항목이 추가되었고, `19 passed` 확인됨. 자동 생성된 6장의 `.png` 화면 검증 캡처 증거물(`_docs/evidence/20260827_03/`)이 Working Tree 상에 확인됨.

## Blocking Findings
- 없음.

## Advisory Findings
- 변경 사항 중 `.gitignore` 내 `scripts/rebuild_total.py` 예외 추가는 기존 계획서(3.1 범위)에는 없었으나 스크립트의 정상적인 버전 관리를 위해 필요한 적절한 조치였습니다. (11.3 변경점 기록 반영됨)
- 재생성 스크립트 내에서 콘솔 출력 시 `I/O operation on closed file` 방지를 위해 `sys.stdout.reconfigure`로 우회 처리한 것도 문제를 잘 회피한 조치입니다.

## Unverified Concerns
- `query-sns.mjs` CLI 및 검색 API 경로(`/api/search`)에서 숨김 기능 작동 및 정렬 결과가 제대로 나오는지 실측 증거는 제시되지 않았습니다. (계획서 내 논리상 해결될 것으로 예상된다는 기록은 존재함)
- `rebuild_total.py` 내의 `_preview_sort_key`가 `total_scrap.py`의 `save_total.sort_key`를 복제하여 이중 관리(Duplication)되고 있습니다. 추후 정렬 로직이 변경될 때 두 곳을 모두 동기화해야 하는 유지보수 상의 약점이 존재합니다.

## Rationale
- 계획서에 기재된 모든 요구사항(T1~T4)이 코드에 적절히 반영되었으며, 계획 시 정의한 통과 기준(V1~V14)에 대해 충분한 검증 증거(테스트 Output, Snapshot Artifact 등)가 확인되었습니다.
- 사용자가 결정을 내려야 했던 D1~D4 항목은 설계 상 권장안이 있었고, 사용자가 `/one-stop`을 통해 명시적으로 일괄 실행을 지시했기 때문에 권장안을 그대로 적용한 것은 올바른 판단이었습니다. 이는 '권장안 없는 결정 요청 금지' 및 '독단적 범위 축소 없이 확정된 내용을 진행한다'는 지침에 부합하므로 `승인 필요` 사유에 해당하지 않습니다.
- 코드 갱신 이후 병행 세션과의 충돌이 없었고 관련 문서(`architecture.md`) 내용 또한 성실하게 최신화되었으므로 `완료` 판정을 내립니다.